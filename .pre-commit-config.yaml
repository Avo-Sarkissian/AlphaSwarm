repos:
  - repo: local
    hooks:
      - id: lint-imports
        name: import-linter (holdings isolation contract, D-02)
        entry: uv run lint-imports
        language: system
        pass_filenames: false
        always_run: true
