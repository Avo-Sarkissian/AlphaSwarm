---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .env.example
  - src/alphaswarm/__init__.py
  - src/alphaswarm/types.py
  - src/alphaswarm/config.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "Pydantic settings model loads with sensible defaults and validates against constraints"
    - "Environment variables with ALPHASWARM_ prefix override default settings values"
    - "Invalid configuration values (e.g. log_level='INVALID') raise pydantic ValidationError"
    - "All 10 bracket archetypes are defined with distinct risk profiles, temperatures, system prompts, and counts totaling exactly 100"
    - "100 agent personas are generated with unique IDs (e.g. quant_01, degen_14) and correct bracket assignment"
  artifacts:
    - path: "pyproject.toml"
      provides: "PEP 621 project metadata, dependencies, tool config"
      contains: "pydantic"
    - path: "src/alphaswarm/__init__.py"
      provides: "Package marker with version"
      contains: "__version__"
    - path: "src/alphaswarm/types.py"
      provides: "BracketType enum, BracketConfig model, AgentPersona model"
      exports: ["BracketType", "BracketConfig", "AgentPersona"]
    - path: "src/alphaswarm/config.py"
      provides: "AppSettings, nested settings models, DEFAULT_BRACKETS, generate_personas"
      exports: ["AppSettings", "OllamaSettings", "Neo4jSettings", "GovernorSettings", "DEFAULT_BRACKETS", "load_bracket_configs", "generate_personas"]
  key_links:
    - from: "src/alphaswarm/config.py"
      to: "src/alphaswarm/types.py"
      via: "imports BracketType, BracketConfig, AgentPersona"
      pattern: "from alphaswarm\\.types import"
---

<objective>
Scaffold the AlphaSwarm project with uv, define all core types (BracketType, BracketConfig, AgentPersona), create the Pydantic settings hierarchy, and define all 10 bracket archetypes with 100 agent persona generation.

Purpose: Every subsequent phase depends on this configuration and type foundation. Without validated settings and typed agent definitions, no simulation code can be written.
Output: A uv-managed Python package with `src/alphaswarm/` layout, complete Pydantic settings, all 10 brackets, and 100 generated personas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold, core types, and Pydantic settings hierarchy</name>
  <files>
    pyproject.toml
    .python-version
    .env.example
    src/alphaswarm/__init__.py
    src/alphaswarm/types.py
    src/alphaswarm/config.py
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-RESEARCH.md
    .planning/phases/01-project-foundation/01-CONTEXT.md
    CLAUDE.md
  </read_first>
  <action>
    **Step 1: Install uv and initialize project.**
    Run: `curl -LsSf https://astral.sh/uv/install.sh | sh` (skip if already installed).
    Run: `uv init --package alphaswarm` from the project root.
    This creates `src/alphaswarm/` layout with `__init__.py` and `__main__.py`.

    **Step 2: Configure pyproject.toml.**
    Set project metadata:
    ```toml
    [project]
    name = "alphaswarm"
    version = "0.1.0"
    description = "Multi-agent financial simulation engine"
    requires-python = ">=3.11"
    dependencies = [
        "pydantic>=2.12.5",
        "pydantic-settings>=2.13.1",
        "structlog>=25.5.0",
        "psutil>=7.2.2",
    ]

    [dependency-groups]
    dev = [
        "pytest>=8.0",
        "pytest-asyncio>=0.24.0",
        "pytest-cov>=6.0",
        "ruff",
        "mypy",
    ]

    [build-system]
    requires = ["hatchling"]
    build-backend = "hatchling.build"

    [tool.ruff]
    target-version = "py311"
    line-length = 100

    [tool.ruff.lint]
    select = ["E", "F", "I", "N", "W", "UP", "B", "SIM"]

    [tool.pytest.ini_options]
    asyncio_mode = "auto"
    testpaths = ["tests"]

    [tool.mypy]
    python_version = "3.11"
    strict = true
    ```

    Run `uv add pydantic pydantic-settings structlog psutil` and `uv add --dev pytest pytest-asyncio pytest-cov ruff mypy` to install deps and generate uv.lock.

    **Step 3: Create .python-version** containing `3.11` (one line, no trailing content).

    **Step 4: Create .env.example** with documented env vars:
    ```
    # AlphaSwarm Configuration
    # All vars prefixed with ALPHASWARM_
    # Nested settings use __ delimiter

    ALPHASWARM_DEBUG=false
    ALPHASWARM_LOG_LEVEL=INFO

    # Ollama
    ALPHASWARM_OLLAMA__ORCHESTRATOR_MODEL=qwen3:32b
    ALPHASWARM_OLLAMA__WORKER_MODEL=qwen3.5:4b
    ALPHASWARM_OLLAMA__NUM_PARALLEL=16
    ALPHASWARM_OLLAMA__MAX_LOADED_MODELS=2
    ALPHASWARM_OLLAMA__BASE_URL=http://localhost:11434

    # Neo4j
    ALPHASWARM_NEO4J__URI=bolt://localhost:7687
    ALPHASWARM_NEO4J__USERNAME=neo4j
    ALPHASWARM_NEO4J__PASSWORD=alphaswarm
    ALPHASWARM_NEO4J__DATABASE=neo4j

    # Resource Governor
    ALPHASWARM_GOVERNOR__BASELINE_PARALLEL=8
    ALPHASWARM_GOVERNOR__MAX_PARALLEL=16
    ALPHASWARM_GOVERNOR__MEMORY_THROTTLE_PERCENT=80.0
    ALPHASWARM_GOVERNOR__MEMORY_PAUSE_PERCENT=90.0
    ALPHASWARM_GOVERNOR__CHECK_INTERVAL_SECONDS=2.0
    ```

    **Step 5: Create src/alphaswarm/__init__.py:**
    ```python
    """AlphaSwarm: Multi-agent financial simulation engine."""

    __version__ = "0.1.0"
    ```

    **Step 6: Create src/alphaswarm/types.py** with these exact definitions:

    `BracketType(str, Enum)` with 10 members: QUANTS="quants", DEGENS="degens", SOVEREIGNS="sovereigns", MACRO="macro", SUITS="suits", INSIDERS="insiders", AGENTS="agents", DOOM_POSTERS="doom_posters", POLICY_WONKS="policy_wonks", WHALES="whales".

    `BracketConfig(BaseModel, frozen=True)` with fields:
    - `bracket_type: BracketType`
    - `display_name: str`
    - `count: int = Field(ge=1, le=100)`
    - `risk_profile: float = Field(ge=0.0, le=1.0)` -- 0.0 conservative, 1.0 aggressive
    - `temperature: float = Field(ge=0.0, le=2.0)` -- LLM sampling temperature
    - `system_prompt_template: str` -- archetype persona prompt
    - `influence_weight_base: float = Field(ge=0.0, le=1.0)` -- initial influence

    `AgentPersona(BaseModel, frozen=True)` with fields:
    - `id: str` -- e.g. "quant_03"
    - `name: str` -- human-readable name
    - `bracket: BracketType`
    - `risk_profile: float`
    - `temperature: float`
    - `system_prompt: str` -- fully rendered from template
    - `influence_weight_base: float`

    Also define `SignalType(str, Enum)` with BUY="buy", SELL="sell", HOLD="hold" for future use.

    Also define `SimulationPhase(str, Enum)` with IDLE="idle", SEEDING="seeding", ROUND_1="round_1", ROUND_2="round_2", ROUND_3="round_3", COMPLETE="complete" for future use.

    **Step 7: Create src/alphaswarm/config.py** with:

    `OllamaSettings(BaseModel)` with fields:
    - `orchestrator_model: str = "qwen3:32b"` (per user decision, NOT llama4:70b)
    - `worker_model: str = "qwen3.5:4b"` (per user decision, NOT qwen3.5:7b)
    - `num_parallel: int = Field(default=16, ge=1, le=32)`
    - `max_loaded_models: int = Field(default=2, ge=1, le=4)`
    - `base_url: str = "http://localhost:11434"`

    `Neo4jSettings(BaseModel)` with fields:
    - `uri: str = "bolt://localhost:7687"`
    - `username: str = "neo4j"`
    - `password: str = "alphaswarm"`
    - `database: str = "neo4j"`

    `GovernorSettings(BaseModel)` with fields:
    - `baseline_parallel: int = Field(default=8, ge=1, le=32)`
    - `max_parallel: int = Field(default=16, ge=1, le=32)`
    - `memory_throttle_percent: float = Field(default=80.0, ge=50.0, le=95.0)`
    - `memory_pause_percent: float = Field(default=90.0, ge=60.0, le=99.0)`
    - `check_interval_seconds: float = Field(default=2.0, ge=0.5, le=10.0)`

    `AppSettings(BaseSettings)` with:
    - `model_config = SettingsConfigDict(env_prefix="ALPHASWARM_", env_nested_delimiter="__", env_file=".env", env_file_encoding="utf-8")`
    - `app_name: str = "AlphaSwarm"`
    - `debug: bool = False`
    - `log_level: str = Field(default="INFO", pattern="^(DEBUG|INFO|WARNING|ERROR|CRITICAL)$")`
    - `ollama: OllamaSettings = OllamaSettings()`
    - `neo4j: Neo4jSettings = Neo4jSettings()`
    - `governor: GovernorSettings = GovernorSettings()`

    `DEFAULT_BRACKETS: list[BracketConfig]` -- all 10 brackets with these exact counts:
    Quants=10, Degens=20, Sovereigns=10, Macro=10, Suits=10, Insiders=10, Agents=15, Doom-Posters=5, Policy Wonks=5, Whales=5. Total=100.

    Each bracket must have a distinct risk_profile, temperature, and system_prompt_template (use 2-3 sentence placeholder prompts per archetype -- mark with `# TODO: Refine for Phase 5`).

    Risk profiles (0=conservative to 1=aggressive): Sovereigns=0.15, Suits=0.2, Policy Wonks=0.25, Whales=0.3, Macro=0.35, Quants=0.4, Insiders=0.5, Agents=0.6, Doom-Posters=0.8, Degens=0.95.

    Temperatures: Agents=0.1, Quants=0.3, Suits=0.3, Sovereigns=0.4, Policy Wonks=0.4, Macro=0.5, Whales=0.5, Insiders=0.6, Doom-Posters=1.0, Degens=1.2.

    Influence weights: Degens=0.3, Doom-Posters=0.4, Agents=0.5, Macro=0.6, Policy Wonks=0.65, Quants=0.7, Insiders=0.75, Suits=0.8, Whales=0.85, Sovereigns=0.9.

    `def load_bracket_configs() -> list[BracketConfig]` -- returns DEFAULT_BRACKETS (simple wrapper for future extensibility).

    `def generate_personas(brackets: list[BracketConfig]) -> list[AgentPersona]` -- iterates brackets, for each creates `bracket.count` AgentPersona instances with:
    - `id` = f"{bracket.bracket_type.value}_{i:02d}" (e.g., "quant_01", "degen_14")
    - `name` = f"{bracket.display_name} {i}" (e.g., "Quants 1", "Degens 14")
    - inherits risk_profile, temperature, influence_weight_base from bracket
    - system_prompt rendered from template (can use simple string format with agent name/bracket)
    Returns flat list of 100 AgentPersona instances.

    `def validate_bracket_counts(brackets: list[BracketConfig]) -> None` -- raises ValueError if sum of counts != 100.
  </action>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm && uv run python -c "from alphaswarm.config import AppSettings, load_bracket_configs, generate_personas; s = AppSettings(); b = load_bracket_configs(); p = generate_personas(b); assert len(b) == 10; assert len(p) == 100; assert sum(x.count for x in b) == 100; print(f'OK: {len(b)} brackets, {len(p)} personas')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "alphaswarm"` and `"pydantic>=2.12.5"` in dependencies
    - .python-version contains `3.11`
    - src/alphaswarm/__init__.py contains `__version__ = "0.1.0"`
    - src/alphaswarm/types.py contains `class BracketType(str, Enum)` with exactly 10 members
    - src/alphaswarm/types.py contains `class AgentPersona(BaseModel, frozen=True)`
    - src/alphaswarm/types.py contains `class BracketConfig(BaseModel, frozen=True)`
    - src/alphaswarm/config.py contains `class AppSettings(BaseSettings)` with `env_prefix="ALPHASWARM_"`
    - src/alphaswarm/config.py contains `class OllamaSettings(BaseModel)` with `orchestrator_model: str = "qwen3:32b"`
    - src/alphaswarm/config.py contains `DEFAULT_BRACKETS` list with exactly 10 BracketConfig entries
    - src/alphaswarm/config.py contains `def generate_personas` that returns 100 AgentPersona instances
    - .env.example contains `ALPHASWARM_OLLAMA__ORCHESTRATOR_MODEL`
    - uv.lock file exists (generated by uv add)
    - `uv run python -c "from alphaswarm.config import AppSettings; AppSettings()"` exits 0
  </acceptance_criteria>
  <done>
    uv project initialized. pyproject.toml has all deps. BracketType enum has 10 members. BracketConfig and AgentPersona are frozen Pydantic models. AppSettings loads from defaults and env vars. All 10 brackets defined with counts summing to 100. generate_personas() produces 100 unique agents.
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings validation tests and persona generation tests</name>
  <files>
    tests/__init__.py
    tests/conftest.py
    tests/test_config.py
    tests/test_personas.py
  </files>
  <read_first>
    src/alphaswarm/config.py
    src/alphaswarm/types.py
    pyproject.toml
  </read_first>
  <action>
    **Step 1: Create tests/__init__.py** (empty file).

    **Step 2: Create tests/conftest.py** with shared fixtures:
    - `clean_env` fixture (autouse=False): context manager that clears all `ALPHASWARM_*` env vars before the test and restores them after. Use `os.environ` manipulation with `monkeypatch` from pytest.
    - `default_settings` fixture: returns `AppSettings()` with clean env (no .env file influence). Use `monkeypatch` to ensure `ALPHASWARM_` vars are cleared.
    - `all_brackets` fixture: returns `load_bracket_configs()`.
    - `all_personas` fixture: returns `generate_personas(load_bracket_configs())`.

    **Step 3: Create tests/test_config.py** with these test functions:

    `test_settings_defaults()` -- Instantiate AppSettings with clean env. Assert:
    - `settings.app_name == "AlphaSwarm"`
    - `settings.debug is False`
    - `settings.log_level == "INFO"`
    - `settings.ollama.orchestrator_model == "qwen3:32b"`
    - `settings.ollama.worker_model == "qwen3.5:4b"`
    - `settings.ollama.num_parallel == 16`
    - `settings.neo4j.uri == "bolt://localhost:7687"`
    - `settings.governor.baseline_parallel == 8`
    - `settings.governor.memory_throttle_percent == 80.0`
    - `settings.governor.memory_pause_percent == 90.0`

    `test_settings_env_override(monkeypatch)` -- Set `ALPHASWARM_DEBUG=true`, `ALPHASWARM_LOG_LEVEL=DEBUG`, `ALPHASWARM_OLLAMA__NUM_PARALLEL=4`. Instantiate AppSettings. Assert:
    - `settings.debug is True`
    - `settings.log_level == "DEBUG"`
    - `settings.ollama.num_parallel == 4`

    `test_settings_invalid_log_level()` -- Set `ALPHASWARM_LOG_LEVEL=INVALID`. Assert `AppSettings()` raises `ValidationError`.

    `test_settings_invalid_governor_percent(monkeypatch)` -- Set `ALPHASWARM_GOVERNOR__MEMORY_PAUSE_PERCENT=150`. Assert raises `ValidationError`.

    `test_bracket_definitions(all_brackets)` -- Assert:
    - `len(all_brackets) == 10`
    - All bracket_type values are unique
    - All display_name values are unique
    - `sum(b.count for b in all_brackets) == 100`
    - Each bracket has risk_profile between 0.0 and 1.0
    - Each bracket has temperature between 0.0 and 2.0
    - Each bracket has non-empty system_prompt_template

    `test_bracket_specific_counts(all_brackets)` -- Build a dict of bracket_type -> count. Assert:
    - QUANTS -> 10, DEGENS -> 20, SOVEREIGNS -> 10, MACRO -> 10, SUITS -> 10, INSIDERS -> 10, AGENTS -> 15, DOOM_POSTERS -> 5, POLICY_WONKS -> 5, WHALES -> 5

    `test_bracket_distinct_risk_profiles(all_brackets)` -- Assert all 10 risk_profile values are distinct (no two brackets share the same risk_profile).

    **Step 4: Create tests/test_personas.py** with:

    `test_persona_count(all_personas)` -- `assert len(all_personas) == 100`

    `test_persona_unique_ids(all_personas)` -- Assert all 100 persona IDs are unique.

    `test_persona_id_format(all_personas)` -- Assert each persona.id matches pattern `{bracket_type}_{NN}` where NN is zero-padded two digits. Use regex `r"^[a-z_]+_\d{2}$"`.

    `test_persona_bracket_distribution(all_personas)` -- Count personas per bracket. Assert matches expected counts: quants=10, degens=20, etc.

    `test_persona_immutability(all_personas)` -- Attempt to set `all_personas[0].name = "changed"`. Assert raises `ValidationError` (frozen model).

    `test_persona_inherits_bracket_values(all_brackets, all_personas)` -- For each bracket, find all personas in that bracket and assert their risk_profile, temperature, and influence_weight_base match the bracket's values.
  </action>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm && uv run pytest tests/test_config.py tests/test_personas.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `def default_settings` and `def all_brackets` and `def all_personas` fixtures
    - tests/test_config.py contains `def test_settings_defaults` and `def test_settings_env_override` and `def test_settings_invalid_log_level`
    - tests/test_config.py contains `def test_bracket_definitions` and `def test_bracket_specific_counts`
    - tests/test_personas.py contains `def test_persona_count` and `def test_persona_unique_ids` and `def test_persona_bracket_distribution`
    - `uv run pytest tests/test_config.py tests/test_personas.py -v` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    All config and persona tests pass. Settings defaults verified. Env var override verified. Validation errors on invalid input verified. 10 brackets with correct counts verified. 100 unique personas with correct bracket distribution verified. Persona immutability verified.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv run pytest tests/test_config.py tests/test_personas.py -v --tb=short` -- all tests green
2. `uv run python -c "from alphaswarm.config import AppSettings, generate_personas, load_bracket_configs; s = AppSettings(); p = generate_personas(load_bracket_configs()); print(len(p))"` -- prints 100
3. `uv run python -c "from alphaswarm.types import BracketType; print(len(BracketType))"` -- prints 10
</verification>

<success_criteria>
- pyproject.toml exists with all core and dev dependencies
- BracketType enum has exactly 10 members
- BracketConfig and AgentPersona are frozen Pydantic models
- AppSettings loads defaults and supports ALPHASWARM_* env overrides
- All 10 brackets defined with distinct profiles and counts summing to 100
- generate_personas() returns 100 AgentPersona instances with unique IDs
- All tests in test_config.py and test_personas.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
