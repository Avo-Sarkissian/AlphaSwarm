---
phase: 23-validation-tracking-and-requirements-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-ticker-extraction/16-VALIDATION.md
  - .planning/phases/17-market-data-pipeline/17-VALIDATION.md
  - .planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md
  - .planning/phases/20-report-enhancement-and-integration-hardening/20-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TICK-01
  - TICK-02
  - TICK-03
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03
  - DECIDE-01
  - DECIDE-02
  - DTUI-01
  - DTUI-02
  - DTUI-03
  - DRPT-01

must_haves:
  truths:
    - "REQUIREMENTS.md has a v3 Requirements section with all 16 requirement IDs described and checked off"
    - "REQUIREMENTS.md traceability table has rows for all 16 v3 requirement IDs mapped to their phases"
    - "Phase 16 has a VALIDATION.md with entries mapping to actual test methods in test_ticker_validator.py, test_parsing.py, test_seed_pipeline.py, and test_cli.py"
    - "Phase 17 VALIDATION.md references actual class names from test_market_data.py (TestMarketDataSnapshotModel, TestYfinanceFetch, TestFallbackDegradation, TestDiskCache)"
    - "Phase 19 VALIDATION.md references actual test methods from test_tui.py (test_ticker_consensus_panel_*), not the nonexistent test_consensus.py"
    - "Phase 20 VALIDATION.md has all status fields flipped to complete"
  artifacts:
    - path: ".planning/phases/16-ticker-extraction/16-VALIDATION.md"
      provides: "Nyquist validation tracking for Phase 16"
      contains: "test_ticker_validator"
    - path: ".planning/phases/17-market-data-pipeline/17-VALIDATION.md"
      provides: "Corrected test references for Phase 17"
      contains: "TestMarketDataSnapshotModel"
    - path: ".planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md"
      provides: "Corrected test references for Phase 19"
      contains: "test_ticker_consensus_panel"
    - path: ".planning/phases/20-report-enhancement-and-integration-hardening/20-VALIDATION.md"
      provides: "Status-reconciled validation for Phase 20"
      contains: "nyquist_compliant: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete v3 requirement definitions and traceability"
      contains: "TICK-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: ".planning/ROADMAP.md phase assignments"
      via: "requirement ID to phase mapping"
      pattern: "TICK-01.*Phase 16"
    - from: "16-VALIDATION.md test references"
      to: "tests/test_ticker_validator.py"
      via: "actual test method names"
      pattern: "test_load_ticker_set_returns_expected_symbols"
---

<objective>
Update VALIDATION.md tracking files for phases 16, 17, 19, and 20 to reflect actual test method names, and add all v3 requirement definitions and traceability rows to REQUIREMENTS.md.

Purpose: Close documentation gaps from the v3.0 audit so that validation tracking files reference real, runnable tests and the requirements traceability table covers all shipped v3 features.

Output: 5 updated/created documentation files (.planning artifacts only, no code changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-validation-tracking-and-requirements-traceability/23-CONTEXT.md
@.planning/REQUIREMENTS.md

# Existing VALIDATION.md files to update
@.planning/phases/17-market-data-pipeline/17-VALIDATION.md
@.planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md
@.planning/phases/20-report-enhancement-and-integration-hardening/20-VALIDATION.md

# Format template (use Phase 21 as the closest recent example)
@.planning/phases/21-restore-ticker-validation-and-tracking/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile VALIDATION.md files for phases 17, 19, 20 and create Phase 16 VALIDATION.md</name>
  <files>
    .planning/phases/16-ticker-extraction/16-VALIDATION.md
    .planning/phases/17-market-data-pipeline/17-VALIDATION.md
    .planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md
    .planning/phases/20-report-enhancement-and-integration-hardening/20-VALIDATION.md
  </files>
  <action>
    **Step 1: Grep all test files to confirm actual test names before writing.**
    Run these commands to get definitive test inventories:
    - `grep -n "^class Test\|^def test_\|^async def test_" tests/test_market_data.py`
    - `grep -n "^def test_ticker_consensus\|^async def test_ticker_consensus" tests/test_tui.py`
    - `grep -n "^class Test\|^def test_\|^async def test_" tests/test_report.py tests/test_graph.py`
    - `grep -n "^def test_\|^async def test_" tests/test_ticker_validator.py`
    - `grep -n "^async def test_" tests/test_seed_pipeline.py`
    - `grep -n "^def test_.*inject\|^def test_.*ticker\|^def test_.*dropped" tests/test_cli.py`
    - `grep -n "^def test_parse_seed\|^def test_parse_seed_event_drop" tests/test_parsing.py`

    **Step 2: Update Phase 17 VALIDATION.md (per D-04, D-05).**
    Read the existing 17-VALIDATION.md. Replace the Per-Task Verification Map rows with actual class-level references:
    - Replace `test_snapshot_model_valid`, `test_fetch_yfinance_returns_snapshot`, `test_parallel_fetch_all_tickers` rows with `TestMarketDataSnapshotModel` (DATA-01, covers model validation)
    - Replace `test_av_fallback_on_yfinance_failure`, `test_degraded_snapshot_both_fail`, `test_av_skipped_no_key` rows with `TestFallbackDegradation` (DATA-02)
    - Replace `test_cache_write_creates_file`, `test_cache_hit_within_ttl`, `test_cache_miss_expired_ttl`, `test_cache_hit_logged` rows with `TestDiskCache` (DATA-04)
    - Add `TestYfinanceFetch` row for DATA-01 (core fetch logic)
    - Update Automated Command column to use class-level refs: `uv run pytest tests/test_market_data.py::TestMarketDataSnapshotModel -x`
    - Set frontmatter: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`
    - Set all task Status columns to `✅ green`
    - Set File Exists columns to `✅`
    - Update Wave 0 Requirements section: check off all items (yfinance installed, test file exists, conftest exists)
    - Check off all Validation Sign-Off items
    - Set Approval to `approved`

    **Step 3: Update Phase 19 VALIDATION.md (per D-04, D-06).**
    Read the existing 19-VALIDATION.md. Replace ALL `tests/test_consensus.py::*` references with actual `tests/test_tui.py::test_ticker_consensus_panel_*` methods:
    - Row 19-01-01 (DTUI-02): Change from `test_consensus.py::test_compute_ticker_consensus` to `test_tui.py::test_ticker_consensus_panel_render_header_both_signals` (confidence-weighted voting visible)
    - Row 19-01-02 (DTUI-02): Change from `test_consensus.py::test_weighted_vs_majority` to `test_tui.py::test_ticker_consensus_panel_render_header_agree` (weighted + majority both shown)
    - Row 19-01-03 (DTUI-02): Change from `test_consensus.py::test_division_by_zero_guard` to `test_tui.py::test_ticker_consensus_panel_empty_state_idle` (edge case: no data)
    - Row 19-02-01 (DTUI-01): Change from `test_tui.py::test_ticker_consensus_panel_render` to `test_tui.py::test_ticker_consensus_panel_title` (panel renders with title)
    - Row 19-02-02 (DTUI-01): Change from `test_tui.py::test_ticker_consensus_panel_scroll` to `test_tui.py::test_ticker_consensus_panel_multiple_tickers` (multiple tickers rendered)
    - Row 19-03-01 (DTUI-03): Change from `test_tui.py::test_bracket_disagreement_display` to `test_tui.py::test_ticker_consensus_panel_render_bracket_bars` (bracket bars show disagreement)
    - Row 19-03-02 (DTUI-01,DTUI-03): Change from `test_simulation.py::test_consensus_wired_end_to_end` to `test_tui.py::test_ticker_consensus_panel_majority_pct_display` (majority percentage visible)
    - Remove the Wave 0 requirement for `tests/test_consensus.py` (file does not exist and never will)
    - Set frontmatter: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`
    - Set all File Exists to `✅` and all Status to `✅ green`
    - Check off Validation Sign-Off items. Set Approval to `approved`

    **Step 4: Update Phase 20 VALIDATION.md (per D-04, D-07).**
    Read the existing 20-VALIDATION.md. Test class names are already correct (TestReportAssemblerMarketContext, TestMarketContextTemplate, TestWriteTickerConsensus, TestReadMarketContext). Only reconcile status:
    - Set frontmatter: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`
    - Set ALL File Exists columns to `✅` (all test files exist)
    - Set ALL Status columns to `✅ green`
    - Check off all Wave 0 Requirements items
    - Check off all Validation Sign-Off items
    - Set Approval to `approved`

    **Step 5: Create Phase 16 VALIDATION.md (per D-08, D-09, D-10).**
    Create new file `.planning/phases/16-ticker-extraction/16-VALIDATION.md` using Phase 21's VALIDATION.md as the format template. Include:

    Frontmatter:
    ```yaml
    phase: 16
    slug: ticker-extraction
    status: complete
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-06
    ```

    Test Infrastructure table: framework=pytest 8.x + pytest-asyncio, quick run=`uv run pytest tests/test_ticker_validator.py tests/test_parsing.py -k "seed" -x -q`, full suite=`uv run pytest tests/ -x -q`, estimated runtime=~15 seconds.

    Per-Task Verification Map entries mapping to actual tests:

    | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
    - 16-01-01: Plan 01, TICK-01, unit, `uv run pytest tests/test_ticker_validator.py::test_load_ticker_set_returns_expected_symbols -x` (SEC JSON parse)
    - 16-01-02: Plan 01, TICK-01, unit, `uv run pytest tests/test_ticker_validator.py::test_load_ticker_set_uppercase_conversion -x` (uppercase normalization)
    - 16-02-01: Plan 02, TICK-02, unit, `uv run pytest tests/test_ticker_validator.py::test_validate_returns_true_for_valid_symbol -x` (valid symbol lookup)
    - 16-02-02: Plan 02, TICK-02, unit, `uv run pytest tests/test_ticker_validator.py::test_validate_case_insensitive -x` (case-insensitive validation)
    - 16-02-03: Plan 02, TICK-02, unit, `uv run pytest tests/test_ticker_validator.py::test_validate_returns_false_for_unknown_symbol -x` (invalid symbol rejected)
    - 16-02-04: Plan 02, TICK-02, unit, `uv run pytest tests/test_ticker_validator.py::test_get_ticker_validator_returns_none_on_connect_error -x` (CDN fallback)
    - 16-02-05: Plan 02, TICK-02, unit, `uv run pytest tests/test_ticker_validator.py::test_get_ticker_validator_returns_none_on_timeout -x` (timeout fallback)
    - 16-03-01: Plan 03, TICK-03, unit, `uv run pytest tests/test_parsing.py::test_parse_seed_with_tickers -x` (tickers extracted from seed)
    - 16-03-02: Plan 03, TICK-03, unit, `uv run pytest tests/test_parsing.py::test_parse_seed_tickers_capped_at_3 -x` (top-3 cap enforced)
    - 16-03-03: Plan 03, TICK-03, unit, `uv run pytest tests/test_parsing.py::test_parse_seed_event_drops_invalid_ticker -x` (invalid ticker dropped)
    - 16-03-04: Plan 03, TICK-03, unit, `uv run pytest tests/test_parsing.py::test_parse_seed_event_dropped_tickers_cap_reason -x` (dropped ticker tracking)
    - 16-04-01: Plan 03, TICK-03, unit, `uv run pytest tests/test_cli.py::test_print_injection_summary_shows_dropped_tickers -x` (CLI display)
    - 16-05-01: Plan 01, TICK-01, integration, `uv run pytest tests/test_seed_pipeline.py -x -q` (inject pipeline end-to-end)

    All File Exists = `✅`, all Status = `✅ green`.

    Wave 0 Requirements: all checked off (tests exist).
    Validation Sign-Off: all checked off.
    Approval: `approved`.

    Retain Manual-Only Verifications section consistent with existing files (e.g., "Full inject-seed pipeline with real Ollama" for TICK-01/03).
  </action>
  <verify>
    Verify all 4 files exist and contain correct references:
    - `grep -c "TestMarketDataSnapshotModel\|TestYfinanceFetch\|TestFallbackDegradation\|TestDiskCache" .planning/phases/17-market-data-pipeline/17-VALIDATION.md` returns 4+
    - `grep -c "test_ticker_consensus_panel" .planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md` returns 7+
    - `grep -c "nyquist_compliant: true" .planning/phases/20-report-enhancement-and-integration-hardening/20-VALIDATION.md` returns 1
    - `grep -c "test_ticker_validator\|test_parsing\|test_seed_pipeline\|test_cli" .planning/phases/16-ticker-extraction/16-VALIDATION.md` returns 4+
    - `grep -c "test_consensus.py" .planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md` returns 0 (dead refs removed)
  </verify>
  <done>
    - Phase 16 has a new 16-VALIDATION.md with Nyquist entries mapped to actual test methods across test_ticker_validator.py, test_parsing.py, test_seed_pipeline.py, and test_cli.py
    - Phase 17 VALIDATION.md references TestMarketDataSnapshotModel, TestYfinanceFetch, TestFallbackDegradation, TestDiskCache (not stale placeholder names)
    - Phase 19 VALIDATION.md references test_tui.py::test_ticker_consensus_panel_* methods (not the nonexistent test_consensus.py)
    - Phase 20 VALIDATION.md has all statuses flipped to complete with nyquist_compliant: true
    - All four files have status: complete, nyquist_compliant: true, wave_0_complete: true, approval: approved
  </done>
</task>

<task type="auto">
  <name>Task 2: Add v3 requirements section and traceability rows to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Read the existing REQUIREMENTS.md. Add a new section following the v2 format exactly (per D-01).

    **Step 1: Insert "## v3 Requirements" section** after the v2 section and before the "## v3 Requirements (Future)" section. Rename the existing "## v3 Requirements (Future)" to "## Future Requirements" to avoid title collision.

    The v3 section must use the same format as v1/v2: grouped by subsystem, each with `- [x] **REQ-ID**: Description`.

    Use ROADMAP.md success criteria as the source for descriptions. Structure:

    ```
    ## v3 Requirements

    Requirements for v3.0 Stock-Specific Recommendations with Live Data milestone. Each maps to roadmap phases.

    ### Ticker Extraction

    - [x] **TICK-01**: Orchestrator LLM extracts stock ticker symbols from natural-language seed rumors alongside entity extraction in a single LLM call
    - [x] **TICK-02**: Extracted tickers are validated against the SEC company_tickers.json symbol table; invalid symbols are rejected with a warning before simulation proceeds
    - [x] **TICK-03**: When more than 3 tickers are extracted, only the top 3 by relevance score are kept; dropped tickers are displayed to the user in the CLI injection summary

    ### Market Data Pipeline

    - [x] **DATA-01**: Before Round 1 begins, the system fetches and caches live market data (price history, financial metrics, earnings) for each extracted ticker via yfinance wrapped in asyncio.to_thread with per-ticker locks
    - [x] **DATA-02**: When yfinance fails for a ticker, the system falls back to Alpha Vantage (GLOBAL_QUOTE + OVERVIEW endpoints); when both fail, simulation proceeds with a degraded-data snapshot and a visible CLI warning
    - [x] **DATA-03**: Each MarketDataSnapshot includes a headlines field populated by Alpha Vantage NEWS_SENTIMENT during context enrichment (Phase 18)
    - [x] **DATA-04**: Disk cache with atomic temp-file-rename pattern and 1-hour TTL prevents redundant API calls; cache hits are logged at INFO level

    ### Context Enrichment

    - [x] **ENRICH-01**: All market data fetching completes before Round 1 begins (pre-simulation enrichment pattern) and each agent prompt includes a formatted market data block within a strict token budget
    - [x] **ENRICH-02**: Different bracket archetypes receive different data slices -- Quants see price/volume/technicals, Macro agents see Earnings/Insider data (earnings surprises, EPS, headlines), providing bracket-tailored context
    - [x] **ENRICH-03**: The 3-tier parse fallback handles enrichment fields gracefully; agents that fail to produce new fields get backward-compatible None defaults without triggering PARSE_ERROR status

    ### Enhanced Decisions

    - [x] **DECIDE-01**: Agent decisions include per-ticker fields: ticker symbol, direction (BUY/SELL/HOLD), expected_return_pct, and time_horizon in structured output via the TickerDecision model
    - [x] **DECIDE-02**: TickerDecision uses lenient parsing (_lenient_parse_ticker_decisions) that drops malformed entries without triggering PARSE_ERROR, maintaining backward compatibility

    ### Data-Grounded TUI

    - [x] **DTUI-01**: After simulation completes, the TUI displays a per-ticker consensus panel (TickerConsensusPanel) showing each ticker symbol, aggregate signal, aggregate confidence, and vote distribution
    - [x] **DTUI-02**: Consensus aggregation uses confidence-weighted voting (confidence multiplied by influence_weight) alongside the discrete majority vote, with both visible in the panel display
    - [x] **DTUI-03**: For each ticker, bracket-level bullish/bearish breakdown is displayed as colored bars, making inter-bracket disagreement visually clear

    ### Data-Grounded Report

    - [x] **DRPT-01**: Post-simulation markdown report includes a market data context section comparing agent consensus with actual market indicators (price trends, financials, news) for each ticker
    ```

    **Step 2: Add traceability rows (per D-02).**
    Append these rows to the existing `## Traceability` table, maintaining the `| Requirement | Phase | Status |` format:

    ```
    | TICK-01 | Phase 16: Ticker Extraction | Complete |
    | TICK-02 | Phase 16: Ticker Extraction, Phase 21: Restore Ticker Validation | Complete |
    | TICK-03 | Phase 16: Ticker Extraction, Phase 21: Restore Ticker Validation | Complete |
    | DATA-01 | Phase 17: Market Data Pipeline | Complete |
    | DATA-02 | Phase 17: Market Data Pipeline | Complete |
    | DATA-03 | Phase 18: Agent Context Enrichment and Enhanced Decisions | Complete |
    | DATA-04 | Phase 17: Market Data Pipeline | Complete |
    | ENRICH-01 | Phase 18: Agent Context Enrichment and Enhanced Decisions | Complete |
    | ENRICH-02 | Phase 18: Agent Context Enrichment and Enhanced Decisions | Complete |
    | ENRICH-03 | Phase 18: Agent Context Enrichment and Enhanced Decisions | Complete |
    | DECIDE-01 | Phase 18: Agent Context Enrichment and Enhanced Decisions | Complete |
    | DECIDE-02 | Phase 18: Agent Context Enrichment and Enhanced Decisions | Complete |
    | DTUI-01 | Phase 19: Per-Stock TUI Consensus Display | Complete |
    | DTUI-02 | Phase 19: Per-Stock TUI Consensus Display | Complete |
    | DTUI-03 | Phase 19: Per-Stock TUI Consensus Display | Complete |
    | DRPT-01 | Phase 20: Report Enhancement, Phase 22: Fix Report Tool Names | Complete |
    ```

    **Step 3: Update Coverage summary (per D-03).**
    Change the Coverage section to:
    ```
    **Coverage:**
    - v1 requirements: 27 total, 27 mapped (Complete)
    - v2 requirements: 13 total, 13 mapped (Complete)
    - v3 requirements: 16 total, 16 mapped (Complete)
    - Unmapped: 0
    ```

    Also update `*Last updated:*` line at the bottom to `2026-04-08 after v3.0 documentation phase`.
  </action>
  <verify>
    Verify REQUIREMENTS.md has complete v3 coverage:
    - `grep -c "TICK-01\|TICK-02\|TICK-03\|DATA-01\|DATA-02\|DATA-03\|DATA-04\|ENRICH-01\|ENRICH-02\|ENRICH-03\|DECIDE-01\|DECIDE-02\|DTUI-01\|DTUI-02\|DTUI-03\|DRPT-01" .planning/REQUIREMENTS.md` returns 32+ (each ID appears in both the definition section and the traceability table)
    - `grep "v3 requirements: 16 total" .planning/REQUIREMENTS.md` returns 1 match
    - `grep -c "## v3 Requirements" .planning/REQUIREMENTS.md` returns 1 (section exists)
  </verify>
  <done>
    - REQUIREMENTS.md has a "## v3 Requirements" section with all 16 requirement IDs, each with [x] checkbox and description matching ROADMAP.md success criteria
    - Traceability table has 16 new rows mapping each v3 requirement ID to its phase(s)
    - Coverage summary shows v3: 16 total, 16 mapped (Complete)
    - The former "## v3 Requirements (Future)" section is renamed to "## Future Requirements" to avoid confusion
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full set of changes:

1. **Phase 16 VALIDATION.md created:** `test -f .planning/phases/16-ticker-extraction/16-VALIDATION.md && echo "EXISTS"`
2. **No dead test_consensus.py refs remain:** `grep -r "test_consensus.py" .planning/phases/ | wc -l` returns 0
3. **All 4 VALIDATION.md files have nyquist_compliant: true:** `grep -l "nyquist_compliant: true" .planning/phases/16-ticker-extraction/16-VALIDATION.md .planning/phases/17-market-data-pipeline/17-VALIDATION.md .planning/phases/19-per-stock-tui-consensus-display/19-VALIDATION.md .planning/phases/20-report-enhancement-and-integration-hardening/20-VALIDATION.md | wc -l` returns 4
4. **REQUIREMENTS.md traceability complete:** `grep -c "^| " .planning/REQUIREMENTS.md` shows 16 more rows than before (v3 additions)
5. **No code files modified:** `git diff --name-only | grep -v ".planning/" | wc -l` returns 0
</verification>

<success_criteria>
- REQUIREMENTS.md contains a v3 Requirements section with all 16 requirement IDs (TICK-01/02/03, DATA-01/02/03/04, ENRICH-01/02/03, DECIDE-01/02, DTUI-01/02/03, DRPT-01) described and marked complete
- REQUIREMENTS.md traceability table has rows for all 16 v3 IDs mapped to their implementing phases
- Phase 16 has a new 16-VALIDATION.md with Nyquist test entries referencing actual test methods in test_ticker_validator.py, test_parsing.py, test_seed_pipeline.py, and test_cli.py
- Phase 17 VALIDATION.md references TestMarketDataSnapshotModel, TestYfinanceFetch, TestFallbackDegradation, TestDiskCache (actual class names)
- Phase 19 VALIDATION.md references test_tui.py::test_ticker_consensus_panel_* methods, with zero references to the nonexistent test_consensus.py
- Phase 20 VALIDATION.md has all entries marked complete with nyquist_compliant: true
- Zero code files changed -- all modifications are .planning/ artifacts only
</success_criteria>

<output>
After completion, create `.planning/phases/23-validation-tracking-and-requirements-traceability/23-01-SUMMARY.md`
</output>
