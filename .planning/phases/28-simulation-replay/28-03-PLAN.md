---
phase: 28-simulation-replay
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified: []
autonomous: false
requirements: [REPLAY-01]
must_haves:
  truths:
    - "TUI replay mode is visually distinct from live simulation"
    - "Auto-advance timer steps through rounds correctly"
    - "Manual mode key bindings work"
    - "Replay exit restores previous TUI state cleanly (or exits app for CLI)"
    - "read_full_cycle_signals completes in under 2000ms (measured via structlog duration_ms)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the TUI replay experience -- visual correctness, timing, key binding behavior, and measured Cypher performance.

Purpose: Confirm all manual-only verification items from 28-VALIDATION.md plus the concrete performance criterion. These require an interactive TUI session with real timing, visual inspection, and measured query duration that cannot be automated.

Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-simulation-replay/28-CONTEXT.md
@.planning/phases/28-simulation-replay/28-UI-SPEC.md
@.planning/phases/28-simulation-replay/28-VALIDATION.md
@.planning/phases/28-simulation-replay/28-02-SUMMARY.md
</context>

<threat_model>
## Threat Model (ASVS Level 1)

No new attack surface in a human verification checkpoint. All code changes were in Plans 01-02.

**No threats to model.**
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Performance measurement -- verify read_full_cycle_signals duration < 2000ms</name>
  <files>src/alphaswarm/graph.py</files>
  <read_first>
    - src/alphaswarm/graph.py (read_full_cycle_signals method -- confirm time.perf_counter() instrumentation exists from Plan 01)
  </read_first>
  <action>
    **Review concern #5 (HIGH):** The `< 2s` Cypher performance success criterion must be concretely verified, not just "observable from load time in logs."

    Plan 01 added `time.perf_counter()` instrumentation to `read_full_cycle_signals` that logs `duration_ms` via structlog at debug level (Review concern #13).

    This task verifies the instrumentation exists and runs a concrete performance check:

    1. **Verify instrumentation exists.** Grep `src/alphaswarm/graph.py` for `duration_ms` in the `read_full_cycle_signals` method. If missing (Plan 01 executor omitted it), add it now:
       ```python
       import time as _time
       t0 = _time.perf_counter()
       # ... query execution ...
       elapsed_ms = (_time.perf_counter() - t0) * 1000
       self._log.debug("read_full_cycle_signals", cycle_id=cycle_id, count=len(records), duration_ms=round(elapsed_ms, 1))
       ```

    2. **Run a replay to generate the structlog output.** Execute:
       ```bash
       ALPHASWARM_LOG_LEVEL=debug uv run alphaswarm replay 2>&1 | grep "read_full_cycle_signals"
       ```
       This will print the structlog line containing `duration_ms`. If no completed cycles exist, this will error -- that is expected and acceptable (the manual test below handles the "run simulation first" prerequisite).

    3. **Extract and verify the duration.** The structlog output will contain something like:
       ```
       read_full_cycle_signals  cycle_id=abc-123  count=300  duration_ms=145.3
       ```
       The `duration_ms` value must be less than 2000. Record this value for the summary.

    If no completed cycle exists in Neo4j yet, skip this step -- the human verification task below will handle the prerequisite and performance observation together.
  </action>
  <verify>
    <automated>uv run python -c "import ast; import subprocess; r = subprocess.run(['grep', '-n', 'duration_ms', 'src/alphaswarm/graph.py'], capture_output=True, text=True); assert 'duration_ms' in r.stdout, 'Missing duration_ms instrumentation in read_full_cycle_signals'; print(f'OK: {r.stdout.strip()}')"</automated>
  </verify>
  <acceptance_criteria>
    - graph.py read_full_cycle_signals contains `duration_ms` logging via structlog
    - If a completed cycle exists: `ALPHASWARM_LOG_LEVEL=debug uv run alphaswarm replay 2>&1 | grep duration_ms` shows a value < 2000
  </acceptance_criteria>
  <done>Performance instrumentation confirmed. duration_ms logged by read_full_cycle_signals. Value verified < 2000ms if data available.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify TUI replay mode visual and interactive behavior + performance</name>
  <files>src/alphaswarm/tui.py</files>
  <read_first>
    - src/alphaswarm/tui.py (confirm replay methods exist from Plan 02)
  </read_first>
  <action>
    This is a human verification checkpoint. No code changes are made.

    Run the full automated test suite first to confirm everything passes, then present the manual verification steps to the user.

    Run: `uv run pytest tests/ -x -q`

    Then instruct the user to perform the 7 manual tests described below.

    **Prerequisites:** A completed simulation cycle must exist in Neo4j. If none exists, run a quick simulation first: `uv run alphaswarm tui` and enter a seed rumor, wait for completion.

    **Test 1: CLI replay entry + Performance measurement (Review #5)**
    1. Run `ALPHASWARM_LOG_LEVEL=debug uv run alphaswarm replay 2>&1 | tee /tmp/replay.log`
    2. Confirm TUI launches showing the replay of the most recent COMPLETED cycle
    3. Confirm header shows amber "REPLAY -- Cycle {8-char-id}" badge
    4. Confirm footer shows `Mode: Replay | Cycle: ... | Space/Right: Next  P: Play/Pause  Esc: Exit`
    5. After exiting, check performance: `grep "duration_ms" /tmp/replay.log`
    6. **CRITICAL (Review #5):** Confirm the `duration_ms` value is less than 2000. Record the exact value.

    **Test 2: Auto-advance timer**
    1. Observe rounds advancing automatically approximately every 3 seconds
    2. Confirm grid cells update to correct signals per round (colors change between rounds)
    3. Confirm bracket panel updates per round
    4. Confirm rationale sidebar updates per round WITHOUT duplicating entries (Review #2)
    5. After Round 3, confirm header shows "[DONE]" and notification "Replay complete -- Esc to exit"

    **Test 3: Manual mode toggle**
    1. Start a new replay (exit and re-enter, or press Esc then `r` from COMPLETE state)
    2. Immediately press `P` to enter manual mode
    3. Confirm header shows "[PAUSED]" and notification "Manual mode -- Space or Right to advance"
    4. Press `Space` -- confirm single round advance (Round 1 -> 2)
    5. Press `Right arrow` -- confirm another advance (Round 2 -> 3)
    6. Press `P` again -- confirm "[AUTO]" resumes if not at Round 3

    **Test 4: CLI replay exit behavior (Review #3)**
    1. Run `uv run alphaswarm replay`
    2. During replay, press `Escape`
    3. Confirm the APPLICATION EXITS entirely (returns to terminal prompt)
    4. This is correct for CLI replay -- there is no prior simulation state to restore

    **Test 5: In-app replay exit and state restoration (Review #3)**
    1. Run a full simulation via `uv run alphaswarm tui`, enter rumor, wait for COMPLETE
    2. Press `r` to start replay from COMPLETE state
    3. During replay, press `Escape`
    4. Confirm header reverts to standard "Complete" format
    5. Confirm footer reverts to telemetry display (or idle)
    6. Confirm grid shows the final simulation state (not replay state)
    7. Confirm `r` key works to start a new replay

    **Test 6: Disabled interactions (Review #10)**
    1. During replay, click an agent cell
    2. Confirm notification "Interviews unavailable during replay" appears
    3. During replay, press `s` (save)
    4. Confirm notification "Save unavailable during replay" appears
    5. During replay, try shock injection key binding (if applicable)
    6. Confirm shock window does NOT open during replay

    **Test 7: CyclePickerScreen (if multiple cycles)**
    1. If multiple completed cycles exist, press `r` from COMPLETE state
    2. Confirm CyclePickerScreen overlay appears with cycle list
    3. Use Up/Down to navigate, Enter to select, verify replay starts
    4. Test Esc to cancel -- verify return to COMPLETE state
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite passes (uv run pytest tests/ -x -q exits 0)
    - User confirms header shows amber "REPLAY -- Cycle {id}" during replay
    - User confirms auto-advance timer steps through 3 rounds at ~3s intervals
    - User confirms rationale sidebar does NOT duplicate entries between rounds (Review #2)
    - User confirms P key toggles between [AUTO] and [PAUSED]
    - User confirms Space/Right advances rounds in manual mode
    - User confirms CLI replay Escape exits app entirely (Review #3)
    - User confirms in-app replay Escape restores COMPLETE state (Review #3)
    - User confirms agent click shows "Interviews unavailable during replay"
    - User confirms shock injection is blocked during replay (Review #10)
    - User confirms read_full_cycle_signals duration_ms < 2000 in structlog output (Review #5)
  </acceptance_criteria>
  <done>Human approves all 7 manual test scenarios. Replay mode is visually correct, interactive controls work, performance criterion met (duration_ms < 2000), CLI vs in-app exit differentiated, no sidebar duplication, shock injection blocked, and no regressions observed in normal TUI operation.</done>
</task>

</tasks>

<verification>
Human visual and interactive verification of all manual-only items from 28-VALIDATION.md plus review concerns:
1. TUI replay visual distinction (amber header badge)
2. Auto-advance 3s timer stepping through rounds
3. P key toggle and Space/Right manual advance
4. Rationale sidebar does not duplicate entries (Review #2)
5. CLI replay exits app on Escape; in-app replay restores COMPLETE (Review #3)
6. Shock injection blocked during replay (Review #10)
7. Cypher query performance: read_full_cycle_signals duration_ms < 2000 measured via structlog (Review #5)
</verification>

<success_criteria>
- Human confirms all 7 test scenarios pass
- Replay mode is visually distinct and functional
- read_full_cycle_signals duration_ms < 2000 (concrete measurement, not just "observable")
- No sidebar entry duplication
- CLI and in-app exit behaviors correct
- No regressions in normal TUI operation
</success_criteria>

<output>
After completion, create `.planning/phases/28-simulation-replay/28-03-SUMMARY.md`
</output>
