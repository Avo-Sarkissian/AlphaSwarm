---
phase: 15-post-simulation-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/alphaswarm/report.py
  - src/alphaswarm/graph.py
  - tests/test_report.py
autonomous: true
requirements: [REPORT-01, REPORT-02]
must_haves:
  truths:
    - "ReACT engine terminates on FINAL_ANSWER action"
    - "ReACT engine terminates on hard iteration cap (10)"
    - "ReACT engine terminates on duplicate tool call"
    - "ACTION/INPUT parser extracts tool name and JSON from structured block"
    - "8 Cypher query tools return typed data from Neo4j graph"
  artifacts:
    - path: "src/alphaswarm/report.py"
      provides: "ReportEngine (ReACT loop), ToolObservation, _parse_action_input, REACT_SYSTEM_PROMPT"
      exports: ["ReportEngine", "ToolObservation", "_parse_action_input"]
    - path: "src/alphaswarm/graph.py"
      provides: "8 new async read methods for report sections + read_latest_cycle_id"
    - path: "tests/test_report.py"
      provides: "Unit tests for parser, engine termination, and Cypher tool return shapes"
  key_links:
    - from: "src/alphaswarm/report.py"
      to: "src/alphaswarm/graph.py"
      via: "tool registry dict mapping tool names to GraphStateManager read methods"
      pattern: "tools.*read_consensus_summary|read_influence_leaders"
    - from: "src/alphaswarm/report.py"
      to: "OllamaClient.chat()"
      via: "direct chat call in ReACT loop (no governor)"
      pattern: "_client\\.chat"
---

<objective>
Build the ReACT report engine and all 8 Cypher query tools that power post-simulation analysis.

Purpose: This is the core intelligence layer -- the ReACT engine autonomously decides which graph queries to run, collects observations, and signals completion. The 8 Cypher tools extract every dimension of simulation data (consensus, brackets, dissenters, influence, flips, entities, social reach).

Output: `src/alphaswarm/report.py` (ReportEngine + data types + parser), 8 new read methods + `read_latest_cycle_id` on `GraphStateManager` in `graph.py`, full unit test suite in `tests/test_report.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-post-simulation-report/15-CONTEXT.md
@.planning/phases/15-post-simulation-report/15-RESEARCH.md
@.planning/phases/15-post-simulation-report/15-VALIDATION.md
@src/alphaswarm/interview.py (structural template for ReportEngine)
@src/alphaswarm/graph.py (GraphStateManager — add read methods here)
@src/alphaswarm/seed.py (orchestrator model lifecycle pattern)
@pyproject.toml (add jinja2 + aiofiles dependencies)

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/alphaswarm/interview.py:
```python
class InterviewEngine:
    """Structural template: standalone module, OllamaClient injection, async run method, structlog component logger."""
    def __init__(self, context: InterviewContext, ollama_client: OllamaClient, model: str) -> None: ...
    async def ask(self, user_message: str) -> str: ...
```

From src/alphaswarm/graph.py:
```python
class GraphStateManager:
    def __init__(self, driver: AsyncDriver, database: str, personas: list[AgentPersona]) -> None: ...
    # Pattern for all read methods:
    async def read_peer_decisions(self, agent_id: str, cycle_id: str, round_num: int, limit: int = 5) -> list[PeerDecision]: ...
    # Uses: session-per-method, execute_read, _tx static helper, Neo4jError catch
```

From src/alphaswarm/ollama_client.py:
```python
class OllamaClient:
    async def chat(self, model: str, messages: list[dict], think: bool = True, format: str | None = None) -> ChatResponse: ...
```

Graph relationship names (confirmed from codebase):
- Agent -[:MADE]-> Decision (decision has: decision_id, cycle_id, round, signal, confidence, sentiment, rationale)
- Decision -[:FOR]-> Cycle
- Decision -[:HAS_EPISODE]-> RationaleEpisode (episode has: flip_type as string e.g. 'HOLD_TO_BUY', round_num, cycle_id)
- Decision -[:REFERENCES]-> Entity (entity has: name, type)
- Decision -[:HAS_POST]-> Post (post has: post_id, agent_id, signal, round_num, content)
- Agent -[:INFLUENCED_BY {cycle_id, round, weight}]-> Agent
- Agent -[:READ_POST {round_num, cycle_id}]-> Post
- Cycle has: cycle_id, seed_rumor, created_at (datetime())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 15-01-01: Wave 0 -- Dependencies, test stubs, and report module skeleton</name>
  <files>pyproject.toml, src/alphaswarm/report.py, tests/test_report.py</files>
  <action>
**1. Add dependencies to pyproject.toml** (per D-09, D-11):
Add `"jinja2>=3.1.6"` and `"aiofiles>=25.1.0"` to `[project.dependencies]` list. Run `uv sync` to install.

**2. Create `src/alphaswarm/report.py`** with skeleton:
- `from __future__ import annotations`
- `import re, json, structlog`
- `from dataclasses import dataclass`
- Component logger: `log = structlog.get_logger(component="report")`
- `MAX_ITERATIONS = 10` constant (per D-03: hard cap 8-10, use 10)
- `@dataclass(frozen=True) class ToolObservation:` with fields `tool_name: str`, `tool_input: dict[str, object]`, `result: object`
- `_ACTION_RE` and `_INPUT_RE` compiled regexes per D-01: `re.compile(r"^ACTION:\s*(.+)$", re.MULTILINE)` and `re.compile(r"^INPUT:\s*(.+)$", re.MULTILINE)`
- `def _parse_action_input(content: str) -> tuple[str | None, str | None]:` per RESEARCH Pattern example (extract first ACTION/INPUT from LLM output; return (None, None) if no ACTION found; default INPUT to "{}" if missing)
- `class ReportEngine:` skeleton with `__init__(self, ollama_client, model, tools)` and `async def run(self, cycle_id: str) -> list[ToolObservation]:` that returns empty list (stub)

**3. Create `tests/test_report.py`** with ALL test stubs from VALIDATION.md:
- Use `from unittest.mock import AsyncMock, MagicMock` and `import pytest`
- `class TestParseActionInput:` with 4 test methods:
  - `test_basic_extraction` — standard ACTION/INPUT block returns correct tuple
  - `test_no_action_returns_none` — prose with no ACTION: returns (None, None)
  - `test_action_without_input_defaults_empty_json` — ACTION present, no INPUT line, returns (action, "{}")
  - `test_thought_before_action_parsed_correctly` — THOUGHT text before ACTION block, still extracts correctly
- `class TestReportEngine:` with 3 test stubs (body = `pass` for now, will be filled in Task 15-01-02):
  - `test_terminates_on_final_answer`
  - `test_hard_cap_termination`
  - `test_duplicate_call_terminates`
- `class TestGraphQueryTools:` with 3 test stubs (body = `pass` for now, will be filled in Task 15-01-03):
  - `test_read_consensus_summary`
  - `test_influence_leaders_round_filter`
  - `test_signal_flip_none_filter`
- `class TestReportAssembler:` with 3 test stubs (body = `pass` for now, Plan 02 fills these):
  - `test_renders_section`
  - `test_async_file_write`
  - `test_sentinel_file_schema`

All stubs in TestParseActionInput must have REAL test bodies (not `pass`) — these test the parser function which is fully implemented in this task.
  </action>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm && uv sync && pytest tests/test_report.py::TestParseActionInput -x -v</automated>
  </verify>
  <done>pyproject.toml has jinja2 + aiofiles, report.py exists with ToolObservation + _parse_action_input + ReportEngine skeleton, test_report.py has all stubs, parser tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-01-02: ReACT engine with 3 termination modes</name>
  <files>src/alphaswarm/report.py, tests/test_report.py</files>
  <behavior>
    - Test 1 (FINAL_ANSWER termination): Engine with mock OllamaClient that returns `ACTION: FINAL_ANSWER\nINPUT: {}` on first call -> run() returns empty observations list, loop ran exactly 1 iteration
    - Test 2 (Hard cap termination): Engine with mock that always returns `ACTION: bracket_summary\nINPUT: {"cycle_id": "abc"}` (new input each time to avoid duplicate detection) -> run() returns MAX_ITERATIONS observations, loop exits at cap
    - Test 3 (Duplicate call termination): Engine with mock that returns same `ACTION: bracket_summary\nINPUT: {"cycle_id": "abc"}` every time -> run() returns 1 observation (first call succeeds), loop exits on second duplicate
  </behavior>
  <action>
**1. Fill in TestReportEngine tests** in `tests/test_report.py`:
- Create a `mock_tool` fixture: an `AsyncMock` returning a dict like `{"buy_count": 50, "sell_count": 30, "hold_count": 20, "total": 100}`
- Create a `mock_ollama_response(content)` helper that returns a mock with `.message.content = content`
- `test_terminates_on_final_answer`: mock client.chat returns response with `THOUGHT: Done\nACTION: FINAL_ANSWER\nINPUT: {}`. Construct `ReportEngine(ollama_client=mock_client, model="test", tools={"bracket_summary": mock_tool})`. Call `await engine.run("cycle1")`. Assert result is empty list (FINAL_ANSWER produces no observation). Assert client.chat was called once.
- `test_hard_cap_termination`: mock client.chat side_effect returns responses with incrementing tool inputs (e.g., `ACTION: bracket_summary\nINPUT: {"cycle_id": "abc", "n": {i}}` for each iteration). Assert len(result) == MAX_ITERATIONS. Assert client.chat call_count == MAX_ITERATIONS.
- `test_duplicate_call_terminates`: mock client.chat always returns `ACTION: bracket_summary\nINPUT: {"cycle_id": "abc"}`. Assert len(result) == 1 (first call succeeds, second is duplicate -> exit). Assert client.chat call_count == 2 (first call dispatched, second detected as duplicate).

**2. Implement full ReportEngine.run()** in `src/alphaswarm/report.py`:
- `REACT_SYSTEM_PROMPT` constant with tool list and format instructions per RESEARCH code example. List all 8 tool names + FINAL_ANSWER. Instruct the LLM to output `THOUGHT:`, `ACTION:`, `INPUT:` blocks.
- `run(self, cycle_id: str) -> list[ToolObservation]`:
  - Initialize `observations: list[ToolObservation] = []` and `seen_calls: set[tuple[str, str]] = set()`
  - Build initial messages list: `[{"role": "system", "content": REACT_SYSTEM_PROMPT}, {"role": "user", "content": f"Generate a comprehensive post-simulation analysis report for cycle {cycle_id}. Begin by gathering data."}]`
  - Loop for `MAX_ITERATIONS`:
    - Call `self._client.chat(model=self._model, messages=messages, think=False)`
    - Append assistant response to messages
    - Parse with `_parse_action_input(content)`
    - If action is None: log warning "no_action_parsed", break
    - If action == "FINAL_ANSWER": log info "react_final_answer", break (per D-02)
    - Build `call_key = (action, input_json)` — if in `seen_calls`: log warning "react_duplicate_call", break (per D-03)
    - Add to `seen_calls`
    - Look up tool in `self._tools` dict. If not found: append `OBSERVATION: ERROR: Unknown tool '{action}'` as user message, continue
    - Parse input_json with `json.loads()`, call `await tool_fn(**parsed_input)`, create `ToolObservation`, append to observations
    - Append `OBSERVATION: {str(result)}` as user message to messages
    - Log debug "react_step" with iteration, action
  - After loop: log info "react_complete" with total iterations and observation count
  - Return observations

Per D-12: The engine itself does NOT manage model lifecycle — that's the CLI handler's job (Plan 02). ReportEngine just calls chat().
  </action>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm && pytest tests/test_report.py::TestReportEngine -x -v</automated>
  </verify>
  <done>ReportEngine.run() implements full Thought-Action-Observation loop. FINAL_ANSWER exits immediately, hard cap at 10 iterations, duplicate (tool+input) pair causes early exit. All 3 engine tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-01-03: 8 Cypher query tools + read_latest_cycle_id on GraphStateManager</name>
  <files>src/alphaswarm/graph.py, tests/test_report.py</files>
  <behavior>
    - Test 1 (read_consensus_summary): Mock session.execute_read returns {buy_count:50, sell_count:30, hold_count:20, total:100} -> method returns dict with those exact keys
    - Test 2 (read_influence_leaders): Mock returns list of dicts with agent_id, name, bracket, total_influence_weight, citation_count -> method returns list
    - Test 3 (read_signal_flips): Mock returns list with flip_type != 'NONE' -> method returns list; verify Cypher filters out NONE
  </behavior>
  <action>
**1. Fill in TestGraphQueryTools tests** in `tests/test_report.py`:
- Import `GraphStateManager` from `alphaswarm.graph`
- Reuse the `mock_driver` fixture pattern from `test_interview.py` (MagicMock driver with async session context manager)
- Create `mock_personas` fixture (empty list is fine — report queries don't use personas)
- `test_read_consensus_summary`: Set `session.execute_read` to return `{"buy_count": 50, "sell_count": 30, "hold_count": 20, "total": 100}`. Call `await gsm.read_consensus_summary("cycle1")`. Assert returned dict has correct keys and values.
- `test_influence_leaders_round_filter`: Set `session.execute_read` to return `[{"agent_id": "a1", "name": "Agent 1", "bracket": "Quants", "total_influence_weight": 0.9, "citation_count": 5}]`. Call `await gsm.read_influence_leaders("cycle1")`. Assert len == 1 and fields match.
- `test_signal_flip_none_filter`: Set `session.execute_read` to return `[{"agent_id": "a1", "name": "Agent 1", "bracket": "Quants", "round_num": 2, "flip_type": "HOLD_TO_BUY", "final_signal": "BUY"}]`. Call `await gsm.read_signal_flips("cycle1")`. Assert result is list with 1 entry, flip_type == "HOLD_TO_BUY".

**2. Add 9 new methods to `GraphStateManager`** in `src/alphaswarm/graph.py` (8 report tools + 1 utility):

Each method follows the established pattern: `async def`, try/except Neo4jError, session-per-method, `execute_read` with `_tx` static helper. All return `list[dict]` or `dict` (plain dicts, not Pydantic — keeps them JSON-serializable for ToolObservation.result).

Add a new section comment block after the Phase 14 interview section:
```python
# ------------------------------------------------------------------
# Phase 15: Report query tools
# ------------------------------------------------------------------
```

**Method 1: `read_consensus_summary(cycle_id: str) -> dict`**
Cypher: `MATCH (a:Agent)-[:MADE]->(d:Decision {cycle_id: $cycle_id, round: 3}) RETURN sum(CASE WHEN d.signal = 'BUY' THEN 1 ELSE 0 END) AS buy_count, sum(CASE WHEN d.signal = 'SELL' THEN 1 ELSE 0 END) AS sell_count, sum(CASE WHEN d.signal = 'HOLD' THEN 1 ELSE 0 END) AS hold_count, count(d) AS total`

**Method 2: `read_round_timeline(cycle_id: str) -> list[dict]`**
Cypher: Returns per-round signal counts (round 1, 2, 3). `MATCH (d:Decision {cycle_id: $cycle_id}) RETURN d.round AS round_num, sum(CASE WHEN d.signal = 'BUY' THEN 1 ELSE 0 END) AS buy_count, sum(CASE WHEN d.signal = 'SELL' THEN 1 ELSE 0 END) AS sell_count, sum(CASE WHEN d.signal = 'HOLD' THEN 1 ELSE 0 END) AS hold_count, count(d) AS total ORDER BY d.round`

**Method 3: `read_bracket_narratives(cycle_id: str) -> list[dict]`**
Cypher: Per-bracket for Round 3. `MATCH (a:Agent)-[:MADE]->(d:Decision {cycle_id: $cycle_id, round: 3}) RETURN a.bracket AS bracket, sum(CASE WHEN d.signal = 'BUY' THEN 1 ELSE 0 END) AS buy_count, sum(CASE WHEN d.signal = 'SELL' THEN 1 ELSE 0 END) AS sell_count, sum(CASE WHEN d.signal = 'HOLD' THEN 1 ELSE 0 END) AS hold_count, avg(d.confidence) AS avg_confidence, avg(d.sentiment) AS avg_sentiment ORDER BY bracket`

**Method 4: `read_key_dissenters(cycle_id: str) -> list[dict]`**
Cypher per RESEARCH example: Find agents whose Round 3 signal differs from their bracket majority. Use the CTE pattern from RESEARCH Cypher (collect agents per bracket, compute majority signal, filter dissenters). Return agent_id, name, bracket, signal, bracket_majority.

**Method 5: `read_influence_leaders(cycle_id: str, limit: int = 10) -> list[dict]`**
Cypher per RESEARCH: `MATCH (src:Agent)-[inf:INFLUENCED_BY {cycle_id: $cycle_id, round: 3}]->(tgt:Agent) RETURN tgt.id AS agent_id, tgt.name AS name, tgt.bracket AS bracket, sum(inf.weight) AS total_influence_weight, count(src) AS citation_count ORDER BY total_influence_weight DESC LIMIT $limit`. Filter round=3 to avoid double-counting (Pitfall 1).

**Method 6: `read_signal_flips(cycle_id: str) -> list[dict]`**
Cypher per RESEARCH: `MATCH (a:Agent)-[:MADE]->(d:Decision {cycle_id: $cycle_id}) MATCH (d)-[:HAS_EPISODE]->(re:RationaleEpisode) WHERE re.flip_type <> 'NONE' RETURN a.id AS agent_id, a.name AS name, a.bracket AS bracket, re.round_num AS round_num, re.flip_type AS flip_type, d.signal AS final_signal ORDER BY re.round_num, a.bracket`. Uses string comparison per Pitfall 2.

**Method 7: `read_entity_impact(cycle_id: str) -> list[dict]`**
Cypher per RESEARCH: `MATCH (d:Decision {cycle_id: $cycle_id})-[:REFERENCES]->(e:Entity) RETURN e.name AS entity_name, e.type AS entity_type, avg(d.sentiment) AS avg_sentiment, count(d) AS mention_count, sum(CASE WHEN d.signal = 'BUY' THEN 1 ELSE 0 END) AS buy_mentions, sum(CASE WHEN d.signal = 'SELL' THEN 1 ELSE 0 END) AS sell_mentions, sum(CASE WHEN d.signal = 'HOLD' THEN 1 ELSE 0 END) AS hold_mentions ORDER BY mention_count DESC`

**Method 8: `read_social_post_reach(cycle_id: str, limit: int = 10) -> list[dict]`**
Cypher per RESEARCH: `MATCH (a:Agent)-[:READ_POST {cycle_id: $cycle_id}]->(p:Post) WITH p, count(a) AS reader_count MATCH (author:Agent {id: p.agent_id}) RETURN p.post_id AS post_id, author.name AS author_name, author.bracket AS bracket, p.signal AS signal, p.round_num AS round_num, p.content AS content, reader_count ORDER BY reader_count DESC LIMIT $limit`

**Method 9 (utility): `read_latest_cycle_id() -> str | None`**
Cypher: `MATCH (c:Cycle) RETURN c.cycle_id AS cycle_id ORDER BY c.created_at DESC LIMIT 1`. Returns None if no cycles exist.

All methods use try/except `Neo4jError` -> raise `Neo4jConnectionError` pattern. All log with `self._log.debug("report_query_name", cycle_id=cycle_id, count=len(records))`.
  </action>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm && pytest tests/test_report.py::TestGraphQueryTools -x -v</automated>
  </verify>
  <done>GraphStateManager has 8 report query methods + read_latest_cycle_id. All return plain dicts/lists. TestGraphQueryTools tests pass for consensus summary, influence leaders, and signal flip queries. Full suite green: `pytest tests/test_report.py -x`</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the full test suite to confirm nothing is broken:

```bash
pytest tests/test_report.py -x -v
pytest tests/ -x
```

All TestParseActionInput (4 tests), TestReportEngine (3 tests), TestGraphQueryTools (3 tests) must pass.
TestReportAssembler stubs (3 tests with `pass` body) are expected to pass trivially -- they will be filled in Plan 02.
</verification>

<success_criteria>
1. `_parse_action_input()` correctly extracts ACTION and INPUT from structured LLM output blocks (per D-01)
2. `ReportEngine.run()` implements full Thought-Action-Observation loop with 3 termination modes: FINAL_ANSWER (D-02), hard cap at 10 (D-03), duplicate call (D-03)
3. 8 Cypher query tools return structured dicts from Neo4j graph covering all 8 report sections (per D-07, D-08)
4. `read_latest_cycle_id()` enables optional `--cycle` flag defaulting to most recent cycle
5. All new code uses `from __future__ import annotations`, structlog component logger, session-per-method pattern
6. `pytest tests/test_report.py -x` passes with 10+ tests green
7. `pytest tests/ -x` full suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/15-post-simulation-report/15-01-SUMMARY.md`
</output>
