---
phase: 999.2
slug: seeding-phase-progress-ux
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-03
---

# Phase 999.2 — Validation Strategy

> Nyquist coverage audit: automated gates + manual-only classification for a frontend-only phase with no test runner installed.

---

## Test Infrastructure

| Layer | Framework | Config | Run Command |
|-------|-----------|--------|-------------|
| TypeScript type-check | `tsc 5.6` | `frontend/tsconfig.json` | `cd frontend && npm run check` |
| Vite build | Vite 6 | `frontend/vite.config.ts` | `cd frontend && npm run build` |
| Backend unit | `pytest 8` + `pytest-asyncio` | `pyproject.toml` → `tests/` | `uv run pytest tests/` |
| Frontend unit | **Not installed** | — | — |

**Note:** Phase 999.2 is 100% frontend UI changes. No backend files were modified. Backend pytest suite is not affected. No vitest/jest/testing-library is installed in the frontend — this is the current project convention (no frontend unit tests established yet per CLAUDE.md).

---

## Per-Task Coverage Map

| Task | Plan | Requirement | Automated Gate | Human Coverage | Status |
|------|------|-------------|----------------|----------------|--------|
| Task 1 (Plan 01) | 999.2-01 | `fmtSeedingElapsed` NaN/negative coercion (T-999.2-01) | `npm run check` catches type errors; function is module-scoped | Human checkpoint Task 3 — timer displayed correctly | PARTIAL → Manual-Only |
| Task 1 (Plan 01) | 999.2-01 | `ollamaChipLabel` 4-state matrix (T-999.2-02) | `npm run check` type-validates call signature | Human checkpoint Task 3 — chip labels observed | PARTIAL → Manual-Only |
| Task 1 (Plan 01) | 999.2-01 | `SeedingState` zero-agent branch (timer + chip render) | `npm run check` + `npm run build` — type/import integrity | Human checkpoint step 2 — seeding window verified | PARTIAL → Manual-Only |
| Task 1 (Plan 01) | 999.2-01 | `SeedingState` >0-agent branch (existing counter preserved) | `npm run build` — no broken JSX | Human checkpoint step 3 — NNN/100 counter observed | PARTIAL → Manual-Only |
| Task 2 (Plan 01) | 999.2-01 | `TaskBanner` `phase` prop + `SEEDING` label switch | `npm run check` — prop type enforced | Human checkpoint steps 2, 3 — banner label observed | PARTIAL → Manual-Only |
| Task 2 (Plan 01) | 999.2-01 | Cancel button visible during seeding (D-07) | `npm run check` — `{running && (...)}` guard unchanged | Human checkpoint step 5 — cancel verified functional | PARTIAL → Manual-Only |
| Task 1 (Plan 02) | 999.2-02 | `LifecycleOverlay` early-R1 routing (`round_1 && agents.length < 100`) | `npm run check` + `npm run build` | Human checkpoint step 3 — 0→100 climb visible | PARTIAL → Manual-Only |
| Task 2 (Plan 02) | 999.2-02 | `demoState` removed; single overlay precedence path | `npm run check` + `npm run build` + `grep -c demoState → 0` | Human checkpoint steps 1–9 | **COVERED** |
| Task 2 (Plan 02) | 999.2-02 | `phase={phase}` wired to `TaskBanner` from `useTelemetry()` | `npm run check` — prop type + usage verified | Human checkpoint step 2 | **COVERED** |
| Task 3 (Plan 02) | 999.2-02 | End-to-end lifecycle (idle→seeding→R1→graph→cancel→reconnect) | Not automatable without E2E framework | Human checkpoint approved 2026-05-03 (all 7 points) | **COVERED** |

---

## Manual-Only Registry

The following requirements have no automated unit tests due to missing frontend test runner (no vitest/testing-library). All are covered by the human-verify checkpoint (Task 3, approved 2026-05-03).

| ID | Requirement | Coverage Evidence | Re-test Trigger |
|----|-------------|------------------|-----------------|
| MO-999.2-01 | `fmtSeedingElapsed` formats NaN/negative/string inputs as `00:00` | Human checkpoint step 2 (timer displayed mm:ss correctly) + SECURITY.md T-999.2-01 | Changes to `fmtSeedingElapsed` |
| MO-999.2-02 | `ollamaChipLabel` returns correct label for all 4 health states | Human checkpoint step 2 (chip states observed) + SECURITY.md T-999.2-02 | Changes to `ollamaChipLabel` or `useOllamaHealth` |
| MO-999.2-03 | `SeedingState` renders timer + chip when `n === 0 && phase === 'seeding'` | Human checkpoint step 2 | Changes to `SeedingState` zero-agent branch |
| MO-999.2-04 | `SeedingState` renders `NNN/100` counter when `n > 0` | Human checkpoint step 3 | Changes to `SeedingState` >0-agent branch |
| MO-999.2-05 | `TaskBanner` shows `SEEDING · mm:ss` during seeding, `SIMULATION RUNNING · mm:ss` at round_1 | Human checkpoint steps 2, 3 | Changes to `TaskBanner.tsx` taskName derivation |
| MO-999.2-06 | `LifecycleOverlay` routes `round_1 && agents.length < 100` to `SeedingState` | Human checkpoint step 3 | Changes to `LifecycleOverlay` |
| MO-999.2-07 | Cancel button functional during seeding window | Human checkpoint step 5 | Changes to `TaskBanner` or `onStop` wiring |
| MO-999.2-08 | `reconnectFailed` precedence beats seeding branch | Human checkpoint step 6 (Codex HIGH fix) | Changes to `LifecycleOverlay` precedence chain |

**Re-test protocol:** When any of the listed trigger conditions occurs, re-run the smoke test from `999.2-02-PLAN.md Task 3 — How to Verify` against a live backend + Ollama.

---

## Automated Gate Summary

Both automated gates passed as part of plan execution (confirmed in SUMMARY self-checks):

```bash
cd frontend && npm run check   # exit 0 — TypeScript clean
cd frontend && npm run build   # exit 0 — 67 modules, no errors
```

Structural grep checks (also confirmed in SUMMARY):
- `grep -c "demoState" frontend/src/components/app_v2.jsx` → `0` ✓
- `grep "phase === 'round_1' && agents.length < 100"` → found in `states.jsx` ✓
- `grep "phase === 'seeding' ? 'SEEDING'"` → found in `TaskBanner.tsx` ✓

---

## Sign-Off

- [x] All tasks have a coverage classification (COVERED / Manual-Only)
- [x] Manual-only items documented with evidence and re-test triggers
- [x] Automated gates (tsc + build) confirmed passing
- [x] Human checkpoint (Task 3) covers all behavioral requirements
- [x] `nyquist_compliant: true` — no uncovered requirements exist
- [x] `status: verified` set in frontmatter

**Note on compliance:** `nyquist_compliant: true` reflects that every requirement has *some* form of verified coverage (type gate, build gate, or human checkpoint). It does not imply automated unit test coverage — that requires installing vitest + @testing-library/react in a future infrastructure phase.

---

## Validation Audit 2026-05-03

| Metric | Count |
|--------|-------|
| Tasks audited | 10 |
| COVERED (automated + structural) | 2 |
| PARTIAL → Manual-Only | 8 |
| MISSING (no coverage) | 0 |
| Gaps resolved | 8 (via manual-only classification) |
| Escalated | 0 |
