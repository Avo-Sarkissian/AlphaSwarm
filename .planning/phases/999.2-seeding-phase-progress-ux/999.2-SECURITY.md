---
phase: 999.2
slug: seeding-phase-progress-ux
status: verified
threats_open: 0
asvs_level: 1
created: 2026-05-03
---

# Phase 999.2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| WebSocket frame → TelemetryContext → `fmtSeedingElapsed` | Backend `snap.elapsed_seconds` adapted to `telemetry.elapsedSeconds` (float). Rendered as mm:ss display string. | Non-sensitive telemetry float |
| `/api/health/ollama` → `useOllamaHealth` → `ollamaChipLabel` → `SeedingState` | Health endpoint returns `{ connected, models_loaded[] }`. Chip label derived from count/boolean fields only — raw model name strings never reach the DOM. | Internal health status (non-sensitive) |
| `useTelemetry().phase` → `LifecycleOverlay` branch logic | `phase` string compared with trusted enum literals for overlay routing. Unknown values fall through to safe default (no overlay). | Public phase enum |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-999.2-01 | Tampering | `states.jsx` — `fmtSeedingElapsed(telemetry.elapsedSeconds)` | mitigate | `Number(seconds)\|\|0` + `Math.max(0,…)` coerces NaN/negative/string to safe zero; render cannot crash | closed |
| T-999.2-02 | Information Disclosure | `states.jsx` — Ollama chip in `SeedingState` | mitigate | `ollamaChipLabel` returns only count-derived literals (`LOADING MODEL…`, `OLLAMA OFFLINE`, `OLLAMA READY`, `ORCHESTRATOR LOADED`); raw `models_loaded` strings never reach DOM | closed |
| T-999.2-03 | Denial of Service | `useOllamaHealth` polling (5 s interval) in `SeedingState` | mitigate | `useOllamaHealth(phase === 'seeding')` — `active=false` cancels the interval; polling stops automatically when phase transitions out of `seeding` | closed |
| T-999.2-04 | XSS | New JSX label strings in `SeedingState` + `TaskBanner` | accept | All new strings are compile-time literals (`'LOADING MODEL…'`, `'ORCHESTRATOR LOADED'`, etc.); no `dangerouslySetInnerHTML`, no user-controlled content; React escapes children by default | closed |
| T-999.2-05 | Repudiation | `TaskBanner` `phase==='seeding'` label | accept | Display-only state indicator; no audit/trust impact; Cancel button remains functional during seeding (D-07 explicit preservation) | closed |
| T-999.2-06 | Tampering | `phase` string comparisons in `LifecycleOverlay` / `SeedingState` | accept | All comparisons are literal-equality (`=== 'seeding'`, `=== 'round_1'`); unknown phase strings fall through to the live-graph default branch (safe default — no overlay shown) | closed |
| T-999.2-07 | Denial of Service | `LifecycleOverlay` overlay stuck if agents.length never reaches 100 | mitigate | Cancel button visible in `TaskBanner` during all seeding phases (D-07 preserved); `reconnectFailed` branch takes priority — `ErrorState` overrides seeding when WS dies (confirmed in human-verify step 9) | closed |
| T-999.2-08 | Information Disclosure | `phase` string passed as `TaskBanner` prop | accept | `phase` is a public enum value already rendered in the topbar phase pill (`phaseLabelFrom`); no new disclosure surface | closed |
| T-999.2-09 | Tampering | Removal of `demoState='live'` constant from `app_v2.jsx` | mitigate | `demoState` was compile-time constant `'live'`; no storybook fixture, test, or external caller toggled it; removal confirmed safe by grep + build gate (`npm run check` + `npm run build` exit 0) | closed |
| T-999.2-10 | Repudiation | Overlay/banner state changes affect user perception during Cancel | accept | Cancel button visibility unchanged (`{running && (…)}`); `simStop()` POST flow unmodified; no audit/log impact | closed |
| T-999.2-11 | Elevation of Privilege | New `round_1 && agents.length < 100` branch in `LifecycleOverlay` could displace `reconnectFailed` precedence | mitigate | New branch inserted between `phase === 'seeding'` and the `memPct` check — `conn.reconnectFailed` check remains first (highest priority, line 258 before new branch at line 267); human-verify step 9 explicitly confirmed ErrorState beats SeedingState on backend kill | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-999.2-01 | T-999.2-04 | New label strings are compile-time literals; React's JSX escaping provides inherent XSS protection; no user-controlled content | Executor agent (999.2-01) | 2026-05-03 |
| AR-999.2-02 | T-999.2-05 | Phase label change is display-only and cannot affect simulation integrity; Cancel remains available | Executor agent (999.2-01) | 2026-05-03 |
| AR-999.2-03 | T-999.2-06 | Literal-equality phase comparisons with trusted enum values; safe fallback for unknown phases (no overlay → live graph renders) | Executor agent (999.2-02) | 2026-05-03 |
| AR-999.2-04 | T-999.2-08 | `phase` is already rendered in the topbar; passing it to TaskBanner exposes no new information | Executor agent (999.2-02) | 2026-05-03 |
| AR-999.2-05 | T-999.2-10 | Display-only state indicator; `simStop()` POST flow untouched; no audit trail impact | Executor agent (999.2-02) | 2026-05-03 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-03 | 11 | 11 | 0 | gsd-security-auditor (orchestrator inline — SUMMARY evidence verified) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-05-03
