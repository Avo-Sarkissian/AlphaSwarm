# Phase 999.2: Seeding Phase Progress UX - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-03
**Phase:** 999.2-seeding-phase-progress-ux
**Areas discussed:** inject_seed feedback, agent count accuracy, TaskBanner wording, backend scope

---

## What to show during inject_seed

| Option | Description | Selected |
|--------|-------------|----------|
| Elapsed timer + step label | Replace frozen 000/100 with 'INTERPRETING SEED RUMOR · 00:42'. Pure frontend, no backend change. | |
| Ollama model health chip | Poll existing Ollama /api/health to show 'ORCHESTRATOR LOADED' or 'LOADING MODEL…'. Reuses useOllamaHealth. | |
| Elapsed timer + Ollama health chip | Both: timer and model health. Most informative, still no backend changes. | ✓ |
| Animated placeholder only | Pulsing dots or spinner — no data. Zero backend and minimal frontend change. | |

**User's choice:** Elapsed timer + Ollama health chip
**Notes:** None

---

## Agent count accuracy (extend overlay to R1?)

| Option | Description | Selected |
|--------|-------------|----------|
| Extend overlay into early R1 until 100/100 | Show SeedingState when phase==='seeding' OR (phase==='round_1' AND agents.length < 100). Real data, no backend change. | ✓ |
| Keep overlay to SEEDING phase only | Exit overlay the moment phase flips to round_1. Agent count never features. | |
| Show overlay until first agent appears, then exit | Stay on overlay during seeding, exit as soon as agents.length > 0. | |

**User's choice:** Extend overlay into early R1 until 100/100
**Notes:** None

---

## TaskBanner wording during SEEDING

| Option | Description | Selected |
|--------|-------------|----------|
| SEEDING · mm:ss | Phase-specific label during inject_seed; switches to SIMULATION RUNNING at R1. | ✓ |
| Keep 'SIMULATION RUNNING' for all active phases | No change to TaskBanner wording. | |
| SEEDING + hide Cancel until R1 | Show SEEDING · mm:ss without Cancel button during inject_seed. | |

**User's choice:** SEEDING · mm:ss
**Notes:** Cancel button remains visible during seeding (not suppressed)

---

## Backend changes scope

| Option | Description | Selected |
|--------|-------------|----------|
| Frontend-only | All changes achievable without touching StateSnapshot or simulation.py. | ✓ |
| Allow one small StateSnapshot field | One new field (seeding_step) if needed. | |
| Allow full seeding progress stream | seeding_progress object with substep + percent + model_name. | |

**User's choice:** Frontend-only
**Notes:** All needed data (elapsed_seconds, agent_states, Ollama health) already available on existing WS snapshot and endpoints.

---

## Claude's Discretion

- Exact label copy inside SeedingState health chip
- Whether health result is passed as prop or hook called directly in SeedingState
- Progress bar behavior during SEEDING window (pulse vs stay at 0)
- CSS layout adjustments for timer + chip in seeding-center

## Deferred Ideas

- Streaming orchestrator token output during inject_seed — requires backend SSE/WS changes
- Estimated time remaining — no reliable prediction basis
- Cancel suppression during inject_seed — deferred by user decision
