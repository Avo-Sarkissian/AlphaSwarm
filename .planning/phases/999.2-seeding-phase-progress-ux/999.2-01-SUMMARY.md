---
phase: 999.2-seeding-phase-progress-ux
plan: 01
subsystem: frontend
tags: [react, ux, seeding, ollama-health, progress]
dependency_graph:
  requires: []
  provides:
    - SeedingState two-branch render (zero-agent timer+chip / >0-agent counter)
    - TaskBannerProps.phase optional prop with SEEDING label switch
  affects:
    - frontend/src/components/states.jsx
    - frontend/src/components/TaskBanner.tsx
tech_stack:
  added: []
  patterns:
    - Module-scoped helper functions (fmtSeedingElapsed, ollamaChipLabel) to avoid per-render recreation
    - Phase-gated hook call: useOllamaHealth(phase === 'seeding') for polling lifecycle management
    - Ternary isPreAgent gate: n === 0 && phase === 'seeding' (tightened per Codex LOW review)
key_files:
  created: []
  modified:
    - frontend/src/components/states.jsx (SeedingState two-branch render, lines 60-139)
    - frontend/src/components/TaskBanner.tsx (phase prop + taskName switch, lines 19-78)
decisions:
  - "isPreAgent gate requires both n===0 AND phase==='seeding' (not just n===0) to avoid timer/chip rendering during early-R1 window when LifecycleOverlay still routes here"
  - "ollamaChipLabel four-state matrix: null→LOADING MODEL…, !connected→OLLAMA OFFLINE, connected+empty→OLLAMA READY, connected+models→ORCHESTRATOR LOADED"
  - "fmtSeedingElapsed placed at module scope (not inside component) to prevent recreation on each render"
  - "TaskBanner phase prop is optional (not required) until Plan 02 wires the only caller in app_v2.jsx"
  - "mm:ss in TaskBanner SEEDING branch is provided by existing fmtElapsed(elapsedMs) wall-clock timer — no additional timer wiring needed"
metrics:
  duration: ~12m
  completed: 2026-05-03
  tasks_completed: 2
  files_modified: 2
---

# Phase 999.2 Plan 01: Seeding Phase Progress UX — Component Changes Summary

**One-liner:** Two-branch SeedingState (elapsed timer + 4-state Ollama chip for zero-agent window; existing NNN/100 counter otherwise) and phase-aware TaskBanner label switch (SEEDING vs SIMULATION RUNNING) using existing wall-clock timer.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add zero-agent branch to SeedingState (timer + health chip) | 8c9eb42 | frontend/src/components/states.jsx |
| 2 | Add phase prop to TaskBanner with SEEDING label switch | 0d6d58b | frontend/src/components/TaskBanner.tsx |

## Task 1: SeedingState Two-Branch Render

**File:** `frontend/src/components/states.jsx`

**Changes (lines 60–139):**

- Added `import { useOllamaHealth } from '../hooks/useOllamaHealth'` at module top
- Added two module-scoped helpers before `SeedingState`:
  - `fmtSeedingElapsed(seconds)` — coerces via `Number(seconds) || 0` and `Math.max(0, ...)`, formats as `mm:ss` (satisfies T-999.2-01 NaN/negative defense)
  - `ollamaChipLabel(health)` — explicit four-state mapping per D-03 + Codex review:
    - `null` → `'LOADING MODEL…'`
    - `!connected` → `'OLLAMA OFFLINE'`
    - `connected && models_loaded.length >= 1` → `'ORCHESTRATOR LOADED'`
    - `connected && models_loaded.length === 0` → `'OLLAMA READY'`
- Modified `SeedingState` body:
  - Added `useTelemetry()` to read `phase` and `telemetry.elapsedSeconds`
  - Added `useOllamaHealth(phase === 'seeding')` — gated polling stops when phase flips (T-999.2-03)
  - Pre-agent gate: `isPreAgent = n === 0 && phase === 'seeding'` (Codex LOW fix: phase guard prevents timer/chip during early-R1 window)
  - **isPreAgent branch:** kicker shows `SEEDING · mm:ss · CHIP_LABEL`, headline shows `'Interpreting seed rumor…'`, bar at `0%` (honest — D-08 forbids fake progress), stream shows static waiting message
  - **>0 branch:** existing `SEEDING · NNN/100` counter, `'Spawning agents…'` headline, real bar width, agent ID stream — unchanged

## Task 2: TaskBanner Phase-Aware Label

**File:** `frontend/src/components/TaskBanner.tsx`

**Signature change (lines 19–25):**
```typescript
export interface TaskBannerProps {
  running: boolean;
  advisorySynthesizing: boolean;
  phase?: string;   // NEW — optional until Plan 02 wires app_v2.jsx
  onCancelSim: () => void;
  onRetryAdvisory?: () => void;
}
```

**taskName derivation (lines 74–76):**
```typescript
const taskName = advisorySynthesizing
  ? 'ADVISORY SYNTHESIZING'
  : phase === 'seeding' ? 'SEEDING' : 'SIMULATION RUNNING';
```

**mm:ss rendering:** The existing JSX `<span>{taskName}</span><span>· {fmtElapsed(elapsedMs)}</span>` automatically produces `SEEDING · mm:ss` when `taskName='SEEDING'`. The wall-clock timer (`startedAt + setInterval`) continues to drive `elapsedMs` for both SEEDING and SIMULATION branches — no additional timer wiring was needed (confirmed by Gemini + Codex review of the file).

**Unchanged:** `useOllamaHealth(advisorySynthesizing)` gating, Cancel button `{running && (...)}` guard (D-07), `data-state` attribute, background-color logic.

## Pre-Agent Gate Condition

`isPreAgent = n === 0 && phase === 'seeding'`

The double condition is intentional (Codex LOW review fix, D-01):
- `n === 0` alone would fire during the early-R1 window (100 agents not yet voted) when `LifecycleOverlay` still routes `phase='round_1'` here
- Adding `phase === 'seeding'` ensures the timer/chip path only shows during `inject_seed()`, not during the 0→100 agent count climb handled by Plan 02's `LifecycleOverlay` extension

## Deviations from Plan

### Minor Formatting Deviation

**Found during:** Task 2 acceptance criteria verification
**Issue:** The plan's acceptance criterion checked for literal `: 'SEEDING'` (which would appear in a multi-line ternary as a standalone `: 'SEEDING'` line). The actual ternary format `phase === 'seeding' ? 'SEEDING' : 'SIMULATION RUNNING'` places `'SEEDING'` after `? ` not `: `, so the literal `: 'SEEDING'` substring is not present.
**Resolution:** The `'SEEDING'` label is present and `phase === 'seeding'` branch condition is present. All substantive criteria pass. This is a criterion wording artifact — no code change needed. Documented here for transparency.

No other deviations. Plan executed as written.

## Plan 02 Responsibility

The following is explicitly **not** in scope for Plan 01 and is deferred to Plan 02:
- Wiring `phase` prop into `TaskBanner` at its mount site in `app_v2.jsx`
- Extending `LifecycleOverlay` exit condition to cover the early-R1 window (`phase === 'seeding' || (phase === 'round_1' && agents.length < 100)`)
- Promoting `phase` from optional to required in `TaskBannerProps` once Plan 02 is the only caller

## Known Stubs

None. Both changes are fully wired to real data sources:
- `telemetry.elapsedSeconds` flows from `TelemetryContext` (real WS snapshot field)
- `useOllamaHealth` polls `/api/health/ollama` (existing endpoint)
- `agents.length` from `AgentsContext` (real `agent_states` data)

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Changes are display-only. The `useOllamaHealth` hook was already present in `TaskBanner.tsx`; its reuse in `SeedingState` adds a second polling consumer during the seeding window only (gated, stops on phase transition). T-999.2-01 through T-999.2-05 from the plan threat register are satisfied as designed.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| frontend/src/components/states.jsx exists | FOUND |
| frontend/src/components/TaskBanner.tsx exists | FOUND |
| 999.2-01-SUMMARY.md exists | FOUND |
| Commit 8c9eb42 (Task 1) exists | FOUND |
| Commit 0d6d58b (Task 2) exists | FOUND |
