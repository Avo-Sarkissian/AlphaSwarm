---
phase: 41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - CLAUDE.md                                                           # D-15: Vue 3 → React in Technology Stack
  - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md  # new: 28-row sign-off log
autonomous: false
requirements: []

# Consolidated regression register for Phase 41.1 (updated per cross-AI review, REVIEWS.md item 1).
# The phase accepts 11 KRs total — KR-01..KR-11 — mapped 1:1 to surfaces the user pre-authorized at CONTEXT time.
# Any 12th surprise discovered during UAT is BLOCKING and triggers revision.
known_regressions:
  - id: KR-41.1-01
    surface: Force graph layout
    description: d3-force replaced by deterministic SVG layout per design export; clusters static, no physics wiggle.
    plan_origin: 02 frontmatter
  - id: KR-41.1-02
    surface: SignalWire + DataSources modal
    description: Both render from mocks/wire.ts SIGNAL_WIRE_SEED and mocks/sources.ts DATA_SOURCES because backend has no signal-wire or source-stats endpoints.
    plan_origin: 02 + 03 + 04
  - id: KR-41.1-03
    surface: Agent animations
    description: flipped / roundLastSpoke / thinking fields are stubbed (0 / null / false) in the adapter — backend snapshot has no equivalents.
    plan_origin: 02 adapter
  - id: KR-41.1-04
    surface: Telemetry memory
    description: telemetry.memMb is the governor percent (0–100), not megabytes. Display label reads "%", not "MB".
    plan_origin: 02 adapter
  - id: KR-41.1-05
    surface: Telemetry slots
    description: telemetry.slotsUsed=0 / slotsMax=8 stubbed — backend does not expose slot counts.
    plan_origin: 02 adapter
  - id: KR-41.1-06
    surface: ReplayBar rewind control
    description: Rewind button disabled — backend has no /api/replay/rewind endpoint.
    plan_origin: 04 ReplayBar
  - id: KR-41.1-07
    surface: CycleHistory columns
    description: consensus / flips / duration / shocks columns rendered as em-dash placeholders — /api/replay/cycles payload does not include them.
    plan_origin: 04 history.jsx
  - id: KR-41.1-08
    surface: ErrorState event log
    description: Event-log lines are illustrative copy — backend emits no structured error events yet.
    plan_origin: 04 states.jsx
  - id: KR-41.1-09
    surface: Top Influencers sort
    description: Top Influencers ordered by agent.confidence DESC rather than CONTRACT §2.7 Report.influence.outDegree. Accepted because backend does not emit Report.influence during live view (outDegree is a post-cycle report field).
    plan_origin: 03 panels.jsx
  - id: KR-41.1-10
    surface: Rationale citations
    description: Rationale.citations / sources stubbed as empty arrays in adapter — backend Rationale stream has no citation field. RationaleFeed renders "—" where citations would live.
    plan_origin: 02 adapter + 03 panels.jsx (RationaleFeed)
  - id: KR-41.1-11
    surface: Interview modal streaming
    description: InterviewModal renders the full non-streaming response body from /api/interview — design export does not implement SSE/stream parsing, and backend endpoint returns a full message (not chunked).
    plan_origin: 04 modals.jsx

must_haves:
  truths:
    - "CLAUDE.md Technology Stack line reads 'React + TypeScript' (Vue 3 is gone)"
    - "41.1-HUMAN-UAT.md exists with all 28 parity-matrix rows recorded against the new React UI"
    - "Every row is either ✅ (parity achieved) or tagged with a KR-41.1-0X known-regression reference drawn from the 11-entry register above (KR-01 through KR-11)"
    - "No regressions outside the documented KR-41.1-01..11 set — any 12th surprise is logged as BLOCKING and triggers a revision cycle"
    - "Running `cd frontend && npm run build && npm run check` against the final state exits 0"
    - "A full 3-round simulation runs end-to-end in the new React UI with a clean browser console (zero errors, warnings limited to React 19 dev-only notices)"
    - "Memory-paused overlay was verified DETERMINISTICALLY via tweaks.jsx 'Force MemoryPaused' dev button (dispatches window 'as:force-mem-pause' event) — not left to hope that the governor will cross 90% during UAT"
    - "frontend-vue-archive/ is preserved at repo root (not deleted this phase) so rollback remains possible — lockfile intact per Plan 01 Task 1"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated Technology Stack section per D-15"
      contains: "React + TypeScript"
    - path: ".planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md"
      provides: "Signed 28-row parity matrix + browser-console log + 11-entry KR register reference + new-regression disposition log"
  key_links:
    - from: "CLAUDE.md §Technology Stack"
      to: "D-15"
      via: "Vue 3 → React replacement"
      pattern: "React + TypeScript"
    - from: "41.1-HUMAN-UAT.md §KR Register"
      to: "frontmatter.known_regressions"
      via: "copied table — the UAT references KR-01..KR-11 literally"
      pattern: "KR-41.1-11"
    - from: "41.1-HUMAN-UAT.md §Parity Matrix"
      to: "41.1-VALIDATION.md §Parity Matrix"
      via: "Signs off rows 1–28"
      pattern: "28"
---

<objective>
Close Phase 41.1. Two deliverables:

1. Update `CLAUDE.md` Technology Stack line "Vue 3 + TypeScript" → "React + TypeScript" per D-15.
2. Run the 28-row parity matrix side-by-side (`frontend-vue-archive/` on `:5174` — archive port, new `frontend/` on `:5173` — canonical dev port), capture results in `41.1-HUMAN-UAT.md`, and get explicit user sign-off.

Purpose: Every row of the parity matrix must resolve to either (a) ✅ live-data match, or (b) a known-regression reference drawn from the **11-entry KR register** (KR-41.1-01 through KR-41.1-11) that the user pre-authorized at CONTEXT time. Any 12th surprise is BLOCKING and triggers a revision cycle.

**REVIEWS.md context (pre-execution reconciliation):** Cross-AI review (Gemini + Codex) flagged two surfaces initially mis-matched against the old 8-KR register — Row 13 (rationale citations) and Row 18 (interview streaming). The user pre-authorized path (ii): expand the register to 11 KRs so the acceptance contract matches reality. This plan's frontmatter.known_regressions is the single source of truth; the UAT matrix below references it verbatim.

Output: Phase 41.1 marked complete. `CLAUDE.md` reflects the new stack. `41.1-HUMAN-UAT.md` is the durable record of parity verification. No archive removal this phase — `frontend-vue-archive/` stays on disk as a rollback path, with its `package-lock.json` preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-VALIDATION.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-REVIEWS.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-01-PLAN.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-02-PLAN.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-03-PLAN.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-04-PLAN.md
@CLAUDE.md
</context>

<interfaces>
<!-- Authoritative 11-entry KR register — UAT must reject anything that is not a ✅ or one of KR-01..KR-11. -->
<!-- Revised per REVIEWS.md item 1 (Codex HIGH): pre-authorized expansion from 8 → 11 KRs. -->

| KR ID       | Surface                          | One-liner                                                                                        |
|-------------|----------------------------------|--------------------------------------------------------------------------------------------------|
| KR-41.1-01  | Force graph layout               | d3-force → deterministic SVG clusters.                                                           |
| KR-41.1-02  | SignalWire + DataSources         | Static render from mocks/wire.ts and mocks/sources.ts — no backend endpoint.                     |
| KR-41.1-03  | Agent animation fields           | flipped / roundLastSpoke / thinking stubbed 0 / null / false in adapter.                         |
| KR-41.1-04  | Telemetry memory                 | memMb is governor percent, not megabytes — label reads "%".                                      |
| KR-41.1-05  | Telemetry slots                  | slotsUsed=0 / slotsMax=8 stubbed.                                                                |
| KR-41.1-06  | Replay rewind                    | Rewind button disabled — no /api/replay/rewind endpoint.                                         |
| KR-41.1-07  | CycleHistory columns             | consensus / flips / duration / shocks rendered as '—' placeholders.                              |
| KR-41.1-08  | ErrorState event log             | Log lines illustrative — backend emits no structured errors.                                     |
| KR-41.1-09  | Top Influencers sort             | Ordered by agent.confidence DESC (CONTRACT §2.7 Report.influence.outDegree is a post-cycle field).|
| KR-41.1-10  | Rationale citations              | Rationale.citations / sources stubbed as [] — backend does not emit citations.                    |
| KR-41.1-11  | Interview streaming              | InterviewModal renders full non-streaming response.                                              |

28-row parity matrix lives at 41.1-VALIDATION.md lines 98–125 (reproduced inline in the UAT doc below with KR annotations updated to match this 11-entry register).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CLAUDE.md Technology Stack (D-15)</name>
  <files>
    CLAUDE.md                                                           (mutate: single-line edit)
  </files>
  <read_first>
    - CLAUDE.md                                                         (confirm current "Vue 3 + TypeScript" wording + exact line)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md D-15 (locked decision)
  </read_first>
  <action>
    Step 1 — Read `CLAUDE.md` and locate the `## Technology Stack` section. The current line reads:

    ```
    - **Web Frontend:** Vue 3 + TypeScript, Vite build, D3 force-directed graph for the live agent "mirofish" view.
    ```

    Step 2 — Replace with:

    ```
    - **Web Frontend:** React + TypeScript, Vite build, D3 (d3-scale/selection/transition) for visualization primitives; deterministic SVG layout for the 100-agent "mirofish" graph (see KR-41.1-01 for d3-force displacement rationale).
    ```

    Notes:
    - Keep the existing bullet style and surrounding lines untouched.
    - Do NOT delete or modify other stack lines (Runtime, Inference, State, Web Backend, Validation, Logging).
    - The parenthetical "(see KR-41.1-01 ...)" is a future-reader hint — if you prefer a shorter line, drop it. Per D-15, the only mandatory change is "Vue 3" → "React".
    - Do NOT remove the "D3" phrase entirely — the phrase "d3-scale/selection/transition" documents what D3 remains for.

    Step 3 — Also update any other line in CLAUDE.md that explicitly references Vue. Grep first (use `grep -F` literal per REVIEWS.md item 24):
    ```
    grep -Fn "Vue" CLAUDE.md
    ```
    If any other line mentions Vue, update or delete it. The only expected match is the Web Frontend line.

    Step 4 — Verify (use `grep -F` for literal-string acceptance checks per REVIEWS.md item 24):
    ```
    grep -Fc "Vue 3" CLAUDE.md                 # must be 0
    grep -Fc "React + TypeScript" CLAUDE.md    # must be ≥ 1
    ```

    Anti-patterns to avoid:
    - Do NOT remove the "D3" mention — D3 primitives (d3-scale/selection/transition) are still in `frontend/package.json`.
    - Do NOT rewrite the whole section — single-line edit per D-15.
    - Do NOT add implementation details about React version, StrictMode, etc. — the stack section is at design level, not runtime detail.
    - Do NOT use `grep` regex on literal strings — use `grep -F` (REVIEWS.md item 24). Brittle regex matches on parentheses/dots caused false-negative readings on prior phases.
  </action>
  <verify>
    <automated>test "$(grep -Fc 'Vue 3' CLAUDE.md)" = "0" &amp;&amp; test "$(grep -Fc 'React + TypeScript' CLAUDE.md)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "Vue 3" CLAUDE.md` returns `0`.
    - `grep -Fc "React + TypeScript" CLAUDE.md` returns at least `1`.
    - The `## Technology Stack` section retains all other bullets (Runtime, Inference, State, Web Backend, Validation, Logging) unchanged.
    - `git diff CLAUDE.md` shows a single-line replacement in the Web Frontend bullet (optionally plus the KR-41.1-01 parenthetical).
  </acceptance_criteria>
  <done>CLAUDE.md Technology Stack line reads "React + TypeScript". No other content in CLAUDE.md is altered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run 28-row parity matrix + capture results in 41.1-HUMAN-UAT.md</name>
  <files>
    .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md  (new)
  </files>
  <read_first>
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-VALIDATION.md §Parity Matrix (rows 1–28)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-05-PLAN.md frontmatter known_regressions (KR-41.1-01 through KR-41.1-11)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-04-PLAN.md task 3 tweaks.jsx (the `as:force-mem-pause` deterministic dev hook — REVIEWS.md item 22)
  </read_first>
  <action>
    Step 1 — Start both frontends in parallel terminals (do NOT run `npm install` in the archive — its lockfile is the parity baseline per REVIEWS.md item 23 / Codex MEDIUM; use `npm ci` instead):

    Terminal A (old Vue — still alive under the archive path):
    ```
    cd frontend-vue-archive && npm ci && npm run dev
    ```
    Serves `http://localhost:5174` (archive port — Plan 01 Task 1 Step 4 sets `server.port: 5174`). `npm ci` installs EXACTLY what package-lock.json specifies — no lockfile regeneration.

    Terminal B (new React):
    ```
    cd frontend && npm ci && npm run dev
    ```
    Serves `http://localhost:5173` (new React dev server — Plan 01 Task 2 Step 2 sets `server.port: 5173`).

    Terminal C (backend):
    ```
    uv run alphaswarm web --reload
    ```
    Serves FastAPI on `http://localhost:8000`.

    Step 2 — Create `41.1-HUMAN-UAT.md` with this structure (KR column references updated to match the 11-entry register per REVIEWS.md item 1):

    ```markdown
    ---
    phase: 41.1
    slug: ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac
    status: in-progress
    ran: <YYYY-MM-DD HH:MM>
    environment: darwin arm64 / Node 22 / Python 3.11.11 / Ollama <version> / Chrome <version>
    seed: <the seed string used for the live run>
    ---

    # Phase 41.1 — Human UAT Log

    ## Run Context
    - Old Vue UI: http://localhost:5174 (frontend-vue-archive/ — archive port per Plan 01 Task 1 Step 4; started with `npm ci`, lockfile intact)
    - New React UI: http://localhost:5173 (frontend/ — canonical dev port per Plan 01 Task 2 Step 2)
    - Backend: http://localhost:8000 (`uv run alphaswarm web --reload`)
    - Seed used: "<seed>"
    - Duration of live run: <m:ss>

    ## KR Register (11 entries — pre-authorized at CONTEXT time, finalized in REVIEWS.md reconciliation)

    | KR ID       | Surface                          | One-liner                                                                                        |
    |-------------|----------------------------------|--------------------------------------------------------------------------------------------------|
    | KR-41.1-01  | Force graph layout               | d3-force → deterministic SVG clusters.                                                           |
    | KR-41.1-02  | SignalWire + DataSources         | Static render from mocks/wire.ts and mocks/sources.ts — no backend endpoint.                     |
    | KR-41.1-03  | Agent animation fields           | flipped / roundLastSpoke / thinking stubbed in adapter.                                          |
    | KR-41.1-04  | Telemetry memory                 | memMb is governor percent, not MB — label reads "%".                                             |
    | KR-41.1-05  | Telemetry slots                  | slotsUsed=0 / slotsMax=8 stubbed.                                                                |
    | KR-41.1-06  | Replay rewind                    | Rewind button disabled — no /api/replay/rewind endpoint.                                         |
    | KR-41.1-07  | CycleHistory columns             | consensus / flips / duration / shocks shown as '—'.                                              |
    | KR-41.1-08  | ErrorState event log             | Log lines illustrative — backend emits no structured errors.                                     |
    | KR-41.1-09  | Top Influencers sort             | Ordered by confidence DESC (outDegree is post-cycle).                                            |
    | KR-41.1-10  | Rationale citations              | Rationale.citations / sources stubbed [] in adapter.                                             |
    | KR-41.1-11  | Interview streaming              | Full non-streaming response body.                                                                |

    ## 28-Row Parity Matrix

    | # | Surface | Data Source | Old UI | New UI | Match | Notes / KR |
    |---|---------|-------------|--------|--------|-------|------------|
    | 1 | KPI: tokens/sec | StateFrame.telemetry.tps | <value> | <value> | ⬜ | |
    | 2 | KPI: memory | StateFrame.telemetry.memMb | <value> | <value>% | ⬜ | KR-41.1-04 (percent not MB) |
    | 3 | KPI: slots used/max | StateFrame.telemetry.slotsUsed/Max | <v>/<m> | 0/8 | ⬜ | KR-41.1-05 (stubbed 0/8) |
    | 4 | KPI: elapsed | StateFrame.telemetry.elapsedSec | <v> | <v> | ⬜ | |
    | 5 | KPI: consensus % | StateFrame.consensus | <v>% | <v>% | ⬜ | |
    | 6 | Force graph: 100 nodes present | StateFrame.agents[] | 100 | 100 | ⬜ | |
    | 7 | Force graph: signal colors (buy/sell/hold) | Agent.signal | visual match | visual match | ⬜ | |
    | 8 | Force graph: influence edges | StateFrame.edges[] (via useEdges hook, centralized in EdgesContext per Plan 02/03) | <N> | <N> | ⬜ | |
    | 9 | Force graph: flipped halos since R1 | Agent.flipped | visible | not rendered | ⬜ | KR-41.1-03 (flipped stub) |
    | 10 | Brackets: 10 rows with buy/sell/hold bars | derived from agents[] | 10 | 10 | ⬜ | |
    | 11 | Brackets: live counts update at 5Hz | agents[] stream | yes | yes | ⬜ | |
    | 12 | Rationale feed: streaming new items | Rationale stream | yes | yes | ⬜ | |
    | 13 | Rationale feed: citation links to agents | Rationale.citations | yes (links) | '—' placeholder in citation cell | ⬜ | **KR-41.1-10** (citations stubbed [] — adapter does not receive them from backend) |
    | 14 | Signal wire: recent API calls | mocks/wire.ts SIGNAL_WIRE_SEED | illustrative | illustrative | ⬜ | KR-41.1-02 (no backend) |
    | 15 | Round indicator (R1/R2/R3/FINAL) | StateFrame.phase | yes | yes | ⬜ | |
    | 16 | Top influencers list | `agents[].confidence desc` (KR-41.1-09 substitutes for CONTRACT §2.7 Report.influence.outDegree) | yes | yes (top 5 by confidence) | ⬜ | KR-41.1-09 (confidence-desc substitute for outDegree) |
    | 17 | Interview modal: opens on agent click | /api/interview | yes | yes | ⬜ | |
    | 18 | Interview modal: streams response | /api/interview | yes (chunked) | yes (full response, rendered when complete) | ⬜ | **KR-41.1-11** (non-streaming — design export does not implement SSE parsing, endpoint returns full message) |
    | 19 | Report modal: renders cycle report | /api/report/{id} | yes | yes (markdown) | ⬜ | |
    | 20 | Advisory panel: portfolio advisory | /api/advisory/{id} | yes | yes (markdown) | ⬜ | |
    | 21 | Shock drawer: post shock text | /api/simulate/shock | yes | yes | ⬜ | |
    | 22 | Cycle picker: list archived cycles | /api/replay/cycles | yes | yes | ⬜ | |
    | 23 | Cycle picker: load replay | /api/replay/start/{id} | yes | yes | ⬜ | |
    | 24 | Idle overlay | no cycle, empty seed | yes | yes | ⬜ | |
    | 25 | Seeding overlay | agents[].length > 0 progressively | yes | yes (reads snap.agents.length) | ⬜ | |
    | 26 | Memory-paused overlay | memMb >= 90 (governor percent per KR-41.1-04) | yes | yes (verified DETERMINISTICALLY via tweaks.jsx "Force MemoryPaused" button — see Step 4) | ⬜ | |
    | 27 | Error overlay | fatal error event (WS reconnectFailed) | yes | yes (on reconnectFailed) | ⬜ | KR-41.1-08 (log illustrative) |
    | 28 | Connection banner: disconnect/reconnect | WS lifecycle | yes | yes | ⬜ | |

    ## Browser Console

    - Errors during full simulation: <count>
    - Warnings: <count> (React 19 dev-only StrictMode notices acceptable; any DOM-hygiene/accessibility warnings must be logged below and treated as candidate new regressions per REVIEWS.md item 21)

    <inline paste of console output>

    ## Known Regressions Encountered (✅ = appeared as expected; ✗ = not encountered this run)

    | KR ID       | Encountered? | User Comment                                         |
    |-------------|--------------|------------------------------------------------------|
    | KR-41.1-01  | ✅ / ✗       | <comment on layout feel>                             |
    | KR-41.1-02  | ✅ / ✗       | <comment on static SignalWire/DataSources>           |
    | KR-41.1-03  | ✅ / ✗       | <comment on lack of flipped/thinking animation>      |
    | KR-41.1-04  | ✅ / ✗       | <comment on memory shown as percent>                 |
    | KR-41.1-05  | ✅ / ✗       | <comment on slots 0/8>                               |
    | KR-41.1-06  | ✅ / ✗       | <comment on replay rewind disabled>                  |
    | KR-41.1-07  | ✅ / ✗       | <comment on em-dash placeholders in history>         |
    | KR-41.1-08  | ✅ / ✗       | <comment on illustrative error log>                  |
    | KR-41.1-09  | ✅ / ✗       | <comment on confidence-desc Top Influencers sort>    |
    | KR-41.1-10  | ✅ / ✗       | <comment on Rationale citations '—'>                 |
    | KR-41.1-11  | ✅ / ✗       | <comment on interview full-response rendering>       |

    ## New Regressions (BLOCKING — disposition rule per REVIEWS.md item 21)

    **Rule:** Any deviation NOT covered by KR-01..KR-11 is a new regression. Each new regression requires one of:
    1. **Immediate fix** — revision cycle before sign-off (preferred).
    2. **Explicit user approval as KR-41.1-12+ (new KR)** — recorded here with:
       - Reproduction steps
       - Surface
       - Expected vs actual
       - User decision: "ACCEPT as KR-41.1-XX" with rationale

    If zero new regressions: leave this section empty and proceed to sign-off.

    | # | Surface | Steps | Expected | Actual | Disposition (FIX / ACCEPT as KR-XX / DEFER to backlog) | User Signature |
    |---|---------|-------|----------|--------|--------------------------------------------------------|----------------|
    | <leave empty if none> | | | | | | |

    ## User Sign-Off

    - [ ] All 28 rows resolved (✅ or KR-41.1-01..11 documented)
    - [ ] No new regressions — OR — each new regression has explicit disposition (FIX / ACCEPT as KR-XX / DEFER)
    - [ ] Row 26 memory-paused overlay verified DETERMINISTICALLY via tweaks.jsx "Force MemoryPaused" dev button (`as:force-mem-pause` event)
    - [ ] npm run build exit 0 after final commit
    - [ ] npm run check exit 0 after final commit
    - [ ] Console clean during live run (or only documented KRs / approved-acceptable warnings)
    - [ ] frontend-vue-archive/package-lock.json preserved (not regenerated by npm ci)

    Signed off by: <user>
    Date: <YYYY-MM-DD>
    ```

    Step 3 — Walk the 28 rows. For each row:
    1. In old UI (:5174), confirm the value/behavior.
    2. In new UI (:5173), confirm the same (or document the KR reference).
    3. Mark ⬜ → ✅ (match) or ❌ (deviation — either existing KR or new regression per Step 5).

    Step 4 — **Memory-paused overlay (Row 26) — DETERMINISTIC test per REVIEWS.md item 22 / Codex MEDIUM:**

    Do NOT try to force the governor to cross 90% during UAT — that's non-deterministic.

    Instead, use the dev hook installed by Plan 04 Task 3 in `tweaks.jsx`:
    1. Open the new UI at http://localhost:5173.
    2. Open the Tweaks panel (dev-only — hidden in production per KR confirmation below).
    3. Click the "Force MemoryPaused" button. This dispatches `window.dispatchEvent(new CustomEvent('as:force-mem-pause'))`.
    4. Verify `<MemoryPausedState>` overlay appears within one render tick.
    5. Click "Clear Force" to dispatch `as:clear-force-mem-pause` — verify overlay dismisses.
    6. Mark Row 26 ✅ with note: "Verified deterministically via tweaks.jsx force event."

    Step 5 — Capture browser console output during a full 3-round simulation in the NEW UI. Paste errors (if any) and warnings into the "Browser Console" section.

    **React 19 dev-only warnings disposition rule per REVIEWS.md item 21 + Codex LOW on Plan 05:**
    - StrictMode double-render notices → acceptable (non-blocking; will not appear in production build).
    - Hydration mismatch warnings → BLOCKING (indicates bug).
    - `<div> cannot appear as a descendant of <p>` or similar DOM-hygiene warnings → log + user decides: FIX or ACCEPT as new KR-41.1-12.
    - Accessibility warnings (missing aria-*, label for, etc.) → log + user decides.
    - Any ReferenceError / TypeError → BLOCKING.

    Step 6 — If zero new regressions and all 28 rows are ✅ or KR-documented (KR-01..KR-11), mark `status: ready-for-signoff` in the UAT frontmatter and hand to the checkpoint (Task 3).

    Step 7 — Before sign-off, verify the production bundle excludes dev-only code per REVIEWS.md item 19 / Plan 04 Task 1 Step 3:
    ```
    cd frontend && npm run build
    grep -Fl "TweaksPanel" dist/assets/*.js         # must return nothing (empty)
    grep -Fl "as:force-mem-pause" dist/assets/*.js  # must return nothing (empty)
    grep -Fl "HOLDINGS" dist/assets/*.js            # must return nothing (mocks/holdings.ts is DEV-only per Plan 04)
    ```
    If any of these grep outputs are non-empty, the production bundle is leaking dev code — BLOCKING.

    Anti-patterns to avoid:
    - Do NOT run `npm install` in `frontend-vue-archive/` — use `npm ci` to preserve the lockfile (REVIEWS.md item 23 / Codex MEDIUM).
    - Do NOT skip rows. If a row cannot be verified (backend down, etc.), the UAT is not complete — fix the environment first.
    - Do NOT close out the checkpoint with unresolved ⬜ rows.
    - Do NOT fold a new regression into an existing KR entry — a "new" regression means the user was not told about it at CONTEXT time, so it needs explicit disposition (fix / accept as KR-12+ / defer).
    - Do NOT skip the browser-console check. React 19 StrictMode surfaces many lifecycle bugs only in dev.
    - Do NOT rely on the governor crossing 90% during UAT to verify Row 26 — use the deterministic `as:force-mem-pause` hook.
    - Do NOT use `grep` regex on literal strings in the production-bundle leak check — use `grep -Fl` (REVIEWS.md item 24).
  </action>
  <verify>
    <automated>test -f .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md &amp;&amp; test "$(grep -Fc 'Parity Matrix' .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md)" -ge "1" &amp;&amp; test "$(grep -Fc 'KR-41.1-11' .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md)" -ge "1" &amp;&amp; test "$(grep -Fc 'as:force-mem-pause' .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - `41.1-HUMAN-UAT.md` exists with all 28 rows present.
    - Every row has a ✅ or an explicit KR-41.1-0X tag drawn from the 11-entry register (KR-01..KR-11).
    - Zero new regressions logged OR each new regression has full reproduction steps and a proposed disposition (FIX / ACCEPT as KR-12+ / DEFER).
    - Row 26 memory-paused overlay is verified DETERMINISTICALLY via the tweaks.jsx `as:force-mem-pause` dev event — not via hoping the governor crosses 90%.
    - Browser console section is populated with counts (even if zero).
    - `cd frontend && npm run build` exits 0 at the moment the UAT is finalized.
    - `cd frontend && npm run check` exits 0 at the moment the UAT is finalized.
    - Production-bundle leak grep returns empty for `TweaksPanel`, `as:force-mem-pause`, and `HOLDINGS` in `dist/assets/*.js`.
    - `frontend-vue-archive/package-lock.json` is unchanged from its pre-UAT state (npm ci, not npm install).
  </acceptance_criteria>
  <done>41.1-HUMAN-UAT.md is a complete 28-row record with dispositions for every row. Memory-paused overlay verified deterministically. Build + check + bundle-leak checks all pass at the captured moment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on Phase 41.1 parity</name>
  <files>
    (no file edits — this is a checkpoint; Claude runs automation, user confirms)
  </files>
  <action>
    This is a checkpoint:human-verify task. Claude runs the automated preflight listed below (builds, boots, smoke tests), then presents the UAT log for explicit user sign-off.

    <what-built>
    Phase 41.1 implements a full Vue-to-React port:
    - Wave 0 (plan 01): archived Vue frontend, scaffolded React + Vite + TS at frontend/
    - Wave 1 (plan 02): ported 12 design JSX files, built adapter (with cycleId/roundNum promotion), 6 split contexts (TelemetryContext / AgentsContext / BracketContext / EdgesContext / RationalesContext / ConnectionContext), typed ApiError, key-based usePolling, canonical /api/simulate/* routes, useCurrentCycle, WebSocket hook with defensive adaptFrame try/catch
    - Wave 2A (plan 03): wired viz/panels/app_v2/v2 to live contexts — main dashboard consumes edges via EdgesContext (no divergence)
    - Wave 2B (plan 04): wired modals/history/states/settings/tweaks — modals + overlays + cycle picker; api/holdings.ts live fetch; TweaksPanelLoader.tsx with React.lazy + DEV gate; deterministic as:force-mem-pause event
    - Wave 3 (plan 05): updated CLAUDE.md stack line + captured 28-row parity sweep in 41.1-HUMAN-UAT.md against the 11-entry KR register

    Known regressions accepted by user via CONTEXT + REVIEWS.md reconciliation (**11 entries — KR-41.1-01 through KR-41.1-11**):
    - KR-41.1-01 d3-force → deterministic SVG layout
    - KR-41.1-02 SignalWire + DataSources static (no backend)
    - KR-41.1-03 flipped/thinking agent fields stubbed
    - KR-41.1-04 memory displayed as percent (not MB)
    - KR-41.1-05 slots stubbed at 0/8
    - KR-41.1-06 replay rewind disabled (no backend endpoint)
    - KR-41.1-07 cycle history consensus/flips hidden (not in /api/replay/cycles)
    - KR-41.1-08 error log lines illustrative (backend emits no structured errors)
    - KR-41.1-09 Top Influencers sorted by confidence desc (live approximation of CONTRACT §2.7 Report.influence.outDegree, which is a post-cycle report field)
    - KR-41.1-10 Rationale.citations / sources stubbed [] in adapter (backend does not emit citations)
    - KR-41.1-11 InterviewModal non-streaming (full response body rendered when complete)
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md` and read the parity matrix. Confirm all 28 rows are ✅ or tagged with one of KR-41.1-01..11.
    2. Open CLAUDE.md. Confirm the Web Frontend line reads "React + TypeScript" (not "Vue 3 + TypeScript"). Use literal grep:
       ```
       grep -Fn "React + TypeScript" CLAUDE.md
       grep -Fn "Vue 3" CLAUDE.md        # must return nothing
       ```
    3. Boot both UIs side-by-side (preserve Vue archive lockfile per REVIEWS.md item 23):
       - Terminal A: `cd frontend-vue-archive && npm ci && npm run dev` (old Vue on :5174 — archive port, lockfile intact)
       - Terminal B: `cd frontend && npm ci && npm run dev` (new React on :5173 — canonical dev port)
       - Terminal C: `uv run alphaswarm web --reload`
    4. In the new UI (:5173), run a full 3-round simulation with a fresh seed. Confirm:
       - 100 agents render in the graph
       - KPI strip ticks at ~5Hz
       - Bracket counts update
       - Rationale feed streams (citations appear as '—' per KR-41.1-10 — not a regression)
       - Round indicator progresses R1 → R2 → R3 → FINAL
       - Interview modal opens on agent click and returns a (non-streaming) response per KR-41.1-11
       - Report modal generates + renders markdown (sanitized via DOMPurify + marked)
       - Advisory modal generates + renders markdown (sanitized)
       - Shock drawer injects via /api/simulate/shock
       - Cycle picker lists real cycles via /api/replay/cycles
       - Browser console has zero errors; only React 19 dev StrictMode notices allowed
    5. Open tweaks.jsx dev panel (new UI only). Click "Force MemoryPaused" — confirm the overlay appears. Click "Clear Force" — confirm overlay dismisses (REVIEWS.md item 22).
    6. Disconnect the backend (Ctrl-C in Terminal C). Confirm ErrorState overlay renders after reconnect backoff exhausts.
    7. Reconnect. Confirm the overlay clears and the simulation resumes.
    8. Confirm `cd frontend && npm run build` exits 0.
    9. Confirm `cd frontend && npm run check` exits 0.
    10. Confirm production-bundle leak check (REVIEWS.md item 19):
        ```
        cd frontend/dist/assets && grep -Fl "TweaksPanel" *.js     # empty
        grep -Fl "as:force-mem-pause" *.js                          # empty
        grep -Fl "HOLDINGS" *.js                                    # empty
        ```
    11. Confirm `frontend-vue-archive/package-lock.json` is unchanged (git status shows it clean — REVIEWS.md item 23).

    If any step fails OR a new regression surfaces outside KR-01..KR-11, reply `BLOCKED: <description>` and return control to the orchestrator — the phase needs a revision cycle, not sign-off.
  </how-to-verify>
  </action>
  <verify>
    <automated>test "$(grep -Fc 'Vue 3' CLAUDE.md)" = "0" &amp;&amp; test -f .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-HUMAN-UAT.md &amp;&amp; (cd frontend &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -Fq "built in")</automated>
  </verify>
  <done>User signs off with "approved" (or "approved with notes"). CLAUDE.md updated. 41.1-HUMAN-UAT.md committed with all 28 parity rows resolved against the 11-entry KR register. Phase 41.1 marked complete downstream in STATE.md + ROADMAP.md by the orchestrator.</done>
  <resume-signal>
    Type `approved` to mark Phase 41.1 complete.
    Type `blocked: <description>` to trigger a revision cycle.
    Optional: `approved with notes: <text>` to accept the phase but log follow-up items into 41.1-HUMAN-UAT.md.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer edit of CLAUDE.md | Low-risk doc change — single line within a project directives file. |
| Manual UAT observations | Subjective visual parity checks; acceptance criteria bound the subjectivity to the 28-row matrix + 11-entry KR register. |
| Production bundle leak surface | TweaksPanel / HOLDINGS / as:force-mem-pause must not ship to production — verified by grep on dist/assets/*.js. |

## STRIDE Threat Register

| Threat ID       | Category | Component | Disposition | Mitigation Plan |
|-----------------|----------|-----------|-------------|-----------------|
| T-41.1-05-01    | Tampering | (inherited) Report + Advisory markdown | mitigate | Confirmed live during UAT Row 19 + 20 — DOMPurify + marked is the only markdown path (T-41.1-01 from Plan 02). |
| T-41.1-05-02    | Repudiation | UAT sign-off lacks attribution | mitigate | UAT.md frontmatter captures `ran:` timestamp + `environment:` fingerprint; git commit records the user. |
| T-41.1-05-03    | Information Disclosure | CLAUDE.md contents leak tech fingerprint | accept | Tech stack is public by repo visibility; no secrets. |
| T-41.1-05-04    | Information Disclosure | Dev-only code (TweaksPanel, HOLDINGS fixtures, as:force-mem-pause) shipping to production | mitigate | React.lazy + import.meta.env.DEV gate in TweaksPanelLoader.tsx (Plan 04 Task 1); UAT Step 10 grep -Fl on dist/assets/*.js confirms exclusion (REVIEWS.md item 19). |
| T-41.1-05-05    | Tampering | frontend-vue-archive/package-lock.json regeneration during UAT | mitigate | Use `npm ci` not `npm install` in archive (REVIEWS.md item 23); verify `git status frontend-vue-archive/` shows clean lockfile at sign-off. |
| T-41.1-05-06    | Denial of Service | Non-deterministic memory-paused UAT (relying on governor crossing 90%) gives false confidence | mitigate | Deterministic `as:force-mem-pause` dev event (Plan 04 Task 3 tweaks.jsx) — UAT Row 26 verified via button click, not natural threshold crossing (REVIEWS.md item 22). |
</threat_model>

<verification>
Phase 41.1 sign-off is complete when:
- CLAUDE.md Technology Stack line reads "React + TypeScript" and `grep -Fc "Vue 3" CLAUDE.md` returns 0.
- 41.1-HUMAN-UAT.md is populated with all 28 rows resolved (✅ or KR-tagged with KR-41.1-01..11).
- No new regressions outside the KR-41.1-01..11 set — or each new regression has explicit disposition per REVIEWS.md item 21 (FIX / ACCEPT as KR-12+ / DEFER).
- Row 26 memory-paused overlay verified DETERMINISTICALLY via the tweaks.jsx `as:force-mem-pause` dev event (not by hoping the governor crosses 90%).
- `npm run build` + `npm run check` exit 0 in frontend/.
- Production-bundle leak check: `grep -Fl "TweaksPanel" dist/assets/*.js`, `grep -Fl "as:force-mem-pause" dist/assets/*.js`, `grep -Fl "HOLDINGS" dist/assets/*.js` all return empty.
- `frontend-vue-archive/package-lock.json` unchanged from pre-UAT (npm ci, not npm install).
- Checkpoint Task 3 receives `approved` (or `approved with notes: ...`) from the user.
- `frontend-vue-archive/` is present at repo root (rollback path preserved).

Per-task:
- Task 1 (CLAUDE.md): `grep -Fc "Vue 3" CLAUDE.md` returns 0; `grep -Fc "React + TypeScript" CLAUDE.md` ≥ 1.
- Task 2 (UAT): file exists; 28 rows present; all KR references drawn from KR-41.1-01..11; Row 26 verified via force-mem-pause event.
- Task 3 (checkpoint): explicit human `approved` resume signal.
</verification>

<success_criteria>
Plan 05 is complete when:
- `CLAUDE.md` Web Frontend bullet reads "React + TypeScript" (D-15 satisfied).
- `41.1-HUMAN-UAT.md` contains a full 28-row parity matrix signed off by the user, with KR-41.1-01..11 references mapped to every accepted deviation (11-entry register per REVIEWS.md item 1).
- Zero unresolved ⬜ rows, zero undocumented new regressions, explicit disposition logged for any new regression (REVIEWS.md item 21).
- Row 26 verified deterministically via `as:force-mem-pause` (REVIEWS.md item 22).
- All acceptance greps use `grep -F` literal-string matching (REVIEWS.md item 24).
- `cd frontend && npm run build && npm run check` both exit 0 at sign-off time.
- Production-bundle leak check (TweaksPanel / as:force-mem-pause / HOLDINGS) returns empty (REVIEWS.md item 19).
- `frontend-vue-archive/package-lock.json` intact (REVIEWS.md item 23).
- `frontend-vue-archive/` retained on disk (no deletion this phase).
- Phase 41.1 marked complete in STATE.md + ROADMAP.md (orchestrator does the state update post-sign-off).
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-05-SUMMARY.md` referencing the signed 41.1-HUMAN-UAT.md and listing the 11-entry KR register as the canonical reference for any downstream phase that inherits UI regressions.
</output>
