---
phase: 41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac
plan: 02
subsystem: ui
tags: [react, typescript, vite, websocket, context, fastapi-contract, tdd-false]

# Dependency graph
requires:
  - phase: 41.1-01
    provides: React + Vite + TS scaffold, Vue archive, dep install (@vitejs/plugin-react ^5.2.0 pinned), entry points (main.tsx, App.tsx placeholder)
provides:
  - Verbatim port of 11 design JSX components (app, app_v2, viz, panels, modals, settings, history, tweaks, states, icons, v2) under frontend/src/components/
  - Verbatim styles.css copy of Claude Design UI tokens
  - Split mocks (wire.ts / sources.ts / holdings.ts / index.ts) — design data.jsx decomposed per KR-41.1-02
  - Typed ApiError with status/body/path and apiFetch/apiPost client
  - Canonical /api/simulate/* clients (simStart, simStop, simShock, simStatus)
  - Shared hooks: useWebSocket (3-retry backoff + defensive parse), useEdges, useApi, usePolling (key+ref redesign), useCurrentCycle
  - 6-context split: Connection / Telemetry / Agents / Bracket / Rationales / Edges
  - Adapter: StateSnapshot → CONTRACT StateFrame (cycleId/roundNum promoted; defensive shape)
  - App.tsx provider tree mounting all 6 contexts around AppShell; dev server boots clean
affects: [41.1-03-dashboard-surfaces, 41.1-04-holdings-tweaks-report, 41.1-05-uat]

# Tech tracking
tech-stack:
  added: [react-context, react-hooks (custom), websocket client, typed error class]
  patterns:
    - "Provider-per-slice context split (React 18 automatic batching → 1 render per frame)"
    - "DEV-only side-effect smoke import harness (forces tsc/Vite to compile every ported component)"
    - "EdgesProvider owns the single useEdges call; viz.jsx consumes via context (no direct fetch)"
    - "Typed ApiError branching (e instanceof ApiError && e.status === 404)"
    - "Key-driven usePolling with fetchFn held in ref (inline arrows do not restart loop)"
    - "Canonical /api/simulate/* route naming (not /api/sim/*)"

key-files:
  created:
    - "frontend/src/types.ts — CONTRACT-shaped types + ApiError/StateFrame with cycleId/roundNum"
    - "frontend/src/adapter/frame.ts — StateSnapshot → StateFrame (adaptSnapshot); defensive shape"
    - "frontend/src/adapter/agentId.ts — prefix → bracketKey (Q/D/S/M/U/I/A/X/P/W)"
    - "frontend/src/hooks/useWebSocket.ts — /ws/state client, 3 retries after initial, defensive parse"
    - "frontend/src/hooks/useEdges.ts — GET /api/edges/{cycle_id}?round=N"
    - "frontend/src/hooks/usePolling.ts — key+fetchFn redesign, fetchFn in ref"
    - "frontend/src/hooks/useApi.ts — thin fetch wrapper"
    - "frontend/src/hooks/useCurrentCycle.ts — derives cycleId (falls back to /api/replay/cycles[0])"
    - "frontend/src/api/client.ts — ApiError class + apiFetch/apiPost"
    - "frontend/src/api/simulation.ts — simStart/simStop/simShock/simStatus against /api/simulate/*"
    - "frontend/src/context/TelemetryContext.tsx — phase/running/telemetry/ts slice"
    - "frontend/src/context/AgentsContext.tsx — agents[] + consensus"
    - "frontend/src/context/BracketContext.tsx — bracketSummaries[]"
    - "frontend/src/context/RationalesContext.tsx — rationale feed"
    - "frontend/src/context/ConnectionContext.tsx — connected/reconnectFailed/lastFrame"
    - "frontend/src/context/EdgesContext.tsx — owns useEdges; exposes edges + cycleId/round"
    - "frontend/src/components/{app,app_v2,viz,panels,modals,settings,history,tweaks,states,icons,v2}.jsx — verbatim ports"
    - "frontend/src/mocks/{wire,sources,holdings,index}.ts — split from design data.jsx"
    - "frontend/src/styles.css — verbatim design CSS"
    - "frontend/src/_smoke.tsx — DEV-only import harness"
  modified:
    - "frontend/src/App.tsx — 6-context provider tree + _smoke side-effect import"
    - "frontend/src/main.tsx — import './styles.css' (landed in Task 1; Task 3 step 1 was a no-op)"

key-decisions:
  - "ConnectionProvider mounts pre-frame (with lastFrame=null) so banner/placeholder UI can always read connected + reconnectFailed — don't gate the entire tree on first frame arrival"
  - "EdgesProvider takes { round } (not frame) — provider derives cycleId internally via useCurrentCycle; keeps viz.jsx consumer ignorant of cycle source"
  - "useWebSocket return shape: { connected, lastFrame, lastRaw, reconnectFailed } — not the { snapshot, latest, all } shape drafted in plan notes; lastFrame is the adapted StateFrame used by all providers"
  - "EdgesCtxValue.edges kept as Array<[string,string]> tuples to match viz.jsx consumer shape"
  - "DoomPosters bracket prefix = 'X' (states.jsx:81), not 'T' — matches design export"
  - "void import('./_smoke') replaces // @ts-expect-error (TS2578 unused directive) — carried over from Task 1.5"
  - "@vitejs/plugin-react ^5.2.0 pinned (not plan's ^6.0.1) — Vite 6 peer-dep conflict (carried over from Plan 01)"

patterns-established:
  - "Provider-per-slice split → 1 render per WS frame under React 18 automatic batching"
  - "DEV-only _smoke harness → type-check every ported JSX even before mount"
  - "EdgesContext owns fetch; viz.jsx consumes via hook wrapper — single /api/edges call per (cycleId,round) pair"
  - "Canonical /api/simulate/* routing for all sim control"
  - "Typed ApiError with status/body/path for branch-able consumer error handling"

requirements-completed: []

# Metrics
duration: ~90 min (Tasks 1+1.5+2+3 cumulative; Task 3 alone <1 min)
completed: 2026-04-20
---

# Phase 41.1 Plan 02: Shared infra + verbatim JSX port Summary

**React 19 + TS 5.6 app with WebSocket-driven state, 6-context provider split, typed ApiError on canonical /api/simulate/* routes, and verbatim port of 11 design JSX components mounted under a live provider tree.**

## Performance

- **Duration:** ~90 min (cumulative across 3 prior commits + Task 3)
- **Started (Task 3):** 2026-04-20T16:53:57Z
- **Completed:** 2026-04-20T16:54:51Z
- **Tasks:** 3 (Task 1 = JSX+CSS+mocks, Task 2 = infra+adapter+hooks+contexts, Task 3 = App.tsx assembly + smoke)
- **Files touched this plan:** 36 created, 1 modified (App.tsx)

## Accomplishments

- 11 design JSX files ported verbatim under `frontend/src/components/` with `window.*` allowlist enforced (no postMessage bridges, no `window.AS_DATA`)
- 6-context split driven off a single `useWebSocket`-produced StateFrame (React 18 auto-batching → one render per frame)
- Typed `ApiError` + canonical `/api/simulate/*` client surface, ready for Plans 03/04 consumers
- `useCurrentCycle` derives cycle identity from `/api/replay/cycles[0]` fallback when `/api/simulate/status` is not exposed
- `_smoke.tsx` DEV-only harness forces tsc + Vite to bundle every ported component even before AppShell mounts them — catches `ReferenceError`s at build time
- App.tsx mounts the full 6-provider tree (Connection/Telemetry/Agents/Bracket/Rationales/Edges) around AppShell, with placeholder mode pre-frame
- `npm run check` + `npm run build` green; dev server boots in ~94 ms with no console errors

## Task Commits

1. **Task 1: verbatim JSX+CSS port + mocks split** — `cda7094` (feat)
2. **Task 1.5: _smoke.tsx harness** — `c3cfef0` (test)
3. **Task 2: shared wiring infra (types/adapter/hooks/api/6 contexts)** — `6daf397` (feat)
4. **Task 3: App.tsx provider tree assembly + dev smoke** — `adaa938` (feat)

**Plan metadata commit:** (this SUMMARY + STATE + ROADMAP commit — hash appended post-commit)

## Files Created/Modified

See `key-files` in frontmatter — 36 files created, 1 modified (App.tsx).

## Decisions Made

See `key-decisions` in frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] main.tsx `import './styles.css'` already landed in Task 1**
- **Found during:** Task 3 (App.tsx provider wiring)
- **Issue:** Task 3 step 1 in the prompt called for adding `import './styles.css'` to main.tsx, but it was already in place from Task 1.
- **Fix:** Verified via Read; left main.tsx untouched; did not include in the Task 3 commit.
- **Files modified:** none
- **Verification:** `grep "styles.css" frontend/src/main.tsx` returns line 2.
- **Committed in:** n/a (no-op)

**2. [Rule 1 - Bug] useWebSocket return shape did not match prompt's drafted shape**
- **Found during:** Task 3 (App.tsx wiring)
- **Issue:** Prompt's pre-verified state described `{ snapshot, latest, all, connected, reconnectFailed }`; actual `useWebSocket` returns `{ connected, lastFrame, lastRaw, reconnectFailed }`.
- **Fix:** Used `lastFrame` as the frame source (intent matches "most recent StateFrame" note in prompt).
- **Files modified:** `frontend/src/App.tsx`
- **Verification:** `npm run check` green.
- **Committed in:** `adaa938`

**3. [Rule 2 - Missing Critical] App.tsx must pass `reconnectFailed` to ConnectionProvider**
- **Found during:** Task 3
- **Issue:** `ConnectionProvider` signature requires `reconnectFailed` (not optional); plan's `<task_3_plan>` already included it, but flagged explicitly for Plan 03 banner consumers.
- **Fix:** Pass `reconnectFailed` from `useWebSocket` through to `ConnectionProvider` in both placeholder and frame-present branches.
- **Files modified:** `frontend/src/App.tsx`
- **Verification:** tsc green.
- **Committed in:** `adaa938`

### Carried-forward deviations (from earlier tasks, not re-opened in Task 3)

**4. `/api/simulate/status` does not exist on backend** — `useCurrentCycle` derives from `/api/replay/cycles[0]` instead. Committed in `6daf397`.

**5. `simShock(shockText: string)` — backend body is `{shock_text: string}`** — callers build the shock_text string; client takes a string not a full object. Committed in `6daf397`.

**6. DoomPosters bracket prefix = `X` (states.jsx:81), not `T`** — adapter/agentId.ts honors the design export. Committed in `cda7094`.

**7. `EdgesCtxValue.edges` kept as `Array<[string,string]>` tuple** — viz.jsx consumer expects tuples; typed `EdgeView` in types.ts is reference-only. Committed in `6daf397`.

**8. `@vitejs/plugin-react ^5.2.0` pinned (not plan's `^6.0.1`)** — Vite 6 peer-dep conflict. Carried from Plan 01.

**9. `void import('./_smoke')` replaces `// @ts-expect-error`** — TS2578 unused-directive error with current tsconfig strictness. Carried from Task 1.5 (`c3cfef0`).

**10. `EdgesProvider` receives `round` prop (not `frame`)** — provider derives cycleId internally via `useCurrentCycle`; App.tsx passes `lastFrame.roundNum ?? null`. Committed in `6daf397` (provider) + `adaa938` (App wire).

---

**Total deviations:** 10 auto-fixed/carried-forward (3 Task-3 specific, 7 carried from earlier Wave-1 tasks). No scope creep; all fixes necessary for correctness or wiring.

## Known Regressions Touched

| KR | Status | Notes |
|----|--------|-------|
| KR-41.1-02 | tracked | wire/sources mocks live under `frontend/src/mocks/` — Plan 04 swaps to live data |
| KR-41.1-03 | tracked | agent animation stubs still present in viz.jsx verbatim port |
| KR-41.1-04 | tracked | telemetry.memMb currently = governor percent (adapter promotes as-is) |
| KR-41.1-05 | tracked | slots stubbed in ported panels — Plan 04 wires live |
| KR-41.1-10 | tracked | citations/sources stubbed as `[]` in RationalesContext consumers |

## Known Stubs

- AppShell body is a placeholder (`<h1>AlphaSwarm — Wave 1 scaffold</h1>`) — Plans 03 (dashboard surfaces) and 04 (holdings/tweaks/report) mount the real JSX under the provider tree.
- Ported JSX files compile via `_smoke.tsx` harness but are NOT yet mounted in the runtime render path. Intentional — Wave 2 work.

## Contract Links (for Plans 03 / 04)

**Context hooks:**
- `useTelemetry` — `frontend/src/context/TelemetryContext.tsx`
- `useAgents` — `frontend/src/context/AgentsContext.tsx`
- `useBrackets` — `frontend/src/context/BracketContext.tsx`
- `useRationales` — `frontend/src/context/RationalesContext.tsx`
- `useEdgesCtx` — `frontend/src/context/EdgesContext.tsx`
- `useConnection` — `frontend/src/context/ConnectionContext.tsx`

**API clients:**
- `simStart`, `simStop`, `simShock(shockText)`, `simStatus` — `frontend/src/api/simulation.ts`
- `ApiError`, `apiFetch`, `apiPost` — `frontend/src/api/client.ts`

**Adapter:**
- `adaptSnapshot` — `frontend/src/adapter/frame.ts` (already wired through useWebSocket)
- `deriveBracket`, `bracketDisplayName` — `frontend/src/adapter/agentId.ts`

**Mocks (DEV-only consumers):**
- `SIGNAL_WIRE_SEED` — `frontend/src/mocks/wire.ts`
- `DATA_SOURCES`, `SOURCE_STATS` — `frontend/src/mocks/sources.ts`
- `HOLDINGS` — `frontend/src/mocks/holdings.ts`

## Issues Encountered

None beyond the documented deviations.

## Self-Check

- `frontend/src/App.tsx` — FOUND
- Commit `adaa938` — FOUND
- Commit `cda7094` — FOUND
- Commit `c3cfef0` — FOUND
- Commit `6daf397` — FOUND
- `npm run check` — exit 0
- `npm run build` — exit 0
- Dev server — booted in 94 ms, HTML served with `<div id="root">` + `/src/main.tsx`, no errors

**Self-Check: PASSED**

## Next Phase Readiness

- Plan 03 (dashboard surfaces) and Plan 04 (holdings/tweaks/report) can proceed in parallel worktrees — both consume the same 6-context API.
- All contract surfaces frozen at Task 3 commit `adaa938`.
- Wave 1 complete; Wave 2 starts.

---
*Phase: 41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac*
*Plan: 02*
*Completed: 2026-04-20*
