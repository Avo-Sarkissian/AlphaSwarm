---
phase: 41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/src/styles.css                       # verbatim copy of design CSS
  - frontend/src/types.ts                         # CONTRACT-shaped types + backend types (ApiError + StateFrame with cycleId/roundNum)
  - frontend/src/_smoke.tsx                       # dev-only import harness: eagerly references every ported component so Vite type-checks + bundles them
  - frontend/src/components/app_v2.jsx            # verbatim + ESM conversion
  - frontend/src/components/app.jsx               # verbatim + ESM conversion (reference only)
  - frontend/src/components/viz.jsx               # verbatim + ESM conversion; edges read from EdgesContext, NOT local useEdges
  - frontend/src/components/panels.jsx            # verbatim + ESM conversion
  - frontend/src/components/modals.jsx            # verbatim + ESM conversion
  - frontend/src/components/settings.jsx          # verbatim + ESM conversion
  - frontend/src/components/history.jsx           # verbatim + ESM conversion
  - frontend/src/components/tweaks.jsx            # verbatim + ESM conversion
  - frontend/src/components/states.jsx            # verbatim + ESM conversion
  - frontend/src/components/icons.jsx             # verbatim + ESM conversion
  - frontend/src/components/v2.jsx                # verbatim + ESM conversion
  - frontend/src/mocks/wire.ts                    # SIGNAL_WIRE_SEED only (KR-41.1-02)
  - frontend/src/mocks/sources.ts                 # DATA_SOURCES + SOURCE_STATS (KR-41.1-02)
  - frontend/src/mocks/holdings.ts                # HOLDINGS mock (DEV-only; Plan 04 swaps to live /api/holdings)
  - frontend/src/mocks/index.ts                   # small barrel — gated re-exports for tweaks.jsx / v2.jsx only
  - frontend/src/adapter/frame.ts                 # backend StateSnapshot → CONTRACT StateFrame (defensive; cycleId/roundNum promoted)
  - frontend/src/adapter/agentId.ts               # agent-ID prefix → bracket key derivation
  - frontend/src/hooks/useWebSocket.ts            # Vue composable ported; defensive onmessage try/catch; backoff = 3 retries after initial
  - frontend/src/hooks/useEdges.ts                # GET /api/edges/{cycle_id}?round=N — used ONLY by EdgesContext provider
  - frontend/src/hooks/usePolling.ts              # key+fetchFn redesigned; fetchFn held in ref; deps are [key, intervalMs, maxAttempts]
  - frontend/src/hooks/useApi.ts                  # thin fetch wrapper
  - frontend/src/hooks/useCurrentCycle.ts         # derived hook: returns current cycleId from /api/simulation/status (promoted from Plan 03)
  - frontend/src/api/client.ts                    # ApiError class + apiFetch/apiPost — typed errors w/ status/body/path
  - frontend/src/api/simulation.ts                # simStart/simStop/simShock/simStatus — canonical /api/simulation/* (promoted from Plan 03)
  - frontend/src/context/TelemetryContext.tsx     # split from SnapshotContext — phase/running/telemetry/ts only
  - frontend/src/context/AgentsContext.tsx        # split from SnapshotContext — agents[] + consensus
  - frontend/src/context/BracketContext.tsx       # split from SnapshotContext — bracketSummaries[]
  - frontend/src/context/EdgesContext.tsx         # owns useEdges — exposes edges + current cycleId/round
  - frontend/src/context/RationalesContext.tsx
  - frontend/src/context/ConnectionContext.tsx
  - frontend/src/main.tsx                         # import styles.css
  - frontend/src/App.tsx                          # mount 5 providers + AppShell + (DEV-only) _smoke import side-effect
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 12 design JSX files live at frontend/src/components/ with zero `window.*` reads except the narrow allowlist: window.location / window.setTimeout / window.clearTimeout / window.WebSocket / window.parent (only in stripped-postMessage guards)"
    - "All `window.parent.postMessage` Claude-Artifacts bridge calls are removed from every JSX file (no __activate_edit_mode strings survive)"
    - "Design's data.jsx is split into mocks/wire.ts (SignalWire), mocks/sources.ts (DataSources), mocks/holdings.ts (DEV-only Plan 04 seed) — each imported by an EXPLICIT caller; no live render path statically imports mocks/holdings except under import.meta.env.DEV"
    - "useWebSocket hook exposes { snapshot, latest, all, connected, reconnectFailed } with reconnect backoff 1s/2s/4s (**3 retries after initial = 4 total connect attempts** before reconnectFailed=true), phase==='idle' rationale reset, and defensive try/catch around both JSON.parse AND adaptFrame"
    - "adapter/frame.ts remaps backend phase strings (round_1 → 1, round_2 → 2, round_3 → 3, complete → 'done', idle/seeding/replay passthrough) and synthesizes ts, consensus tally, agent array, **cycleId (null if backend does not emit), roundNum (from raw.round_num)**"
    - "adapter/agentId.ts derives bracketKey from agent ID prefix (Q→quants, D→degens, S→sovereigns, M→macro, U→suits, I→insiders, A→agents, X→doom_posters, P→policy_wonks, W→whales)"
    - "Five Context providers wrap AppShell in App.tsx: ConnectionContext, TelemetryContext, AgentsContext, BracketContext, RationalesContext. EdgesContext nests under AgentsContext (needs cycleId/round to fetch). React 18+ automatic batching inside onmessage so a single frame produces ONE render, not five."
    - "EdgesContext owns useEdges(cycleId, round); viz.jsx consumes edges via useEdges hook wrapper that reads from EdgesContext — viz.jsx MUST NOT call useEdges directly and MUST NOT own any fetch of /api/edges"
    - "usePolling is redesigned to { key, fetchFn, intervalMs, maxAttempts, readyWhen, stopWhen? } — the hook's useEffect deps are [key, intervalMs, maxAttempts] (NOT the fetchFn identity); fetchFn is held in a ref so caller-inline arrows do not restart the loop on every render"
    - "api/client.ts exports class ApiError extends Error { status: number; body: unknown; path: string } and apiFetch throws ApiError (not plain Error) so consumers can branch on `e instanceof ApiError && e.status === 404` without string-parsing"
    - "api/simulation.ts exports simStart / simStop / simShock / simStatus — all hit **canonical** /api/simulate/* paths matching backend (see Canonical Route Table below)"
    - "useCurrentCycle() returns `{ cycleId: string | null, roundNum: number | null }` derived from /api/simulate/status (polled once + refreshed on WS phase transitions); fallback to /api/replay/cycles[0] if status endpoint not exposed"
    - "frontend/src/styles.css is verbatim copy of Claude Design UI/AlphaSwarm/styles.css — every CSS custom property and rule preserved"
    - "frontend builds (npm run build) AND type-checks (npm run check) without TypeScript or Vite errors after _smoke.tsx import harness has eagerly referenced every ported component"
    - "dompurify+marked remain the only markdown→HTML path for rationale/report content (T-41.1-01 mitigation stays live from Wave 1 onwards)"
  artifacts:
    - path: "frontend/src/styles.css"
      provides: "Design system CSS tokens + layout, verbatim from Claude Design UI/AlphaSwarm/styles.css"
    - path: "frontend/src/hooks/useWebSocket.ts"
      provides: "React hook with reconnect backoff (3 retries after initial), rationale dedup+cap 20, phase-idle clear, teardown null-of-onclose, defensive onmessage try/catch"
      exports: ["useWebSocket"]
    - path: "frontend/src/adapter/frame.ts"
      provides: "Backend StateSnapshot → CONTRACT StateFrame remap with cycleId/roundNum promotion + defensive shape validation"
      exports: ["adaptFrame"]
    - path: "frontend/src/adapter/agentId.ts"
      provides: "Agent-ID prefix → bracket key/display derivation"
      exports: ["deriveBracket", "bracketDisplayName"]
    - path: "frontend/src/context/TelemetryContext.tsx"
      provides: "React Context for phase/running/telemetry/ts/cycleId/roundNum only"
      exports: ["TelemetryContext", "useTelemetry"]
    - path: "frontend/src/context/AgentsContext.tsx"
      provides: "React Context for agents[] and consensus"
      exports: ["AgentsContext", "useAgents"]
    - path: "frontend/src/context/BracketContext.tsx"
      provides: "React Context for bracketSummaries[]"
      exports: ["BracketContext", "useBrackets"]
    - path: "frontend/src/context/EdgesContext.tsx"
      provides: "React Context for edges + cycleId/round drive; owns the single useEdges call for the app"
      exports: ["EdgesContext", "EdgesProvider", "useEdgesCtx"]
    - path: "frontend/src/context/RationalesContext.tsx"
      provides: "React Context for the rationale feed"
      exports: ["RationalesContext", "useRationales"]
    - path: "frontend/src/context/ConnectionContext.tsx"
      provides: "React Context for ws connection status"
      exports: ["ConnectionContext", "useConnection"]
    - path: "frontend/src/hooks/usePolling.ts"
      provides: "Key-driven polling hook: callers pass `key` (rerun trigger) + `fetchFn` (callback form, T|null = pending). fetchFn held in ref so inline arrows do not re-run the loop."
      exports: ["usePolling"]
    - path: "frontend/src/hooks/useEdges.ts"
      provides: "fetches /api/edges/{cycleId}?round=N — called once, from EdgesProvider"
      exports: ["useEdges"]
    - path: "frontend/src/hooks/useCurrentCycle.ts"
      provides: "derives `{ cycleId, roundNum }` from /api/simulate/status + telemetry context; single source of truth for both Plan 03 (CycleHistory) and Plan 04 (Report/Advisory/EdgesProvider)"
      exports: ["useCurrentCycle"]
    - path: "frontend/src/api/client.ts"
      provides: "Shared fetch wrapper + typed ApiError class"
      exports: ["apiFetch", "apiPost", "ApiError"]
    - path: "frontend/src/api/simulation.ts"
      provides: "simStart / simStop / simShock / simStatus — canonical /api/simulate/* paths"
      exports: ["simStart", "simStop", "simShock", "simStatus"]
    - path: "frontend/src/App.tsx"
      provides: "Root with five Context providers wrapping AppShell"
      exports: ["App"]
    - path: "frontend/src/types.ts"
      provides: "CONTRACT StateFrame/Agent/Rationale/BracketSummary + backend types + ApiError type"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/hooks/useWebSocket.ts"
      via: "const { snapshot, latest, all, connected, reconnectFailed } = useWebSocket()"
      pattern: "useWebSocket\\("
    - from: "frontend/src/hooks/useWebSocket.ts"
      to: "frontend/src/adapter/frame.ts"
      via: "adaptFrame(raw) on every WS message, inside try/catch"
      pattern: "adaptFrame\\("
    - from: "frontend/src/main.tsx"
      to: "frontend/src/styles.css"
      via: "import './styles.css'"
      pattern: "import './styles.css'"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/app_v2"
      via: "import { AppShell } from './components/app_v2'"
      pattern: "from './components/app_v2'"
    - from: "frontend/src/context/EdgesContext.tsx"
      to: "frontend/src/hooks/useEdges.ts"
      via: "EdgesProvider calls useEdges(cycleId, round) once; viz consumes via useEdgesCtx()"
      pattern: "useEdges\\("
    - from: "frontend/src/api/simulation.ts"
      to: "frontend/src/api/client.ts"
      via: "apiPost/apiFetch — throws ApiError"
      pattern: "ApiError"

known_regressions:
  - id: KR-41.1-01
    title: "D3 force layout tuning parity not guaranteed (design verbatim)"
    description: "Design's viz.jsx uses pure-math radial/cluster layout (useMemo-cached) instead of d3-force simulation. Current Vue ForceGraph.vue uses forceSimulation/forceManyBody/forceCollide. Visual 'feel' under live 100-agent data may differ — clusters deterministic, no physics wobble."
    per: "D-06 verbatim drop + RESEARCH §Pattern 2 + RESEARCH §Assumption A2"
    accepted_by_user: "D-06 locks verbatim drop"
    revisit_trigger: "If Wave 3 parity sweep shows live data clusters badly, tune the layout math inside viz.jsx rather than reintroducing d3-force"
  - id: KR-41.1-02
    title: "SignalWire ticker + DataSources modal render from static mocks"
    description: "Backend has no /api/wire or /api/sources endpoint (CONTRACT §2.4/§2.5 unimplemented). v2.jsx SignalWire + DataSourcesModal render seeded fixtures from mocks/wire.ts + mocks/sources.ts until backend support lands."
    per: "RESEARCH §Pitfall 9 + CONTEXT deferred (implicit)"
    accepted_by_user: "Deferred"
    revisit_trigger: "When a phase adds /api/wire and /api/sources"
  - id: KR-41.1-03
    title: "Agent flipped / roundLastSpoke / thinking are stubbed"
    description: "CONTRACT Agent type has flipped, roundLastSpoke, thinking fields. Backend does not emit them. Adapter stubs flipped=false, roundLastSpoke=currentRound, thinking=false."
    per: "RESEARCH §Open Question 4"
    accepted_by_user: "Implicit"
    revisit_trigger: "Post-parity if user flags missing flip/thinking animations as regressions"
  - id: KR-41.1-04
    title: "telemetry.memMb shows governor percent, not megabytes"
    description: "CONTRACT field is memMb; backend emits memory_percent. Adapter passes percent through; UI label must read '%'."
    per: "RESEARCH §Open Question 1 + §Pitfall (mem unit)"
    accepted_by_user: "Implicit"
    revisit_trigger: "User requests MB display — compute Math.round(48 * percent / 100) + ' GB' in adapter"
  - id: KR-41.1-05
    title: "telemetry.slotsUsed/slotsMax stubbed to 0/8"
    description: "Backend does not emit slot usage in snapshot. Adapter returns {slotsUsed: 0, slotsMax: 8}. KPI shows '0/8' constantly."
    per: "RESEARCH §Open Question 2"
    accepted_by_user: "Implicit"
    revisit_trigger: "Backend adds /api/telemetry/slots or embeds slotsUsed in StateSnapshot"
  - id: KR-41.1-10
    title: "Rationale citations / sources stubbed as empty arrays"
    description: "The WebSocket `rationale_entries` payload does not include citations/sources fields. Adapter / useWebSocket synthesise `citations: []`, `sources: []` so CONTRACT Rationale type stays stable. Advisory / rationale modals render 'No sources' / hidden citation chip."
    per: "User pre-authorized decision point (ii) (CONTEXT.md known_regressions)"
    accepted_by_user: "User locked at pre-planning — see CONTEXT.md KR-41.1-10"
    revisit_trigger: "Backend enhancement that emits `citations`/`sources` per rationale and matching React polish"
---

<objective>
Copy all 12 design JSX files into `frontend/src/components/` verbatim, convert their `window.*` globals to ES module imports (excluding the narrow dev/browser allowlist: `window.location`, `window.setTimeout`, `window.clearTimeout`, `window.WebSocket`), strip the Claude-Artifacts `window.parent.postMessage` bridge, and stand up the **shared wiring infrastructure** (WebSocket hook, adapter, **five** Contexts, polling+edges+API hooks, **canonical simulation client**, CONTRACT types, shared fetch client with **typed ApiError**). This plan produces everything downstream plans 03 and 04 need; by design, Plans 03 and 04 now do only per-surface wiring and never define new clients, hooks, or contexts.

Purpose: D-06 locks verbatim drop of the design JSX; the only edits allowed are (a) ESM conversion, (b) wiring imports. This plan performs (a) + ALL shared wiring-target infrastructure in bulk so Waves 2–3 are pure component-by-component wiring and UAT.

### Parallel Contract (what this plan exports — Plans 03 + 04 consume, never redefine)

| Export | Location | Consumed by |
|---|---|---|
| `apiFetch`, `apiPost`, `ApiError` | `frontend/src/api/client.ts` | Plan 03 (history/cycles), Plan 04 (report, advisory, holdings) |
| `simStart`, `simStop`, `simShock`, `simStatus` | `frontend/src/api/simulation.ts` | Plan 04 (TopBar wire-up for start/stop/shock/report buttons) |
| `useWebSocket` | `frontend/src/hooks/useWebSocket.ts` | `App.tsx` (this plan), not re-called elsewhere |
| `useCurrentCycle` | `frontend/src/hooks/useCurrentCycle.ts` | Plan 03 (CycleHistory highlights active cycle), Plan 04 (Report/Advisory modal cycleId) |
| `usePolling` | `frontend/src/hooks/usePolling.ts` | Plan 04 (report + advisory polling) |
| `useEdges` | `frontend/src/hooks/useEdges.ts` | `EdgesProvider` in this plan only |
| `TelemetryContext`, `AgentsContext`, `BracketContext`, `EdgesContext`, `RationalesContext`, `ConnectionContext` + `useTelemetry`/`useAgents`/`useBrackets`/`useEdgesCtx`/`useRationales`/`useConnection` | `frontend/src/context/*.tsx` | Plans 03 + 04 consume via hooks only; never subscribe to the broader `SnapshotContext` (it no longer exists) |

**Rule:** If Plans 03 or 04 need a new client, a new context, or a new hook, revise this plan FIRST. Do not duplicate wiring surfaces.

Output: A React app root that compiles, type-checks, renders the design's AppShell, reads a live backend WebSocket through the five-Context provider tree, exposes a typed simulation client + current-cycle hook, and surfaces a `KR-41.1-*` register matching CONTEXT.md `<known_regressions>` for the UAT sweep in Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-VALIDATION.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-REVIEWS.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-01-PLAN.md
@Claude Design UI/AlphaSwarm/CONTRACT.md
@Claude Design UI/AlphaSwarm/styles.css
@Claude Design UI/AlphaSwarm/src/app_v2.jsx
@Claude Design UI/AlphaSwarm/src/viz.jsx
@Claude Design UI/AlphaSwarm/src/panels.jsx
@Claude Design UI/AlphaSwarm/src/modals.jsx
@Claude Design UI/AlphaSwarm/src/states.jsx
@Claude Design UI/AlphaSwarm/src/history.jsx
@Claude Design UI/AlphaSwarm/src/settings.jsx
@Claude Design UI/AlphaSwarm/src/tweaks.jsx
@Claude Design UI/AlphaSwarm/src/icons.jsx
@Claude Design UI/AlphaSwarm/src/data.jsx
@Claude Design UI/AlphaSwarm/src/v2.jsx
@Claude Design UI/AlphaSwarm/src/app.jsx
@frontend-vue-archive/src/composables/useWebSocket.ts
@frontend-vue-archive/src/types.ts
@src/alphaswarm/state.py
@src/alphaswarm/web/broadcaster.py
@src/alphaswarm/web/app.py
@src/alphaswarm/web/routes/simulation.py
@src/alphaswarm/web/routes/websocket.py
</context>

<canonical_route_table>

## Canonical API Route Table (verified against backend)

Grepped from `src/alphaswarm/web/routes/*.py` + `src/alphaswarm/web/app.py` at revision `284c76a` (CONTEXT.md commit) — router prefix `/api` is applied via `app.include_router(..., prefix="/api")`. **All frontend requests MUST use these exact paths.** No `/api/simulation/*` aliases; the backend router is named `simulation` and its endpoints are `/simulate/start`, `/simulate/stop`, `/simulate/shock`. Combined with the `/api` prefix the **canonical wire paths** are:

| Surface | HTTP | Canonical path | Notes |
|---|---|---|---|
| Simulation start | POST | `/api/simulate/start` | Body: `{ seed: string }` |
| Simulation stop | POST | `/api/simulate/stop` | No body |
| Simulation shock | POST | `/api/simulate/shock` | Body: `{ kind, magnitude, text }` |
| Simulation status | GET | `/api/simulate/status` | Check `src/alphaswarm/web/routes/simulation.py` — if no `status` route exists, `useCurrentCycle` falls back to `GET /api/replay/cycles` and reads the first entry (documented in Task 2 Step 4). |
| Edges | GET | `/api/edges/{cycle_id}?round=N` | `cycle_id` may be literal `"current"` |
| Replay cycle list | GET | `/api/replay/cycles` | Also used as `useCurrentCycle` fallback when `/api/simulate/status` not exposed |
| Replay start | POST | `/api/replay/start/{cycle_id}` | |
| Replay advance | POST | `/api/replay/advance` | |
| Replay stop | POST | `/api/replay/stop` | |
| Interview | POST | `/api/interview/{agent_id}` | |
| Holdings | GET | `/api/holdings` | Plan 04 wires |
| Health | GET | `/api/health` | |
| Report GET | GET | `/api/report/{cycle_id}` | 404 = still generating (404 → null in `reportFetch`) |
| Report generate | POST | `/api/report/{cycle_id}/generate` | Returns 202 |
| Advisory generate | POST | `/api/advisory/{cycle_id}` | Returns 202 |
| Advisory GET | GET | `/api/advisory/{cycle_id}` | 404 = still generating |

The exact backend routes used by `api/simulation.ts`:
- `simStart` → `POST /api/simulate/start`
- `simStop` → `POST /api/simulate/stop`
- `simShock` → `POST /api/simulate/shock`
- `simStatus` → `GET /api/simulate/status` (if exposed; otherwise fallback — see `useCurrentCycle` below)

Any future plan that needs a new frontend→backend route MUST:
1. Update this Canonical Route Table.
2. Add the helper to `api/simulation.ts` (simulation surface) or its own file — never inline fetch paths in components.
3. Reference the path by constant, not by string literal in the component.

</canonical_route_table>

<interfaces>
<!-- CONTRACT types (target) vs backend snapshot (source) — adapter sits between. -->

CONTRACT §2.1 Agent:
  interface Agent {
    id: string;
    bracket: BracketKey;
    bracketDisplay: string;
    signal: 'buy'|'sell'|'hold'|'parse_error' | null;
    confidence: number;
    flipped: boolean;
    flipFrom?: Signal;
    thinking: boolean;
    roundLastSpoke: 1|2|3|null;
  }
  type BracketKey = 'quants'|'degens'|'sovereigns'|'macro'|'suits'|'insiders'|'agents'|'doom_posters'|'policy_wonks'|'whales';

CONTRACT §3 StateFrame (**extended** in this port — cycleId/roundNum promoted for reviewer item 8):
  interface StateFrame {
    ts: number | string;
    phase: 1|2|3|'done'|'idle'|'seeding'|'replay';
    running: boolean;
    cycleId: string | null;      // NEW: null if backend does not emit it in the live frame
    roundNum: number | null;     // NEW: mirrors raw.round_num; null if absent
    agents: Agent[];
    consensus: { buy: number; sell: number; hold: number; dominant: Signal };
    telemetry: { tps: number; memMb: number; slotsUsed: number; slotsMax: number; elapsedSec: number };
    edges: Array<[string, string]>;
  }

Backend broadcaster (verified at revision 284c76a — `src/alphaswarm/web/broadcaster.py` calls `json.dumps(dataclasses.asdict(state_store.snapshot()))` where `StateSnapshot` is defined in `src/alphaswarm/state.py` and contains `round_num: int` **but NO `cycle_id` field**; `ReplayStore.snapshot()` also has no `cycle_id` in the emitted dict since it is stored on the store itself). **Therefore the live `/ws/state` frame does NOT currently carry `cycle_id`.** The adapter sets `cycleId = null` by default; `useCurrentCycle` fills the gap via REST. If a later phase adds `cycle_id` to the broadcaster payload, the adapter starts reading it and `useCurrentCycle` becomes redundant.

CONTRACT §2.3 Rationale:
  interface Rationale {
    id: string;
    ts: string;
    agentId: string;
    round: 1|2|3;
    signal: Signal;
    confidence: number;
    text: string;
    citations: string[];  // KR-41.1-10 — stubbed []
    sources: string[];    // KR-41.1-10 — stubbed []
  }

ApiError (new, reviewer item 4):
  class ApiError extends Error {
    readonly status: number;        // HTTP status from the failed response
    readonly body: unknown;         // parsed JSON body if Content-Type was application/json, else raw text
    readonly path: string;          // request path (so callers can branch on which endpoint failed)
    constructor(status: number, path: string, body: unknown, message?: string);
  }

Allowed `window.*` usages in ported JSX (narrow allowlist — reviewer item 16):
  window.location.*           — used by useWebSocket for protocol sniff
  window.WebSocket            — useWebSocket only
  window.setTimeout / clearTimeout — useWebSocket reconnect timer + usePolling tick
  window.parent               — ONLY to confirm absence (never a read; the postMessage callsite is stripped)
  window.React                — FORBIDDEN (drop the global read)
  window.AS_DATA              — FORBIDDEN (mocks moved to ESM imports)
  window.parent.postMessage   — FORBIDDEN (Claude-Artifacts bridge; strip Rule 5)

Current Vue composable (port target — from frontend-vue-archive/src/composables/useWebSocket.ts):
  - exposes: { snapshot, connected, reconnectFailed, latestRationales, allRationales }
  - reconnect: exponential backoff 1s/2s/4s/8s, 3-attempt cap
  - rationale dedup: composite key `${agent_id}:${round_num}`, cap 20
  - phase-idle watcher: clears allRationales when phase==='idle'
  - onUnmounted: clears reconnect timer, nulls ws.onclose, closes socket

Agent-ID prefix map (RESEARCH §Open Question 3):
  Q → quants (display "Quants")
  D → degens (display "Degens")
  S → sovereigns (display "Sovereigns")
  M → macro (display "Macro")
  U → suits (display "Suits")
  I → insiders (display "Insiders")
  A → agents (display "Agents")
  X → doom_posters (display "Doom-Posters")
  P → policy_wonks (display "Policy Wonks")
  W → whales (display "Whales")
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop 12 JSX files + CSS + split mocks verbatim; convert window.* globals (narrow allowlist); strip postMessage bridge</name>
  <files>
    frontend/src/styles.css
    frontend/src/components/app_v2.jsx
    frontend/src/components/app.jsx
    frontend/src/components/viz.jsx
    frontend/src/components/panels.jsx
    frontend/src/components/modals.jsx
    frontend/src/components/settings.jsx
    frontend/src/components/history.jsx
    frontend/src/components/tweaks.jsx
    frontend/src/components/states.jsx
    frontend/src/components/icons.jsx
    frontend/src/components/v2.jsx
    frontend/src/mocks/wire.ts
    frontend/src/mocks/sources.ts
    frontend/src/mocks/holdings.ts
    frontend/src/mocks/index.ts
    frontend/src/main.tsx                          # add `import './styles.css';`
  </files>
  <read_first>
    - Claude Design UI/AlphaSwarm/src/app_v2.jsx
    - Claude Design UI/AlphaSwarm/src/viz.jsx
    - Claude Design UI/AlphaSwarm/src/panels.jsx
    - Claude Design UI/AlphaSwarm/src/modals.jsx
    - Claude Design UI/AlphaSwarm/src/history.jsx
    - Claude Design UI/AlphaSwarm/src/states.jsx
    - Claude Design UI/AlphaSwarm/src/settings.jsx
    - Claude Design UI/AlphaSwarm/src/tweaks.jsx
    - Claude Design UI/AlphaSwarm/src/icons.jsx
    - Claude Design UI/AlphaSwarm/src/data.jsx
    - Claude Design UI/AlphaSwarm/src/v2.jsx
    - Claude Design UI/AlphaSwarm/src/app.jsx
    - Claude Design UI/AlphaSwarm/styles.css
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md <known_regressions>
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md §Pattern 3 (conversion rules 1-5) §Per-JSX Wiring Plan §Pitfall 4 (postMessage leak) §Pitfall 5 (mocks vs live brackets)
  </read_first>
  <action>
    This task is 95% mechanical: copy files, apply 5 sed-style conversions per file, strip postMessage, SPLIT mocks into three narrow files. DO NOT add any wiring logic — Task 2 builds infra; Wave 2 consumes.

    Step A — Copy CSS (verbatim):
    ```
    cp "Claude Design UI/AlphaSwarm/styles.css" frontend/src/styles.css
    ```
    Then add `import './styles.css';` as the SECOND line of `frontend/src/main.tsx` (after `import React from 'react';`). Run `cd frontend && npm run build` — it must still pass (styles.css is plain CSS, Vite handles it).

    Step B — Copy all 12 JSX files:
    ```
    mkdir -p frontend/src/components frontend/src/mocks
    cp "Claude Design UI/AlphaSwarm/src/app_v2.jsx" frontend/src/components/app_v2.jsx
    cp "Claude Design UI/AlphaSwarm/src/app.jsx"    frontend/src/components/app.jsx
    cp "Claude Design UI/AlphaSwarm/src/viz.jsx"    frontend/src/components/viz.jsx
    cp "Claude Design UI/AlphaSwarm/src/panels.jsx" frontend/src/components/panels.jsx
    cp "Claude Design UI/AlphaSwarm/src/modals.jsx" frontend/src/components/modals.jsx
    cp "Claude Design UI/AlphaSwarm/src/settings.jsx" frontend/src/components/settings.jsx
    cp "Claude Design UI/AlphaSwarm/src/history.jsx"  frontend/src/components/history.jsx
    cp "Claude Design UI/AlphaSwarm/src/tweaks.jsx"   frontend/src/components/tweaks.jsx
    cp "Claude Design UI/AlphaSwarm/src/states.jsx"   frontend/src/components/states.jsx
    cp "Claude Design UI/AlphaSwarm/src/icons.jsx"    frontend/src/components/icons.jsx
    cp "Claude Design UI/AlphaSwarm/src/v2.jsx"       frontend/src/components/v2.jsx
    ```
    Do NOT copy `data.jsx` verbatim into one file — instead, split its exports into three narrow modules (reviewer item 18).

    Step B.1 — **Split mocks into narrow files (reviewer item 18 — tree-shake-friendly)**:

    `frontend/src/mocks/wire.ts` — contains ONLY `SIGNAL_WIRE_SEED`:
    ```ts
    // KR-41.1-02: static wire ticker seed used by v2.jsx SignalWire component.
    export const SIGNAL_WIRE_SEED: ReadonlyArray<{ id: string; ts: string; src: string; text: string }> = [
      // copy verbatim entries from Claude Design UI/AlphaSwarm/src/data.jsx SIGNAL_WIRE_SEED
    ];
    ```

    `frontend/src/mocks/sources.ts` — contains `DATA_SOURCES` + `SOURCE_STATS`:
    ```ts
    // KR-41.1-02: static data-sources + source-stats fixtures used by v2.jsx DataSourcesModal.
    export const DATA_SOURCES = [
      // copy verbatim from data.jsx DATA_SOURCES
    ];
    export const SOURCE_STATS = {
      // copy verbatim from data.jsx SOURCE_STATS
    };
    ```

    `frontend/src/mocks/holdings.ts` — contains `HOLDINGS` **and is imported ONLY inside `import.meta.env.DEV` guards** so production bundles tree-shake it out (reviewer item 18):
    ```ts
    // DEV-ONLY fixture used by tweaks.jsx to preview the holdings modal during development.
    // Plan 04 swaps production holdings to /api/holdings. This file MUST NOT be statically
    // imported anywhere on the live render path outside an `import.meta.env.DEV` block.
    export const HOLDINGS = [
      // copy verbatim from data.jsx HOLDINGS
    ];
    ```

    `frontend/src/mocks/index.ts` — a narrow barrel that re-exports ONLY wire + sources (the mocks that ship in production per KR-41.1-02). `holdings.ts` is imported DYNAMICALLY so dead-code elimination kicks in:
    ```ts
    export { SIGNAL_WIRE_SEED } from './wire';
    export { DATA_SOURCES, SOURCE_STATS } from './sources';
    // holdings intentionally NOT re-exported here — it is DEV-only.
    // Dev consumer pattern (inside a component):
    //   if (import.meta.env.DEV) {
    //     const { HOLDINGS } = await import('../mocks/holdings');
    //     ...
    //   }
    ```

    Also DELETE `data.jsx` wholesale (the verbatim copy is no longer needed — everything is now in the three narrow files). This replaces the old "cp data.jsx → mocks/data.jsx" step.

    Step C — Apply the 5 ESM conversion rules from RESEARCH §Pattern 3 to EACH of the 12 JSX files:

    Rule 1. **React destructuring → named imports.**
      Find: `const { useState, useEffect, useRef, useMemo, useCallback, useLayoutEffect } = React;`
      Replace with: `import { useState, useEffect, useRef, useMemo, useCallback, useLayoutEffect } from 'react';` (only hooks actually used in that file — grep first)
      Drop any `const React = window.React;` line.

    Rule 2. **Icon / cross-file component imports.** Same mapping as original plan — convert `window.<Component>` reads to relative ESM imports.

    Rule 3. **Mock data reads — narrow allowlist (reviewer items 16 + 18).**
      Convert `const { ... } = window;` / `const { ... } = window.AS_DATA;` to explicit imports FROM THE NARROW FILES.

      - `SIGNAL_WIRE_SEED` → `import { SIGNAL_WIRE_SEED } from '../mocks/wire';`
      - `DATA_SOURCES` / `SOURCE_STATS` → `import { DATA_SOURCES, SOURCE_STATS } from '../mocks/sources';`
      - `HOLDINGS` — **only allowed inside `import.meta.env.DEV`** blocks (reviewer item 18). Must be dynamically imported:
        ```js
        if (import.meta.env.DEV) {
          import('../mocks/holdings').then(({ HOLDINGS }) => { /* dev seed */ });
        }
        ```
      - `BRACKETS`, `buildAgents`, `RATIONALES`, `REPLAY_CYCLES` — **DO NOT import from mocks on the live render path**; those names were only used in the design to seed initial state before WS was wired. For Wave 1, remove the import + the callsite; the component will temporarily render zero rows until Wave 2 wires it to the contexts. Mark each removal with a `// WAVE-1-NOTE:` comment.

      **Exception — live render path MUST NOT read mocks.** The only components allowed to statically import from `../mocks/wire` or `../mocks/sources` are:
        - `v2.jsx` SignalWire (imports `SIGNAL_WIRE_SEED` — KR-41.1-02)
        - `v2.jsx` DataSourcesModal (imports `DATA_SOURCES`, `SOURCE_STATS` — KR-41.1-02)
        - `tweaks.jsx` (DEV-only; all mock uses guarded by `import.meta.env.DEV`)

      Enforce via grep in the acceptance criteria.

    Rule 4. **Exports — drop Object.assign(window, ...).**
      Find: `Object.assign(window, { Foo, Bar, Baz });`
      Replace with: `export { Foo, Bar, Baz };`

    Rule 5. **Strip Claude-Artifacts bridge.**
      Find ANY of: `window.parent.postMessage`, `__activate_edit_mode`, `postMessage({ type: '__activate_edit_mode' }`
      Delete the entire statement. If the parent `useEffect` body becomes empty, delete the `useEffect`.

    **Narrow `window.*` allowlist (reviewer item 16):** The grep assertions in acceptance criteria MUST permit these readings (they are legitimate browser APIs, not globals-as-state):
      - `window.location` (useWebSocket protocol sniff)
      - `window.setTimeout`, `window.clearTimeout` (reconnect timer + poll tick)
      - `window.WebSocket` (useWebSocket constructor)
    But FORBID:
      - `window.parent.postMessage`, `__activate_edit_mode`
      - `window.AS_DATA`, `window.React`
      - Any other `window.<PascalCase>` component read
      - Any `window.<lowerCase>` mock-data read not in the allowlist

    Step D — Per-file nuances (from RESEARCH §Per-JSX Wiring Plan):

    - **app.jsx** — kept for reference; ADD `// WAVE-1-NOTE: app.jsx is a design reference. Not imported by App.tsx. app_v2.jsx is the mounted shell.` Apply all 5 rules.

    - **tweaks.jsx** — wrap ALL mock-reading logic behind `if (import.meta.env.DEV) { ... }`. Dynamic-import `../mocks/holdings`; static-import from `../mocks/wire` / `../mocks/sources` is permissible ONLY inside a DEV block. If Vite's dead-code elimination is sensitive, prefer dynamic imports throughout so the mock files don't ship in prod at all.

    - **v2.jsx** — SignalWire + DataSourcesModal keep static imports from `../mocks/wire` + `../mocks/sources` (KR-41.1-02). ReportModalV2 gets live-wired in Wave 2 Plan 04.

    - **viz.jsx** — **reviewer item 9:** Remove any local `useEdges` definition or inline fetch of `/api/edges/...`; read edges from `useEdgesCtx()` (the context provided by EdgesContext in Task 2 Step 10). Add `// WAVE-1-NOTE: edges read from EdgesContext — do NOT re-introduce local useEdges here` at the top.

    - **states.jsx** — keep overlay components pure presentational (props in, JSX out).

    Step E — Verify the build survives:
    ```
    cd frontend && npm run build
    ```
    Expected: exit 0. Any TS/JSX error points to a missed `window.*` reference or a circular import.

    Anti-patterns:
    - Do NOT statically import `../mocks/holdings` anywhere (reviewer item 18).
    - Do NOT leave a single `mocks/data.jsx` behind; it was split into `wire.ts` / `sources.ts` / `holdings.ts`.
    - Do NOT re-introduce `@babel/standalone` / `type="text/babel"`.
    - Do NOT touch `frontend-vue-archive/`.
    - Do NOT rewire bracket counts; `BRACKETS` from mocks is NOT imported on live render path (Wave 2 wires BracketContext).
    - Do NOT leave `window.AS_DATA` / `window.React` reads anywhere.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build 2&gt;&amp;1 | tail -20 | grep -q "built in" &amp;&amp; ! grep -rn "window\.AS_DATA\|window\.React\|window\.parent\.postMessage\|__activate_edit_mode" src/components/*.jsx src/mocks/*.ts 2&gt;/dev/null &amp;&amp; test -f src/styles.css &amp;&amp; test -f src/mocks/wire.ts &amp;&amp; test -f src/mocks/sources.ts &amp;&amp; test -f src/mocks/holdings.ts &amp;&amp; ! test -f src/mocks/data.jsx &amp;&amp; test $(ls src/components/*.jsx | wc -l) -eq 11</automated>
    <manual>Spot-check one component (e.g. `cat frontend/src/components/panels.jsx | head -10`) — should begin with `import { useState, ... } from 'react';`, no `const React = window.React;`.</manual>
  </verify>
  <done>
    - `frontend/src/styles.css` exists (verbatim copy).
    - `main.tsx` imports `./styles.css` as its first non-React import.
    - 11 JSX files in `frontend/src/components/` (app_v2, app, viz, panels, modals, settings, history, tweaks, states, icons, v2).
    - `frontend/src/mocks/wire.ts`, `sources.ts`, `holdings.ts`, `index.ts` exist; `data.jsx` does NOT.
    - `grep -rn "window\.AS_DATA\|window\.React\|window\.parent\.postMessage\|__activate_edit_mode" frontend/src/components/*.jsx frontend/src/mocks/*.ts` → empty.
    - `grep -rn "window\." frontend/src/components/*.jsx` → only lines matching the narrow allowlist (`window.location`, `window.setTimeout`, `window.clearTimeout`, `window.WebSocket`).
    - `grep -l "from '\.\./mocks/holdings'" frontend/src/components/*.jsx` → either empty OR only returns `tweaks.jsx`, and every match inside `tweaks.jsx` is wrapped in an `import.meta.env.DEV` guard.
    - `npm run build` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.5: Smoke-test the converted JSX tree via a temporary import harness</name>
  <what-built>
    The 12-file verbatim drop + ESM conversion (Task 1) compiles — but `npm run build` alone does NOT catch runtime `ReferenceError`s for components that are declared but never imported, because Vite's tree-shaker will silently omit unused files from the bundle (reviewer item 13: "Task 1.5 browser smoke test gives false confidence — runtime ReferenceErrors will not surface").

    To force a real compile+type-check of EVERY ported component without mounting them yet, create `frontend/src/_smoke.tsx` as a dev-only import harness that eagerly references each JSX file. This harness is imported as a side-effect from `App.tsx` ONLY inside `import.meta.env.DEV` so it compiles in dev, type-checks via `npm run check`, but is tree-shaken out of production.

    `frontend/src/_smoke.tsx`:
    ```tsx
    // DEV-ONLY smoke harness: eagerly references each ported component so Vite / tsc
    // surface ReferenceErrors, missing imports, and type holes that would otherwise stay
    // hidden until Wave 2 mounts them. NEVER import this from a production code path.
    import * as AppV2  from './components/app_v2';
    import * as AppRef from './components/app';
    import * as Viz    from './components/viz';
    import * as Pan    from './components/panels';
    import * as Mod    from './components/modals';
    import * as Set    from './components/settings';
    import * as Hist   from './components/history';
    import * as Twe    from './components/tweaks';
    import * as Sta    from './components/states';
    import * as Ico    from './components/icons';
    import * as V2     from './components/v2';

    // Force the bundler + tsc to keep every symbol live.
    export const __SMOKE__ = { AppV2, AppRef, Viz, Pan, Mod, Set, Hist, Twe, Sta, Ico, V2 };
    ```

    In `App.tsx` (Task 2 Step 11) add at the very top, inside a DEV guard:
    ```tsx
    if (import.meta.env.DEV) {
      // @ts-expect-error — side-effect import for smoke harness
      import('./_smoke');
    }
    ```

    Then run BOTH:
    ```
    cd frontend && npm run build       # Vite production bundling — _smoke is tree-shaken out
    cd frontend && npm run check       # tsc --noEmit — MUST compile _smoke.tsx
    ```

    Both must pass. Any TS error in `_smoke.tsx` means a ported component has a type hole that Task 2 wiring would otherwise hit at Wave 2 runtime.
  </what-built>
  <how-to-verify>
    1. `cd frontend && npm run check` — must exit 0.
    2. `cd frontend && npm run build` — must exit 0 AND the build output must include a log line indicating `_smoke.tsx` was seen in dev but NOT in the prod bundle (inspect `dist/assets/*.js` for the string `__SMOKE__` — it should be absent in prod build per DEV guard + tree-shaking).
    3. `cd frontend && npm run dev` — spin up the dev server briefly and confirm no `ReferenceError` in the browser console at page load (AppShell body is still blank at this wave — that's expected).
    4. `grep -c "from '\./components/" frontend/src/_smoke.tsx` → must return ≥ 11.
  </how-to-verify>
  <resume-signal>Type "approved — smoke harness confirms all 11 ported components type-check and bundle cleanly" or describe any TS / ReferenceError surfaced.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build adapter (cycleId/roundNum promotion + defensive validation) + 6 React contexts + useWebSocket (try/catch guard) + typed ApiError + redesigned usePolling + canonical api/simulation.ts + useCurrentCycle + App.tsx provider tree</name>
  <files>
    frontend/src/types.ts
    frontend/src/adapter/frame.ts
    frontend/src/adapter/agentId.ts
    frontend/src/hooks/useWebSocket.ts
    frontend/src/hooks/useEdges.ts
    frontend/src/hooks/usePolling.ts
    frontend/src/hooks/useApi.ts
    frontend/src/hooks/useCurrentCycle.ts
    frontend/src/context/TelemetryContext.tsx
    frontend/src/context/AgentsContext.tsx
    frontend/src/context/BracketContext.tsx
    frontend/src/context/EdgesContext.tsx
    frontend/src/context/RationalesContext.tsx
    frontend/src/context/ConnectionContext.tsx
    frontend/src/api/client.ts
    frontend/src/api/simulation.ts
    frontend/src/App.tsx
  </files>
  <read_first>
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md §Pattern 1 (adapter) §Pattern 4 (Context boundary) §Open Question 1/2/4 §Pitfall 6 (backoff cap) §Pitfall 7 (rationale dedup)
    - frontend-vue-archive/src/composables/useWebSocket.ts   # port target (Vue → React hook)
    - src/alphaswarm/state.py                                # StateSnapshot shape — confirm no cycle_id, round_num:int
    - src/alphaswarm/web/broadcaster.py                      # confirms asdict(snapshot) wire shape
    - src/alphaswarm/web/routes/simulation.py                # confirms canonical /simulate/{start,stop,shock} routes + whether /simulate/status exists
    - Claude Design UI/AlphaSwarm/CONTRACT.md                # target StateFrame/Agent/Rationale shapes
    - .planning/phases/41.1-.../41.1-CONTEXT.md <known_regressions>  # KR-41.1-01..11 pre-authorized
    - .planning/phases/41.1-.../41.1-REVIEWS.md items 1..14 (HIGH) + 15..24 (MEDIUM)
  </read_first>
  <action>
    This task stands up all shared wiring surfaces so Wave 2 (Plans 03 + 04) consumes them without redefining. Every file is small and focused — the review items baked in are (1) typed ApiError, (2) redesigned usePolling, (3) cycleId/roundNum on StateFrame, (4) six-context split, (5) defensive adapter + onmessage guards, (6) canonical `/api/simulate/*` routes, (7) useCurrentCycle promoted from Plan 03, (8) `ws.onclose = null` teardown (B11).

    Step 1 — `frontend/src/types.ts`:
    - Export `Signal`, `BracketKey`, `Agent`, `Rationale`, `Consensus`, `Telemetry`, `BracketSummary`, `StateFrame` (with `cycleId: string | null` and `roundNum: number | null`).
    - Export the raw-backend shapes too (`RawStateSnapshot`, `RawRationaleEntry`) for adapter input typing.
    - Export `ApiError` as a `class` — NOT a type alias — so it can be used with `instanceof` at call sites (reviewer item 4):
      ```ts
      export class ApiError extends Error {
        readonly status: number;
        readonly body: unknown;
        readonly path: string;
        constructor(status: number, path: string, body: unknown, message?: string) {
          super(message ?? `HTTP ${status} at ${path}`);
          this.name = 'ApiError';
          this.status = status;
          this.body = body;
          this.path = path;
        }
      }
      ```

    Step 2 — `frontend/src/adapter/agentId.ts`:
    - Export `deriveBracket(id: string): BracketKey` from the single-letter prefix map (Q→quants, D→degens, S→sovereigns, M→macro, U→suits, I→insiders, A→agents, X→doom_posters, P→policy_wonks, W→whales); unknown prefix → throw or default to `'agents'` (document choice).
    - Export `bracketDisplayName(key: BracketKey): string`.

    Step 3 — `frontend/src/adapter/frame.ts` (**reviewer item 5 — defensive validation**):
    - Signature: `export function adaptFrame(raw: unknown): StateFrame`
    - **Runtime guard FIRST**: if `raw == null` or `typeof raw !== 'object'`, or `!Array.isArray(raw.agent_states)`, throw a well-formed `Error('adaptFrame: malformed snapshot, missing agent_states')`. Do NOT default to `{}` — this surfaces backend breakage loudly instead of rendering empty UI silently.
    - Phase remap: `round_1 → 1`, `round_2 → 2`, `round_3 → 3`, `complete → 'done'`; `idle` / `seeding` / `replay` pass through; unknown → default `'idle'` + `console.warn` in `import.meta.env.DEV`.
    - Build `agents[]` from `raw.agent_states`: each entry becomes `{ id, bracket: deriveBracket(id), bracketDisplay: bracketDisplayName(...), signal, confidence, flipped: false, thinking: false, roundLastSpoke: Number(raw.round_num) || null }` (KR-41.1-03).
    - Build `consensus = { buy, sell, hold, dominant }` by counting `agents[].signal`.
    - Build `telemetry = { tps: raw.telemetry?.tokens_per_sec ?? 0, memMb: raw.telemetry?.memory_percent ?? 0, slotsUsed: 0, slotsMax: 8, elapsedSec: raw.telemetry?.elapsed_sec ?? 0 }` (KR-41.1-04, KR-41.1-05).
    - **cycleId / roundNum promotion (reviewer item 8 + Gemini suggestion)**: `cycleId = typeof raw.cycle_id === 'string' ? raw.cycle_id : null` (null until backend emits it); `roundNum = Number.isFinite(raw.round_num) ? Number(raw.round_num) : null`.
    - Compute `edges = Array.isArray(raw.edges) ? raw.edges : []` (adapter does NOT fetch edges — EdgesContext does; this field exists only so CONTRACT compatibility survives when backend later emits edges in the frame).
    - Add `if (import.meta.env.DEV) console.debug('[adaptFrame] first frame received:', { phase, cycleId, roundNum, agentsLen: agents.length })` — one-shot log guarded by a module-scoped boolean `let _loggedFirstFrame = false;` so it fires exactly once per session (Gemini suggestion).

    Step 4 — `frontend/src/hooks/useCurrentCycle.ts` (promoted from Plan 03 per reviewer item 3):
    - Signature: `export function useCurrentCycle(): { cycleId: string | null; roundNum: number | null; refresh: () => Promise<void> }`.
    - Primary path: poll `GET /api/simulate/status` once at mount + whenever `useTelemetry().phase` transitions from `'idle'` to non-idle OR back to `'idle'`.
    - Fallback path: if `/api/simulate/status` returns 404 (route not exposed), the hook falls back to `GET /api/replay/cycles` and reads `cycles[0]?.cycle_id` (fallback documented in Canonical Route Table row).
    - **Stale-state clearing (Gemini suggestion):** when `useTelemetry().phase === 'idle'`, clear `cycleId` to `null` so dependent modals (Report/Advisory) don't fetch old cycles.
    - Returns `refresh()` so App.tsx can call `refresh()` when WS announces a new phase.

    Step 5 — `frontend/src/api/client.ts` (**reviewer item 4 — typed ApiError**):
    - Export `apiFetch<T>(path: string, init?: RequestInit): Promise<T>`:
      - `const res = await fetch(path, { ...init, headers: { 'Content-Type': 'application/json', ...init?.headers } })`
      - If `!res.ok`, read the body (`const contentType = res.headers.get('content-type') || ''; const body = contentType.includes('application/json') ? await res.json().catch(() => null) : await res.text().catch(() => '');`) and `throw new ApiError(res.status, path, body)`.
      - On success, return `res.status === 204 ? (null as T) : await res.json()`.
    - Export `apiPost<Tin, Tout>(path: string, body: Tin): Promise<Tout>` that wraps `apiFetch(path, { method: 'POST', body: JSON.stringify(body) })`.
    - **Do NOT catch `ApiError` inside the client** — callers must branch on `e instanceof ApiError && e.status === 404`.

    Step 6 — `frontend/src/api/simulation.ts` (**reviewer items 2 + 3 — canonical routes, promoted from Plan 03**):
    - Import from `./client` and `../types`.
    - `export async function simStart(seed: string): Promise<{ ok: true; cycle_id: string }> { return apiPost('/api/simulate/start', { seed }); }`
    - `export async function simStop(): Promise<{ ok: true }> { return apiPost('/api/simulate/stop', {}); }`
    - `export async function simShock(input: { kind: string; magnitude: number; text: string }): Promise<{ ok: true }> { return apiPost('/api/simulate/shock', input); }`
    - `export async function simStatus(): Promise<{ running: boolean; cycle_id: string | null; round_num: number | null }> { try { return await apiFetch('/api/simulate/status'); } catch (e) { if (e instanceof ApiError && e.status === 404) return { running: false, cycle_id: null, round_num: null }; throw e; } }`
    - **All paths are literal strings** — no `const BASE_URL` trickery; grep must find exactly `/api/simulate/start`, `/api/simulate/stop`, `/api/simulate/shock`, `/api/simulate/status`.

    Step 7 — `frontend/src/hooks/usePolling.ts` (**reviewer item 5 — redesigned signature**):
    - New signature: `export function usePolling<T>(opts: { key: string; fetchFn: () => Promise<T | null>; intervalMs?: number; maxAttempts?: number; readyWhen?: (v: T | null) => boolean; stopWhen?: (v: T | null) => boolean }): { value: T | null; attempts: number; status: 'idle'|'polling'|'ready'|'stopped'|'error'; error: unknown | null }`
    - `fetchFn` is held in a `useRef` and updated on every render via a small effect — the polling `useEffect`'s dep array is **`[opts.key, opts.intervalMs ?? 1000, opts.maxAttempts ?? 10]`** (NEVER `fetchFn`), so inline arrow callers do not restart the loop every render (reviewer item 5).
    - 404 convention: `fetchFn` should return `null` when the resource is still pending (wrap its internal `apiFetch` in a try/catch that returns `null` on `e instanceof ApiError && e.status === 404`). The hook treats `null` as "not ready; retry" until `maxAttempts` reached.
    - Teardown: useEffect cleanup clears the timer + flips a `cancelled` ref so late promises don't set state after unmount.

    Step 8 — `frontend/src/hooks/useApi.ts`:
    - Thin React wrapper around `apiFetch` that tracks `{ data, error, loading }` — convenience only. Do NOT re-export `apiFetch` from here.

    Step 9 — `frontend/src/hooks/useWebSocket.ts` (port of Vue composable + reviewer items 11 + 12):
    - Port `frontend-vue-archive/src/composables/useWebSocket.ts` semantics:
      - Exponential backoff **1s → 2s → 4s** (3 retries after initial connect = 4 total connect attempts), then emit `reconnectFailed=true` and stop.
      - Rationale dedup key `${agent_id}:${round_num}`; cap the `all` array at 20 entries.
      - `phase === 'idle'` watcher clears `all` (fresh cycle starts empty).
    - **Teardown (B11 carry-over — reviewer item 15 / ordering):** inside the cleanup function, do `ws.onclose = null; ws.close();` + `clearTimeout(reconnectTimer.current)` + flip a `unmountedRef` so pending `setTimeout(reconnect)` callbacks no-op.
    - **Defensive `onmessage` guard (reviewer item 5 — adaptFrame crash):** wrap BOTH `JSON.parse` AND `adaptFrame` in try/catch. On parse failure, `console.error('[ws] malformed JSON from server', err); return;`. On `adaptFrame` failure, `console.error('[ws] adaptFrame failed', { err, rawSnippet: event.data?.slice?.(0, 200) }); return;` — do NOT update state, do NOT crash the socket. React 18+ automatic batching ensures setters coalesce into one render even without `unstable_batchedUpdates` (reviewer item 11 + Gemini suggestion on context splitting).
    - Expose: `{ snapshot: StateFrame | null, latest: Rationale[], all: Rationale[], connected: boolean, reconnectFailed: boolean }`.

    Step 10 — `frontend/src/hooks/useEdges.ts`:
    - Signature: `export function useEdges(cycleId: string | null, round: number | null): { edges: Array<[string, string]>; error: unknown | null; loading: boolean }`.
    - If `cycleId == null || round == null`, short-circuit to `{ edges: [], error: null, loading: false }` — no fetch.
    - Otherwise: `useEffect([cycleId, round])` fires `apiFetch<{ edges: Array<[string,string]> }>('/api/edges/' + cycleId + '?round=' + round)` — on `ApiError 404` return `[]` silently (replay cycles may have no edges for a given round).

    Step 11 — Six Context modules (**reviewer item 3 + Gemini context-split suggestion**). Each file is ~20-30 lines:
    - `context/TelemetryContext.tsx`: provides `{ phase, running, telemetry, ts, cycleId, roundNum }` — tiny, changes every tick but downstream consumers are small (KPI strip, round indicators).
    - `context/AgentsContext.tsx`: provides `{ agents, consensus }` — only the 100-agent view re-renders on tick.
    - `context/BracketContext.tsx`: provides `{ bracketSummaries }` (computed from `agents` + `consensus` by a memoized hook at provider level so Plan 03 / 04 don't recompute).
    - `context/EdgesContext.tsx`: provides `{ edges, cycleId, roundNum }` — the provider body calls `useEdges(cycleId, roundNum)` ONCE and pushes result down; viz.jsx consumes via `useEdgesCtx()`. **reviewer item 9**: viz MUST NOT call useEdges directly. Also exposes edge array so AppShell's active-edge counter reads from context, not `snapshot.edges` (fixes the `snapshot.edges.length` bug from reviewer item 9).
    - `context/RationalesContext.tsx`: provides `{ latest, all }`.
    - `context/ConnectionContext.tsx`: provides `{ connected, reconnectFailed }`.

    Each file exports its `Context` + a named `use<Name>()` hook that throws if consumed outside the provider (defensive — catches Plan 03/04 wiring mistakes at dev time).

    Step 12 — `frontend/src/App.tsx`:
    - Import AppShell from `./components/app_v2`, all six contexts, `useWebSocket`, `useCurrentCycle`.
    - `App()` body:
      ```tsx
      const ws = useWebSocket();                              // single-root WS hook
      const { cycleId, roundNum, refresh } = useCurrentCycle();
      // Refresh cycleId whenever the WS phase transitions into / out of idle.
      useEffect(() => { refresh(); }, [ws.snapshot?.phase]);
      ```
    - Render tree (Gemini suggestion — order matters for subscription granularity):
      ```tsx
      <ConnectionContext.Provider value={{ connected: ws.connected, reconnectFailed: ws.reconnectFailed }}>
        <TelemetryContext.Provider value={{
          phase: ws.snapshot?.phase ?? 'idle',
          running: ws.snapshot?.running ?? false,
          telemetry: ws.snapshot?.telemetry ?? defaultTelemetry,
          ts: ws.snapshot?.ts ?? 0,
          cycleId: ws.snapshot?.cycleId ?? cycleId ?? null,
          roundNum: ws.snapshot?.roundNum ?? roundNum ?? null,
        }}>
          <AgentsContext.Provider value={{
            agents: ws.snapshot?.agents ?? [],
            consensus: ws.snapshot?.consensus ?? defaultConsensus,
          }}>
            <BracketContext.Provider value={{ bracketSummaries }}>
              <RationalesContext.Provider value={{ latest: ws.latest, all: ws.all }}>
                <EdgesProvider cycleId={effectiveCycleId} roundNum={effectiveRoundNum}>
                  <AppShell />
                </EdgesProvider>
              </RationalesContext.Provider>
            </BracketContext.Provider>
          </AgentsContext.Provider>
        </TelemetryContext.Provider>
      </ConnectionContext.Provider>
      ```
    - `EdgesProvider` is a small wrapper exported from `EdgesContext.tsx` that calls `useEdges(cycleId, roundNum)` and provides the result.
    - **React 18+ automatic batching (reviewer item 11 / 24):** the repeated setState calls inside `useWebSocket.onmessage` are automatically batched by React 18's root API (`createRoot`) — NO `unstable_batchedUpdates` / `flushSync` required. This matches the Gemini-suggested context-split performance fix.
    - **Smoke harness side-effect (Task 1.5):** at the very top of `App.tsx`, `if (import.meta.env.DEV) { import('./_smoke'); }`.

    Step 13 — Build + type-check + commit:
    ```
    cd frontend && npm run check   # tsc --noEmit, includes _smoke.tsx
    cd frontend && npm run build   # Vite production bundle, _smoke tree-shaken out
    ```
    Both must pass. Then commit.

    Anti-patterns:
    - Do NOT re-introduce a single `SnapshotContext` — the six-context split is the review-mandated design (reviewer item 3 + Gemini consensus).
    - Do NOT use `unstable_batchedUpdates` — it is React 18 legacy; automatic batching covers this (reviewer item 11).
    - Do NOT call `useEdges` anywhere except inside `EdgesProvider` (reviewer item 9).
    - Do NOT let `apiFetch` throw a plain `Error` — it MUST throw `ApiError` with `status`, `body`, `path` (reviewer item 4).
    - Do NOT let `usePolling`'s `useEffect` deps include `fetchFn` (reviewer item 5).
    - Do NOT hardcode `/api/simulation/*` paths — backend routes are `/api/simulate/*` (reviewer item 2; see Canonical Route Table).
    - Do NOT skip `ws.onclose = null` in teardown (B11 / reviewer item 15).
    - Do NOT fetch `/api/edges` from viz.jsx — edges live in EdgesContext (reviewer item 9).
    - Do NOT swallow `adaptFrame` failures silently — console.error with a raw snippet so debugging is possible (reviewer item 5).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run check 2&gt;&amp;1 | tail -5 &amp;&amp; npm run build 2&gt;&amp;1 | tail -10 | grep -q "built in" &amp;&amp; grep -F "class ApiError extends Error" src/types.ts &amp;&amp; grep -F "/api/simulate/start" src/api/simulation.ts &amp;&amp; grep -F "/api/simulate/stop" src/api/simulation.ts &amp;&amp; grep -F "/api/simulate/shock" src/api/simulation.ts &amp;&amp; grep -F "ws.onclose = null" src/hooks/useWebSocket.ts &amp;&amp; test $(ls src/context/*.tsx | wc -l) -eq 6 &amp;&amp; ! grep -F "unstable_batchedUpdates" src/hooks/useWebSocket.ts src/App.tsx</automated>
    <manual>Open browser devtools on `npm run dev` — WebSocket connects, Network tab shows `/ws/state` frames at ~200ms cadence; React DevTools shows the 6 providers wrapping AppShell.</manual>
  </verify>
  <done>
    - `frontend/src/types.ts` exports a concrete `class ApiError extends Error` with `status`, `body`, `path`.
    - `frontend/src/api/client.ts` throws `ApiError` on non-2xx (not plain `Error`).
    - `frontend/src/api/simulation.ts` uses literal `/api/simulate/{start,stop,shock,status}` paths (greppable via `grep -F`).
    - `frontend/src/hooks/usePolling.ts` signature takes `{ key, fetchFn, ... }`; `fetchFn` is held in a ref; useEffect deps exclude `fetchFn`.
    - `frontend/src/hooks/useCurrentCycle.ts` exists and returns `{ cycleId, roundNum, refresh }` derived from `/api/simulate/status` (with `/api/replay/cycles` fallback).
    - `frontend/src/adapter/frame.ts` throws on malformed snapshot, promotes `cycleId` + `roundNum`, emits dev-only first-frame `console.debug`.
    - `frontend/src/hooks/useWebSocket.ts` wraps both `JSON.parse` AND `adaptFrame` in try/catch; teardown nulls `ws.onclose`.
    - `frontend/src/context/` contains exactly six files (TelemetryContext, AgentsContext, BracketContext, EdgesContext, RationalesContext, ConnectionContext); no SnapshotContext.
    - `frontend/src/App.tsx` wires ws + useCurrentCycle + the 6 providers and imports `_smoke` only under `import.meta.env.DEV`.
    - `npm run check` + `npm run build` both pass.
    - `grep -F "unstable_batchedUpdates" frontend/src/**/*.ts*` returns nothing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (delta for Plan 02)

| Boundary | Description |
|----------|-------------|
| browser ↔ FastAPI WebSocket | Untrusted JSON snapshot frames parsed every ~200ms; adapter sits at the boundary and must tolerate malformed input without crashing the React tree. |
| browser ↔ FastAPI REST `/api/*` | All fetches flow through `apiFetch` → `ApiError` — typed status surface for Plan 03/04 polling. |
| component ↔ mock fixtures | `mocks/wire.ts` / `mocks/sources.ts` ship in production (KR-41.1-02 — tolerated); `mocks/holdings.ts` MUST NOT reach production bundles (dynamic-import guarded by `import.meta.env.DEV`). |

## STRIDE Threat Register (Plan 02 increments)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-02-01 | Tampering | `adapter/frame.ts` | mitigate | Defensive runtime guard — throw on missing `agent_states`; `onmessage` try/catch logs raw snippet + returns without state update (no partial-state rendering). |
| T-41.1-02-02 | Denial of Service | `useWebSocket.ts` | mitigate | Reconnect backoff capped at 3 retries after initial (1s/2s/4s) then `reconnectFailed=true`; teardown nulls `ws.onclose` to prevent late reconnects after unmount (B11). |
| T-41.1-02-03 | Information Disclosure | `api/client.ts` | mitigate | `ApiError.body` logs only to console in dev; production error paths show generic toast (Plan 04 handles UX). Backend error bodies never stored in sessionStorage. |
| T-41.1-02-04 | Tampering | rationale markdown | mitigate | DOMPurify + marked pipeline stays live (KR-41.1-10 stubs citations — empty arrays pose no XSS surface; advisory/report markdown still sanitised). |
| T-41.1-02-05 | Denial of Service | `usePolling.ts` | mitigate | Redesigned signature + `fetchFn` ref + stable `key`-dep array prevents inline-callback restart loop (reviewer item 5); max-attempts cap bounds retries. |
| T-41.1-02-06 | Repudiation | `adapter/frame.ts` | accept | One-shot dev `console.debug` on first frame — no persistent audit trail (simulation engine is local, no repudiation surface). |
| T-41.1-02-07 | Elevation of Privilege | `mocks/holdings.ts` in production bundle | mitigate | Reviewer item 18: dynamic import inside `import.meta.env.DEV`; acceptance criterion greps `dist/assets/*.js` for `HOLDINGS` and must be absent. |
| T-41.1-02-08 | Spoofing | `useWebSocket.ts` origin check | accept | WebSocket derives URL from `window.location` — same-origin policy in browser is sufficient; no separate auth token in dev. |

All new-threat dispositions are `mitigate` or `accept` with explicit rationale — no deferred threats.
</threat_model>

<verification>
## Phase-level checks for Plan 02

**Build + type-check:**
- `cd frontend && npm run check` exits 0 (tsc --noEmit picks up `_smoke.tsx`)
- `cd frontend && npm run build` exits 0
- `dist/assets/*.js` does NOT contain the string `HOLDINGS` (tree-shaking proof — reviewer item 18)
- `dist/assets/*.js` does NOT contain the string `__SMOKE__` (dev-only harness stripped)

**Grep assertions (positive):**
- `grep -F "class ApiError extends Error" frontend/src/types.ts` → 1 match
- `grep -F "/api/simulate/start" frontend/src/api/simulation.ts` → 1 match
- `grep -F "/api/simulate/stop" frontend/src/api/simulation.ts` → 1 match
- `grep -F "/api/simulate/shock" frontend/src/api/simulation.ts` → 1 match
- `grep -F "/api/simulate/status" frontend/src/api/simulation.ts` → 1 match
- `grep -F "ws.onclose = null" frontend/src/hooks/useWebSocket.ts` → 1 match (B11 teardown)
- `ls frontend/src/context/*.tsx | wc -l` → 6
- `ls frontend/src/mocks/*.ts | wc -l` → 4 (wire.ts, sources.ts, holdings.ts, index.ts)
- `ls frontend/src/components/*.jsx | wc -l` → 11

**Grep assertions (negative — MUST be empty):**
- `grep -F "unstable_batchedUpdates" frontend/src/` → empty (reviewer item 11: React 18 auto-batching)
- `grep -rn "window\.AS_DATA\|window\.React\|window\.parent\.postMessage\|__activate_edit_mode" frontend/src/components/*.jsx frontend/src/mocks/*.ts` → empty
- `grep -rn "/api/simulation/" frontend/src/` → empty (canonical routes are `/api/simulate/*`)
- `grep -n "^import.*from.*mocks/holdings" frontend/src/components/*.jsx` → only `tweaks.jsx`, AND every match inside tweaks.jsx is enclosed in an `import.meta.env.DEV` block
- `grep -F "new Error(" frontend/src/api/client.ts` → empty (client only throws `ApiError`)

**Runtime smoke (manual, confirmed in Task 1.5 checkpoint):**
- `npm run dev`, open browser — no `ReferenceError` in console at page load
- React DevTools shows 6 Context.Provider layers wrapping AppShell
- Network tab shows `/ws/state` frames at ~200ms cadence when backend is running

**Cross-plan parallel contract:**
- Plan 03 + Plan 04 consume from `api/client`, `api/simulation`, `hooks/useCurrentCycle`, `hooks/usePolling`, `context/*` — they do NOT redefine any of these (verified at Plan 03 + 04 plan review time).
</verification>

<success_criteria>
- All 11 review-derived high-severity requirements implemented: typed ApiError (item 4), redesigned usePolling (item 5), 6-context split (item 3), cycleId/roundNum on StateFrame (item 8), defensive adapter + onmessage guards (item 5 + 12), canonical `/api/simulate/*` routes (item 2), `api/simulation.ts` + `useCurrentCycle` promoted to Plan 02 (item 3), ws.onclose=null teardown (item 15), mocks split into wire/sources/holdings (item 18), _smoke.tsx harness (item 13), React 18 auto-batching (item 11).
- All 5 MEDIUM-severity requirements implemented: narrow `window.*` allowlist (item 16), KR-41.1-10 documented (item 24), edges centralised via EdgesContext (item 9), rationale citations stubbed to `[]` with KR reference (item 14), mock fixtures tree-shake-friendly (item 18).
- `frontend/src/` compiles + type-checks + builds.
- Task 1.5 checkpoint passed (smoke harness confirms no hidden ReferenceErrors).
- Plan 03 + Plan 04 have everything they need from this plan's exports — no hidden dependencies force them back into Plan 02.
- `<known_regressions>` frontmatter matches CONTEXT.md KR-41.1-01..11 (Plan 02 tracks KR-41.1-01..05 + KR-41.1-10; Plans 03/04/05 track the remainder).
- STRIDE register complete with 8 threats, all dispositions explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-02-SUMMARY.md` documenting:
- Final file tree under `frontend/src/` (list all created/modified files + one-line purpose each).
- Each of the 6 Context files + their `use*` hook signatures.
- The exact `ApiError`, `StateFrame`, `Agent`, `Rationale` TypeScript definitions shipped (copy-paste from `types.ts`).
- `api/simulation.ts` public signature (4 functions) + the exact backend routes they hit.
- `useWebSocket` public signature + the reconnect/dedup/teardown semantics it ships with.
- `usePolling` public signature + the stable-deps guarantee (fetchFn-in-ref).
- Smoke harness (`_smoke.tsx`) status: present in dev, tree-shaken in prod (confirmed by `dist/assets/*.js` grep).
- Known-regression register linkage: this plan tracks KR-41.1-01..05 + KR-41.1-10; Plans 03 / 04 / 05 will pick up KR-41.1-06..09 + KR-41.1-11.
- Parallel Contract table (copied from objective section) so Plans 03 + 04 have a single reference point.
</output>
