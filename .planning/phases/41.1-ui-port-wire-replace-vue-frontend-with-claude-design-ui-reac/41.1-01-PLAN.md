---
phase: 41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend-vue-archive/              # created by archive move
  - frontend/package.json              # fresh React scaffold
  - frontend/package-lock.json         # regenerated
  - frontend/vite.config.ts            # plugin-react + dev proxy
  - frontend/tsconfig.app.json         # jsx: react-jsx, allowJs: true
  - frontend/tsconfig.json             # entry points
  - frontend/tsconfig.node.json        # node tooling config
  - frontend/index.html                # stripped React shell (local fonts only — KR-01-INFRA)
  - frontend/src/main.tsx              # ReactDOM.createRoot entry
  - frontend/src/App.tsx               # placeholder root
  - frontend/src/env.d.ts              # Vite client types
  - frontend/design-reference/         # copied reference HTML/screenshots
autonomous: false
requirements: []
known_regressions_touched: []          # Wave 0 does not touch any product-level KR. Infra-level font swap tracked below.

must_haves:
  truths:
    - "Old Vue frontend preserved at frontend-vue-archive/ with package.json AND package-lock.json present for reproducible parity runs"
    - "Fresh React + Vite + TypeScript scaffold lives at frontend/ with no Vue deps"
    - "Vite dev server boots, serves empty React root at http://localhost:5173"
    - "Vite dev proxy forwards /api/health to FastAPI on :8000 and returns backend JSON"
    - "Vite dev proxy forwards /ws/state to ws://localhost:8000/ws/state"
    - "frontend/design-reference/ holds verbatim copies of v2.html, Motion Spec.html, and screenshots/ per D-07"
    - "frontend/index.html uses a local-first font strategy (system-ui stack) — NO Google Fonts preconnect/links (CLAUDE.md local-first constraint)"
    - "tsconfig.node.json 'types: [node]' is backed by devDependency '@types/node' so tsc -b does not fail"
  artifacts:
    - path: "frontend-vue-archive/package.json"
      provides: "preserved Vue package manifest for parity runs"
      contains: "\"vue\":"
    - path: "frontend-vue-archive/package-lock.json"
      provides: "preserved Vue lockfile so the archive reinstalls deterministically for parity sweeps"
    - path: "frontend/package.json"
      provides: "React + Vite + TS dependency manifest"
      contains: "\"react\":"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with @vitejs/plugin-react and /api + /ws dev proxy"
      contains: "@vitejs/plugin-react"
    - path: "frontend/tsconfig.app.json"
      provides: "TS config with jsx: react-jsx and allowJs: true"
      contains: "\"jsx\": \"react-jsx\""
    - path: "frontend/tsconfig.node.json"
      provides: "TS config for vite.config.ts tooling with types: [node]"
      contains: "\"types\": [\"node\"]"
    - path: "frontend/index.html"
      provides: "Stripped HTML shell mounting #root via /src/main.tsx — local-first font stack"
      contains: "id=\"root\""
    - path: "frontend/src/main.tsx"
      provides: "ReactDOM createRoot entry point"
      contains: "createRoot"
    - path: "frontend/src/App.tsx"
      provides: "placeholder root component pending Wave 1 wiring"
      exports: ["App"]
    - path: "frontend/design-reference/v2.html"
      provides: "design source HTML preserved for parity reference"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "server.proxy['/api']"
      pattern: "localhost:8000"
    - from: "frontend/vite.config.ts"
      to: "ws://localhost:8000/ws/state"
      via: "server.proxy['/ws']"
      pattern: "ws: true"
    - from: "frontend/index.html"
      to: "frontend/src/main.tsx"
      via: "<script type=\"module\" src=\"/src/main.tsx\">"
      pattern: "src=\"/src/main.tsx\""
    - from: "frontend/tsconfig.node.json"
      to: "@types/node (devDependency in frontend/package.json)"
      via: "compilerOptions.types: [node]"
      pattern: "\"@types/node\":"
---

<objective>
Archive the current Vue frontend to `frontend-vue-archive/` (preserving its `package-lock.json` so later parity sweeps reinstall deterministically) and scaffold a fresh React 19 + Vite 6 + TypeScript 5.6 project in `frontend/`. The React scaffold must preserve D3/dompurify/marked deps (per RESEARCH §Standard Stack), declare `@types/node` so the `tsconfig.node.json` `types: ["node"]` reference resolves, use a **local-first font stack** (no Google Fonts network calls) per CLAUDE.md's "Local First" hard constraint, and wire a dev proxy that routes `/api/*` and `/ws/state` to the FastAPI backend on :8000. This plan produces an empty-but-running React shell that Wave 1 can populate with ported components.

Purpose: Guarantee side-by-side parity verification is possible (old Vue preserved, including its lockfile) and give Wave 1 a clean, compiling, dev-proxy-correct foundation to drop verbatim JSX files into — without introducing any network dependency at boot (local-first discipline).

Output: A new `frontend/package.json` with React deps AND `@types/node`, a swapped `vite.config.ts`, a React-aware `tsconfig.app.json`, a `tsconfig.node.json` whose `types` array is backed by an actual dep, a stripped `index.html` that loads system fonts locally, a minimal `main.tsx + App.tsx`, a `frontend/design-reference/` directory with design source HTML/screenshots, and a working `npm run dev` that serves at :5173 and proxies `/api/health` successfully to :8000.

Framing note: This Wave 0 plan does not introduce or close any of the Known Regressions KR-41.1-01..11 enumerated in CONTEXT.md `<known_regressions>`. The only sanctioned deviation at this wave is the local-first font swap (system stack vs. the design's Google Fonts CDN links); that is enforced infrastructure policy (CLAUDE.md), not a product regression, and is therefore NOT assigned a KR ID. Product KRs are touched exclusively in Waves 1–3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md
@.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-VALIDATION.md
@Claude Design UI/AlphaSwarm/CONTRACT.md
@frontend/package.json
@frontend/vite.config.ts
@frontend/tsconfig.app.json
@CLAUDE.md
</context>

<interfaces>
<!-- Current Vue deps to preserve, Vue deps to drop, React deps to add. -->
<!-- Source: frontend/package.json + 41.1-RESEARCH.md §Standard Stack §Installation -->

Current frontend/package.json dependencies (to be REPLACED wholesale):
- dependencies: vue ^3.5.0 + d3-force/scale/selection/transition + dompurify ^3.4.0 + marked ^18.0.1
- devDependencies: @types/d3-* + @vitejs/plugin-vue ^5.0.0 + typescript ~5.6.0 + vite ^6.0.0 + vue-tsc ^2.0.0

Target frontend/package.json dependencies (Wave 0 end state):
- dependencies: react ^19.2.5 + react-dom ^19.2.5 + d3-force ^3.0.0 + d3-scale ^4.0.0 + d3-selection ^3.0.0 + d3-transition ^3.0.1 + dompurify ^3.4.0 + marked ^18.0.1
- devDependencies: @types/d3-force ^3.0.0 + @types/d3-scale ^4.0.0 + @types/d3-selection ^3.0.0 + @types/d3-transition ^3.0.9 + @types/node ^22.9.0 + @types/react ^19.2.14 + @types/react-dom ^19.2.3 + @vitejs/plugin-react ^6.0.1 + typescript ~5.6.0 + vite ^6.0.0

Note: `@types/node ^22.9.0` is REQUIRED because `tsconfig.node.json` sets `compilerOptions.types: ["node"]` for `vite.config.ts`. Without the dep, `tsc -b` fails with `Cannot find type definition file for 'node'`.

Removed (per D-02 + RESEARCH §Removed): vue, @vitejs/plugin-vue, vue-tsc.
Kept (per D-03 + RESEARCH §Supporting): vite, typescript, d3-*, dompurify, marked.
Added (per D-01 + RESEARCH §Core): react, react-dom, @vitejs/plugin-react, @types/react, @types/react-dom, @types/node.

Scripts target shape:
  "dev": "vite",
  "build": "tsc -b && vite build",            // replaces vue-tsc
  "preview": "vite preview"

Current vite.config.ts proxy (MUST PRESERVE, swap plugin only):
  proxy: {
    '/api': { target: 'http://localhost:8000', changeOrigin: true },
    '/ws':  { target: 'ws://localhost:8000', ws: true }
  }

Current tsconfig.app.json (MUST MUTATE):
  "jsx": "preserve"          → "jsx": "react-jsx"
  (add) "allowJs": true
  "include": ["src/**/*.ts", "src/**/*.vue", "env.d.ts"]  → ["src/**/*.ts", "src/**/*.tsx", "src/**/*.jsx", "env.d.ts"]

Local-first font policy (CLAUDE.md "Local First" + RESEARCH §Anti-Patterns):
  The design's `Claude Design UI/AlphaSwarm/v2.html` declares four Google Fonts families via
  `<link rel="preconnect">` + `<link href="https://fonts.googleapis.com/css2?family=...">`.
  Wave 0 MUST NOT reproduce those tags. The stripped `frontend/index.html` instead relies on
  the design CSS (ported in Wave 1) to declare a system-stack font via CSS variables or the
  body selector (e.g. `font-family: ui-monospace, 'SF Mono', 'JetBrains Mono', Menlo, monospace;`
  for monospace surfaces and `font-family: ui-sans-serif, system-ui, -apple-system, 'Segoe UI',
  Roboto, sans-serif;` for body). If, later, the user approves self-hosted woff2 files, those
  belong in `frontend/public/fonts/` and are declared via `@font-face` inside the styles bundle.
  They MUST NOT be fetched from a CDN.

Design reference (per D-07, copy verbatim to frontend/design-reference/):
  Claude Design UI/AlphaSwarm/v2.html
  Claude Design UI/AlphaSwarm/Motion Spec.html
  Claude Design UI/AlphaSwarm/screenshots/v2-dashboard.png
  Claude Design UI/AlphaSwarm/screenshots/v2-data-sources.png
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Archive current Vue frontend to frontend-vue-archive/ (preserving its lockfile)</name>
  <files>
    frontend-vue-archive/          (new — receives current frontend/ contents)
    frontend/                      (will be re-created empty in Task 2)
  </files>
  <read_first>
    - frontend/package.json                                             (confirm this is the Vue manifest to archive)
    - frontend/package-lock.json                                        (confirm lockfile exists — preserve for reproducible parity reinstalls)
    - frontend/src/App.vue                                              (confirm it's the current root)
    - frontend/src/composables/useWebSocket.ts                          (confirm composable exists — needed for Wave 1 reference)
    - frontend/src/components/ (directory listing)                      (confirm 10 Vue SFCs are present)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md  (D-02 "Drop Vue entirely" — archive first, then clean swap)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md §Runtime State Inventory  (must rm node_modules + dist; KEEP package-lock.json)
    - CLAUDE.md §Hard Constraints (Local First)                         (confirms no CDN / network boot)
  </read_first>
  <action>
    Preserve the current Vue frontend in a sibling directory so the parity sweep in Wave 3 can run `:5173` (new React) and `:5174` (old Vue) side-by-side per D-14. Critically, this task preserves `package-lock.json` inside the archive so a future `cd frontend-vue-archive && npm ci` reproduces the EXACT dep tree the Vue baseline was built against (reviewer item 15).

    Steps (run from repo root):

    1. Verify the working tree is clean for `frontend/` so nothing is lost:
       ```
       git status --short frontend | tee /tmp/41.1-01-frontend-status.txt
       ```
       If there are staged/unstaged changes inside `frontend/`, STOP and surface the diff to the user — do NOT archive dirty files. The CONTEXT file has no mention of in-flight Vue work, so the tree is expected to be clean.

    2. Move the directory with `git mv` so history follows:
       ```
       git mv frontend frontend-vue-archive
       ```
       Rationale: `git mv` preserves per-file history; a plain `mv` would show every file as deleted+added and bloat the diff.

    3. Rename `frontend-vue-archive/package.json` `"name"` field from `"alphaswarm-frontend"` → `"alphaswarm-frontend-vue-archive"` so `npm install` inside the archive never races with the new `frontend/` project.
       ```
       # edit the "name" line in frontend-vue-archive/package.json
       ```

    4. Change the dev port used by the archive so it does not conflict with the new React dev server (React keeps :5173 per current Vue config; archive moves to :5174):
       Edit `frontend-vue-archive/vite.config.ts` → change `server.port: 5173` → `server.port: 5174`.
       If the archived config has no `port` key but you see a line with `port: 5173`, change it there. If neither is present, add `port: 5174` inside the `server:` block.

    5. Delete the archive's stale BUILD artifacts so `npm ci && npm run dev` inside the archive produces fresh output — **but keep `package-lock.json`** (reviewer item 15 — lockfile is the contract for reproducible parity reinstalls):
       ```
       rm -rf frontend-vue-archive/node_modules frontend-vue-archive/dist
       # DO NOT delete frontend-vue-archive/package-lock.json — it is the archive's reproducibility contract.
       ```
       Rationale: The purpose of the archive is to reliably reconstitute the Vue baseline during Wave 3 parity sweeps. Without the lockfile, a later `npm install` could resolve to different transitive versions and the "parity" comparison would become invalid.

    6. Recreate an empty `frontend/` directory at the repo root so Task 2's files have a home:
       ```
       mkdir -p frontend/src frontend/design-reference/screenshots
       ```

    Do NOT commit yet — Task 3 commits the entire Wave 0 together so the archive, scaffold, and install are a single atomic change.

    Anti-patterns to avoid:
    - Do NOT `rm -rf frontend/` — you lose history and the composable reference Wave 1 needs.
    - Do NOT copy (vs move) the Vue files into an archive — two copies diverge.
    - Do NOT leave `frontend-vue-archive/node_modules` in place; it bloats the repo and is stale against any future dep change.
    - Do NOT delete `frontend-vue-archive/package-lock.json` — it is the archive's reproducibility contract (reviewer item 15).
  </action>
  <verify>
    <automated>test -f frontend-vue-archive/package.json &amp;&amp; test -f frontend-vue-archive/package-lock.json &amp;&amp; test -f frontend-vue-archive/src/App.vue &amp;&amp; test -f frontend-vue-archive/src/composables/useWebSocket.ts &amp;&amp; test -d frontend &amp;&amp; test -z "$(ls -A frontend)" -o -d frontend/design-reference</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend-vue-archive/` shows `package.json`, `package-lock.json`, `vite.config.ts`, `src/` with `App.vue` and `composables/useWebSocket.ts`.
    - `grep -F '"name": "alphaswarm-frontend-vue-archive"' frontend-vue-archive/package.json | wc -l` returns `1` (literal string match — reviewer item 24).
    - `grep -F 'port: 5174' frontend-vue-archive/vite.config.ts | wc -l` returns `1` (literal).
    - `test -f frontend-vue-archive/package-lock.json` exits 0 (lockfile preserved — reviewer item 15).
    - `ls frontend-vue-archive/node_modules 2>&amp;1 | head -1` does NOT list any files (directory absent).
    - `ls -d frontend` exists AND `ls -d frontend/design-reference/screenshots` exists.
    - `git log --follow --oneline -1 -- frontend-vue-archive/src/App.vue` shows a commit, confirming history was preserved by `git mv`.
  </acceptance_criteria>
  <done>Old Vue frontend lives at `frontend-vue-archive/` with git history, renamed package, :5174 port, cleaned build artifacts, **preserved `package-lock.json`**. Empty `frontend/` + `frontend/design-reference/screenshots/` exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold React + Vite + TS skeleton with dev proxy (local-first fonts) and copy design reference</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/env.d.ts
    frontend/design-reference/v2.html
    frontend/design-reference/Motion Spec.html
    frontend/design-reference/screenshots/v2-dashboard.png
    frontend/design-reference/screenshots/v2-data-sources.png
  </files>
  <read_first>
    - frontend-vue-archive/package.json                                 (source of D3/dompurify/marked versions to preserve)
    - frontend-vue-archive/vite.config.ts                               (source of dev-proxy rules to preserve verbatim)
    - frontend-vue-archive/tsconfig.app.json                            (source of target/module/paths to preserve)
    - Claude Design UI/AlphaSwarm/CONTRACT.md §1 Transport              (confirms /ws/state + 5Hz — proxy must route ws:)
    - Claude Design UI/AlphaSwarm/v2.html                               (design reference, copy as-is — DO NOT reproduce the Google Fonts links in frontend/index.html)
    - Claude Design UI/AlphaSwarm/Motion Spec.html                      (design reference, copy as-is)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md §Example A (index.html) §Example B (main.tsx) §Example C (App.tsx stub) §Example D (vite.config.ts) §Example E (tsconfig.app.json)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-CONTEXT.md  (D-01 React, D-03 Vite, D-04 TS, D-06 verbatim drop target, D-07 design-reference placement)
    - CLAUDE.md §Hard Constraints 2 (Local First — no cloud APIs / no boot-time network calls)
  </read_first>
  <action>
    Produce a minimal, compiling React + Vite + TS skeleton in `frontend/`. No components yet — Wave 1 copies the 12 JSX files in. This task only stands up the toolchain and the empty root. Font loading is **local-first** (system stack or later self-hosted woff2); no Google Fonts CDN.

    1. **Create `frontend/package.json`** with these EXACT values:
       - `"name": "alphaswarm-frontend"`
       - `"private": true`
       - `"version": "0.0.0"`
       - `"type": "module"`
       - `"scripts"`: `{ "dev": "vite", "build": "tsc -b && vite build", "preview": "vite preview", "check": "tsc -b --noEmit" }`
       - `"dependencies"`: react `^19.2.5`, react-dom `^19.2.5`, d3-force `^3.0.0`, d3-scale `^4.0.0`, d3-selection `^3.0.0`, d3-transition `^3.0.1`, dompurify `^3.4.0`, marked `^18.0.1`
       - `"devDependencies"`: @types/d3-force `^3.0.0`, @types/d3-scale `^4.0.0`, @types/d3-selection `^3.0.0`, @types/d3-transition `^3.0.9`, **@types/node `^22.9.0`**, @types/react `^19.2.14`, @types/react-dom `^19.2.3`, @vitejs/plugin-react `^6.0.1`, typescript `~5.6.0`, vite `^6.0.0`

       The `@types/node` entry is mandatory (reviewer item 7). `tsconfig.node.json` sets `compilerOptions.types: ["node"]`; without this dep, `tsc -b` emits `Cannot find type definition file for 'node'` and `npm run build` fails.

       Do NOT add `vue`, `@vitejs/plugin-vue`, or `vue-tsc`. Do NOT add `zustand`, `immer`, `axios`, `react-router-dom`, or `vitest` — those are deferred per CONTEXT/RESEARCH (Zustand is a fallback only if Wave 2 profiles show context jank; vitest is optional per VALIDATION.md). Keep all D3 deps even though `viz.jsx` uses deterministic SVG — D3 is still used by `d3-scale` for bracket bars (per RESEARCH §Supporting) and we preserve it during the port per Open Question #5 (delete in a later cleanup phase).

    2. **Create `frontend/vite.config.ts`** with:
       ```ts
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';

       export default defineConfig({
         plugins: [react()],
         server: {
           port: 5173,
           proxy: {
             '/api': { target: 'http://localhost:8000', changeOrigin: true },
             '/ws':  { target: 'ws://localhost:8000', ws: true }
           }
         },
         build: {
           outDir: 'dist'
         }
       });
       ```
       Rationale: `outDir: 'dist'` is explicit so FastAPI's static mount at `frontend/dist` continues to work (RESEARCH §Runtime State Inventory A12). Port 5173 matches the archived Vue config, and the archive now uses :5174 (Task 1).

    3. **Create `frontend/tsconfig.json`** (project references entry):
       ```json
       {
         "files": [],
         "references": [
           { "path": "./tsconfig.app.json" },
           { "path": "./tsconfig.node.json" }
         ]
       }
       ```

    4. **Create `frontend/tsconfig.app.json`**:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "lib": ["ES2022", "DOM", "DOM.Iterable"],
           "moduleResolution": "bundler",
           "strict": true,
           "jsx": "react-jsx",
           "allowJs": true,
           "resolveJsonModule": true,
           "isolatedModules": true,
           "esModuleInterop": true,
           "noEmit": true,
           "noUnusedLocals": true,
           "noUnusedParameters": true,
           "skipLibCheck": true,
           "paths": { "@/*": ["./src/*"] }
         },
         "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.jsx", "env.d.ts"]
       }
       ```
       Key changes vs archive: `jsx: "react-jsx"` (was `"preserve"`), `allowJs: true` (new — permits `.jsx` files during transition per D-04 discretion), `include` swapped `*.vue` → `*.tsx` + `*.jsx`.

    5. **Create `frontend/tsconfig.node.json`** (note: `types: ["node"]` is only valid because we added `@types/node` in step 1):
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "lib": ["ES2022"],
           "moduleResolution": "bundler",
           "strict": true,
           "noEmit": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "allowSyntheticDefaultImports": true,
           "types": ["node"]
         },
         "include": ["vite.config.ts"]
       }
       ```

    6. **Create `frontend/index.html`** — **LOCAL-FIRST FONT POLICY** (reviewer item 6 + CLAUDE.md §Hard Constraints "Local First"):

       Strip all `<link rel="preconnect">` and `<link href="https://fonts.googleapis.com/...">` tags. The dev server MUST NOT make a network request to `fonts.googleapis.com` or `fonts.gstatic.com` at boot. Font families are provided by the system stack via CSS (Wave 1 ports `styles.css`, which declares body font-family using `ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif` for body and `ui-monospace, 'SF Mono', Menlo, monospace` for monospace surfaces). If, in a later phase, the user approves self-hosted fonts, place woff2 files under `frontend/public/fonts/` and declare `@font-face` inside the CSS bundle. CDNs remain forbidden.

       ```html
       <!doctype html>
       <html lang="en">
         <head>
           <meta charset="UTF-8" />
           <meta name="viewport" content="width=device-width, initial-scale=1.0" />
           <title>AlphaSwarm</title>
         </head>
         <body>
           <div id="root"></div>
           <script type="module" src="/src/main.tsx"></script>
         </body>
       </html>
       ```
       No `<script type="text/babel">`, no `@babel/standalone`, no `Claude Design UI/AlphaSwarm/v2.html` UMD wiring — Vite handles JSX at build time. No Google Fonts, no CDN preconnects — local-first.

    7. **Create `frontend/src/env.d.ts`**:
       ```ts
       /// <reference types="vite/client" />
       ```

    8. **Create `frontend/src/main.tsx`**:
       ```tsx
       import React from 'react';
       import { createRoot } from 'react-dom/client';
       import { App } from './App';

       const rootEl = document.getElementById('root');
       if (!rootEl) throw new Error('#root missing from index.html');

       createRoot(rootEl).render(
         <React.StrictMode>
           <App />
         </React.StrictMode>
       );
       ```
       Do NOT import `./styles.css` yet — Wave 1 copies the design CSS; importing a non-existent file will break `npm run dev`.

    9. **Create `frontend/src/App.tsx`** (Wave 0 placeholder — Wave 1 replaces it with the provider tree):
       ```tsx
       export function App() {
         return (
           <div style={{ padding: 24, fontFamily: 'ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif' }}>
             <h1>AlphaSwarm — Wave 0 scaffold</h1>
             <p>React + Vite + TS toolchain online. Components port in Wave 1.</p>
           </div>
         );
       }
       ```
       Inline `fontFamily` uses the system stack (matches CLAUDE.md local-first). No remote font fetch.

    10. **Copy design reference (D-07) into `frontend/design-reference/`**:
        ```
        cp "Claude Design UI/AlphaSwarm/v2.html" frontend/design-reference/v2.html
        cp "Claude Design UI/AlphaSwarm/Motion Spec.html" "frontend/design-reference/Motion Spec.html"
        cp "Claude Design UI/AlphaSwarm/screenshots/v2-dashboard.png" frontend/design-reference/screenshots/v2-dashboard.png
        cp "Claude Design UI/AlphaSwarm/screenshots/v2-data-sources.png" frontend/design-reference/screenshots/v2-data-sources.png
        ```
        Per D-07 these are read-only parity references. Do NOT symlink — a copy is robust against later repo moves of the design folder. Note: the preserved design reference files still contain the original Google Fonts links; that is expected (they are verbatim documentation of the design), and they are NEVER loaded by the live React shell because `frontend/index.html` (the Vite entry) does not include them.

    Do NOT run `npm install` in this task — Task 3 does that and boots the dev server to prove the proxy works.

    Anti-patterns to avoid:
    - Do NOT add a `"vue"` key anywhere in `package.json`.
    - Do NOT leave `"jsx": "preserve"` — Vite + React demands `"react-jsx"` (RESEARCH §Pitfall 1).
    - Do NOT import `./styles.css` in `main.tsx` yet (Wave 1 job).
    - Do NOT include `<script type="text/babel">` or `@babel/standalone` in `index.html` (RESEARCH §Anti-Patterns).
    - Do NOT include `<link rel="preconnect" href="https://fonts.googleapis.com">` or any other external CDN preconnect / stylesheet in `frontend/index.html` (CLAUDE.md local-first).
    - Do NOT omit `@types/node` from devDependencies when `tsconfig.node.json` lists `types: ["node"]` (reviewer item 7).
    - Do NOT add `server.host: true` or expose the dev server on the network — local-first per CLAUDE.md.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; test -f package.json &amp;&amp; test -f vite.config.ts &amp;&amp; test -f tsconfig.app.json &amp;&amp; test -f tsconfig.node.json &amp;&amp; test -f index.html &amp;&amp; test -f src/main.tsx &amp;&amp; test -f src/App.tsx &amp;&amp; grep -Fq '"react":' package.json &amp;&amp; ! grep -Fq '"vue":' package.json &amp;&amp; grep -Fq '"@types/node":' package.json &amp;&amp; grep -Fq '"jsx": "react-jsx"' tsconfig.app.json &amp;&amp; grep -Fq '@vitejs/plugin-react' vite.config.ts &amp;&amp; grep -Fq 'ws: true' vite.config.ts &amp;&amp; ! grep -Fq 'fonts.googleapis.com' index.html &amp;&amp; ! grep -Fq 'fonts.gstatic.com' index.html &amp;&amp; test -f design-reference/v2.html &amp;&amp; test -f design-reference/screenshots/v2-dashboard.png</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"(react|react-dom|@vitejs/plugin-react)":' frontend/package.json` returns `3`.
    - `grep -cE '"(vue|@vitejs/plugin-vue|vue-tsc)":' frontend/package.json` returns `0` (exit 1 acceptable — the count is the contract).
    - `grep -Fc '"@types/node":' frontend/package.json` returns `1` (reviewer item 7; literal-string check — reviewer item 24).
    - `grep -Fc 'outDir' frontend/vite.config.ts` returns `1` (FastAPI static mount invariant).
    - `grep -Fc "'/api'" frontend/vite.config.ts` returns `1` AND `grep -Fc "'/ws'" frontend/vite.config.ts` returns `1`.
    - `grep -Fc 'port: 5173' frontend/vite.config.ts` returns `1`.
    - `grep -Fc '"jsx": "react-jsx"' frontend/tsconfig.app.json` returns `1`.
    - `grep -Fc '"allowJs": true' frontend/tsconfig.app.json` returns `1`.
    - `grep -Fc '"types": ["node"]' frontend/tsconfig.node.json` returns `1`.
    - `grep -Fc 'type="text/babel"' frontend/index.html` returns `0` (no in-browser Babel).
    - `grep -Fc 'fonts.googleapis.com' frontend/index.html` returns `0` (reviewer item 6 — NO Google Fonts CDN; literal-string check per reviewer item 24).
    - `grep -Fc 'fonts.gstatic.com' frontend/index.html` returns `0` (reviewer item 6).
    - `grep -Fc 'rel="preconnect"' frontend/index.html` returns `0` (no external CDN preconnects at all).
    - `grep -Fc 'id="root"' frontend/index.html` returns `1`.
    - `grep -Fc 'createRoot' frontend/src/main.tsx` returns `1`.
    - `grep -Fc 'React.StrictMode' frontend/src/main.tsx` returns `1`.
    - `ls frontend/design-reference/v2.html frontend/design-reference/"Motion Spec.html" frontend/design-reference/screenshots/v2-dashboard.png frontend/design-reference/screenshots/v2-data-sources.png` lists all four files with exit 0.
  </acceptance_criteria>
  <done>All frontend/ scaffold files created with correct React/Vite/TS contents, **@types/node declared**, **index.html free of Google Fonts CDN**, design-reference/ populated, no Vue residue, no `styles.css` import yet (Wave 1 job).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Install deps, boot dev server, verify proxy against live backend + network-quiet boot</name>
  <files>
    frontend/package-lock.json   (generated by npm install)
    frontend/node_modules/       (generated by npm install — not committed)
  </files>
  <read_first>
    - frontend/package.json                                             (confirms deps pinned by Task 2)
    - frontend/vite.config.ts                                           (confirms proxy rules)
    - frontend/index.html                                               (must be CDN-free — reviewer item 6)
    - frontend/tsconfig.node.json                                       (must resolve @types/node — reviewer item 7)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-RESEARCH.md §Pitfall 11 (dev proxy port mismatch)
    - .planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-VALIDATION.md §Per-Task Verification Map 41.1-01-02 (npm build exit 0) 41.1-01-03 (curl proxy)
    - CLAUDE.md §Hard Constraints 2 (Local First)
  </read_first>
  <action>
    This is a checkpoint:human-verify task. Claude runs the automated preflight below, then presents logs to the user for manual confirmation.

    <what-built>
    Install React + Vite + TS deps cleanly, boot the Vite dev server, and prove the dev proxy actually forwards `/api/health` to the FastAPI backend on :8000. This is the gate that proves the toolchain swap succeeded before Wave 1 copies 12 JSX files into `src/`. Additionally, verify the local-first invariant: the dev server must boot WITHOUT any `fonts.googleapis.com` / `fonts.gstatic.com` request, and `tsc -b` must resolve `@types/node` (no `Cannot find type definition file for 'node'`).

    Automated preflight (Claude runs before presenting the checkpoint):

    1. From repo root, confirm scaffold files from Task 2 exist:
       ```
       test -f frontend/package.json &amp;&amp; test -f frontend/vite.config.ts &amp;&amp; test -f frontend/src/main.tsx || echo "SCAFFOLD MISSING — Task 2 did not complete"
       ```

    2. Clean install (RESEARCH §Pitfall 10 — stale node_modules/dist MUST be removed):
       ```
       rm -rf frontend/node_modules frontend/dist frontend/package-lock.json
       cd frontend &amp;&amp; npm install 2>&amp;1 | tee /tmp/41.1-01-npm-install.log
       ```
       Expected: exit 0, `package-lock.json` written, no `ERESOLVE` errors.
       If npm reports `peer dep warning` for `@types/react` vs react, that's acceptable; only hard errors block.

    3. Verify the build passes (no component code yet, but toolchain must compile `main.tsx + App.tsx`, and `tsc -b` must resolve `@types/node` for `tsconfig.node.json` without failing):
       ```
       cd frontend &amp;&amp; npm run build 2>&amp;1 | tee /tmp/41.1-01-npm-build.log
       # Assert no 'Cannot find type definition file for node' error (reviewer item 7):
       ! grep -Fi "Cannot find type definition file for 'node'" /tmp/41.1-01-npm-build.log
       ```
       Expected: exit 0, `dist/index.html` + `dist/assets/*.js` produced, no `@types/node` resolution errors.

    4. Confirm FastAPI is running on :8000 (required for the proxy test):
       ```
       curl -fsS http://localhost:8000/api/health 2>&amp;1 | tee /tmp/41.1-01-backend-health.json
       ```
       If this fails with `Connection refused`, Claude should surface to the user: "Backend not running on :8000. Start it with `uvicorn src.alphaswarm.web.app:app --reload --port 8000` before proceeding." Do NOT auto-start the backend — the user may already have one running in another terminal.

    5. Boot Vite dev server in background, verify proxy + index.html body is CDN-free:
       ```
       cd frontend &amp;&amp; (npm run dev &amp;) &amp;&amp; sleep 3
       curl -fsS http://localhost:5173/api/health 2>&amp;1 | tee /tmp/41.1-01-proxy-health.json
       curl -fsSI http://localhost:5173/ 2>&amp;1 | head -5 | tee /tmp/41.1-01-index-headers.txt
       # CDN-free invariant (reviewer item 6): the served index.html MUST NOT reference Google Fonts.
       curl -fsS http://localhost:5173/ | tee /tmp/41.1-01-index-body.html | (! grep -F "fonts.googleapis.com") &amp;&amp; (! grep -F "fonts.gstatic.com" /tmp/41.1-01-index-body.html)
       # tear down
       pkill -f "vite" || true
       ```
       Expected: both curls return 200. The `/api/health` response body must match `/tmp/41.1-01-backend-health.json`. The served `index.html` must contain NEITHER `fonts.googleapis.com` NOR `fonts.gstatic.com`.

    After automation completes, present the logs to the user for manual confirmation (Vite output, npm install log, both curl bodies matching, CDN-free body confirmation).
  </what-built>
  <how-to-verify>
    User must manually confirm:

    1. **Open a browser to `http://localhost:5173`** (Claude restarts the dev server on request). You should see the Wave 0 placeholder page: "AlphaSwarm — Wave 0 scaffold" and "React + Vite + TS toolchain online. Components port in Wave 1."

    2. **Open browser DevTools → Console tab.** Expected: ZERO red errors. Acceptable: a single "Download the React DevTools" info message. If any red errors, the scaffold has a regression — report back.

    3. **In DevTools → Network tab**, reload the page. Confirm `index.html` returns 200 from `:5173`, `main.tsx` is served (Vite transforms it on the fly). **Filter by `Domain != localhost`** — there should be ZERO requests to `fonts.googleapis.com`, `fonts.gstatic.com`, or any other external domain (reviewer item 6 + CLAUDE.md local-first).

    4. **Prove proxy works manually.** With the backend running on :8000 and the dev server on :5173, run:
       ```
       curl -s http://localhost:5173/api/health
       ```
       You should see a JSON response (same as `curl http://localhost:8000/api/health`). If you see 500 ECONNREFUSED, the backend isn't running — start it and retry.

    5. **Confirm archive coexistence (with preserved lockfile — reviewer item 15).** In a separate terminal:
       ```
       cd frontend-vue-archive &amp;&amp; ls node_modules 2>/dev/null | head -1
       test -f frontend-vue-archive/package-lock.json &amp;&amp; echo "OK: lockfile preserved"
       ```
       Expected: no `node_modules/` output, AND `package-lock.json` is present (if later you run `npm ci` inside the archive, it reproduces the Vue baseline deterministically).

    6. **Confirm no Vue bundle artifacts serve from FastAPI.** If your FastAPI static mount points to `frontend/dist/`, and we just rebuilt, the old Vue SPA is gone. Visit `http://localhost:8000/` — you should see a React-shell-only page (same placeholder as :5173) or a 404 if the mount requires rebuilds. Either is acceptable; the point is the Vue UI is NOT still being served.

    7. **Sign off** with "approved" ONLY if:
       - :5173 serves the React placeholder
       - Console is clean
       - **Network tab shows NO requests to fonts.googleapis.com / fonts.gstatic.com** (reviewer item 6)
       - `/api/health` proxy returns backend JSON
       - `npm run build` exits 0 with NO `Cannot find type definition file for 'node'` error (reviewer item 7; check `/tmp/41.1-01-npm-build.log`)
       - `frontend-vue-archive/package.json` AND `frontend-vue-archive/package-lock.json` both exist

    If any check fails, paste the failing output and Claude will fix before retry.
  </how-to-verify>
  </action>
  <verify>
    <automated>test -f frontend/package-lock.json &amp;&amp; test -d frontend/node_modules &amp;&amp; cd frontend &amp;&amp; npm run build 2>&amp;1 | tail -20 | tee /tmp/41.1-01-build-tail.log | grep -q "built in" &amp;&amp; ! grep -Fi "Cannot find type definition file for 'node'" /tmp/41.1-01-build-tail.log</automated>
  </verify>
  <done>User signs off with "approved". Old Vue frontend archived (with lockfile), fresh React scaffold at frontend/, npm install clean, npm run build exit 0 AND free of @types/node resolution errors, Vite dev proxy forwards /api/health to :8000, served index.html contains NO Google Fonts CDN references, frontend-vue-archive/package.json + package-lock.json preserved.</done>
  <resume-signal>Type "approved" to lock Wave 0 and unblock Wave 1. If any check failed, describe the failure (e.g., "dev server port collision with old Vue archive", "proxy 500 on /api/health", "tsc says Cannot find type definition file for 'node'", or "Network tab shows a request to fonts.googleapis.com") and Claude will remediate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Vite dev server | Local only; dev server binds 127.0.0.1:5173. No remote surface. |
| Vite dev proxy ↔ FastAPI (:8000) | Localhost-only HTTP+WS. Same machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-01 | Tampering | `dompurify`+`marked` pipeline for rationale markdown | scaffolded | Wave 0 only sets up `dompurify` + `marked` as deps; Wave 1 wires them into rationale rendering. Mitigation is live from Wave 1 onwards. This plan preserves the deps in `package.json`. |
| T-41.1-W0-02 | Information Disclosure | Vite `.env` exposure to client bundle | accept (N/A) | No `.env` files in `frontend/` (verified by RESEARCH §Runtime State Inventory). No `import.meta.env.VITE_*` reads introduced. Plan forbids adding any. |
| T-41.1-W0-03 | Tampering | `window.parent.postMessage` Claude-Artifacts bridge | accept | Not yet in scope — Wave 0 does not import any design JSX. Wave 1 plan T-41.1-02-* strips all `window.parent.postMessage` calls before any design file is imported. |
| T-41.1-W0-04 | Information Disclosure | Third-party font CDN (`fonts.googleapis.com` / `fonts.gstatic.com`) learning user IP at boot | mitigate (Wave 0) | `frontend/index.html` is CDN-free by construction. Verified by curl body check (reviewer item 6). Local-first per CLAUDE.md §Hard Constraints 2. If self-hosted fonts are later approved, they are served from `frontend/public/fonts/` and referenced via `@font-face`. |
</threat_model>

<verification>
Phase-level verification after Wave 0 completes:

- `test -d frontend-vue-archive &amp;&amp; test -f frontend-vue-archive/package.json &amp;&amp; test -f frontend-vue-archive/package-lock.json` → 0 (reviewer item 15)
- `test -d frontend &amp;&amp; test -f frontend/package.json &amp;&amp; test -f frontend/vite.config.ts` → 0
- `grep -Fc '"@types/node":' frontend/package.json` → `1` (reviewer item 7)
- `grep -Fc 'fonts.googleapis.com' frontend/index.html` → `0` (reviewer item 6)
- `cd frontend &amp;&amp; npm run build` → 0, no `Cannot find type definition file for 'node'` in log
- `curl -fsS http://localhost:5173/api/health` → 200 with backend JSON (backend must be running)
- `curl -fsS http://localhost:5173/ | grep -F 'fonts.googleapis.com' | wc -l` → `0`
- User manually approves the checkpoint
</verification>

<success_criteria>
Wave 0 is complete when:
- Old Vue frontend archived at `frontend-vue-archive/` with history via `git mv`.
- **`frontend-vue-archive/package-lock.json` preserved** (reviewer item 15 — reproducible parity reinstalls).
- Fresh React + Vite + TS scaffold at `frontend/` with no Vue deps.
- **`@types/node` declared in devDependencies** (reviewer item 7 — `tsconfig.node.json types: [node]` resolves).
- **`frontend/index.html` contains NO references to `fonts.googleapis.com` or `fonts.gstatic.com`** (reviewer item 6 — local-first per CLAUDE.md).
- `npm install` completes without `ERESOLVE` hard errors.
- `npm run build` exits 0 — toolchain compiles `main.tsx` + `App.tsx`, AND emits NO `Cannot find type definition file for 'node'` error.
- `vite dev` serves `:5173`, renders the placeholder, proxies `/api/health` to `:8000`, makes NO request to any external font CDN at boot.
- `frontend/design-reference/` holds `v2.html`, `Motion Spec.html`, and both screenshots (D-07).
- User signs off on the checkpoint.

Known regressions introduced vs current Vue UI:
- Wave 0 closes NO product KR (KR-41.1-01..11 are touched only in Waves 1–3).
- Wave 0 enforces an infrastructure policy: local-first font stack replaces the design's Google Fonts CDN. This is a CLAUDE.md "Local First" mandate, not a product regression, and is therefore NOT assigned a KR ID (reviewer items 6 + 23).
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-ui-port-wire-replace-vue-frontend-with-claude-design-ui-reac/41.1-01-SUMMARY.md`.
</output>
