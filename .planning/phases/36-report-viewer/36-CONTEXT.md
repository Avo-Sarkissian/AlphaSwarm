# Phase 36: Report Viewer - Context

**Gathered:** 2026-04-16
**Status:** Ready for planning

<domain>
## Phase Boundary

Surface the post-simulation market analysis report (generated by Phase 15's ReportAssembler) in the browser as a readable formatted modal. Includes a `GET /api/report/{cycle_id}` endpoint to fetch existing reports, a `POST /api/report/{cycle_id}/generate` endpoint to trigger generation, a "Generate Report" button with polling spinner, and a full-screen Report Viewer modal with rendered markdown.

This completes the full v5.0 Web UI feature set.

Out of scope: report history/archive, report sharing/export, report diff between cycles, notification when background generation completes (polling is sufficient).

</domain>

<decisions>
## Implementation Decisions

### In-Browser Report Generation
- **D-01:** Full trigger model. `POST /api/report/{cycle_id}/generate` spawns the ReACT engine (`ReactEngine` + `ReportAssembler`) as an `asyncio` background task. Returns 202 Accepted immediately. Phase guard: only available when `phase === 'complete'`.
- **D-02:** Generation in-progress guard — if a generation task is already running for this cycle, `POST /generate` returns 409 Conflict (prevents duplicate runs from double-clicking).
- **D-03:** Report output path: `reports/{cycle_id}_report.md` (same convention as CLI). Sentinel at `.alphaswarm/last_report.json` updated on completion (same as CLI path).
- **D-04:** Frontend polls `GET /api/report/{cycle_id}` every 3 seconds after triggering generation. Shows a "Generating report..." spinner while polling (until the endpoint returns 200 with content). No WebSocket integration — polling is sufficient for a minutes-long operation.
- **D-05:** `GET /api/report/{cycle_id}` reads `reports/{cycle_id}_report.md` from disk. Returns `{ cycle_id, content, generated_at }` on 200. Returns 404 if file doesn't exist. No database storage — file system is the source of truth.

### Panel Placement
- **D-06:** Full-screen modal overlay (~80% of viewport width and height). Follows the `CyclePicker.vue` pattern — mounted in `App.vue` with `v-if` and a `showReportViewer` ref. Force graph stays visible behind a dimmed overlay.
- **D-07:** New `ReportViewer.vue` component. Structure: header row (title + close button), scrollable content area, footer with generation button or status indicator.

### Access Trigger
- **D-08:** 'Report' button in `ControlBar.vue`, visible only when `snapshot.value.phase === 'complete'`. Emits `open-report-viewer` event up to `App.vue` (same pattern as `open-cycle-picker` → `showCyclePicker`). The button sits alongside the existing Stop button in the active/complete control row.
- **D-09:** On open: `ReportViewer.vue` immediately calls `GET /api/report/{cycle_id}`. If 200 → render report. If 404 → show "Generate Report" button. `cycle_id` is resolved from `GET /api/replay/cycles` limit=1 (most recent completed cycle) — same source of truth used by the interview flow.

### Markdown Rendering
- **D-10:** `marked` + `DOMPurify` client-side. `npm install marked dompurify @types/dompurify`. Component calls `marked.parse(content)` then `DOMPurify.sanitize(html)`, injects via `v-html`. Report is locally generated so XSS risk is minimal, but DOMPurify is best practice.
- **D-11:** Report modal content area gets scoped CSS for markdown typography: `h2` section headers, `strong` bold, `table` borders using existing `--color-border` token. Keeps the report readable without importing a full markdown stylesheet.

### Claude's Discretion
- Exact CSS for the report modal size and overlay backdrop
- Whether to debounce the 3s polling loop (cancel on modal close)
- Error handling for failed `POST /generate` calls (show inline error, don't crash modal)
- Whether `cycle_id` is shown in the modal header as a full UUID or truncated
- Exact button label copy ("Generate Report" vs "Create Report")

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Report generation (core — read before any backend work)
- `src/alphaswarm/report.py` — `ReactEngine` class (line 121), `ReportAssembler.assemble()` (line 281), `write_report()` (line 319), `write_sentinel()` (line 333); full generation pipeline
- `src/alphaswarm/cli.py` lines ~664–760 — `_handle_report()` async function: exact sequence of graph→ReactEngine→ReportAssembler→write_report→write_sentinel to replicate in the web route

### Phase requirements
- `.planning/ROADMAP.md` §"Phase 36: Report Viewer" — goal, success criteria SC-1 through SC-4

### Frontend integration points
- `frontend/src/App.vue` — `showCyclePicker` ref + `onOpenCyclePicker` / `onCloseCyclePicker` pattern to replicate for `showReportViewer`; where `<ReportViewer>` modal is mounted
- `frontend/src/components/ControlBar.vue` — where to add 'Report' button (complete-phase conditional); existing `@open-cycle-picker` emit pattern to follow
- `frontend/src/components/CyclePicker.vue` — modal component pattern: fetch-on-open, loading state, overlay/backdrop, close button

### Web infrastructure patterns
- `src/alphaswarm/web/app.py` — lifespan pattern for mounting singletons on `app.state`; router registration
- `src/alphaswarm/web/routes/interview.py` — phase guard pattern (`phase !== COMPLETE` → 409), `app.state` access, route file structure to replicate
- `src/alphaswarm/web/routes/replay.py` — background task and `app.state.sim_manager` access patterns

### Prior phase context
- `.planning/phases/35-agent-interviews-web-ui/35-CONTEXT.md` — established Vue modal/panel patterns and ControlBar button conventions

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `ReactEngine` (`report.py:121`): `async def run(self, cycle_id: str) -> list[ToolObservation]` — takes `ollama_client` and `graph_manager` in `__init__`. Backend route constructs it the same way as `_handle_report` in `cli.py`.
- `ReportAssembler.assemble()` (`report.py:281`): synchronous, takes `observations` + `cycle_id`, returns markdown string
- `write_report()` / `write_sentinel()` (`report.py:319,333`): async, aiofiles-based; `write_sentinel` updates `.alphaswarm/last_report.json`
- `read_completed_cycles()` (`graph.py:1878`): already used by interview flow — `cycles[0]["cycle_id"]` gives the most recent completed cycle_id
- `CyclePicker.vue`: full modal component reference — overlay backdrop, loading spinner, close-on-backdrop-click, header + scrollable list + action buttons pattern

### Established Patterns
- `asyncio.create_task()` for background work: broadcaster uses `asyncio.create_task(start_broadcaster(...))` in lifespan — report generation follows same pattern; store task reference on `app.state.report_task` to detect in-progress state
- ControlBar conditional buttons: `v-if` based on `snapshot.value.phase` (replay strip already does this for the replay/advance buttons)
- `emit('open-cycle-picker')` → `App.vue` `@open-cycle-picker="onOpenCyclePicker"`: exact emit/handler chain to replicate for `open-report-viewer`

### Integration Points
- `src/alphaswarm/web/app.py` lifespan: add `app.state.report_task = None` initialization; import and register new `report_router`
- `frontend/src/App.vue`: add `showReportViewer = ref(false)`, `onOpenReportViewer` / `onCloseReportViewer` handlers; mount `<ReportViewer v-if="showReportViewer" @close="onCloseReportViewer" />`
- `frontend/src/components/ControlBar.vue`: add `emit('open-report-viewer')` and Report button guarded by `snapshot.phase === 'complete'`
- New files: `src/alphaswarm/web/routes/report.py`, `frontend/src/components/ReportViewer.vue`

</code_context>

<specifics>
## Specific Ideas

- Full-screen modal selected for the report — content is too long for a sidebar panel, needs viewport width to render report tables and section headers legibly.
- ControlBar Report button is the primary entry point — globally accessible in `complete` phase without needing to open an agent first.
- Polling every 3s is sufficient — no WebSocket event needed for a minutes-long background task.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 36-report-viewer*
*Context gathered: 2026-04-16*
