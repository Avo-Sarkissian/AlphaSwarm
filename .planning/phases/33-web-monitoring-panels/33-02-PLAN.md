---
phase: 33-web-monitoring-panels
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - frontend/src/App.vue
autonomous: false
requirements:
  - WEB-03
  - WEB-04

user_setup: []

must_haves:
  truths:
    - "Panel strip appears below the force graph when simulation is active (!isIdle)"
    - "BracketPanel renders in the left half of the panel strip, RationaleFeed in the right half"
    - "Panel strip disappears in idle and complete states"
    - "Panel strip shrinks when AgentSidebar is open (same pattern as graph-container)"
    - "allRationales is provided to child components via App.vue provide chain"
  artifacts:
    - path: "frontend/src/App.vue"
      provides: "Layout integration of monitoring panels"
      contains: "panel-strip"
  key_links:
    - from: "frontend/src/App.vue"
      to: "frontend/src/components/BracketPanel.vue"
      via: "import and render in panel-strip"
      pattern: "import BracketPanel"
    - from: "frontend/src/App.vue"
      to: "frontend/src/components/RationaleFeed.vue"
      via: "import and render in panel-strip"
      pattern: "import RationaleFeed"
    - from: "frontend/src/App.vue"
      to: "frontend/src/composables/useWebSocket.ts"
      via: "destructure allRationales and provide to children"
      pattern: "provide.*allRationales"
---

<objective>
Integrate BracketPanel and RationaleFeed into App.vue by restructuring the main-content layout to a flex column (force graph on top, panel strip at bottom) and wiring the allRationales provide. Conclude with human visual verification.

Purpose: Wires Plan 01's artifacts into the live application, delivering the complete monitoring panels feature visible in the browser.
Output: Modified App.vue with bottom panel strip layout. Visual verification of both panels updating from WebSocket data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-web-monitoring-panels/33-CONTEXT.md
@.planning/phases/33-web-monitoring-panels/33-UI-SPEC.md
@.planning/phases/33-web-monitoring-panels/33-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

From frontend/src/composables/useWebSocket.ts (Plan 01 extended):
```typescript
export interface WebSocketState {
  snapshot: Readonly<Ref<StateSnapshot>>
  connected: Readonly<Ref<boolean>>
  reconnectFailed: Readonly<Ref<boolean>>
  latestRationales: Readonly<Ref<Map<string, RationaleEntry>>>
  allRationales: Readonly<Ref<RationaleEntry[]>>
}
```

From frontend/src/components/BracketPanel.vue (Plan 01 created):
```typescript
// Injects: inject<Ref<StateSnapshot>>('snapshot')
// No props, no emits -- pure display component
```

From frontend/src/components/RationaleFeed.vue (Plan 01 created):
```typescript
// Injects: inject<Ref<RationaleEntry[]>>('allRationales')
// No props, no emits -- pure display component
```

From frontend/src/assets/variables.css (Plan 01 added):
```css
--panel-strip-height: 232px;
--duration-feed-enter: 200ms;
--duration-feed-exit: 200ms;
```

From frontend/src/App.vue (current structure, line 41):
```html
<!-- Current: single graph-container div when not idle -->
<div v-else class="graph-container" :class="{ 'graph-container--sidebar-open': sidebarOpen }" id="graph-container">
  <ForceGraph @select-agent="onSelectAgent" />
</div>
```
</interfaces>
</context>

<threat_model>
## Threat Model (ASVS Level 1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via provided reactive data | LOW | allRationales data flows through Vue's reactive system. Components use mustache interpolation (auto-escaped). No raw HTML rendering anywhere in the provide chain. |
| Layout injection via CSS custom properties | LOW | All CSS vars defined in variables.css under project control. No user-supplied values in CSS custom properties. |

No HIGH severity threats. This plan modifies layout and wiring only -- no new data inputs or external integrations.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Restructure App.vue layout and wire monitoring panels</name>
  <files>frontend/src/App.vue</files>
  <read_first>
    - frontend/src/App.vue (current full implementation -- MUST read to understand existing template, script, and style)
    - frontend/src/components/BracketPanel.vue (created by Plan 01 -- verify it exists and uses inject)
    - frontend/src/components/RationaleFeed.vue (created by Plan 01 -- verify it exists and uses inject)
    - frontend/src/composables/useWebSocket.ts (Plan 01 extended -- verify allRationales is in return)
    - frontend/src/assets/variables.css (verify --panel-strip-height exists)
    - .planning/phases/33-web-monitoring-panels/33-UI-SPEC.md (Layout Contract section)
  </read_first>
  <action>
Modify `frontend/src/App.vue` with three changes: script imports, template restructure, and new CSS classes.

**Change 1: Script setup -- add imports and provide (per D-09)**

Add imports for the two new components (after existing component imports):
```typescript
import BracketPanel from './components/BracketPanel.vue'
import RationaleFeed from './components/RationaleFeed.vue'
```

Modify the useWebSocket destructure to include `allRationales`:
```typescript
const { snapshot, connected, reconnectFailed, latestRationales, allRationales } = useWebSocket()
```

Add the new provide after existing provides (after `provide('latestRationales', latestRationales)` on line 13):
```typescript
provide('allRationales', allRationales)
```

**Change 2: Template -- restructure main-content (per D-01, D-02, D-03)**

Replace the current `<div class="main-content">` block (lines 36-44):

FROM:
```html
<div class="main-content">
  <div v-if="isIdle" class="empty-state">
    <h1 class="empty-state__heading">Waiting for Simulation</h1>
    <p class="empty-state__body">Start a simulation to see 100 agents deliberate in real time.</p>
  </div>
  <div v-else class="graph-container" :class="{ 'graph-container--sidebar-open': sidebarOpen }" id="graph-container">
    <ForceGraph @select-agent="onSelectAgent" />
  </div>
</div>
```

TO:
```html
<div class="main-content">
  <div v-if="isIdle" class="empty-state">
    <h1 class="empty-state__heading">Waiting for Simulation</h1>
    <p class="empty-state__body">Start a simulation to see 100 agents deliberate in real time.</p>
  </div>
  <template v-else>
    <div class="graph-container" :class="{ 'graph-container--sidebar-open': sidebarOpen }" id="graph-container">
      <ForceGraph @select-agent="onSelectAgent" />
    </div>
    <div class="panel-strip" :class="{ 'panel-strip--sidebar-open': sidebarOpen }">
      <BracketPanel />
      <div class="panel-strip__divider" />
      <RationaleFeed />
    </div>
  </template>
</div>
```

Key layout changes:
- `.main-content` becomes `display: flex; flex-direction: column;` (was just `flex: 1; overflow: hidden;`)
- Graph container keeps `flex: 1; min-height: 0;` (shrinks to accommodate panel strip)
- Panel strip is 232px fixed height at the bottom
- `<template v-else>` wraps both graph + panel strip so they share the `!isIdle` condition (per D-03)
- Panel strip gets the same `--sidebar-open` pattern as graph container

**Change 3: CSS -- add panel-strip styles and update main-content**

Update `.main-content` style to add flex column layout:
```css
.main-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  position: relative;
  min-height: 0;
}
```

Keep `.graph-container` as-is but ensure it has `flex: 1; min-height: 0;` so it shrinks when panel strip appears. Current styles already have `width: 100%; height: 100%;` -- change `height: 100%;` to `flex: 1; min-height: 0;` since the parent is now a flex column:
```css
.graph-container {
  width: 100%;
  flex: 1;
  min-height: 0;
  transition: width var(--duration-sidebar-enter) var(--easing-enter);
}
```

Add new CSS classes:
```css
/* Phase 33: Bottom monitoring panel strip (per D-01, D-02) */
.panel-strip {
  height: var(--panel-strip-height);
  flex-shrink: 0;
  display: flex;
  background-color: var(--color-bg-secondary);
  border-top: 1px solid var(--color-border);
  transition: width var(--duration-sidebar-enter) var(--easing-enter);
}

.panel-strip--sidebar-open {
  width: calc(100vw - var(--sidebar-width));
}

.panel-strip__divider {
  width: 1px;
  flex-shrink: 0;
  background-color: var(--color-border);
}

/* Each panel half fills equally */
.panel-strip > :first-child,
.panel-strip > :last-child {
  flex: 1;
  overflow: hidden;
  padding: var(--space-md);
  min-width: 0;
}

/* Responsive: stack panels vertically below 1024px */
@media (max-width: 1023px) {
  .panel-strip {
    flex-direction: column;
  }
  .panel-strip__divider {
    width: 100%;
    height: 1px;
    flex-shrink: 0;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.vue` contains `import BracketPanel from './components/BracketPanel.vue'`
    - `frontend/src/App.vue` contains `import RationaleFeed from './components/RationaleFeed.vue'`
    - `frontend/src/App.vue` contains `allRationales` in the useWebSocket destructure
    - `frontend/src/App.vue` contains `provide('allRationales', allRationales)`
    - `frontend/src/App.vue` contains `<template v-else>` wrapping graph-container and panel-strip
    - `frontend/src/App.vue` contains `class="panel-strip"`
    - `frontend/src/App.vue` contains `<BracketPanel />`
    - `frontend/src/App.vue` contains `<RationaleFeed />`
    - `frontend/src/App.vue` contains `class="panel-strip__divider"`
    - `frontend/src/App.vue` contains `.panel-strip--sidebar-open`
    - `frontend/src/App.vue` contains `var(--panel-strip-height)`
    - `frontend/src/App.vue` CSS contains `flex-direction: column` on `.main-content`
    - `frontend/src/App.vue` CSS contains `@media (max-width: 1023px)`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>App.vue restructured: main-content is flex column with force graph (flex: 1) on top and 232px panel strip at bottom. Panel strip contains BracketPanel (left), divider, RationaleFeed (right). Both panels hidden in idle/complete state. allRationales provided to children. Sidebar-open width adjustment applied to panel strip. Responsive stacking at <1024px. Full production build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of monitoring panels</name>
  <files>frontend/src/App.vue</files>
  <action>
Human visually verifies the complete monitoring panels feature in the browser. No code changes -- this is a verification-only checkpoint. The executor should start the dev servers if not already running and present the verification steps to the user.
  </action>
  <what-built>
    Complete monitoring panels feature: BracketPanel (D3 stacked bars) and RationaleFeed (animated entry list) integrated into the browser dashboard below the force graph.
  </what-built>
  <how-to-verify>
    1. Start the backend: `cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm && uv run python -m alphaswarm.web`
    2. Start the frontend dev server: `cd frontend && npm run dev`
    3. Open browser to http://localhost:5173

    **Idle state check:**
    4. Verify: "Waiting for Simulation" empty state shown. NO panel strip visible at the bottom.

    **Start simulation check:**
    5. Click "Start Simulation" in the ControlBar.
    6. Verify: Force graph appears at the top. A horizontal panel strip appears at the bottom (~232px height).
    7. Verify: Panel strip has two halves separated by a thin border line.
    8. Verify LEFT half: "Brackets" title in blue (accent color). 10 horizontal bars with bracket names (Quants, Degens, Sovereigns, Macro, Suits, Insiders, Agents, Doom-Posters, Policy Wonks, Whales) as Y-axis labels. Bars initially show gray (pending) fill, then fill with green (buy), red (sell), gray (hold) segments as agents respond.
    9. Verify RIGHT half: "Rationale Feed" title in blue. Initially shows "Awaiting agent rationale..." in italic muted text. As agents respond, entries appear with agent name ("Agent 01"), colored signal chip (BUY in green, SELL in red, HOLD in gray), and truncated rationale text.
    10. Verify ANIMATION: New feed entries slide in from above (subtle translateY + opacity). When more than 20 entries arrive, oldest entries fade out.
    11. Verify BAR ANIMATION: Between rounds, bar widths change smoothly (not snapping).

    **Sidebar interaction check:**
    12. Click a node in the force graph. Verify: AgentSidebar slides in from the right. Both the graph and the panel strip shrink in width to accommodate.

    **Responsive check (optional):**
    13. Resize browser below 1024px width. Verify: Panel strip panels stack vertically instead of side-by-side.
  </how-to-verify>
  <verify>
    <automated>cd /Users/avosarkissian/Documents/VS\ Code/AlphaSwarm/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms panel strip appears below force graph during active simulation
    - Human confirms bracket bars show 10 brackets with animated width transitions
    - Human confirms rationale feed shows entries with slide-in animation
    - Human confirms panels hidden in idle state
    - Human confirms sidebar interaction shrinks both graph and panel strip
  </acceptance_criteria>
  <done>Human has visually verified: (1) panel strip layout correct, (2) bracket bars render and animate, (3) rationale feed entries animate in/out, (4) panels hidden in idle, (5) sidebar interaction works. Phase 33 monitoring panels feature complete.</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
After both tasks:
1. `cd frontend && npm run build` passes (full production build)
2. App.vue has flex column layout with graph + panel strip
3. Both BracketPanel and RationaleFeed render in the panel strip
4. Panels appear only during active simulation
5. Human has visually verified animations and layout
</verification>

<success_criteria>
- App.vue layout is flex column: graph (flex:1) + panel strip (232px fixed)
- BracketPanel visible in left half with 10 bracket bars
- RationaleFeed visible in right half with animated entries
- Panel strip hidden in idle/complete state
- Panel strip shrinks when sidebar opens
- Human approves visual behavior
</success_criteria>

<output>
After completion, create `.planning/phases/33-web-monitoring-panels/33-02-SUMMARY.md`
</output>
