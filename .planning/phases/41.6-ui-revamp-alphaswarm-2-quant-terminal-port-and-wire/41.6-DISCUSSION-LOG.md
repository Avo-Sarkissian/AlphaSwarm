# Phase 41.6: UI Revamp — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-08
**Phase:** 41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire
**Areas discussed:** Porting strategy, AdvisoryV2 wiring approach, 5 net-new files, Verification + Known Regressions

---

## Porting strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Surface-by-surface | W1 shell, W2 viz/panels/states, W3 modals/v2, W4 5 new files + UAT. ~4 plans, partial-progress visibility, per-surface rollback | ✓ |
| All-at-once swap | Single wave, single PR — fastest if it works, total break if it doesn't | |
| Shell-first, then modals | W1 shell + dashboard surfaces; W2 modals + new files + UAT. ~2 plans | |

**User's choice:** Surface-by-surface.

---

| Option | Description | Selected |
|--------|-------------|----------|
| In place on current branch | Edit `frontend/` directly per wave, atomic commits per plan | |
| Side branch + final swap | Build new UI on feature branch, merge once-and-done | |
| Parallel folder (`frontend-v2/`) | Build alongside current, A/B compare, then rename | |
| **(User-introduced)** Treat as new frontend: archive current, scaffold fresh, lift wiring wholesale | Archive `frontend/` → `frontend-react-archive/`. Scaffold fresh `frontend/` from AlphaSwarm-2 sources as ES modules. Lift `api/` + `context/` + `hooks/` + `adapter/` + `types.ts` + `mocks/` from archive. Wire surface-by-surface. | ✓ |

**User's quote:** *"I want this to be treated as a separate and new front end. some of the wiring can be copied over, but this is the new standard."*

**Notes:** This decision implicitly resolves the "breakage policy" question (no preserve-current option exists) and the AdvisoryV2 question (lift wiring pattern from archived AdvisoryModal into AdvisoryV2; not a merge-into-old-modal option).

---

## AdvisoryV2 wiring approach

| Option | Description | Selected |
|--------|-------------|----------|
| Render only what backend gives + hide/stub the rest | Wire 5 backend fields; em-dash the rest as KR | |
| Mix: derive what's reasonable, stub the rest | Wire backend fields + derive weight/agents + best-effort parse rec/risk/upside from portfolio_outlook + stub the rest | ✓ |
| Extend backend to emit missing fields | Add shares/basis/last via /api/holdings join + new AdvisoryReport fields | |
| Defer extras to a later phase, ship overview tab only | Wire only Overview tab; Holdings/Risk/Isolation = 'Coming in v6.x' placeholder | |

**User's choice:** Mix: derive what's reasonable, stub the rest.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Render portfolio_outlook verbatim, drop hardcoded headline | renderMarkdown(portfolio_outlook) in body; headline = generic label or first line | |
| Parse first line as headline / rest as body | Convention-based; requires orchestrator prompt discipline | |
| Body only, no headline | Removes headline entirely | |
| **(User clarification)** Hardcoded data is Claude Design filler | Renders fall under "derive reasonable" rule from previous question; exact presentation is Claude's discretion | ✓ |

**User's clarification:** *"The primary recommendations are likely hardcoded, bc claude design which build the UI used filler information to show what it would look like in a real use case."*

---

## 5 net-new files — canonical or experimental?

| Option | Description | Selected |
|--------|-------------|----------|
| Port + wire interview_v2 / onboarding / bracket_deep; defer new_features; data.jsx as reference only | 3 surfaces wire to existing endpoints; data.jsx → BRACKETS constant only; new_features → future phase | ✓ |
| Port + wire all 5 (including new_features) | What-If / Watch / Multi-seed UI-only / mocked initially; backend follow-up | |
| Just interview_v2 + onboarding (skip bracket_deep + new_features + data.jsx) | Minimal port surface | |

**User's choice:** Port + wire interview_v2 / onboarding / bracket_deep; defer new_features; data.jsx as reference only.

---

## Verification + Known Regressions

| Option | Description | Selected |
|--------|-------------|----------|
| New parity matrix tailored to new surfaces | ~25–30 rows; one row per surface; AdvisoryV2 = 4 rows (one per tab); rigorous human UAT | ✓ |
| Reuse 41.1's 28-row matrix verbatim | Patch only changed rows; faster but doesn't cover new surfaces | |
| Minimal smoke check | ~12 binary checks; renders + golden path + live data populates | |

**User's choice:** New parity matrix tailored to new surfaces.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Carry forward all 11 + add new KR-41.6-NN for AdvisoryV2 schema gaps | None of the 11 KR-41.1-NN have closed in the adapter; add new KRs only | |
| Carry forward 11 + try to close KR-41.1-10 (citations/sources) as stretch goal | Attempt per-rationale citation wire-up; drop KR if it works | |
| Audit all 11 KRs and close anything cheap | Investigate each KR; close what's cheap; add new KR-41.6-NN for AdvisoryV2 gaps | ✓ |

**User's choice:** Audit all 11 KRs and close anything cheap.

---

## Claude's Discretion

- CSS reconciliation (take new 536-LOC styles.css as foundation, merge in only necessary current overrides)
- `.jsx` → `.tsx` conversion timing (incremental during port is fine)
- Mocks fate (lift as-is per D-05 behind dev-only gate)
- Onboarding localStorage flag name + gating logic
- Exact regex for citation agent ID extraction in AdvisoryV2 `agents` derivation
- Whether `bracket_deep` mounts as modal or replaces dashboard

## Deferred Ideas

- `new_features.jsx` (What-If / Watch / Multi-seed / Seed Suggestions) — needs backend; future phase
- Backend extension for AdvisoryV2 rich fields (shares/basis/last/sentiment, structured risk/upside) — future advisory pipeline phase
- Streaming interview (closes KR-41.1-11)
- Backend signal-wire + sources endpoints (closes KR-41.1-02)
- Backend cycle-list extension (closes KR-41.1-07)
- Backend rewind endpoint (closes KR-41.1-06)
- Backend structured error stream (closes KR-41.1-08)
- Backend live `influence.outDegree` (closes KR-41.1-09)
- Backend Ollama slot counts (closes KR-41.1-05)
- Backend memory MB on telemetry (closes KR-41.1-04)
- Full `.jsx` → `.tsx` conversion as cleanup phase
