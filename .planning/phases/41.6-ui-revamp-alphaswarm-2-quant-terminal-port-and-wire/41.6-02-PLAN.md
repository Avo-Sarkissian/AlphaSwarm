---
phase: 41.6
plan: 02
type: execute
wave: 2
depends_on: ["41.6-01"]
files_modified:
  - frontend/src/components/icons.jsx
  - frontend/src/components/viz.jsx
  - frontend/src/components/panels.jsx
  - frontend/src/components/states.jsx
  - frontend/src/components/tweaks.jsx
  - frontend/src/components/app_v2.tsx       # REPLACES placeholder from W1
  - frontend/src/components/TweaksPanelLoader.tsx   # dev-only gate around tweaks (re-creates archive pattern)
  - frontend/src/styles.css                   # MERGE: live-wiring overrides from archive's 1417-LOC stylesheet
autonomous: false
requirements_addressed: ["IMPLICIT-PARITY-W2-DASHBOARD"]
known_regressions:
  carry_forward:
    - KR-41.1-01 (force graph deterministic SVG — design choice, no d3-force)
    - KR-41.1-03 (agent flipped/roundLastSpoke/thinking stubbed in adapter — visual surfaces fall back to neutral)
    - KR-41.1-04 (telemetry memory shown as % not MB; MemoryPausedState GB literal stays as design placeholder)
    - KR-41.1-05 (slotsMax stubbed 8 in adapter; slotsUsed live)
    - KR-41.1-08 (ErrorState event log lines design-static; backend has no structured error stream)
    - KR-41.1-09 (Top Influencers ranks by confidence DESC during live; post-cycle re-rank by edges happens in W3)
    - KR-41.1-10 (RationaleFeed renders "—" for cites — RationaleEntry dataclass lacks cited_agents)
  files_new:
    - KR-41.6-08 (ModelStatus chip shows hardcoded 'qwen3:8b' matching config.py worker_model default — no /api/settings to read configured model)
    - KR-41.6-11 (states.jsx IdleState SystemCheck: Ollama from /api/health; Agents/Data sources/Storage rows informational design copy)
threat_model:
  trust_boundaries:
    - "User browser ↔ ported components (XSS surface for any innerHTML rendering)"
    - "Vite production bundle ↔ user (mocks must not leak)"
  threats:
    - id: T-41.6-04
      category: I (Information Disclosure)
      component: "frontend/src/mocks/* in production bundle"
      disposition: mitigate
      mitigation: "W2 task 4 acceptance criteria runs the consolidated production grep gate post-`npm run build`: forbid `window\\.AS_DATA`, `window\\.Icon`, `Object\\.assign(window`, `SIGNAL_WIRE_SEED`, `MOCK_`, `_MOCK`, `mocks/wire`, `mocks/sources` in `dist/assets/*.js`. SignalWire + DataSources mock import sites are gated to W3 Plan 03 — they don't load from W2 components."
    - id: T-41.6-05
      category: T (Tampering)
      component: "viz.jsx SVG node hover tooltip"
      disposition: accept
      mitigation: "Tooltip renders agent.bracketDisplay/signal/confidence — all are server-emitted string fields with no user input. No XSS surface."
    - id: T-41.6-06
      category: I (Information Disclosure)
      component: "design literals in ported source (e.g., 'Trim AAPL', '$500B')"
      disposition: mitigate
      mitigation: "Pitfall #3 grep gate in W4 (Plan 04) forbids these literals in `frontend/src/`. W2 components don't carry advisory literals (those are in v2.jsx → Plan 03)."

must_haves:
  truths:
    - "Force graph renders 100 agents from useAgents() — no d3-force, deterministic SVG layout"
    - "KPI strip displays live tps/memMb/slotsUsed/elapsedSeconds from useTelemetry() (memory shown as %)"
    - "BracketList renders bracket summaries from useBrackets()"
    - "RationaleFeed renders rationales from useRationales() with '—' for missing citations (KR-41.1-10)"
    - "ConsensusRing renders consensus from useAgents() agents"
    - "Top Influencers panel ranks agents by confidence DESC (KR-41.1-09 live default)"
    - "Tooltip on agent hover reads agent.bracketDisplay, agent.signal, agent.confidence, agent.flipped (flipped=false stub OK)"
    - "ModelStatus chip shows phase + tps + slots from useTelemetry() + useConnection(); model name hardcoded 'qwen3:8b' per KR-41.6-08 (matches config.py worker_model)"
    - "Lifecycle: IdleState mounts when no simulation running (telemetry phase=='idle'); SeedingState when phase=='initializing'; MemoryPausedState when telemetry.memMb >= 90; ErrorState when useConnection().reconnectFailed"
    - "Topbar Run/Stop wires to simStart(seed) / simStop() from api/simulation"
    - "ZERO `window.AS_DATA` / `window.Icon` / `Object.assign(window` references anywhere in frontend/src/"
    - "ZERO mock data identifiers (SIGNAL_WIRE_SEED, MOCK_, _MOCK, mocks/wire, mocks/sources) in frontend/dist/assets/*.js"
    - "TS check passes; Vite production build passes"
    - "Human verifies live KPI strip ticks, force graph renders 100 agents, lifecycle states toggle correctly"
  artifacts:
    - path: "frontend/src/components/icons.jsx"
      provides: "Icon component, named export"
      contains: "export const Icon"
      min_lines: 25
    - path: "frontend/src/components/viz.jsx"
      provides: "Viz force graph component (deterministic SVG)"
      contains: "export function Viz"
      min_lines: 200
    - path: "frontend/src/components/panels.jsx"
      provides: "BracketList, RationaleFeed, KpiStrip, ConsensusRing"
      contains: "export function BracketList"
    - path: "frontend/src/components/states.jsx"
      provides: "IdleState, SeedingState, MemoryPausedState, ErrorState"
      contains: "export function IdleState"
    - path: "frontend/src/components/tweaks.jsx"
      provides: "TweaksPanel (dev-only)"
      contains: "export function TweaksPanel"
    - path: "frontend/src/components/app_v2.tsx"
      provides: "DashboardShell — main shell composing viz + panels + topbar + lifecycle dispatch"
      contains: "export function App"
      min_lines: 250
  key_links:
    - from: "frontend/src/components/app_v2.tsx"
      to: "frontend/src/context/{Agents,Bracket,Telemetry,Rationales,Connection}Context.tsx"
      via: "useAgents() / useBrackets() / useTelemetry() / useRationales() / useConnection() hooks"
      pattern: "useAgents\\(\\)|useBrackets\\(\\)|useTelemetry\\(\\)|useRationales\\(\\)|useConnection\\(\\)"
    - from: "frontend/src/components/app_v2.tsx"
      to: "frontend/src/api/simulation.ts"
      via: "simStart(seed) / simStop() called from topbar Run/Stop handlers"
      pattern: "simStart|simStop"
    - from: "frontend/src/components/states.jsx"
      to: "frontend/src/data.ts"
      via: "import { BRACKETS } from '../data'"
      pattern: "from '\\.\\./data'"
    - from: "frontend/src/components/panels.jsx (KpiStrip)"
      to: "frontend/src/context/TelemetryContext.tsx"
      via: "props from app_v2 sourced from useTelemetry()"
      pattern: "tps|memMb|slotsUsed|elapsedSeconds"
---

<objective>
Wave 2: Port the 6 dashboard surface files from `AlphaSwarm-2/src/` (icons, viz, panels, states, tweaks, app_v2) into `frontend/src/components/` as native ES modules per D-03. Convert every `const { useState } = React;` destructure to `import { useState } from 'react'`, every `Object.assign(window, {...})` / `window.X = X` export to `export function X` / `export const X`, every `window.AS_DATA.X` consumer to a context hook (`useAgents` / `useBrackets` / `useTelemetry` / `useRationales` / `useConnection`) or import from `../data` (BRACKETS only).

Then replace the W1 placeholder `app_v2.tsx` with the fully-ported `app_v2.jsx` from AlphaSwarm-2, wired through the 6-context tree. The local `setInterval` mock telemetry clock at `AlphaSwarm-2/src/app_v2.jsx:74-83` is REPLACED with `useTelemetry()` per RESEARCH.md "Anti-Patterns to Avoid" #1.

CSS reconciliation: take a single audit pass on `frontend-react-archive/src/styles.css` (1417 LOC) to identify additions made during Phase 41.2 / 41.4 / 999.x that the new live-wired surfaces depend on (loading spinners, error banners, polling-timeout styling). Cherry-pick those into `frontend/src/styles.css` (currently a byte copy of AlphaSwarm-2/styles.css from W1). Do NOT carry the whole archived stylesheet over.

Purpose: After this wave, the main dashboard surfaces are live. Force graph renders 100 agents, KPI strip ticks at 5Hz, brackets panel shows live bracket summaries, rationale feed prints rationale events, lifecycle states toggle on phase changes, Topbar Run/Stop fires simStart / simStop. Modals + AdvisoryV2 + 5 net-new still pending W3/W4.

Output:
- 6 ported component files
- Replaced `app_v2.tsx` (full shell)
- TweaksPanelLoader.tsx (dev-only gate around tweaks per archive pattern)
- Reconciled `styles.css`
- TS check + Vite build pass
- Production grep gate green (no globals, no mock leaks)
- Human-verified live dashboard ticks
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-CONTEXT.md
@.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-RESEARCH.md
@.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-VALIDATION.md
@.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-01-SUMMARY.md
@CLAUDE.md
@AlphaSwarm-2/CONTRACT.md
@AlphaSwarm-2/src/icons.jsx
@AlphaSwarm-2/src/viz.jsx
@AlphaSwarm-2/src/panels.jsx
@AlphaSwarm-2/src/states.jsx
@AlphaSwarm-2/src/tweaks.jsx
@AlphaSwarm-2/src/app_v2.jsx
@AlphaSwarm-2/styles.css
@frontend-react-archive/src/styles.css
@frontend-react-archive/src/components/TweaksPanelLoader.tsx
@frontend/src/context/AgentsContext.tsx
@frontend/src/context/BracketContext.tsx
@frontend/src/context/TelemetryContext.tsx
@frontend/src/context/RationalesContext.tsx
@frontend/src/context/ConnectionContext.tsx
@frontend/src/data.ts
@frontend/src/api/simulation.ts

<interfaces>
<!-- Context hooks W2 components consume — exported by frontend/src/context/* lifted in W1 Plan 01 task 2 -->

useAgents() returns: { agents: Agent[] } where Agent matches CONTRACT §2.1
useBrackets() returns: { summaries: BracketSummary[] } per CONTRACT §2.2
useTelemetry() returns: { tps: number; memMb: number; slotsUsed: number; slotsMax: number; elapsedSeconds: number; phase: string; ... }
useRationales() returns: { rationales: Rationale[] } per CONTRACT §2.3 (with citations: [] stubbed per KR-41.1-10)
useConnection() returns: { connected: boolean; reconnectFailed: boolean; lastFrame: StateFrame | null }
useEdgesCtx() returns: { edges: Edge[] } where Edge = { source_id: string; target_id: string; weight: number }

<!-- Source-of-truth files for each W2 port -->

AlphaSwarm-2/src/icons.jsx (28 LOC):
- Defines Icon function, sets `window.Icon = Icon`
- 18 icon names: play, pause, stop, bolt, close, rewind, forward, graph, grid, radial, chat, doc, replay, brief, search, lock, x, settings

AlphaSwarm-2/src/viz.jsx (251 LOC):
- function Viz({ agents, onAgentClick, mode, ringRadius, focusBracket }) — deterministic SVG layout (no d3-force)
- Sets `window.Viz = Viz`
- No window.AS_DATA reads, no Icon usage

AlphaSwarm-2/src/panels.jsx (144 LOC):
- function BracketList({ summaries, onClick })
- function RationaleFeed({ rationales, onAgentClick, onCiteClick })
- function KpiStrip({ agents, tps, mem, slots, elapsed, round })
- function ConsensusRing({ agents })
- Sets `window.{BracketList,RationaleFeed,KpiStrip,ConsensusRing} = ...`
- No window.AS_DATA reads, no Icon usage

AlphaSwarm-2/src/states.jsx (158 LOC):
- function IdleState({ seed, setSeed, onStart, ollamaOk })
- function SeedingState({ seed }) — uses window.AS_DATA.BRACKETS for spawn cascade
- function MemoryPausedState({ pct })
- function ErrorState({ onRetry })
- Sets `window.{IdleState,SeedingState,MemoryPausedState,ErrorState}`
- Reads `window.AS_DATA.BRACKETS` (line 47)

AlphaSwarm-2/src/tweaks.jsx (79 LOC):
- function TweaksPanel({ ... })
- Sets `window.TweaksPanel`
- Uses Icon

AlphaSwarm-2/src/app_v2.jsx (407 LOC):
- function App() — main shell
- Inline Tooltip + Legend (lines 17-43)
- Reads `window.AS_DATA.{buildAgents, bracketSummaries, RATIONALES}` (lines 87, 90, 351)
- Local `setInterval` mock telemetry clock (lines 74-83) — DROP, replace with useTelemetry()
- Topbar Run/Stop handlers — wire to simStart(seed) / simStop()
- Topbar overflow menu (line 202): "Advisory (v6.0)" sets `advisoryOpen: true`
- AdvisoryV2 mount point (line 378): `<AdvisoryV2 onClose={...} />` — ported in W3 Plan 03
- BracketDeepDive mount handler (bracket-row click) — wired in W4 Plan 04
- Onboarding mount: handled at App.tsx level in W1 (Plan 01 task 3) — app_v2.tsx renders AFTER onboarding gate passes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port icons.jsx + viz.jsx + panels.jsx (no AS_DATA, no live-wire deps yet — pure presentational with named exports)</name>
  <files>
    - frontend/src/components/icons.jsx
    - frontend/src/components/viz.jsx
    - frontend/src/components/panels.jsx
  </files>
  <read_first>
    - AlphaSwarm-2/src/icons.jsx                                           (28 LOC — exemplar conversion)
    - AlphaSwarm-2/src/viz.jsx                                             (251 LOC — deterministic SVG; no Icon usage)
    - AlphaSwarm-2/src/panels.jsx                                          (144 LOC — 4 named exports)
    - frontend-react-archive/src/components/icons.jsx                      (current ported version — reference for any prior port quirks)
    - frontend-react-archive/src/components/viz.jsx
    - frontend-react-archive/src/components/panels.jsx
    - .planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-RESEARCH.md (Pattern 1 + Example 1)
  </read_first>
  <action>
    Three per-file ports. Each follows the SAME recipe (RESEARCH.md Pattern 1 + Example 1):

    Recipe (apply per file):
    a) Read `AlphaSwarm-2/src/{file}.jsx` end-to-end first.
    b) Replace top-line `const { useState, useEffect, useMemo, useRef } = React;` with `import { useState, useEffect, useMemo, useRef } from 'react';` (only the hooks actually used).
    c) Replace `function Foo(...)` with `export function Foo(...)` for every component the file exposes via `window.X = X` or `Object.assign(window, {...})`.
    d) DELETE the trailing `window.X = X;` or `Object.assign(window, {...})` lines.
    d2) **CRITICAL — scrub stale comments** referencing `window.AS_DATA`, `window.Icon`, or `Object.assign(window`. The Wave-2 production grep gate (`! grep -rE 'window\.AS_DATA\|window\.Icon\|Object\.assign(window' frontend/src/`) does NOT distinguish comments from code, so a leftover `// was: window.Icon = Icon` comment will fail the gate. Either delete the comment outright, or rewrite to remove the literal pattern (e.g. `// was attached as a global before D-03 conversion`). This applies to ALL ported files in W2/W3.
    e) Add file-level imports for any cross-file deps (none for these three — viz, panels, icons are leaf modules; viz/panels do NOT use Icon per RESEARCH.md grep).
    f) `.jsx` extension preserved per D-03 (TSX rename is Claude's discretion — recommend stay JSX for W2 to minimize delta).

    ---
    FILE: `frontend/src/components/icons.jsx`

    Source: `AlphaSwarm-2/src/icons.jsx` (28 LOC).

    BEFORE:
    ```jsx
    // Inline icons — small, stroke-based, dir-agnostic (currentColor)
    const Icon = ({ name, size = 14 }) => {
      const s = size;
      const stroke = { ... };
      switch (name) { /* 18 cases */ }
    };
    window.Icon = Icon;
    ```

    AFTER:
    ```jsx
    // Inline icons — stroke-based, dir-agnostic (currentColor)
    // Lifted from AlphaSwarm-2/src/icons.jsx; converted to named export per D-03.
    export const Icon = ({ name, size = 14 }) => {
      const s = size;
      const stroke = { stroke: 'currentColor', strokeWidth: 1.5, fill: 'none', strokeLinecap: 'round', strokeLinejoin: 'round' };
      switch (name) {
        // ... preserve ALL 18 case branches verbatim from source ...
        default: return null;
      }
    };
    ```
    NO function body changes. NO React import needed (JSX-only, no hooks used).

    ---
    FILE: `frontend/src/components/viz.jsx`

    Source: `AlphaSwarm-2/src/viz.jsx` (251 LOC).

    Conversion edits (verified by grep — no Icon, no AS_DATA in this file):
    1. Top: change `const { useMemo, useRef, useState, useEffect } = React;` → `import { useMemo, useRef, useState, useEffect } from 'react';` (use whichever subset the source actually destructures).
    2. Component declaration: `function Viz({ agents, onAgentClick, mode, ringRadius, focusBracket })` → `export function Viz({ agents, onAgentClick, mode, ringRadius, focusBracket })`.
    3. Bottom: DELETE `window.Viz = Viz;`.
    4. All other code (deterministic SVG layout per KR-41.1-01) preserved verbatim.

    ---
    FILE: `frontend/src/components/panels.jsx`

    Source: `AlphaSwarm-2/src/panels.jsx` (144 LOC). 4 components: BracketList, RationaleFeed, KpiStrip, ConsensusRing.

    Conversion edits:
    1. Top: `const { useState, useEffect } = React;` → `import { useState, useEffect } from 'react';` (subset actually used).
    2. Each `function {Name}(...)` declaration → `export function {Name}(...)`.
    3. Bottom: DELETE `window.BracketList = BracketList;` (and the other 3 lines or `Object.assign(window, {...})` if used).
    4. All component bodies preserved verbatim.

    NOTE on RationaleFeed: panels.jsx renders the rationale entries with a `cites` field (e.g., line 49 in source). Per KR-41.1-10 the lifted adapter `frame.ts:129-130` stubs `citations: []`. Render `cites` only when array is non-empty (the source likely already conditional-renders); confirm by grep — if not, leave the render code alone and let RationalesContext supply `citations: []`. UAT row references KR-41.1-10.

    Run `cd frontend && npm run check` after each file conversion. Address any TS errors (most likely: missing prop types — let TS infer, no need to add explicit types yet).
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/icons.jsx \
      && test -f frontend/src/components/viz.jsx \
      && test -f frontend/src/components/panels.jsx \
      && grep -q 'export const Icon' frontend/src/components/icons.jsx \
      && grep -q 'export function Viz' frontend/src/components/viz.jsx \
      && grep -q 'export function BracketList' frontend/src/components/panels.jsx \
      && grep -q 'export function RationaleFeed' frontend/src/components/panels.jsx \
      && grep -q 'export function KpiStrip' frontend/src/components/panels.jsx \
      && grep -q 'export function ConsensusRing' frontend/src/components/panels.jsx \
      && ! grep -E 'window\.(Icon|Viz|BracketList|RationaleFeed|KpiStrip|ConsensusRing)' frontend/src/components/icons.jsx frontend/src/components/viz.jsx frontend/src/components/panels.jsx \
      && ! grep -E 'Object\.assign\(window' frontend/src/components/icons.jsx frontend/src/components/viz.jsx frontend/src/components/panels.jsx \
      && ! grep -E 'window\.AS_DATA' frontend/src/components/icons.jsx frontend/src/components/viz.jsx frontend/src/components/panels.jsx \
      && ! grep -E 'const \{ use(State|Effect|Memo|Ref) \} = React' frontend/src/components/icons.jsx frontend/src/components/viz.jsx frontend/src/components/panels.jsx \
      && cd frontend && npm run check
    </automated>
  </verify>
  <acceptance_criteria>
    - icons.jsx: 18 named SVG cases preserved; `export const Icon` declared; no `window.Icon`
    - viz.jsx: `export function Viz`; deterministic SVG layout preserved (no d3-force per KR-41.1-01); no `window.Viz`
    - panels.jsx: 4 named exports (BracketList, RationaleFeed, KpiStrip, ConsensusRing); no `window.*` writes
    - All three files: NO `const { ... } = React` destructures, NO `Object.assign(window, ...)`, NO `window.AS_DATA` reads
    - `cd frontend && npm run check` exits 0
  </acceptance_criteria>
  <done>
    Three leaf-module ports complete. Pure presentational, named exports, no globals.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port states.jsx + tweaks.jsx + recreate TweaksPanelLoader.tsx (dev-only gate)</name>
  <files>
    - frontend/src/components/states.jsx
    - frontend/src/components/tweaks.jsx
    - frontend/src/components/TweaksPanelLoader.tsx
  </files>
  <read_first>
    - AlphaSwarm-2/src/states.jsx                                          (158 LOC — reads window.AS_DATA.BRACKETS at line ~47)
    - AlphaSwarm-2/src/tweaks.jsx                                          (79 LOC — uses Icon)
    - frontend-react-archive/src/components/states.jsx                    (already-ported reference)
    - frontend-react-archive/src/components/tweaks.jsx                    (already-ported reference)
    - frontend-react-archive/src/components/TweaksPanelLoader.tsx          (dev-gate pattern to re-create)
    - frontend/src/data.ts                                                  (BRACKETS export)
    - frontend/src/components/icons.jsx                                    (created in Task 1)
  </read_first>
  <action>
    ---
    FILE: `frontend/src/components/states.jsx`

    Source: `AlphaSwarm-2/src/states.jsx` (158 LOC). 4 components: IdleState, SeedingState, MemoryPausedState, ErrorState.

    Conversion edits:
    1. Top: replace `const { useState, useEffect } = React;` with `import { useState, useEffect } from 'react';`
    2. Add: `import { Icon } from './icons';` (states.jsx uses Icon — verified per RESEARCH.md component-dependency order)
    3. Add: `import { BRACKETS } from '../data';` (replaces `window.AS_DATA.BRACKETS` reference at ~line 47 of source)
    4. Replace any `window.AS_DATA.BRACKETS` usage with the imported `BRACKETS` constant.
    5. Each `function {Name}(...)` → `export function {Name}(...)` for IdleState, SeedingState, MemoryPausedState, ErrorState.
    6. DELETE `window.IdleState = IdleState;` (and the 3 sibling exports OR `Object.assign(window, {...})` block).

    NOTE on IdleState SystemCheck (per RESEARCH.md wiring map row #11): the design's SystemCheck has 4 rows (Ollama, Agents, Data sources, Storage) all hardcoded `ok`. In W2, leave them as DESIGN COPY (stay-as-rendered). The Onboarding component in W4 will wire `/api/health` to a real Ollama check. Note this as KR-41.6-11 in PLAN frontmatter (already listed). MemoryPausedState's "43.7 / 48.0 GB" literal stays per KR-41.1-04.

    NOTE on MemoryPausedState trigger: this is a presentational component. The TRIGGER (mount when telemetry.memMb >= 90) lives in `app_v2.tsx` (Task 4). The component itself accepts a `pct` prop and renders.

    NOTE on ErrorState: `event log` lines stay as design copy per KR-41.1-08 (no backend structured error stream). Trigger comes from `useConnection().reconnectFailed` in app_v2.tsx.

    ---
    FILE: `frontend/src/components/tweaks.jsx`

    Source: `AlphaSwarm-2/src/tweaks.jsx` (79 LOC). 1 component: TweaksPanel.

    Conversion edits:
    1. Top: `const { useState, useEffect } = React;` → `import { useState, useEffect } from 'react';` (subset)
    2. Add `import { Icon } from './icons';`
    3. `function TweaksPanel(...)` → `export function TweaksPanel(...)`
    4. DELETE `window.TweaksPanel = TweaksPanel;`

    ---
    FILE: `frontend/src/components/TweaksPanelLoader.tsx`

    Recreate from `frontend-react-archive/src/components/TweaksPanelLoader.tsx` pattern. This is the dev-only mount wrapper. Either:
    - Direct copy (preferred — preserves prior behavior):
      `cp frontend-react-archive/src/components/TweaksPanelLoader.tsx frontend/src/components/TweaksPanelLoader.tsx`
    - If archive's loader has different paths, adjust the `import` path for `./tweaks` to match.

    The loader gates `<TweaksPanel />` mount behind `import.meta.env.DEV` so production bundle doesn't include the dev-only tweaks UI in the runtime tree. (Tree-shaking should drop it from production output even without the gate, but the gate makes intent explicit and the production grep gate verifies.)

    Run `cd frontend && npm run check` — expect exit 0.
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/states.jsx \
      && test -f frontend/src/components/tweaks.jsx \
      && test -f frontend/src/components/TweaksPanelLoader.tsx \
      && grep -q 'export function IdleState' frontend/src/components/states.jsx \
      && grep -q 'export function SeedingState' frontend/src/components/states.jsx \
      && grep -q 'export function MemoryPausedState' frontend/src/components/states.jsx \
      && grep -q 'export function ErrorState' frontend/src/components/states.jsx \
      && grep -q "import { BRACKETS } from '../data'" frontend/src/components/states.jsx \
      && grep -q "import { Icon } from './icons'" frontend/src/components/states.jsx \
      && grep -q 'export function TweaksPanel' frontend/src/components/tweaks.jsx \
      && grep -q "import { Icon } from './icons'" frontend/src/components/tweaks.jsx \
      && grep -q 'import.meta.env.DEV' frontend/src/components/TweaksPanelLoader.tsx \
      && ! grep -E 'window\.(IdleState|SeedingState|MemoryPausedState|ErrorState|TweaksPanel)' frontend/src/components/states.jsx frontend/src/components/tweaks.jsx \
      && ! grep -E 'window\.AS_DATA' frontend/src/components/states.jsx frontend/src/components/tweaks.jsx \
      && ! grep -E 'Object\.assign\(window' frontend/src/components/states.jsx frontend/src/components/tweaks.jsx \
      && cd frontend && npm run check
    </automated>
  </verify>
  <acceptance_criteria>
    - states.jsx: 4 named exports; imports BRACKETS from '../data' and Icon from './icons'; no window writes; ErrorState event log stays as design copy (KR-41.1-08); MemoryPausedState GB literal stays (KR-41.1-04)
    - tweaks.jsx: `export function TweaksPanel`; imports Icon; no window writes
    - TweaksPanelLoader.tsx: gates `<TweaksPanel />` mount behind `import.meta.env.DEV`
    - All `window.AS_DATA` reads in states.jsx replaced with `BRACKETS` import
    - TS check exit 0
  </acceptance_criteria>
  <done>
    Lifecycle states + tweaks ported with proper imports. Dev gate in place.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Port app_v2.jsx (full dashboard shell, replaces W1 placeholder) — wire to all 6 contexts + simStart/simStop + lifecycle dispatch</name>
  <files>
    - frontend/src/components/app_v2.tsx       # REPLACES W1 placeholder (rename .tsx → .jsx OR keep .tsx — keep .tsx so the existing import in App.tsx resolves)
  </files>
  <read_first>
    - AlphaSwarm-2/src/app_v2.jsx                                          (407 LOC — main shell to port)
    - frontend-react-archive/src/components/app_v2.jsx                    (current 41.1-era port — reference for prior wiring patterns)
    - frontend/src/context/AgentsContext.tsx
    - frontend/src/context/BracketContext.tsx
    - frontend/src/context/TelemetryContext.tsx
    - frontend/src/context/RationalesContext.tsx
    - frontend/src/context/ConnectionContext.tsx
    - frontend/src/api/simulation.ts
    - frontend/src/components/viz.jsx                                     (created Task 1)
    - frontend/src/components/panels.jsx                                  (created Task 1)
    - frontend/src/components/states.jsx                                  (created Task 2)
    - frontend/src/components/icons.jsx                                   (created Task 1)
    - frontend/src/components/TweaksPanelLoader.tsx                       (created Task 2)
    - frontend/src/data.ts
    - src/alphaswarm/config.py                                            (verify ModelStatus hardcoded model name matches `OllamaSettings.worker_model` default — KR-41.6-08; per project memory Phase 41.4 locked qwen3:8b worker)
    - .planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-RESEARCH.md (wiring map rows 1-14, "Anti-Patterns to Avoid" #1)
  </read_first>
  <action>
    Replace the W1 placeholder `frontend/src/components/app_v2.tsx` with the fully-ported AlphaSwarm-2 shell.

    Source: `AlphaSwarm-2/src/app_v2.jsx` (407 LOC). Per CONTEXT.md, this exports `App` which `App.tsx` imports as `DashboardShell`.

    Conversion + wiring edits (apply ALL — these come from RESEARCH.md wiring map rows 1-14 + 9):

    1. **Top of file imports** — replace `const { useState, useEffect, useMemo } = React;` with explicit ES module imports + cross-file deps:
       ```typescript
       import { useState, useEffect, useMemo, useCallback } from 'react';
       import { Icon } from './icons';
       import { Viz } from './viz';
       import { BracketList, RationaleFeed, KpiStrip, ConsensusRing } from './panels';
       import { IdleState, SeedingState, MemoryPausedState, ErrorState } from './states';
       import { useAgents } from '../context/AgentsContext';
       import { useBrackets } from '../context/BracketContext';
       import { useTelemetry } from '../context/TelemetryContext';
       import { useRationales } from '../context/RationalesContext';
       import { useConnection } from '../context/ConnectionContext';
       import { simStart, simStop } from '../api/simulation';
       // W3 Plan 03 will add: import { ModelStatus, SignalWire, DataSourcesTakeover } from './v2';
       // W3 Plan 03 will add: import { ReportModal, ShockDrawer, ReplayBar, CyclePickerModal, AdvisoryV2 } from './modals' / './v2';
       // W4 Plan 04 will add: import { InterviewV2 } from './interview_v2'; import { BracketDeepDive } from './bracket_deep';
       // For W2: stub the modal mounts as no-ops or show a "coming in W3" placeholder so layout exists but doesn't crash.
       ```
       NOTE: The W2 port does NOT need W3/W4 imports yet — replace topbar overflow handlers and BracketDeepDive mount with no-op or `console.log('W3 modal pending')` stubs. W3 Plan 03 task 3 + W4 Plan 04 task 1 will rewire them. This is a planned interface seam.

    2. **DROP the local `setInterval` mock telemetry clock** at source `lines 74-83` (RESEARCH.md "Anti-Patterns to Avoid" #1). The mock clock writes to local state for `tps`/`mem`/`slots`/`elapsed`. REMOVE that entire block. Replace with destructure from `useTelemetry()`:
       ```typescript
       const { tps, memMb, slotsUsed, slotsMax, elapsedSeconds, phase } = useTelemetry();
       ```

    3. **Replace `window.AS_DATA.buildAgents(round, sigMix)` (source line ~87)** with `const { agents } = useAgents();`.

    4. **Replace `window.AS_DATA.bracketSummaries(agents)` (source line ~90)** with `const { summaries } = useBrackets();`.

    5. **Replace `window.AS_DATA.RATIONALES` (source line ~351)** with `const { rationales } = useRationales();`.

    6. **Replace ALL `<Tooltip>` and `<Legend>` inline-defined components** — keep them inline in this same file (they're at source lines 17-43). They're presentational helpers. NO conversion needed beyond `function` → `function` (no exports).

    7. **Topbar Run handler**: bind to `() => simStart(seed)` from `api/simulation` instead of any mock dispatch. Topbar Stop: `() => simStop()`.

    8. **Topbar overflow menu (source line ~202)** — preserve menu structure. The "Advisory (v6.0)" item handler sets `advisoryOpen: true`. AdvisoryV2 mount at source line ~378 — for W2, replace `<AdvisoryV2 onClose={...} />` with `{advisoryOpen && <div style={{position:'fixed',inset:0,background:'rgba(0,0,0,0.8)',display:'flex',alignItems:'center',justifyContent:'center',color:'white'}}>AdvisoryV2 ports in Plan 41.6-03 (W3) <button onClick={() => setAdvisoryOpen(false)}>Close</button></div>}`. W3 Plan 03 task 2 replaces this stub.

    9. **ModelStatus chip** — for W2, render a minimal inline ModelStatus showing `phase` + `tps` + `slots` from `useTelemetry()` and HARDCODED model name `'qwen3:8b'` per KR-41.6-08:
       ```typescript
       <div className="model-status">
         <span>{phase}</span> · <span>{tps.toFixed(1)} t/s</span> · <span>{slotsUsed}/{slotsMax} slots</span> · <span>qwen3:8b</span>
       </div>
       ```
       NOTE: `'qwen3:8b'` matches `src/alphaswarm/config.py:32` `OllamaSettings.worker_model = "qwen3:8b"` (the WORKER model — what runs the 100 agent inferences). Phase 41.4 locked qwen3:8b worker + qwen3.6:27b orchestrator (think=OFF) per project memory + STATE.md. Do NOT use `llama3.3:70b` — that was an earlier candidate, never the default.
       (W3 Plan 03 task 3 imports the real `ModelStatus` from ported `v2.jsx`.)

    10. **Lifecycle dispatch** — wrap dashboard render in lifecycle gate:
        ```typescript
        const { connected, reconnectFailed } = useConnection();
        // Lifecycle priority: error > paused > seeding > idle > running
        if (reconnectFailed) return <ErrorState onRetry={() => window.location.reload()} />;
        if (memMb >= 90) return <MemoryPausedState pct={Math.round(memMb)} />;
        if (phase === 'initializing') return <SeedingState seed={seed} />;
        if (phase === 'idle' && !running) return <IdleState seed={seed} setSeed={setSeed} onStart={() => simStart(seed)} ollamaOk={true /* W4 wires to /api/health */} />;
        // else: render full running dashboard (viz + panels + KpiStrip + topbar)
        ```
        Keep `running` derived from `phase !== 'idle'` or local UI state per source.

    11. **SignalWire ticker bar (source mount around line ~256)** — for W2, render a minimal `<div className="signal-wire-stub">SignalWire ports in Plan 41.6-03 (W3)</div>` placeholder. W3 Plan 03 task 3 replaces with real `<SignalWire />` from ported `v2.jsx`.

    12. **DataSources takeover** — same: stub for W2, real wire in W3.

    13. **Modal mounts** (Interview, Report, Shock, Replay, CyclePicker, AdvisoryV2) — all stub for W2 with placeholder divs. W3 Plan 03 task 3 wires them all. Keep the `setInterviewOpen`/`setReportOpen`/etc state local (per source's structure) so handlers exist; just don't render the modals yet.

    14. **BracketDeepDive mount** — stub for W2: bracket-row click handler logs `console.log('BracketDeepDive opens in W4', bracket)`. W4 Plan 04 task 1 wires it.

    15. **Top Influencers (source lines ~248-261)** — replace the inline hardcoded `[{id:'Q-03',out:31,...}]` with `useAgents().agents.sort((a,b) => b.confidence - a.confidence).slice(0,5)` and render columns showing `id`, `bracketDisplay`, `confidence`. Per KR-41.1-09 the live view sorts by `confidence` DESC; post-cycle re-rank by edges happens in W3 Plan 03 task 3 (when `<Viz>`'s edges fetch is wired through `useEdges` for cycle-complete views).

    16. **DELETE the trailing `Object.assign(window, {...})` or `window.App = App;` line.**

    17. **Add the TweaksPanelLoader mount** somewhere safe (e.g., end of root return):
        ```typescript
        import { TweaksPanelLoader } from './TweaksPanelLoader';
        // ... in JSX root:
        <TweaksPanelLoader />
        ```

    18. **Final export**: change `function App() { ... }` → `export function App() { ... }`. App.tsx already imports as `import { App as DashboardShell } from './components/app_v2';` (W1 Plan 01 task 3) so the export name `App` matches.

    Run `cd frontend && npm run check` — expect exit 0. Address TS errors (most likely: missing prop types on inline Tooltip — let TS infer or add `// @ts-expect-error` if a JSX-only file resists typing; Claude's discretion).
  </action>
  <verify>
    <automated>
      test -f frontend/src/components/app_v2.tsx \
      && grep -q 'export function App' frontend/src/components/app_v2.tsx \
      && grep -q 'useAgents' frontend/src/components/app_v2.tsx \
      && grep -q 'useBrackets' frontend/src/components/app_v2.tsx \
      && grep -q 'useTelemetry' frontend/src/components/app_v2.tsx \
      && grep -q 'useRationales' frontend/src/components/app_v2.tsx \
      && grep -q 'useConnection' frontend/src/components/app_v2.tsx \
      && grep -q 'simStart' frontend/src/components/app_v2.tsx \
      && grep -q 'simStop' frontend/src/components/app_v2.tsx \
      && grep -q "import { Viz } from './viz'" frontend/src/components/app_v2.tsx \
      && grep -q "import { BracketList, RationaleFeed, KpiStrip, ConsensusRing } from './panels'" frontend/src/components/app_v2.tsx \
      && grep -q "import { IdleState, SeedingState, MemoryPausedState, ErrorState } from './states'" frontend/src/components/app_v2.tsx \
      && grep -q "import { Icon } from './icons'" frontend/src/components/app_v2.tsx \
      && grep -q 'TweaksPanelLoader' frontend/src/components/app_v2.tsx \
      && grep -q "qwen3:8b" frontend/src/components/app_v2.tsx \
      && ! grep -E 'window\.AS_DATA' frontend/src/components/app_v2.tsx \
      && ! grep -E 'Object\.assign\(window' frontend/src/components/app_v2.tsx \
      && ! grep -E 'window\.App\s*=' frontend/src/components/app_v2.tsx \
      && ! grep -E 'setInterval.*tps|setInterval.*memMb|setInterval.*slots' frontend/src/components/app_v2.tsx \
      && cd frontend && npm run check
    </automated>
  </verify>
  <acceptance_criteria>
    - app_v2.tsx exports `App` (default function — App.tsx imports as DashboardShell)
    - All 5 context hooks consumed: useAgents, useBrackets, useTelemetry, useRationales, useConnection
    - simStart/simStop wired from api/simulation (Topbar Run/Stop handlers)
    - Mock setInterval telemetry clock (source lines 74-83) DELETED
    - All `window.AS_DATA.*` reads replaced with context hooks
    - Lifecycle dispatch present: ErrorState (reconnectFailed) > MemoryPausedState (memMb>=90) > SeedingState (phase=='initializing') > IdleState (phase=='idle' && !running) > running dashboard
    - W3/W4 mount points stubbed with placeholder divs (no crashes)
    - Hardcoded `'qwen3:8b'` per KR-41.6-08 (matches `src/alphaswarm/config.py:32` `OllamaSettings.worker_model` default — verified)
    - Top Influencers ranks via `useAgents().agents.sort((a,b) => b.confidence - a.confidence).slice(0,5)` per KR-41.1-09 live default
    - TweaksPanelLoader mounted
    - TS check exit 0
  </acceptance_criteria>
  <done>
    Full dashboard shell ported and wired. Live data flows through 6-context tree. Modals stubbed for W3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: CSS reconciliation + Wave 2 build gate + production grep gate</name>
  <files>
    - frontend/src/styles.css
    - frontend/dist/                  # produced by npm run build (gitignored)
  </files>
  <read_first>
    - frontend/src/styles.css                                              (current = AlphaSwarm-2 byte copy from W1)
    - frontend-react-archive/src/styles.css                                (1417 LOC — extract live-wiring overrides only)
    - .planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-RESEARCH.md (CSS Reconciliation Surface section)
    - .planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-VALIDATION.md
  </read_first>
  <action>
    TWO things: CSS audit + production grep gate.

    ---
    PART A — CSS reconciliation via CLASS-INVENTORY DIFF (codex MEDIUM-5 + gemini #G3 namespace policy):

    Current `frontend/src/styles.css` is a byte copy of `AlphaSwarm-2/styles.css` (536 LOC, set in W1 task 1). Archive `frontend-react-archive/src/styles.css` is 1417 LOC with ~881 LOC delta added during 41.1/41.2/41.4/999.x.

    Replace the previous "diff first 200 lines" approach with a deterministic CLASS-INVENTORY DIFF: compare the set of class names actually used by ported W2 components against the set of class names defined in the merged stylesheet. Only ADD missing required classes from the archive — do not lift visual polish that has no consumer.

    Process:
    1. Extract class names actually USED by ported W2 components (icons, viz, panels, states, tweaks, app_v2.tsx, TweaksPanelLoader):
       ```bash
       rg -oh 'className="[^"]+"' frontend/src/components/ \
         | sed -E 's/className="([^"]+)"/\1/' \
         | tr ' ' '\n' \
         | grep -v '^$' \
         | sort -u > /tmp/41.6-w2-classes-used.txt
       # Also catch className={...template literal...} cases — extract literal class tokens
       rg -oh 'className=\{`[^`]+`\}' frontend/src/components/ 2>/dev/null \
         | sed -E 's/className=\{`([^`]+)`\}/\1/' \
         | tr ' ' '\n' \
         | grep -E '^[a-zA-Z_-][a-zA-Z0-9_-]*$' \
         | sort -u >> /tmp/41.6-w2-classes-used.txt
       sort -u -o /tmp/41.6-w2-classes-used.txt /tmp/41.6-w2-classes-used.txt
       ```

    2. Extract class names DEFINED in AlphaSwarm-2/styles.css (the new foundation) AND in the archive stylesheet:
       ```bash
       rg -oh '^\s*\.[a-zA-Z_-][a-zA-Z0-9_-]*' AlphaSwarm-2/styles.css \
         | sed -E 's/^\s*\.//' \
         | sort -u > /tmp/41.6-w2-classes-new.txt
       rg -oh '^\s*\.[a-zA-Z_-][a-zA-Z0-9_-]*' frontend-react-archive/src/styles.css \
         | sed -E 's/^\s*\.//' \
         | sort -u > /tmp/41.6-w2-classes-archive.txt
       ```

    3. Compute the MISSING set: classes used by W2 components but NOT defined in the new stylesheet:
       ```bash
       comm -23 /tmp/41.6-w2-classes-used.txt /tmp/41.6-w2-classes-new.txt > /tmp/41.6-w2-classes-missing.txt
       wc -l /tmp/41.6-w2-classes-missing.txt
       ```

    4. For each class in `/tmp/41.6-w2-classes-missing.txt`:
       (a) Check if it exists in `/tmp/41.6-w2-classes-archive.txt` (i.e., the archive defines it).
       (b) If yes: lift its rule(s) verbatim from `frontend-react-archive/src/styles.css` into the END of `frontend/src/styles.css` (after AlphaSwarm-2 design tokens).
       (c) If no: the component's `className=` is referencing a class no stylesheet defines — likely a typo in the port OR a class only added by JS at runtime. Investigate the component, fix or document.

    5. **gemini #G3 — namespace conflict policy.** When lifting a rule from the archive, check whether the rule REDEFINES a CSS variable (`--foo:`) or a selector that AlphaSwarm-2 already defines. If yes:
       - For `:root` token redefinitions: rename the lifted token (e.g., `--error` from archive → `--legacy-error`) and update the consumer's `var(--error)` → `var(--legacy-error)` in the lifted rule. Do NOT clobber AlphaSwarm-2's design tokens.
       - For selector overlaps that are non-token: scope the lifted rule with a parent selector (e.g., `.legacy-banner-error` instead of `.banner-error`) AND update component className accordingly OR drop the lift if the AlphaSwarm-2 version is sufficient.
       - Apply this BOTH to `:root { --foo: ... }` blocks and to bare selectors that already exist.

    6. Append all merged rules under a clearly-marked comment header. Each lifted block must cite its consumer:
       ```css
       /* === LIVE-WIRING OVERRIDES (cherry-picked from frontend-react-archive/src/styles.css per W2 Plan 41.6-02 task 4 — class-inventory diff codex MEDIUM-5) === */
       /* Used by: <component>; namespaced per gemini #G3 if conflict */
       .adv-loading { ... }
       /* etc. */
       ```

    7. If `/tmp/41.6-w2-classes-missing.txt` is empty (or after subtracting AlphaSwarm-2 atoms it's empty), document "empty merge" in the W2 SUMMARY.md and move on.

    8. Re-run the inventory diff after the merge to verify zero missing:
       ```bash
       # After merging archive rules into frontend/src/styles.css, regenerate the new defined set
       rg -oh '^\s*\.[a-zA-Z_-][a-zA-Z0-9_-]*' frontend/src/styles.css \
         | sed -E 's/^\s*\.//' \
         | sort -u > /tmp/41.6-w2-classes-merged.txt
       comm -23 /tmp/41.6-w2-classes-used.txt /tmp/41.6-w2-classes-merged.txt > /tmp/41.6-w2-classes-still-missing.txt
       test ! -s /tmp/41.6-w2-classes-still-missing.txt   # MUST be empty
       ```
       If still-missing is non-empty: either fix the component className (typo) or add the class definition. The acceptance criteria below requires this final emptiness check.

    ---
    PART B — Wave 2 build + production grep gate (VALIDATION.md row "41.6-02-*"):

    1. `cd frontend && npm run check` — expect exit 0.
    2. `cd frontend && npm run build` — produces `frontend/dist/`. Expect exit 0.
    3. Run the consolidated grep gate from VALIDATION.md row 02 — must exit 0 (i.e. NO matches found):
       ```bash
       ! grep -rn 'window\.AS_DATA\|window\.Icon\|Object\.assign(window' frontend/src/
       ```
    4. Run the production bundle mock-leak grep (Pitfall #4 + RESEARCH.md "Validation Architecture"):
       ```bash
       ! grep -rE 'SIGNAL_WIRE_SEED|MOCK_|_MOCK|mocks/wire|mocks/sources' frontend/dist/assets/ 2>/dev/null
       ```
       This MUST return non-zero exit (no matches). If it finds matches, the SignalWire / DataSources mock imports leaked into the production bundle even though their consumer (v2.jsx) hasn't been ported in W2 yet. If matches are found, investigate the import chain — likely a top-level `import` in `mocks/index.ts` reaching dist via tree-shaking failure. (Solutions: convert top-level imports to dynamic `import.meta.env.DEV ? await import(...) : null`, or remove `mocks/index.ts` re-exports until W3 mocks consumer ports.)
    5. Also verify NO design literal leakage from W2 surfaces (Pitfall #3 — these literals come from W3 advisory mocks, but spot-check W2 source):
       ```bash
       ! grep -rE 'Trim AAPL|Apple acquiring Anthropic|\$500B|C_2026_0418_A1' frontend/src/components/icons.jsx frontend/src/components/viz.jsx frontend/src/components/panels.jsx frontend/src/components/states.jsx frontend/src/components/tweaks.jsx frontend/src/components/app_v2.tsx
       ```
  </action>
  <verify>
    <automated>
      cd frontend && npm run check && npm run build && test -d dist \
      && ! grep -rn 'window\.AS_DATA\|window\.Icon\|Object\.assign(window' src/ \
      && ! grep -rE 'SIGNAL_WIRE_SEED|MOCK_|_MOCK|mocks/wire|mocks/sources' dist/assets/ 2>/dev/null \
      && ! grep -rE 'Trim AAPL|Apple acquiring Anthropic|\$500B|C_2026_0418_A1' src/components/icons.jsx src/components/viz.jsx src/components/panels.jsx src/components/states.jsx src/components/tweaks.jsx src/components/app_v2.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/styles.css` contains cherry-picked live-wiring overrides marked with comment header (or documented-empty merge in SUMMARY)
    - **Zero className references where the class is undefined in the merged stylesheet** (codex MEDIUM-5): `comm -23 /tmp/41.6-w2-classes-used.txt /tmp/41.6-w2-classes-merged.txt` returns empty
    - **Any retained archive token that conflicted with AlphaSwarm-2 `:root` tokens has been namespaced** (gemini #G3 — e.g., `--error` → `--legacy-error`) — verify by `rg '^\s*--' AlphaSwarm-2/styles.css | sort -u` matches the corresponding tokens in `frontend/src/styles.css` for any name collision
    - `npm run check` exit 0
    - `npm run build` exit 0; `dist/` populated
    - NO `window.AS_DATA`, `window.Icon`, `Object.assign(window` references anywhere in `frontend/src/`
    - NO mock identifiers (SIGNAL_WIRE_SEED, MOCK_, _MOCK, mocks/wire, mocks/sources) in `frontend/dist/assets/*.js` (Pitfall #4)
    - NO advisory design literals in W2 component sources (Pitfall #3 spot check)
  </acceptance_criteria>
  <done>
    Wave 2 build clean. Production bundle proven mock-free. CSS reconciled.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verifies Wave 2 dashboard</name>
  <files>N/A — checkpoint task; no files modified by Claude (human verifies prior wave artifacts)</files>
  <action>This is a human-verification checkpoint. Claude pauses and awaits user input. The verification steps are documented in the &lt;how-to-verify&gt; block below — execute those steps as the user.</action>
  <verify>
    <automated>echo 'manual checkpoint — see how-to-verify block'</automated>
  </verify>
  <done>Human types 'approved' (or describes blocking issues) per &lt;resume-signal&gt; below.</done>
  <what-built>
    - 6 ported component files: icons.jsx, viz.jsx, panels.jsx, states.jsx, tweaks.jsx, app_v2.tsx
    - TweaksPanelLoader.tsx (dev-only mount gate)
    - Reconciled `styles.css` (AlphaSwarm-2 foundation + cherry-picked live-wiring overrides)
    - Live-wired surfaces: force graph (Viz), KPI strip (KpiStrip), brackets panel (BracketList), rationale feed (RationaleFeed), consensus ring (ConsensusRing), Top Influencers (confidence DESC), Tooltip on agent hover, Topbar Run/Stop (simStart/simStop), Lifecycle dispatch (Idle/Seeding/MemoryPaused/Error)
    - ModelStatus chip with hardcoded `'qwen3:8b'` (KR-41.6-08, matches config.py worker_model)
    - Modal/W4 mount stubs (will be wired in W3 Plan 03 + W4 Plan 04)
    - Production bundle grep gate green
  </what-built>
  <how-to-verify>
    Prerequisites:
    - Backend running: `uv run uvicorn alphaswarm.web.app:create_app --factory --port 8000` (in repo root)
    - Neo4j optional for W2 (only needed for cycle history / advisory in W3)

    Steps:
    1. `cd frontend && npm run dev` → open http://localhost:5173
    2. If onboarding stub appears, click "Continue (W1 stub)"
    3. Expect IdleState to render with seed input — type any seed, e.g. "Apple acquires X"
    4. Click Run → expect simStart() to fire (network tab shows POST /api/simulate/start). Lifecycle should transition to SeedingState briefly, then to running dashboard.
    5. Verify each W2 surface (per parity matrix from RESEARCH.md wiring map):
       - **Force graph (Viz)**: 100 agents render in deterministic SVG layout (no physics wiggle per KR-41.1-01). Hover an agent → Tooltip shows bracketDisplay/signal/confidence. Flipped column shows "No" (KR-41.1-03 stub).
       - **KPI strip**: tps/memMb (as %)/slotsUsed/elapsedSeconds tick at ~5Hz. memMb labeled "%" not "MB" (KR-41.1-04). slotsUsed live, slotsMax=8 stub (KR-41.1-05).
       - **BracketList (left)**: 10 brackets shown with summaries from useBrackets().
       - **Top Influencers (left)**: 5 agents sorted by confidence DESC (KR-41.1-09).
       - **ConsensusRing (center)**: ring renders showing buy/sell/hold split.
       - **RationaleFeed (right)**: rationale events stream in. Empty `cites` shows as "—" (KR-41.1-10).
       - **ModelStatus chip (topbar)**: "qwen3:8b" hardcoded (KR-41.6-08, matches `config.py:32` worker_model default); phase + tps + slots from useTelemetry.
       - **Topbar overflow**: open menu — "Advisory (v6.0)" item should set advisoryOpen and show stub overlay "AdvisoryV2 ports in Plan 41.6-03 (W3)".
       - **SignalWire**: stub div "SignalWire ports in Plan 41.6-03 (W3)".
       - **Modal stubs**: clicking modal triggers shows stub overlays (won't crash).
    6. Lifecycle smoke:
       - Stop the backend → expect ErrorState within ~10s (3-retry backoff per useWebSocket).
       - Restart backend → click "Retry" → expect dashboard re-mounts.
       - (MemoryPausedState manual test deferred — would need to spike memory >=90%, not realistic to trigger in UAT. Verify the lifecycle gate code path exists by inspection.)
    7. Browser DevTools Console: ZERO red errors. Check Network tab — `/ws/state` connection open at ws://localhost:8000/ws/state, frames arriving at ~5Hz.
    8. Stop dev server. Run `cd frontend && npm run check && npm run build` — both exit 0.

    KR-41.1 acknowledgements expected to appear in UAT (NOT failures):
    - KR-41.1-01: no force-graph wiggle
    - KR-41.1-03: agent.flipped always false
    - KR-41.1-04: memory shown as %, MemoryPausedState GB literal stays as design copy
    - KR-41.1-05: slotsMax stuck at 8
    - KR-41.1-08: ErrorState event log static
    - KR-41.1-09: Top Influencers by confidence
    - KR-41.1-10: rationale cites "—"

    KR-41.6 expected (new):
    - KR-41.6-08: ModelStatus shows "qwen3:8b" only (matches config.py worker_model)
    - KR-41.6-11: states.jsx IdleState SystemCheck rows static (Ollama wires in W4 Onboarding)

    If any blocking issue (crash, missing surface, TS error, mock leak in production bundle), describe the failure mode.
  </how-to-verify>
  <resume-signal>Type "approved" if W2 dashboard renders live data correctly, or describe issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser ↔ ported W2 components | XSS surface for any innerHTML/dangerouslySetInnerHTML rendering |
| Vite production bundle ↔ user | Mocks must not leak (KR-41.1-02 production grep gate) |
| Frontend ↔ FastAPI WS @ ws://localhost:8000/ws/state | 5Hz state frames; no auth (local-first per CLAUDE.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.6-04 | I (Information Disclosure) | `frontend/src/mocks/*` reaching production bundle | mitigate | W2 task 4 acceptance criteria runs `! grep -rE 'SIGNAL_WIRE_SEED\|MOCK_\|_MOCK\|mocks/wire\|mocks/sources' frontend/dist/assets/` — must exit non-zero (no matches). SignalWire/DataSources consumer (v2.jsx) ports in W3 — W2 components don't import mocks. |
| T-41.6-05 | T (Tampering) | viz.jsx tooltip rendering agent fields | accept | Tooltip renders agent.bracketDisplay/signal/confidence/flipped — all server-emitted enum/numeric/boolean fields. No user input crosses this boundary. No XSS risk. |
| T-41.6-06 | I (Information Disclosure) | Design literals in ported W2 source | mitigate | Pitfall #3 grep gate in task 4 forbids `Trim AAPL`, `Apple acquiring Anthropic`, `$500B`, `C_2026_0418_A1` in W2 component files. (W2 components don't carry advisory literals — those are in v2.jsx Plan 03.) |
| T-41.6-07 | T (Tampering) | localStorage `as_onboarding_v1_complete` | accept | Boolean flag, no auth tokens, no PII. User can clear localStorage to re-trigger onboarding. |
</threat_model>

<verification>
- All 6 component files ported with named exports, no `window.*` writes, no `window.AS_DATA` reads
- `app_v2.tsx` consumes all 5 context hooks; mock setInterval clock deleted; lifecycle dispatch present
- TweaksPanelLoader gates dev-only TweaksPanel mount
- `styles.css` reconciled (cherry-pick documented in comments OR empty-merge documented in SUMMARY)
- `npm run check` exit 0
- `npm run build` exit 0
- Production grep gate (W2 task 4) passes (no globals, no mocks in dist)
- Pitfall #3 grep on W2 sources passes (no advisory literals)
- Human verifies live KPI strip ticks, force graph renders 100 agents, lifecycle states toggle, modal stubs render placeholders without crashing
- Human resumes with "approved"
</verification>

<success_criteria>
- All artifacts in must_haves.artifacts exist
- All key_links in must_haves.key_links present (verified by grep)
- Wave 2 build gate green
- Live dashboard parity confirmed against backend
- W3 Plan 03 can begin: modals + v2.jsx (incl AdvisoryV2 + ReportModal rich lift) + usePolling D-19 patch
</success_criteria>

<output>
After completion, create `.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-02-SUMMARY.md` documenting:
- Per-file conversion notes (any source quirks encountered: hoisting issues, missing destructures, TS friction)
- CSS cherry-pick list (or "empty merge — no live-wiring deltas needed")
- Production grep gate output
- KR-41.6-08 + KR-41.6-11 confirmed filed
- Modal/W4 mount stubs inventory (so W3/W4 know what placeholders to replace)
- Sign-off note from human verification
</output>