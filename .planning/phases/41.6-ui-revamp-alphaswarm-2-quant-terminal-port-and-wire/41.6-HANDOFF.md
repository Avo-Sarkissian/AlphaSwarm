---
phase: 41.6
status: paused-mid-execution
paused_at: 2026-05-10
paused_after: W3 merged + 5 visual hotfixes applied
next_action: investigate backend agent_states emission OR proceed to W4
---

# Phase 41.6 Handoff — Resume Instructions

## Where we are

**Three of four waves merged + pushed to origin/master:**
- **W1** (`6de8cb2`..`96442fe`) — scaffold + wiring lift
- **W2** (`3a3205b`..`67d0d7c`) — dashboard surfaces + 4 hotfixes
- **W3** (`0e5dcba`..`fd3e584`) — modals + AdvisoryV2 + ReportModal + Settings + usePolling D-19

**5 visual hotfixes applied to W2** (post-merge, all on master):
1. `a134742` — IdleState chips honesty (48GB→64GB, derived BRACKETS counts, mocked-data disclosure)
2. `80180d6` — viz.jsx NaN circle radius + path d (enriched agents with index + BRACKETS-lookup radius)
3. `f0879ee` — visual fidelity hotfix #1 (ported missing CSS: signal-wire/legend/tooltip/influencer-row, added empty-state fallbacks to BracketList/RationaleFeed, KpiStrip div-by-zero guard)
4. `a3e6d7e` — round derivation + SignalWire inner CSS + ModelStatus chip CSS

**Latest commit on master:** `a3e6d7e` (origin/master in sync)

**W4 NOT STARTED** — final wave (3 net-new tsx files: interview_v2 + onboarding + bracket_deep + edges.ts + 30-row parity matrix UAT + KR audit + VALIDATION.md flag flip)

## What works (visually verified by user)

- IdleState ("Swarm is dormant" with chips) renders correctly
- Running dashboard layout: SignalWire 30px ticker, Brackets panel, Top Influencers with empty state, force graph viewport with R1/R2/R3/FINAL tabs + Layout switcher + Legend + ConsensusRing, Rationale Feed with empty state, KPI strip
- ModelStatus chip in topbar
- Round derivation now consistent (`ROUND_2` topbar / `R2` bracket header / `R2/3` elapsed all match)
- Settings modal + Advisory overflow trigger working
- Build gates: `npm run check` + `npm run build` green
- Production grep gates: no mock leaks, no `window.AS_DATA`/`window.Icon` globals, no design literals
- All cross-AI review concerns folded (4 HIGH + 4 MEDIUM + 3 gemini suggestions)

## What does NOT work (real blockers)

### Critical — backend issue, blocks live data

**Backend not emitting `agent_states` in WS frames mid-sim.** Even after 65+ min of sim runtime, frontend `agents.length === 0`. Symptoms:
- Force graph is empty (no agent dots)
- Bracket bars stay green-default (no signal distribution)
- Top Influencers shows "no agents yet — waiting for first frame"
- Rationale Feed shows "no rationales yet — agents thinking…"
- KPI Consensus stays at "BUY 0%"

This is **same root cause** as Phase 41.4-era issues: post-Round-3 orchestrator-load path stalls AND agent_states aren't being broadcast during rounds. NOT a W2/W3 frontend bug.

### Frontend follow-ups (cosmetic)

- **AdvisoryV2 returns 404** when opened — backend doesn't auto-trigger synthesis on cycle complete (was supposed to per quick task `260507-19f` but appears regressed). AdvisoryV2 modal renders blank on 404 — needs better error UI.
- **CycleHistory polls `/api/replay/cycles` 8x in rapid succession** — needs debouncing.
- **Bracket bars** render flat per-bracket palette colors when summaries are empty — should show empty state similar to other panels.
- **NaN circle errors** on viz still fire ~100 per render burst even after the fix (during HMR transitions). Fully gone after hard refresh.

## Resume options

### Option A: Backend debugging first (recommended)

```
/gsd-debug
```

Investigate:
1. Why `agent_states` aren't in WS frames mid-sim (broadcaster.py / connection_manager.py)
2. Why post-Round-3 orchestrator load hangs/silently fails
3. Why advisory auto-trigger doesn't fire on cycle complete (regression from `260507-19f`)

Without this, no live UAT of W2/W3 surfaces is possible.

### Option B: Proceed to W4 anyway

```
/gsd-execute-phase 41.6 --wave 4
```

W4 plan ports interview_v2 + onboarding + bracket_deep + creates parity matrix UAT + KR audit. The parity matrix UAT will surface all the empty-data issues as KR-41.6-NN entries (gap closure for backend phase). UAT can't be signed off without backend fix, but the code work and KR documentation can finish.

### Option C: Polish W2 visual fidelity to 100%

Remaining minor visual gaps:
- Bracket bars empty-state
- AdvisoryV2 error UI for 404 case
- CycleHistory polling debounce

These are 5-15 min surgical fixes each. Then W4.

## Critical context for next session

### Backend stack state

User runs 3 terminals manually in VS Code panes:
- **Pane 1:** `docker compose up neo4j` (BUT see Neo4j note below)
- **Pane 2:** `uv run uvicorn alphaswarm.web.app:app --port 8000`
- **Pane 3:** `cd frontend && npm run dev`

Plus Ollama running as desktop app (already loaded).

### Neo4j gotcha

User has TWO Neo4j containers:
- `neo4j` (image `neo4j:community`, container ID `45a5a8359b8b`) — **THE WORKING ONE**, has historical cycles, was running 6 weeks
- `alphaswarm-neo4j` (image `neo4j:5.26-community`, defined in docker-compose.yml) — **conflicts on ports 7474/7687**, exits with code 3 when `docker compose up` tries to start it

**Resolution:** ignore `docker compose up neo4j` failure. The other `neo4j` container serves the data. Start it via `docker start neo4j` or Docker Desktop ▶ button.

### Ollama / model state

Per project memory `project_phase_41.4_model_decision.md`:
- Worker: `qwen3:8b` (~5.9 GB, alphaswarm-worker:latest registered)
- Orchestrator: `qwen3.6:27b-q4_K_M` (~17.4 GB, alphaswarm-orchestrator:latest registered)
- `OLLAMA_NUM_PARALLEL=4`, `OLLAMA_MAX_LOADED_MODELS=2`
- Both registered in Ollama; orchestrator loads on-demand

### Architecture reminders

- Frontend `frontend/src/api/advisory.ts` lines 1-7 EXPLICITLY forbid POST `advisoryTrigger()` — backend auto-fires on cycle complete (per quick task `260507-19f`). Re-introducing the trigger from frontend would re-create a ~17 GB orchestrator-load footgun. **DO NOT add advisoryTrigger to frontend.**
- `useAgents()` returns agents in CONTRACT.md §2.1 shape (no `index`, no `radius` fields). The Viz component must enrich them — already done.
- Mocks (`mocks/wire.ts`, `mocks/sources.ts`) MUST use DEV-only dynamic imports per KR-41.6-14. Production grep gate forbids `mocks/wire`/`mocks/sources` references in `dist/assets/`.
- TypeScript files (`v2.tsx`, `history.tsx`, `settings.tsx`, `interview_v2.tsx`, `onboarding.tsx`, `bracket_deep.tsx`) MUST be `.tsx` per codex HIGH-1. `.jsx` files (`modals.jsx`) MUST have NO TypeScript syntax.

### KR register state

- 11 KR-41.1-NN carry forward (per Phase 41.1 register, audit done in W4 Plan 04)
- 13 KR-41.6-NN filed across W2/W3 plans + KR-41.6-14 added by W3 (DEV-only mock dynamic import)

### Files modified in 5 visual hotfixes (NOT in any plan, applied directly to master)

- `frontend/src/components/states.jsx` — IdleState chips
- `frontend/src/components/viz.jsx` — NaN guards
- `frontend/src/styles.css` — visual fidelity CSS additions
- `frontend/src/components/panels.jsx` — empty-state fallbacks + KpiStrip guard
- `frontend/src/components/app_v2.tsx` — round derivation fix

These hotfixes should probably be folded into 41.6-02-SUMMARY.md retroactively, OR documented as a 41.6-02 addendum SUMMARY in the next session.

## Quick smoke test for "is everything still working" after /clear

```bash
cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm"
git log --oneline -10
git status
cd frontend && npm run check && npm run build
curl -sf http://localhost:8000/api/health
curl -sf http://localhost:7474 > /dev/null && echo "Neo4j up"
```

Expected:
- HEAD at `a3e6d7e` (or newer if more work done)
- Clean working tree (or untracked .claude/)
- Build green
- Backend + Neo4j responding

## Next session opener

Recommended prompt to start fresh session:

> Resume Phase 41.6. Read `.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-HANDOFF.md` for full state. Three options to choose from:
> A) /gsd-debug to investigate backend agent_states emission
> B) /gsd-execute-phase 41.6 --wave 4 to finish the phase
> C) Polish remaining W2 visual gaps before W4
>
> Recommend A so live UAT becomes possible.
