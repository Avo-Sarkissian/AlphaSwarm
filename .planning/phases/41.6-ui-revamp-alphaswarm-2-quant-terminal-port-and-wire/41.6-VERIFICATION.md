---
phase: 41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire
verified: 2026-05-10T15:33:04Z
status: human_needed
score: 13/15 must-haves verified
re_verification: false
human_verification:
  - test: "Run full end-to-end simulation with live backend and sign off all 31 parity matrix rows"
    expected: "Force graph renders 100 agents, KPI strip ticks at 5Hz, BracketList shows summaries, RationaleFeed streams, lifecycle states transition, all 31 surface rows pass with KR-acknowledged deviations"
    why_human: "Live UAT requires running backend (Ollama + Neo4j + FastAPI) and actively exercising each surface. Static code verification cannot confirm real WebSocket frame delivery or inference responses. The 31-row parity matrix in 41.6-PARITY-MATRIX.md has all rows at status ⬜ (unsigned). No human has signed off any row against a live backend."
  - test: "Verify InterviewV2 sends real /api/interview responses (not PERSONA filler)"
    expected: "Click any agent in viz during a running simulation, type a question, receive a response from POST /api/interview/{agent_id} — NOT a static keyword-matched reply"
    why_human: "KR-41.6-03 removes PERSONA filler; the askAgent() wiring is verified statically, but the actual backend response path requires a live sim cycle with agents loaded."
  - test: "Verify Onboarding /api/health gate disables Continue when backend is down"
    expected: "Stop backend, open http://localhost:5173 (clear localStorage as_onboarding_v1_complete first), confirm Onboarding Step 1 shows Ollama unreachable and Continue is disabled"
    why_human: "KR-41.6-04 coarse Ollama gate — the 3-state ollamaOk (null|true|false) wiring exists statically but behavior on backend-down state can only be confirmed at runtime."
  - test: "Verify AdvisoryV2 4-tab layout renders against a completed simulation cycle"
    expected: "After a full sim cycle completes: open Topbar overflow -> Advisory (v6.0). Overview/Holdings tabs show consensus_signal/confidence/portfolio_outlook data from backend. Risk tab shows placeholder copy (KR-41.6-05). Isolation tab shows static copy. Stubs (shares/basis/last/sentiment) render as '—' per KR-41.6-01."
    why_human: "Advisory auto-fires on FINAL round (per quick task 260507-19f). Requires a completed sim cycle with advisory synthesis having run. The polling + parsing + tab rendering needs real backend data to verify faithfully."
  - test: "Verify BracketDeepDive shows live member agents and '—' for influences during live cycle"
    expected: "Click any bracket row in BracketList. BracketDeepDive takeover opens showing agents filtered to that bracket. Influences table shows '—' for out/in counts (no edges mid-cycle). Post-cycle: influences show real /api/edges aggregation."
    why_human: "KR-41.6-13 live-view degrade requires a running sim with populated agents. case-insensitive bracket compare (PascalCase vs lowercase) can only be confirmed against real WS data where backend emits 'Quants' vs 'quants'."
---

# Phase 41.6: UI Revamp AlphaSwarm-2 Quant Terminal — Verification Report

**Phase Goal:** Replace the current React frontend with the AlphaSwarm-2 quant-terminal design, ported as React/TS components and wired to the existing live data plane (WS frames, advisory, replay, edges, settings). End state: a single, real-data-wired, design-faithful frontend with parity matrix, KR audit, and VALIDATION sign-off.

**Verified:** 2026-05-10T15:33:04Z
**Status:** HUMAN VERIFICATION NEEDED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Current frontend/ is the new React 19/Vite 6 scaffold (not the old archive) | ✓ VERIFIED | `frontend-react-archive/` exists with original src/; `frontend/package.json` has `react@^19.2.5`, `@vitejs/plugin-react@^5.2.0`; new scaffold boots |
| 2 | Wiring layer lifted: api/ (7), context/ (6), hooks/ (5), adapter/ (2), types.ts, mocks/ (4) | ✓ VERIFIED | All files exist in `frontend/src/`; byte-identical to archive (mocks/index.ts re-written as empty barrel per Step 6b) |
| 3 | New App.tsx 6-context shell + Onboarding (real, not stub) above WS hook (Pitfall #6) | ✓ VERIFIED | `frontend/src/App.tsx` imports Onboarding, useWebSocket called above gate; no `onboarding-stub` div; ONBOARDING_FLAG = `as_onboarding_v1_complete` |
| 4 | Dashboard surfaces ported as ES modules with named exports (icons, viz, panels, states, tweaks, app_v2) | ✓ VERIFIED | All 6 files exist with correct `export function`/`export const` declarations; no `window.*` writes |
| 5 | app_v2.tsx wired to all 5 context hooks + simStart/simStop; mock setInterval clock removed | ✓ VERIFIED | grep confirms useAgents/useBrackets/useTelemetry/useRationales/useConnection + simStart/simStop imports; no `setInterval.*tps` |
| 6 | W3 modals/advisory/history/settings ported and wired (v2.tsx, ReportModal.tsx, modals.jsx, history.tsx, settings.tsx) | ✓ VERIFIED | All 5 files exist; AdvisoryV2/SignalWire/ModelStatus/DataSourcesTakeover exported from v2.tsx; ShockDrawer/ReplayBar/CyclePickerModal from modals.jsx; Settings wired via normalizeSettingsUiState |
| 7 | usePolling D-19 timeout-as-Error patch applied | ✓ VERIFIED | `usePolling.ts:61` has `setError(new Error('polling timed out after ${maxAttempts} attempts'))` |
| 8 | W4 net-new components (interview_v2, onboarding, bracket_deep) exist and are wired to real data | ✓ VERIFIED | All 3 .tsx files exist (437/326/291 LOC); askAgent() wired in interview_v2, fetchEdges() in bracket_deep, /api/health 3-state check in onboarding |
| 9 | api/edges.ts provides fetchEdges(cycleId, round) with codex MEDIUM-7 JSDoc semantics | ✓ VERIFIED | `fetchEdges` exported from `frontend/src/api/edges.ts` |
| 10 | Production grep gates green: no window.AS_DATA/window.Icon/Object.assign(window in src/; no mock leaks in dist/; no Pitfall #3 literals | ✓ VERIFIED | Gate 1 (src globals): PASS; Gate 2 (dist mock leaks): PASS; Gate 3 (Pitfall #3 literals): PASS; Gate 4 (AGENTS RELEVANT absent): PASS; Gate 5 (HOLDINGS AFFECTED present): PASS |
| 11 | DEV-only dynamic mock import for SignalWire/DataSourcesTakeover (codex HIGH-2 / KR-41.6-14) | ✓ VERIFIED | No top-level `import.*from '../mocks/(wire|sources)'` in v2.tsx; DEV-only dynamic import pattern confirmed |
| 12 | 31-row parity matrix (D-16) and KR-AUDIT.md (11 KR-41.1 + 14 KR-41.6) produced | ✓ VERIFIED | Both files exist; parity matrix has 31 numbered rows; KR-AUDIT has correct counts |
| 13 | VALIDATION.md flags flipped: nyquist_compliant=true, wave_0_complete=true | ✓ VERIFIED | Frontmatter confirms `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved` |
| 14 | TS check passes; Vite production build passes | ✓ VERIFIED | `npm run check` exit 0 (zero TS errors); `dist/` with 1 JS bundle and CSS confirmed |
| 15 | All 31 parity matrix rows signed off by human against live backend (D-16) | ✗ NOT VERIFIED | All 31 rows remain at `⬜` status — no human UAT has been performed against live backend. Auto-chain approval was used in W4 but explicitly deferred this. Requires human. |

**Score: 13/15 truths verified**

---

### Deferred Items

None. No truths are addressed in later milestone phases — the live UAT gap must be closed within Phase 41.6.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend-react-archive/src/App.tsx` | Pre-41.6 port-of-record | ✓ VERIFIED | Exists |
| `frontend/package.json` | Fresh dep manifest with react@^19.2.5 | ✓ VERIFIED | Confirmed |
| `frontend/src/App.tsx` | 6-context shell + real Onboarding | ✓ VERIFIED | 6 contexts + `<Onboarding>` mount confirmed |
| `frontend/src/data.ts` | BRACKETS constant only (D-14) | ✓ VERIFIED | No mulberry32/buildAgents/window.AS_DATA |
| `frontend/src/lib/markdown.ts` | renderMarkdown helper | ✓ VERIFIED | `export function renderMarkdown` present |
| `frontend/src/api/advisory.ts` | advisoryFetch (GET-only) | ✓ VERIFIED | Exists; no advisoryTrigger POST |
| `frontend/src/hooks/useWebSocket.ts` | WS hook with 3-retry backoff | ✓ VERIFIED | Exists |
| `frontend/src/components/icons.jsx` | Icon component, 18 SVG cases | ✓ VERIFIED | `export const Icon` with switch, no window.Icon |
| `frontend/src/components/viz.jsx` | Viz deterministic SVG (no d3-force) | ✓ VERIFIED | 437 LOC, export function Viz, deterministic layout |
| `frontend/src/components/panels.jsx` | BracketList, RationaleFeed, KpiStrip, ConsensusRing | ✓ VERIFIED | All 4 named exports; empty-state fallbacks present |
| `frontend/src/components/states.jsx` | IdleState, SeedingState, MemoryPausedState, ErrorState | ✓ VERIFIED | All 4 exports; BRACKETS from '../data'; Icon from './icons' |
| `frontend/src/components/tweaks.jsx` | TweaksPanel dev-only | ✓ VERIFIED | export function TweaksPanel; Icon import |
| `frontend/src/components/TweaksPanelLoader.tsx` | DEV gate around TweaksPanel | ✓ VERIFIED | import.meta.env.DEV gate confirmed |
| `frontend/src/components/app_v2.tsx` | Full dashboard shell, 5 context hooks | ✓ VERIFIED | 548 LOC; all hooks + simStart/simStop + lifecycle dispatch |
| `frontend/src/components/v2.tsx` | SignalWire, ModelStatus, DataSourcesTakeover, AdvisoryV2 + helpers | ✓ VERIFIED | 1157 LOC; all 4 major exports + 6 sub-helpers |
| `frontend/src/components/ReportModal.tsx` | Canonical-first rich report (codex HIGH-4) | ✓ VERIFIED | canonicalHtml renders first; heading-gated rich sections |
| `frontend/src/components/modals.jsx` | ShockDrawer, ReplayBar, CyclePickerModal | ✓ VERIFIED | All 3 exports present |
| `frontend/src/components/history.tsx` | CycleHistory + hidden CompareModal (D-21) | ✓ VERIFIED | Exists; comparableCycles=[] always |
| `frontend/src/components/settings.tsx` | D-23 split persistence via normalizeSettingsUiState | ✓ VERIFIED | normalizeSettingsUiState wired on both read and write |
| `frontend/src/components/interview_v2.tsx` | InterviewV2, real askAgent wiring, PERSONA removed | ✓ VERIFIED | 437 LOC; askAgent imported + called; PERSONA absent |
| `frontend/src/components/onboarding.tsx` | Onboarding, /api/health gate, simStart on Run | ✓ VERIFIED | 326 LOC; /api/health fetch; ollamaOk 3-state |
| `frontend/src/components/bracket_deep.tsx` | BracketDeepDive, fetchEdges wired | ✓ VERIFIED | 291 LOC; fetchEdges imported + called |
| `frontend/src/api/edges.ts` | fetchEdges(cycleId, round) with MEDIUM-7 semantics | ✓ VERIFIED | export async function fetchEdges; JSDoc direction semantics |
| `frontend/src/lib/advisoryParse.ts` | 10 named parsers + AGENT_ID_RE | ✓ VERIFIED | 10 export functions confirmed |
| `frontend/src/lib/settingsState.ts` | normalizeSettingsUiState + SETTINGS_UI_ALLOWLIST | ✓ VERIFIED | Both exported |
| `.planning/phases/41.6-.../41.6-PARITY-MATRIX.md` | 31-row UAT matrix (D-16) | ✓ VERIFIED | 31 rows; all ⬜ (awaiting human sign-off) |
| `.planning/phases/41.6-.../41.6-KR-AUDIT.md` | 11 KR-41.1 verdicts + 14 KR-41.6 entries (D-17/D-18) | ✓ VERIFIED | Counts confirmed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `frontend/src/App.tsx` | `frontend/src/hooks/useWebSocket.ts` | useWebSocket() above onboarding gate | ✓ WIRED | Confirmed at line 23 before gate check |
| `frontend/src/main.tsx` | `frontend/src/App.tsx` | createRoot().render(<App />) | ✓ WIRED | Lifted from archive unchanged |
| `frontend/vite.config.ts` | FastAPI backend :8000 | proxy /api + /ws with changeOrigin:true | ✓ WIRED | changeOrigin: true on both /api and /ws proxies |
| `frontend/src/components/app_v2.tsx` | `frontend/src/context/*Context.tsx` | useAgents/useBrackets/useTelemetry/useRationales/useConnection | ✓ WIRED | All 5 imports + destructures confirmed |
| `frontend/src/components/app_v2.tsx` | `frontend/src/api/simulation.ts` | simStart(seed)/simStop() on topbar | ✓ WIRED | Import + call sites confirmed |
| `frontend/src/components/states.jsx` | `frontend/src/data.ts` | import { BRACKETS } from '../data' | ✓ WIRED | Confirmed |
| `frontend/src/components/panels.jsx (KpiStrip)` | `frontend/src/context/TelemetryContext.tsx` | props from app_v2 sourced via useTelemetry() | ✓ WIRED | tps/memMb/slotsUsed/elapsedSeconds flow through |
| `frontend/src/components/interview_v2.tsx` | `frontend/src/api/interview.ts` | askAgent(agent.id, message) | ✓ WIRED | Confirmed at line 250 |
| `frontend/src/components/bracket_deep.tsx` | `frontend/src/api/edges.ts` | fetchEdges(cycleId, 3) | ✓ WIRED | Confirmed |
| `frontend/src/App.tsx` | `frontend/src/components/onboarding.tsx` | <Onboarding onComplete={...}/> | ✓ WIRED | Real Onboarding, not W1 stub |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `app_v2.tsx` | `agents` | `useAgents()` → AgentsContext → WS frame `agent_states` | Yes (backend fix fe366ce/1da741e ships per-agent state) | ✓ FLOWING (static; live UAT needed to confirm end-to-end) |
| `app_v2.tsx` | `tps/memMb/slotsUsed/phase` | `useTelemetry()` → TelemetryContext → WS frame telemetry | Yes (5Hz broadcaster) | ✓ FLOWING |
| `panels.jsx:BracketList` | `summaries` | `useBrackets()` → BracketContext → WS frame brackets | Yes | ✓ FLOWING |
| `panels.jsx:RationaleFeed` | `rationales` | `useRationales()` → RationalesContext → WS frame rationales | Yes | ✓ FLOWING |
| `v2.tsx:AdvisoryV2` | `report` | `usePolling(advisoryFetch)` → GET /api/advisory/{cycleId} | Yes (backend auto-fires on FINAL round) | ✓ FLOWING (requires completed cycle for non-null result) |
| `ReportModal.tsx` | `report` | `reportGenerate` POST then `reportFetch` poll | Yes (trigger-then-poll per D-20) | ✓ FLOWING |
| `interview_v2.tsx` | `response` | `askAgent(agent.id, msg)` → POST /api/interview/{agent_id} | Yes | ✓ FLOWING |
| `bracket_deep.tsx` | `influences` | `fetchEdges(cycleId, 3)` → GET /api/edges/{cycleId}?round=3 | Yes (post-cycle only; live='—' per KR-41.6-13) | ⚠️ STATIC during live cycle — KR-41.6-13 documents intentional '—' fallback |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for running-server checks — cannot start Ollama/FastAPI/Neo4j in CI context. TS check + production build serve as automated behavioral proxies.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TS compile | `npm run check` | exit 0, zero errors | ✓ PASS |
| Production bundle builds | `npm run build` | exit 0, dist/ produced (1 JS bundle) | ✓ PASS |
| No globals in src/ | `! grep -rn 'window.AS_DATA\|window.Icon\|Object.assign(window' frontend/src/` | zero matches | ✓ PASS |
| No mock leaks in dist/ | `! grep -rE 'SIGNAL_WIRE_SEED\|DATA_SOURCES\|...\|mocks/wire\|mocks/sources' dist/assets/*.js` | zero matches | ✓ PASS |
| No Pitfall #3 literals | `! grep -rE 'Trim AAPL\|Apple acquiring Anthropic\|...' src/components/` | zero matches | ✓ PASS |
| D-22 label correct | `grep 'HOLDINGS AFFECTED' v2.tsx` | 3 matches; 0 for AGENTS RELEVANT | ✓ PASS |
| D-19 patch present | `grep 'polling timed out' usePolling.ts` | line 61 confirmed | ✓ PASS |
| No advisoryTrigger POST | `grep -rn 'advisoryTrigger\|POST.*advisory' frontend/src/` | zero matches | ✓ PASS |
| Live UAT (31 parity rows) | Run against live backend | All 31 rows unsigned ⬜ | ✗ FAIL (human required) |

---

### Requirements Coverage

Per the phase brief: requirements are implicit parity surfaces tracked via KR-41.1-NN and KR-41.6-NN registers (no formal REQ-IDs). Cross-reference against KR-AUDIT.md:

| KR | Description | Status | Evidence |
|----|-------------|--------|---------|
| KR-41.1-01 | Deterministic SVG (no d3-force) | ✓ SATISFIED | viz.jsx ships SVG layout without d3-force |
| KR-41.1-02 | Mocks dev-only (production bundle clean) | ✓ SATISFIED | Dynamic import gate + production grep PASS |
| KR-41.1-03 | agent.flipped/roundLastSpoke/thinking stubs | ✓ SATISFIED | frame.ts adapter stubs preserved; carry-forward |
| KR-41.1-04 | Memory shown as %, MemoryPausedState GB literal stays | ✓ SATISFIED | KpiStrip uses memMb%; states.jsx literal carries |
| KR-41.1-05 | slotsMax stub=8; slotsUsed live | ✓ SATISFIED | Both flows through useTelemetry() |
| KR-41.1-06 | ReplayBar Rewind disabled | ✓ SATISFIED | modals.jsx ReplayBar carry-forward |
| KR-41.1-07 | CycleHistory em-dash columns | ✓ SATISFIED | history.tsx: consensus/flips/duration/shocks placeholder |
| KR-41.1-08 | ErrorState event log static | ✓ SATISFIED | states.jsx design-copy lines preserved |
| KR-41.1-09 | Top Influencers confidence DESC during live | ✓ SATISFIED | app_v2 sort by confidence DESC |
| KR-41.1-10 | RationaleFeed cites=[] (no cited_agents) | ✓ SATISFIED | frame.ts stubs citations:[] |
| KR-41.1-11 | InterviewV2 non-streaming | ✓ SATISFIED | Single askAgent POST; KR-41.6-03 removes PERSONA |
| KR-41.6-01 | AdvisoryV2 stubs shares/basis/last/sentiment | ✓ SATISFIED (design) | '—' render documented |
| KR-41.6-08 | ModelStatus hardcoded 'qwen3:8b' | ✓ SATISFIED | v2.tsx ModelStatus confirms |
| KR-41.6-14 | DEV-only dynamic mock import in v2.tsx | ✓ SATISFIED | No top-level import; dynamic DEV gate confirmed |
| All 31 parity rows (D-16) | Live behavior sign-off | ? NEEDS HUMAN | All ⬜ unsigned |

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `frontend/src/adapter/frame 2.ts` | D-06 Finder-duplicate not removed (plan explicitly required absence) | ⚠️ WARNING | File exists but is NOT imported anywhere in frontend/src/ — no build impact, no runtime impact. Orphaned artifact only. Plan verify block listed `! test -e 'frontend/src/adapter/frame 2.ts'` as a gate but the file was never cleaned up. |

No other anti-patterns found. Specifically confirmed absent:
- No `window.AS_DATA` / `window.Icon` / `Object.assign(window` anywhere in src/
- No mock identifiers in production dist/
- No TODO/FIXME in core component files
- No `return null` stubs in shipped components (all stubs replaced by W4)
- No `setInterval` mock telemetry clock in app_v2.tsx
- No PERSONA dict in interview_v2.tsx

---

### Human Verification Required

#### 1. Full Live UAT Against Backend — 31 Parity Matrix Rows

**Test:** Start the full backend stack (Ollama desktop + `docker start neo4j` + `uv run uvicorn alphaswarm.web.app:create_app --factory --port 8000`), then `cd frontend && npm run dev`. Work through all 31 rows of `.planning/phases/41.6-ui-revamp-alphaswarm-2-quant-terminal-port-and-wire/41.6-PARITY-MATRIX.md`. For each row, verify the surface against its expected behavior and tick ✅ or ❌.

**Expected:** All 31 rows ✅ (with KR-acknowledged deviations counting as passes per the UAT enforcement rule).

**Why human:** Requires running Ollama inference, active WebSocket frames at 5Hz, completed simulation cycles for advisory/report/edges, and visual inspection of all surfaces. Cannot be automated without a running system.

Key surfaces requiring special attention:
- Row 1 (Force graph): 100 agents must render from real WS `agent_states` frames (backend fixes fe366ce/1da741e required)
- Row 7 (Signal Wire): dev mode only — mocks/wire.ts dynamic import; verify in dev not production build
- Row 17 (InterviewV2): confirm PERSONA filler is absent from response (real /api/interview response only)
- Row 22 (AdvisoryV2 Overview): requires at least one completed cycle with auto-advisory synthesis
- Row 27 (Onboarding): clear localStorage, stop backend, confirm Continue disabled
- Row 28 (BracketDeepDive): influences = '—' during live, real values post-cycle (KR-41.6-13)

#### 2. Onboarding /api/health Gate (KR-41.6-04)

**Test:** Stop backend. Clear `as_onboarding_v1_complete` from localStorage. Open http://localhost:5173. Observe Onboarding Step 1.
**Expected:** Ollama row shows unreachable/loading indicator; Continue button is disabled.
**Why human:** 3-state ollamaOk runtime behavior cannot be verified without toggling backend availability.

#### 3. InterviewV2 Real Response Verification (KR-41.6-03)

**Test:** Run a sim to at least Round 1. Click any agent dot in the force graph. Type a question. Observe the chat response.
**Expected:** Response comes from POST /api/interview/{agent_id} with real inference content. No static keyword-matched text.
**Why human:** Confirming absence of PERSONA filler requires an actual inference response from the backend.

#### 4. AdvisoryV2 4-Tab Layout With Live Data

**Test:** Run a sim to completion (all 3 rounds + FINAL). Click Topbar overflow menu -> Advisory (v6.0). Verify all 4 tabs.
**Expected:** Overview and Holdings tabs show real backend data (consensus_signal, confidence, portfolio_outlook, etc.). Risk tab shows placeholder copy (KR-41.6-05). '—' for shares/basis/last/sentiment (KR-41.6-01). HOLDINGS AFFECTED stat uses affected_holdings/total_holdings values from backend.
**Why human:** Advisory requires a completed sim cycle; portfolio_outlook markdown rendering requires real content.

---

### Gaps Summary

No blocking code gaps were found. The single open item is **live UAT** — all 31 parity matrix rows are unsigned. This is a process gap, not a code gap:

- All artifacts exist and are substantive
- All key wiring links are present in code
- All production grep gates pass
- TS check and Vite build pass
- The four waves (W1-W4) are complete with all planned components

The phase cannot be declared fully complete until a human verifies the running UI against each parity matrix row. The HANDOFF.md's noted backend issues (agent_states emission) have since been fixed by backend commits fe366ce, 1da741e, bd682df — so live UAT is now unblocked.

**Minor finding (non-blocking):** `frontend/src/adapter/frame 2.ts` (D-06 Finder-duplicate) still exists in the repo. It is not imported anywhere and does not affect the build, but the W1 plan verify block listed its absence as a gate. Recommend removing in a quick task.

---

_Verified: 2026-05-10T15:33:04Z_
_Verifier: Claude (gsd-verifier)_
