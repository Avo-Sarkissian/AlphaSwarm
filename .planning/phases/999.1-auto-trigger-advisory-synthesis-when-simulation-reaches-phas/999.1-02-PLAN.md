---
phase: 999.1
plan: 02
type: execute
wave: 2
depends_on:
  - 999.1-01
files_modified:
  - src/alphaswarm/web/app.py
autonomous: false
requirements:
  - AUTO-06

must_haves:
  truths:
    - "SimulationManager in lifespan() receives an on_complete callback that calls _auto_trigger_advisory(app, cycle_id)"
    - "Starting a simulation with holdings loaded: backend log shows auto_advisory_trigger_accepted within ~5s of COMPLETE"
    - "Starting a simulation without holdings: backend log shows auto_advisory_trigger_skipped_unavailable (no error or crash)"
    - "Opening Advisory panel after simulation completes: panel shows synthesized data without clicking Analyze"
  artifacts:
    - path: "src/alphaswarm/web/app.py"
      provides: "Wired on_complete callback closing over app reference"
      contains: "on_complete"
  key_links:
    - from: "lifespan() in app.py"
      to: "SimulationManager(on_complete=...)"
      via: "lambda/closure calling _auto_trigger_advisory(app, cycle_id)"
      pattern: "on_complete.*_auto_trigger_advisory"
---

<objective>
Wire the `on_complete` callback in `app.py:lifespan()` and perform human UAT sign-off.

Purpose: The backend implementation from Plan 01 is complete but not yet wired into the live app. This plan adds one line to `lifespan()` that passes a closure over `app` as the `on_complete` callback to `SimulationManager`, activating auto-trigger in production.

Output: `src/alphaswarm/web/app.py` updated with the callback wire. Human confirms the full end-to-end behavior via 5 UAT rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-auto-trigger-advisory-synthesis-when-simulation-reaches-phas/999.1-01-SUMMARY.md
@.planning/phases/999.1-auto-trigger-advisory-synthesis-when-simulation-reaches-phas/999.1-RESEARCH.md

<interfaces>
<!-- Extracted from src/alphaswarm/web/app.py lines 63-69 — the construction site. -->

```python
# Current SimulationManager construction in lifespan():
sim_manager = SimulationManager(
    app_state,
    brackets,
    on_start=lambda: app.state.interview_sessions.clear(),
    replay_manager=replay_manager,
)
```

<!-- After Plan 01, SimulationManager.__init__ accepts: -->
```python
SimulationManager(
    app_state: AppState,
    brackets: list[BracketConfig],
    on_start: Callable[[], None] | None = None,
    replay_manager: ReplayManager | None = None,
    on_complete: Callable[[str], Awaitable[None]] | None = None,
)
```

<!-- _auto_trigger_advisory is now importable from: -->
```python
from alphaswarm.web.simulation_manager import _auto_trigger_advisory
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire on_complete callback in app.py lifespan()</name>
  <files>src/alphaswarm/web/app.py</files>
  <read_first>
    - src/alphaswarm/web/app.py (full file — must read before editing to understand exact construction site)
    - src/alphaswarm/web/simulation_manager.py (lines 1-20 — confirm _auto_trigger_advisory export location)
  </read_first>
  <action>
Read `src/alphaswarm/web/app.py` first.

Locate the `SimulationManager(...)` construction block (currently lines ~63-69).

Add import at the top of the file (with the other simulation_manager imports):
```python
from alphaswarm.web.simulation_manager import SimulationManager, _auto_trigger_advisory
```
(Replace the existing `from alphaswarm.web.simulation_manager import SimulationManager` line — add `_auto_trigger_advisory` to it.)

Then update the SimulationManager construction to add the `on_complete` keyword argument:
```python
sim_manager = SimulationManager(
    app_state,
    brackets,
    on_start=lambda: app.state.interview_sessions.clear(),
    replay_manager=replay_manager,
    on_complete=lambda cycle_id: _auto_trigger_advisory(app, cycle_id),
)
```

The `lambda cycle_id: _auto_trigger_advisory(app, cycle_id)` expression creates a closure over the `app` object that is already in scope in `lifespan()`. This is the correct pattern from RESEARCH.md Open Question 3.

After editing, verify the import and type-check:
`uv run python -c "from alphaswarm.web.app import app; print('import ok')"` — must print `import ok`.

Also run the full unit suite to confirm no regressions:
`uv run pytest tests/ -x --ignore=tests/integration`
  </action>
  <acceptance_criteria>
    - `grep -n "on_complete" src/alphaswarm/web/app.py` prints a line containing `_auto_trigger_advisory` (covered by grep check in verify)
    - `grep -n "_auto_trigger_advisory" src/alphaswarm/web/app.py` prints the import line and the lambda line (covered by grep check in verify)
    - `uv run python -c "from alphaswarm.web.app import app; print('ok')"` exits 0 and prints `ok` (covered by import check in verify)
    - `uv run pytest tests/ -x --ignore=tests/integration` exits 0 (covered by pytest in verify)
  </acceptance_criteria>
  <verify>
    <automated>
uv run pytest tests/ -x --ignore=tests/integration && uv run python -c "from alphaswarm.web.app import app; print('import ok')" && grep -n "on_complete" src/alphaswarm/web/app.py && grep -n "_auto_trigger_advisory" src/alphaswarm/web/app.py
    </automated>
  </verify>
  <done>app.py imports _auto_trigger_advisory; SimulationManager construction includes on_complete lambda; unit suite green; import check passes; both grep checks show matching lines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — verify auto-trigger end-to-end</name>
  <what-built>
    Backend auto-trigger of advisory synthesis when simulation completes:
    - SimulationManager now stores cycle_id from SimulationResult and schedules _auto_trigger_advisory on successful completion
    - _auto_trigger_advisory POSTs to /api/advisory/{cycle_id} via httpx.ASGITransport (in-process, no network socket)
    - The lifespan() callback wiring is live
  </what-built>
  <how-to-verify>
    Start the server: `uv run uvicorn alphaswarm.web.app:app --reload`

    **U1 — With holdings CSV configured:**
    1. Ensure settings.holdings_csv_path points to a valid Schwab CSV
    2. POST a simulation via the UI or: `curl -s -X POST http://localhost:8000/api/simulation/start -H "Content-Type: application/json" -d '{"seed": "AAPL splits 10:1"}' | python3 -m json.tool`
    3. Watch server logs. Within ~5 seconds of "simulation_complete" appearing, look for: `auto_advisory_trigger_accepted`
    4. Expected: log line appears with `cycle_id` matching the simulation
    **PASS criteria:** `auto_advisory_trigger_accepted` in logs

    **U2 — Advisory panel pre-warmed (requires U1 pass):**
    1. After U1 completes, open the frontend at http://localhost:5173
    2. Click the Advisory button WITHOUT having clicked it before
    3. The panel should show synthesized advisory content (or "Synthesizing..." briefly if synthesis is still in progress)
    4. You should NOT need to click an "Analyze" button
    **PASS criteria:** Advisory content visible without manual "Analyze" click

    **U3 — Without holdings CSV:**
    1. Remove or comment out `settings.holdings_csv_path` (or point it to a nonexistent file)
    2. Restart server; run a simulation
    3. Watch logs: look for `auto_advisory_trigger_skipped_unavailable` (WARNING level)
    4. Confirm: no ERROR log, no crash, no unhandled exception
    **PASS criteria:** WARNING log appears, server stays healthy

    **U4 — Advisory panel graceful when no holdings:**
    1. (Continuing from U3) Open Advisory panel in the frontend
    2. Expected: panel shows "No completed cycle" or "holdings unavailable" message (existing behavior), not a crash
    **PASS criteria:** Panel renders without crash

    **U5 — Report + Advisory race (optional if U1-U4 pass):**
    1. Immediately after simulation completes, click "Report" in the UI
    2. Watch logs for `auto_advisory_trigger_skipped_conflict` (409 swallowed) if advisory and report race
    3. Confirm no unhandled exception logged
    **PASS criteria:** Either 202 (advisory wins) or 409 (report wins and is logged as conflict, not error)
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved U1-U4" — U1 through U4 passed, phase complete
    - "approved U1-U5" — all 5 UAT rows passed
    - "failed U{N}: {description}" — describe which UAT row failed and what was observed
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifespan() closure → _auto_trigger_advisory | Closure captures `app` reference at construction time; no user-controlled input in the callback path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-06 | Denial of Service | lifespan closure over app | accept | The `app` reference is the same FastAPI instance already used by all routes; holding it in a closure adds no new attack surface; GC will not collect it while `sim_manager` is alive (also on `app.state`) |
| T-999.1-07 | Information Disclosure | lambda in lifespan logs | accept | The lambda `lambda cycle_id: _auto_trigger_advisory(app, cycle_id)` passes only `cycle_id` (non-PII UUID) to the function; no portfolio data enters the closure |
</threat_model>

<verification>
After Task 1 (automated):
```
uv run pytest tests/ -x --ignore=tests/integration
uv run python -c "from alphaswarm.web.app import app; print('import ok')"
grep -n "on_complete" src/alphaswarm/web/app.py
grep -n "_auto_trigger_advisory" src/alphaswarm/web/app.py
```

After Task 2 (human):
All 4-5 UAT rows must pass before phase close.
</verification>

<success_criteria>
- `src/alphaswarm/web/app.py` SimulationManager construction includes `on_complete=lambda cycle_id: _auto_trigger_advisory(app, cycle_id)`
- `_auto_trigger_advisory` imported in app.py from `alphaswarm.web.simulation_manager`
- Full unit suite passes
- UAT U1: `auto_advisory_trigger_accepted` in logs after simulation with holdings CSV
- UAT U2: Advisory panel shows synthesized data without "Analyze" click
- UAT U3: `auto_advisory_trigger_skipped_unavailable` in logs when no holdings CSV (no error/crash)
- UAT U4: Panel renders gracefully when no holdings
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-auto-trigger-advisory-synthesis-when-simulation-reaches-phas/999.1-02-SUMMARY.md`
</output>
