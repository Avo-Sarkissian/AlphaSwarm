---
phase: 35-agent-interviews-web-ui
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified: []
autonomous: false
requirements: [WEB-06]
must_haves:
  truths:
    - "User can click an agent node, see AgentSidebar, click Interview button, and open InterviewPanel"
    - "InterviewPanel sends a message and receives an agent response"
    - "InterviewPanel can be closed and reopened with conversation history intact"
    - "Force graph remains interactive behind the interview panel"
    - "Interview button only appears after simulation completes"
    - "Interview button does NOT appear during round_1/round_2/round_3 or replay"
    - "Interview always targets the most recent completed cycle (documented behavior even during replay)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the full agent interview flow in the browser.

Purpose: Confirms the end-to-end integration works visually and functionally -- node click, sidebar, interview button, chat panel, multi-turn conversation, loading indicator, panel dismissal, phase gating, and documented replay-mode behavior.

Output: Verification confirmation or issue list for remediation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-agent-interviews-web-ui/35-CONTEXT.md
@.planning/phases/35-agent-interviews-web-ui/35-UI-SPEC.md
@.planning/phases/35-agent-interviews-web-ui/35-REVIEWS.md
@.planning/phases/35-agent-interviews-web-ui/35-01-SUMMARY.md
@.planning/phases/35-agent-interviews-web-ui/35-02-SUMMARY.md
</context>

<threat_model>
No new threats -- this plan is verification only, no code changes. Reviewers confirmed MEDIUM on Plan 03 is primarily about prerequisite clarity and an impossible idle-state step, both addressed below.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full interview flow verification in browser</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/35-agent-interviews-web-ui/35-UI-SPEC.md (expected visual appearance)
    - .planning/phases/35-agent-interviews-web-ui/35-CONTEXT.md (D-01 through D-11 behavioral expectations)
    - .planning/phases/35-agent-interviews-web-ui/35-REVIEWS.md (Plan 03 corrections: no idle-nodes step, explicit prerequisites, replay cycle note)
  </read_first>
  <action>
    Present the human verification checklist. Start the backend and frontend dev servers, then guide the user through 7 test groups.

    **Prerequisites (explicit — addresses Codex Plan 03 HIGH):**
    1. Neo4j Community running locally (Docker: `docker ps` shows neo4j container).
    2. Ollama running locally (`ollama list` shows `alphaswarm-worker` model, or whatever `worker_model_alias` is configured to).
    3. At least one completed simulation cycle exists in Neo4j. If none, run a fresh simulation to completion before starting Test Group 1.
    4. Backend dev server:
       ```bash
       uv run uvicorn alphaswarm.web.app:create_app --factory --reload
       ```
    5. Frontend dev server (explicit — addresses Codex Plan 03 LOW):
       ```bash
       cd frontend && npm run dev
       ```
       Or equivalently from repo root:
       ```bash
       npm --prefix frontend run dev
       ```
    6. Open http://localhost:5173 (Vite default) in the browser.

    **Test Group 1: Interview button visibility in non-complete phases (D-07)**
    1. While the simulation is idle (no force graph visible — empty state shown), confirm the "Waiting for Simulation" heading is displayed. Skip any click test here — there are no nodes to click in idle.
    2. Start a simulation. During `seeding` / `round_1` / `round_2` / `round_3` phases the graph is visible and agent nodes are clickable.
    3. Click any agent node during `round_2` (or any active round). AgentSidebar opens.
    4. Verify AgentSidebar does NOT show the "Interview" button in active rounds.
    5. Close the sidebar and let the simulation complete.

    **Test Group 2: Interview button visibility in complete phase (D-07, BLOCKER FIX)**
    6. After the simulation finishes, verify that the phase is `complete` AND the force graph is still visible (Plan 02 fixed the `isIdle` blocker).
    7. Click any agent node.
    8. Verify AgentSidebar shows "Interview Agent NN" button at the bottom with accent-colored border.

    **Test Group 3: Panel transition (D-01, D-03, D-04)**
    9. Click the "Interview Agent NN" button.
    10. Verify AgentSidebar slides out and InterviewPanel slides in (same right-side slot, NOT overlaid — per D-03 mutual exclusion).
    11. Verify InterviewPanel header shows "Agent NN" with X close button.
    12. Verify the force graph and panel strip shrink to make room (same as sidebar behavior).

    **Test Group 4: Conversation flow (D-08, D-11, WEB-06-SC3)**
    13. Type a question in the textarea (e.g., "Why did you choose to buy?").
    14. Press Enter to send.
    15. Verify user message appears immediately (right-aligned, blue-tinted bubble).
    16. Verify "Thinking..." indicator appears (left-aligned, pulsing opacity).
    17. Verify Send button shows "..." and is disabled during loading.
    18. Wait for agent response (~5-30 seconds on first call while Ollama loads the model).
    19. Verify agent response appears (left-aligned, dark bubble).
    20. Verify chat auto-scrolls to the newest message.
    21. Send a follow-up question and verify multi-turn works.
    22. Edge check (per Plan 01 validation): try sending an empty message — Send button should be disabled; if somehow submitted, backend returns 422 and the panel shows "Message must be 1-4000 characters."

    **Test Group 5: Panel persistence (D-05)**
    23. Note the conversation history in the panel.
    24. Click X to close the InterviewPanel.
    25. Verify the panel slides out and returns to empty state (no sidebar shown — per D-03).
    26. Click the same agent node again (opens AgentSidebar).
    27. Click "Interview Agent NN" again.
    28. Verify the previous conversation messages are still displayed (reactive Map persisted across close/reopen).

    **Test Group 6: Panel dismissal and bidirectional mutex (D-03, D-04, WEB-06-SC4)**
    29. With InterviewPanel open, click a DIFFERENT agent node on the graph (if the graph is interactive behind the panel).
    30. Verify: Plan 02's bidirectional mutex kicks in — InterviewPanel closes AND AgentSidebar opens for the new agent. Interview button on the new sidebar is visible.
    31. Click X on the sidebar to close.
    32. Verify force graph is interactive (can zoom, pan, click other nodes).

    **Test Group 7: Replay-mode cycle behavior (documented limitation — addresses Gemini LOW)**
    33. If Phase 34 replay mode is accessible: start a replay of an OLDER cycle (not the most recent).
    34. During replay the Interview button should NOT appear (phase is `replay`, not `complete`).
    35. Exit replay (return to `complete` phase of the most recent simulation).
    36. Open the interview for any agent.
    37. DOCUMENTED BEHAVIOR: The interview always targets the MOST RECENT completed cycle. If the user was viewing a replay of an older cycle, the interview still reflects the newest cycle's persona/decisions. This is intentional in Phase 35 — the backend resolves `cycle_id = (await read_completed_cycles(limit=1))[0]["cycle_id"]`. Persisting per-cycle interview targeting is Deferred.

    **Test Group 8 (Optional): Error handling**
    38. Stop the backend server, send a message, verify inline error appears in red below input ("Could not reach the server. Try again.").
    39. Restart server, send another message, verify error clears on success.
    40. While a simulation is running (phase != complete), manually POST to `/api/interview/agent_01` via DevTools console or curl and verify backend returns 409 with `interview_unavailable`.

    **Expected visual appearance:**
    - InterviewPanel has dark secondary background (#1a1d27)
    - User messages: right-aligned, subtle blue tint, rounded corners with sharp bottom-right
    - Agent messages: left-aligned, primary dark background, rounded corners with sharp bottom-left
    - "Thinking..." text pulses between 40% and 100% opacity
    - Send button is blue (#3b82f6) when enabled, 50% opacity when disabled
    - Error text is red (#ef4444), appears below the input row
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_interview.py -x -q && uv run pytest tests/test_web.py -x -q</automated>
    Automated tests confirm backend is still green. Visual verification is performed by the human.
  </verify>
  <acceptance_criteria>
    - All 7 mandatory test groups pass visual/functional verification (Test Group 8 optional)
    - Interview button only visible in complete phase (confirmed across active rounds AND replay)
    - BLOCKER verified fixed: force graph is visible during `complete` phase
    - Panel transition is smooth (sidebar CSS reused)
    - Multi-turn conversation works with real Ollama responses
    - Conversation persists across panel close/reopen (reactive Map)
    - Bidirectional mutex verified: selecting a different agent while interview is open closes the interview panel
    - Replay-mode cycle behavior acknowledged as documented limitation
  </acceptance_criteria>
  <done>User confirms all 7 mandatory test groups pass. Phase 35 agent interview feature is complete and verified. Documented replay-mode limitation acknowledged and noted for future work.</done>
</task>

</tasks>

<verification>
Human verification of all WEB-06 success criteria:
- SC-1: POST /api/interview/{agent_id} returns agent response (verified via browser network tab in Test Group 4)
- SC-2: Clicking node in complete state opens interview panel (Test Group 2-3)
- SC-3: Multi-turn conversation appends responses (Test Group 4)
- SC-4: Panel dismissible, force graph interactive (Test Group 6)
- SC-5: Loading indicator while LLM responds (Test Group 4, steps 16-17)

Review concerns verified:
- CONFIRMED BLOCKER (isIdle) — verified in Test Group 2 step 6
- Backend phase guard — verified in Test Group 1 + Test Group 8 step 40
- Bidirectional mutex — verified in Test Group 6 step 30
- Replay cycle ambiguity — documented in Test Group 7
- Explicit frontend startup command — in Prerequisites
</verification>

<success_criteria>
User confirms all 7 mandatory test groups pass. Phase 35 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/35-agent-interviews-web-ui/35-03-SUMMARY.md`
</output>
