---
phase: 35-agent-interviews-web-ui
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified: []
autonomous: false
requirements: [WEB-06]
must_haves:
  truths:
    - "User can click an agent node, see AgentSidebar, click Interview button, and open InterviewPanel"
    - "InterviewPanel sends a message and receives an agent response"
    - "InterviewPanel can be closed and reopened with conversation history intact"
    - "Force graph remains interactive behind the interview panel"
    - "Interview button only appears after simulation completes"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the full agent interview flow in the browser.

Purpose: Confirms the end-to-end integration works visually and functionally -- node click, sidebar, interview button, chat panel, multi-turn conversation, loading indicator, panel dismissal.

Output: Verification confirmation or issue list for remediation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-agent-interviews-web-ui/35-CONTEXT.md
@.planning/phases/35-agent-interviews-web-ui/35-UI-SPEC.md
@.planning/phases/35-agent-interviews-web-ui/35-01-SUMMARY.md
@.planning/phases/35-agent-interviews-web-ui/35-02-SUMMARY.md
</context>

<threat_model>
No new threats -- this plan is verification only, no code changes.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full interview flow verification in browser</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/35-agent-interviews-web-ui/35-UI-SPEC.md (expected visual appearance)
    - .planning/phases/35-agent-interviews-web-ui/35-CONTEXT.md (D-01 through D-11 behavioral expectations)
  </read_first>
  <action>
    Present the human verification checklist. Start the backend and frontend dev servers, then guide the user through 6 test groups covering interview button visibility, panel transitions, conversation flow, session persistence, panel dismissal, and error handling.

    Prerequisites: Start the app with `uv run uvicorn alphaswarm.web.app:create_app --factory --reload` and open the Vue dev server at http://localhost:5173.

    **Test Group 1: Interview button visibility (D-07)**
    1. While the simulation is idle, click any agent node in the graph
    2. Verify AgentSidebar opens WITHOUT an "Interview" button at the bottom
    3. Start a simulation and let it complete (all 3 rounds)
    4. Click any agent node
    5. Verify AgentSidebar now shows "Interview Agent NN" button at the bottom with accent-colored border

    **Test Group 2: Panel transition (D-01, D-03, D-04)**
    6. Click the "Interview Agent NN" button
    7. Verify AgentSidebar slides out and InterviewPanel slides in (same right-side slot)
    8. Verify InterviewPanel header shows "Agent NN" with X close button
    9. Verify the force graph and panel strip shrink to make room (same as sidebar behavior)

    **Test Group 3: Conversation flow (D-08, D-11, WEB-06-SC3)**
    10. Type a question in the textarea (e.g., "Why did you choose to buy?")
    11. Press Enter to send
    12. Verify user message appears immediately (right-aligned, blue-tinted bubble)
    13. Verify "Thinking..." indicator appears (left-aligned, pulsing opacity)
    14. Verify Send button shows "..." and is disabled during loading
    15. Wait for agent response (~5-30 seconds, first call loads Ollama model)
    16. Verify agent response appears (left-aligned, dark bubble)
    17. Verify chat auto-scrolls to the newest message
    18. Send a follow-up question and verify multi-turn works

    **Test Group 4: Panel persistence (D-05)**
    19. Note the conversation history in the panel
    20. Click X to close the InterviewPanel
    21. Verify the panel slides out and returns to empty state (no sidebar shown)
    22. Click the same agent node again (opens AgentSidebar)
    23. Click "Interview Agent NN" again
    24. Verify the previous conversation messages are still displayed

    **Test Group 5: Panel dismissal (D-03, WEB-06-SC4)**
    25. With InterviewPanel open, click X to close
    26. Verify force graph is interactive (can zoom, pan, click other nodes)
    27. Click a different agent node
    28. Verify AgentSidebar opens for that agent (not the interviewed one)

    **Test Group 6: Error handling**
    29. (Optional) Stop the backend server, send a message, verify inline error appears in red below input
    30. Restart server, send another message, verify error clears on success

    **Expected visual appearance:**
    - InterviewPanel has dark secondary background (#1a1d27)
    - User messages: right-aligned, subtle blue tint, rounded corners with sharp bottom-right
    - Agent messages: left-aligned, primary dark background, rounded corners with sharp bottom-left
    - "Thinking..." text pulses between 40% and 100% opacity
    - Send button is blue (#3b82f6) when enabled, 50% opacity when disabled
    - Error text is red (#ef4444), appears below the input row
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_interview.py -x -q && uv run pytest tests/test_web.py -x -q</automated>
    Automated tests confirm backend is still green. Visual verification is performed by the human.
  </verify>
  <done>User confirms all 6 test groups pass. Phase 35 agent interview feature is complete and verified.</done>
  <acceptance_criteria>
    - All 6 test groups pass visual/functional verification
    - Interview button only visible in complete phase
    - Panel transition is smooth (sidebar CSS reused)
    - Multi-turn conversation works with real Ollama responses
    - Conversation persists across panel close/reopen
    - Force graph interactive when panel is closed
  </acceptance_criteria>
</task>

</tasks>

<verification>
Human verification of all WEB-06 success criteria:
- SC-1: POST /api/interview/{agent_id} returns agent response (verified via browser network tab)
- SC-2: Clicking node in complete state opens interview panel (Test Group 1-2)
- SC-3: Multi-turn conversation appends responses (Test Group 3)
- SC-4: Panel dismissible, force graph interactive (Test Group 5)
- SC-5: Loading indicator while LLM responds (Test Group 3, step 13-14)
</verification>

<success_criteria>
User confirms all 6 test groups pass. Phase 35 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/35-agent-interviews-web-ui/35-03-SUMMARY.md`
</output>
