---
phase: 32-rest-controls-and-simulation-control-bar
plan: 04
type: execute
wave: 2
depends_on: [32-01, 32-02, 32-03]
files_modified: []
autonomous: false
requirements: [CTL-01, CTL-02]

must_haves:
  truths:
    - "User visually confirms the control bar renders correctly in idle and active states"
    - "User confirms shock drawer opens, submits, and handles errors"
    - "User confirms the force graph is not clipped by the control bar"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of the control bar and shock drawer UI components.

Purpose: The control bar layout, shock drawer animation, button states, and phase label rendering require visual inspection that automated tests cannot verify.
Output: Human approval or issue list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-rest-controls-and-simulation-control-bar/32-UI-SPEC.md
@.planning/phases/32-rest-controls-and-simulation-control-bar/32-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build frontend and start dev server for verification</name>
  <files></files>
  <read_first>
    - frontend/package.json
  </read_first>
  <action>
    1. Build the frontend production bundle:
       ```bash
       cd frontend && npm run build
       ```

    2. Start the FastAPI server with Uvicorn (uses built frontend from dist/):
       ```bash
       cd /path/to/project && uv run python -m uvicorn alphaswarm.web.app:create_app --factory --host 0.0.0.0 --port 8000
       ```
       Or use the project CLI if available: `uv run alphaswarm web`

    3. Confirm server starts without errors and serves the frontend at http://localhost:8000
  </action>
  <verify>
    <automated>cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm/frontend" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 without errors
    - frontend/dist directory exists with index.html
  </acceptance_criteria>
  <done>Frontend builds successfully, dev server ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of control bar and shock drawer</name>
  <files></files>
  <action>
    Present the running application at http://localhost:8000 for human visual inspection of the control bar, shock drawer, and layout integrity. The verification checklist below covers all visual and interaction requirements from the UI-SPEC.
  </action>
  <verify>
    Open http://localhost:8000 in a browser. Verify the following:

    **1. Idle State (no simulation running):**
    - [ ] Control bar visible at top of page with dark secondary background (#1a1d27)
    - [ ] Seed textarea visible with placeholder "Enter a seed rumor..."
    - [ ] Start button visible, DISABLED (greyed out) when textarea is empty
    - [ ] Type some text in textarea -- Start button enables (blue #3b82f6 background)
    - [ ] No Stop button, no phase label, no +Inject Shock button visible
    - [ ] Empty state "Waiting for Simulation" renders BELOW the control bar (not behind it)

    **2. Start a Simulation (if Ollama + Neo4j are running):**
    - [ ] Click Start -- button shows "Starting..." briefly
    - [ ] Control bar transitions: seed textarea becomes disabled (opacity 0.5), Start greys out
    - [ ] Phase label appears (e.g., "Seeding...", then "Round 1 / 3")
    - [ ] Stop button (red) and +Inject Shock button (ghost blue border) appear

    **3. Shock Drawer (during active simulation):**
    - [ ] Click "+Inject Shock" -- drawer slides down below control bar
    - [ ] Drawer has label "Inject Market Shock", textarea with placeholder "Describe the shock event...", and two buttons
    - [ ] Textarea auto-focuses on open
    - [ ] Click "Discard Shock" -- drawer slides up, text cleared
    - [ ] Open again, type text, click "Inject Shock" -- drawer closes on success
    - [ ] If you click "Inject Shock" again quickly (shock already queued), inline error "A shock is already queued." appears in red

    **4. Layout Integrity:**
    - [ ] Force graph (when active) renders fully below the control bar with no overlap
    - [ ] Agent sidebar (click a node) still slides in from the right correctly
    - [ ] Page does not scroll vertically -- everything fits in viewport

    **5. Replay Endpoints (curl verification):**
    - [ ] `curl http://localhost:8000/api/replay/start/test-123 -X POST` returns `{"status":"ok","cycle_id":"test-123","round_num":1}`
    - [ ] `curl http://localhost:8000/api/replay/advance -X POST` returns `{"status":"ok","round_num":1}`
  </verify>
  <done>Human approves visual appearance and interaction behavior of control bar and shock drawer, or provides specific issues to fix.</done>
</task>

</tasks>

<verification>
Human confirms visual correctness of control bar and shock drawer.
</verification>

<success_criteria>
- Control bar renders in idle and active states with correct button visibility
- Shock drawer slides down/up with correct animation
- Force graph not clipped by control bar
- Replay endpoint stubs return correct JSON
- Human approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/32-rest-controls-and-simulation-control-bar/32-04-SUMMARY.md`
</output>
