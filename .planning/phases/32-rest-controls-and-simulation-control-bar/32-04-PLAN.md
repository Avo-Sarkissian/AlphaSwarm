---
phase: 32-rest-controls-and-simulation-control-bar
plan: 04
type: execute
wave: 2
depends_on: [32-01, 32-02, 32-03]
files_modified: []
autonomous: false
requirements: [CTL-01, CTL-02]

must_haves:
  truths:
    - "User visually confirms the control bar renders correctly in idle and active states"
    - "User confirms shock drawer opens, submits, and handles errors"
    - "User confirms the force graph is not clipped by the control bar"
    - "All backend tests pass after Wave 1 + Wave 2 integration"
  artifacts: []
  key_links: []
---

<objective>
Run backend test gates, build frontend, and perform human visual verification of the control bar and shock drawer UI components.

Purpose: The control bar layout, shock drawer animation, button states, and phase label rendering require visual inspection that automated tests cannot verify. Backend tests must also pass after all Wave 1 + 2 changes are integrated.
Output: Human approval or issue list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-rest-controls-and-simulation-control-bar/32-UI-SPEC.md
@.planning/phases/32-rest-controls-and-simulation-control-bar/32-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend test gates and build frontend</name>
  <files></files>
  <read_first>
    - frontend/package.json
    - tests/test_web.py (scan for all test_ functions to confirm count)
  </read_first>
  <action>
    Run the following verification steps in order (addresses Codex review suggestion to add backend test gates before visual checkpoint):

    1. Run full backend web test suite to confirm all Wave 1 changes integrate:
       ```bash
       cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm" && uv run pytest tests/test_web.py -x -v
       ```
       All tests must pass: existing Phase 31 tests + Plan 32-01 tests (start, stop, shock, cancellation) + Plan 32-02 tests (replay, edges regression).

    2. Build the frontend production bundle:
       ```bash
       cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm/frontend" && npm run build
       ```

    3. Start the development environment for verification.

       Option A -- Full stack (if Ollama + Neo4j are running):
       ```bash
       cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm" && uv run python -m uvicorn alphaswarm.web.app:create_app --factory --host 0.0.0.0 --port 8000
       ```

       Option B -- Frontend dev server only (lighter, uses Vite proxy to port 8000):
       ```bash
       cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm/frontend" && npm run dev
       ```
       Note: API calls and WebSocket will proxy to localhost:8000. Start the backend separately if needed.

    4. Confirm server starts without errors and serves the frontend.
  </action>
  <verify>
    <automated>cd "/Users/avosarkissian/Documents/VS Code/AlphaSwarm" && uv run pytest tests/test_web.py -x -q 2>&1 | tail -3 && cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_web.py -x` exits 0 (all backend web tests pass)
    - `npm run build` exits 0 without errors
    - frontend/dist directory exists with index.html
  </acceptance_criteria>
  <done>All backend web tests pass. Frontend builds successfully. Dev server ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of control bar and shock drawer</name>
  <files></files>
  <read_first>
    - .planning/phases/32-rest-controls-and-simulation-control-bar/32-UI-SPEC.md
  </read_first>
  <action>
    Present the running application at http://localhost:8000 (or Vite dev server URL) for human visual inspection of the control bar, shock drawer, and layout integrity. The verification checklist below covers all visual and interaction requirements from the UI-SPEC.
  </action>
  <verify>
    Open the application in a browser. Verify the following:

    **1. Idle State (no simulation running):**
    - [ ] Control bar visible at top of page with dark secondary background (#1a1d27)
    - [ ] Seed textarea visible with placeholder "Enter a seed rumor..."
    - [ ] Start button visible, DISABLED (greyed out) when textarea is empty
    - [ ] Type some text in textarea -- Start button enables (blue #3b82f6 background)
    - [ ] No Stop button, no phase label, no +Inject Shock button visible
    - [ ] Empty state "Waiting for Simulation" renders BELOW the control bar (not behind it)

    **2. Start a Simulation (if Ollama + Neo4j are running):**
    - [ ] Click Start -- button shows "Starting..." briefly (double-click prevention active)
    - [ ] Clicking Start again while "Starting..." is shown does nothing (button disabled)
    - [ ] Control bar transitions: seed textarea becomes disabled (opacity 0.5), Start greys out
    - [ ] Phase label appears with formatted text (e.g., "Seeding...", then "Round 1 / 3")
    - [ ] Phase label does NOT show raw enum like "round_1" -- must show "Round 1 / 3"
    - [ ] Stop button (red) and +Inject Shock button (ghost blue border) appear

    **3. Shock Drawer (during active simulation):**
    - [ ] Click "+Inject Shock" -- drawer slides down below control bar (NOT as modal overlay)
    - [ ] Drawer has label "Inject Market Shock", textarea with placeholder "Describe the shock event...", and two buttons
    - [ ] Textarea auto-focuses on open
    - [ ] Click "Discard Shock" -- drawer slides up, text cleared
    - [ ] Open again, type text, click "Inject Shock" -- drawer closes on success
    - [ ] If you click "Inject Shock" again quickly (shock already queued), inline error "A shock is already queued." appears in red
    - [ ] ShockDrawer is visually part of the ControlBar area (no gap between bar and drawer)

    **4. Stop Flow:**
    - [ ] Click Stop -- simulation cancels, control bar returns to idle state
    - [ ] Phase label disappears, seed textarea re-enables, Start button awaits new input

    **5. Layout Integrity:**
    - [ ] Force graph (when active) renders fully below the control bar with no overlap
    - [ ] Agent sidebar (click a node) still slides in from the right correctly
    - [ ] Page does not scroll vertically -- everything fits in viewport

    **6. Replay Endpoints (curl verification):**
    - [ ] `curl http://localhost:8000/api/replay/start/test-123 -X POST` returns `{"status":"ok","cycle_id":"test-123","round_num":1}`
    - [ ] `curl http://localhost:8000/api/replay/advance -X POST` returns `{"status":"ok","round_num":1}`
    - [ ] `curl "http://localhost:8000/api/edges/current?round=1"` returns edges or empty list (SC-3 regression)
  </verify>
  <acceptance_criteria>
    - Human confirms control bar renders correctly in idle state (textarea + Start)
    - Human confirms control bar renders correctly in active state (Stop + phase label + Inject Shock)
    - Human confirms phase label shows formatted "Round N / 3" text (not raw enum)
    - Human confirms shock drawer opens below control bar, not as overlay
    - Human confirms Start button double-click prevention works
    - Human confirms force graph is not clipped by control bar
    - Human confirms replay curl commands return expected JSON
  </acceptance_criteria>
  <done>Human approves visual appearance and interaction behavior of control bar and shock drawer, or provides specific issues to fix.</done>
</task>

</tasks>

<verification>
Human confirms visual correctness of control bar, shock drawer, and layout integrity after all backend tests pass.
</verification>

<success_criteria>
- All backend web tests pass (integration gate)
- Frontend production build succeeds
- Control bar renders in idle and active states with correct button visibility
- Phase label shows formatted "Round N / 3" text
- Start button has double-click prevention
- Shock drawer slides down/up with correct animation, owned by ControlBar
- Force graph not clipped by control bar
- Replay endpoint stubs return correct JSON
- Edges endpoint regression (SC-3) returns expected response
- Human approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/32-rest-controls-and-simulation-control-bar/32-04-SUMMARY.md`
</output>
