__pycache__/
*.pyc
.env
results/
.DS_Store
*.vue.js
frontend/tsconfig.*.tsbuildinfo
Schwab/
/*.csv
.alphaswarm/
data/sec_tickers.json

# Runtime cycle outputs — not source, regenerated every run.
# Patterns are root-anchored: an unanchored `advisory/` would also match
# src/alphaswarm/advisory/ (the actual source package).
/advisory/
/reports/
/frontend/reports/
# mypy stubs installed by `mypy --install-types`
/typings/
