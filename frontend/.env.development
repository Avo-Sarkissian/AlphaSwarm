# Dev-mode default for the FastAPI WebSocket origin.
# Used by frontend/src/hooks/useWebSocket.ts to build /ws/state URL.
# Production builds override via .env.production or CI env injection.
VITE_BACKEND_WS=ws://localhost:8000
