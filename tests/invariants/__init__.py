"""tests/invariants — architectural invariants (not feature tests).

D-16: this directory contains tests enforcing cross-cutting constraints
that do not belong to any single feature, e.g., import-graph contracts,
holdings isolation canaries, schema assertions.
"""
